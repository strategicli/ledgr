# Notes

**Tyler → Brandon (2026-06-15):** The Changelog and these shared notes are live (ADR-054). To turn them on for your Vercel deploy, add a `GITHUB_TOKEN` env var and redeploy. Since you own the repo, you can use a fine-grained token scoped to `brandonscollins/ledgr` with Contents: read and write (cleaner than a classic `repo` token). Steps are in runbook §1g. I'm running locally for now, so it's just your Vercel that needs it. Clear this once it's set.
