# roadmap.md: Ledgr Phasing Tracker

Phase-by-phase checklist derived from PRD §8. Check boxes as slices ship. The point is twofold: progress is visible, and scope discipline holds (what's deliberately *not* in a phase matters as much as what is). When something moves, update `next_steps.md` too.

Status legend: `[ ]` not started, `[~]` in progress, `[x]` done.

> **⚠️ Markdown epoch (v0.18, ADR-037, 2026-06-13).** The canonical body format pivoted from BlockNote JSON to **Markdown**, the system is now **bespoke-first with per-type canvases**, and Ledgr is built by two people (Brandon + Tyler). Completed Phase-1 items below that read "BlockNote" record what was actually built and stay as history; the **editor and body-format code will be reworked** in the foundation-rework section below. New work follows the markdown-canonical model.

---

## Phase M: Markdown epoch foundation rework (v0.18 — gates the new modules)

The pivot (ADR-037) is recorded; this is the code arc that makes it real. A key de-risking asset: the existing `src/lib/markdown.ts` BlockNote→markdown serializer (proven, 22 checks in ADR-006) becomes the one-time **migration tool**, so the data move reuses code that already works. Editor selection (M1) is a **core decision** → both-agree + ADR per CLAUDE.md "Building together."

> **⚠️ Rescoped by ADR-039 (alpha/build phase, 2026-06-13; HISTORICAL, superseded by ADR-115's v1.0 production flip on 2026-06-26: "reseed freely" no longer applies, migration caution is back on).** Ledgr was pre-production with no data to protect, so the original "each slice leaves the app working / careful staged migration" constraint is **dropped**. **M3 and M4 are collapsed into one direct cutover** (now listed as M3). Convert-or-reseed dev data freely; no DB-copy dry run. M1–M2 are done; M5/M6 unchanged.

> **⚠️ Scope of the foundational build (ADR-042, 2026-06-13).** Phase M delivers the **capability** for workflow modules to plug in — the per-type canvas seam (M5) and the module-registration boundary (M6) — so a module can be added later and fit the architecture cleanly. **Adding the modules themselves (Papers, Songs, Sermons/Lessons, Discipleship) is not foundational/core work**; they're built on top of the finished capability afterward (mostly Tyler's lane). Phase M is "done" when the slots exist and are proven, not when any module ships.

- [x] **M1 — Editor selection (spike + ADR).** Evaluate **tiptap** (ProseMirror; Savor-proven, Tyler's stack), **Milkdown**, **Lexical** against: lossless markdown round-trip; custom color/highlight marks that serialize to `<mark>`/`<span>` (reuse the `src/lib/colors.ts` table); `@`-mention as the markdown link `[@Title](ledgr://item/<uuid>)` the serializer already emits; lazy-load + bundle size; and a credible path to the later "Notion feel" (slash menu, block drag) and to Obsidian-style `^block-id` anchors. Output: an ADR picking the editor. **(2026-06-13: ADR-038 picks Tiptap + first-party `@tiptap/markdown`; Milkdown documented as runner-up, Lexical rejected. CORE decision — Brandon recommended, Tyler agreed → accepted.)**
- [~] **M2 — Markdown editor stood up in isolation.** The chosen editor on a scratch/dev route, reading and writing `{format:"markdown", text}`; colors + mentions working; verified round-trip (type → markdown → reload → identical, colors intact). Does **not** touch the live BlockNote path yet, so nothing user-facing breaks. **(2026-06-13: built — Tiptap v3.26.1 + `@tiptap/markdown` at `/scratch/editor`, new `src/components/markdown-editor/` (BlockNote dir untouched); `TextColor`/`Highlight`/`LedgrMention` extensions emit the exact `colors.ts`/`ledgr://` encoding; 49/49 in `verify-tiptap-markdown.mts` incl. the migration-tool agreement cross-check; `tsc` clean. In-browser round-trip eyeball + full `next build` pending — dev server was up.)**
- [x] **M3 — The cutover (merged M3+M4 per ADR-039; the flip, done directly).** **(2026-06-13, ADR-040: done.** `{format,text}` body via `src/lib/body.ts`; server render via **markdown-it** in `src/lib/markdown-render.ts` (`markdownToHtml`/`markdownToText`, heading-shift + mention-as-span + color-HTML passthrough); print/export/FTS/mentions all read markdown; Tiptap wired into the real canvas (`ItemEditor` → `markdown-editor/`); `@blocknote/*` uninstalled + `src/components/editor/` deleted; **dev data wiped, not migrated** (Brandon's call — the BlockNote serializer `src/lib/markdown.ts` was deleted, no migration tool kept); a pre-existing export-idempotency flake fixed along the way. `tsc` + `next build` clean; 24/24 verify scripts; in-browser editor + share render + write round-trip confirmed.) One focused slice, no parallel BlockNote path: (a) **renderers read markdown** — reimplement the print render (`src/lib/print-html.ts`) and OneDrive export as markdown → HTML, and switch `body_text` to a markdown-syntax strip for FTS (likely justifies one vetted markdown lib — markdown-it / remark — a Principle-5 dependency call); (b) **editor swap** — wire the M2 Tiptap editor into the real `ItemEditor`/`ItemCanvas`, replacing BlockNote; (c) **body contract** — `items.body` read/write becomes `{format:"markdown", text}`; (d) **mentions** — `src/lib/mentions.ts` extracts `ledgr://item/<id>` from markdown instead of walking JSON; email-in writes markdown (trivial); (e) **data** — convert existing dev bodies (+ revisions) best-effort via the existing serializer, **or just reseed — no DB-copy dry run, no eyeball gate** (alpha, ADR-039); (f) **retire** `@blocknote/*` and delete `src/components/editor/`. After this slice BlockNote is gone and markdown is the only path. Verify print/export/FTS on sample content; `tsc` + `next build` clean.
- [x] **M5 — Per-type canvas seam.** A `type → canvas` registry (default = the markdown canvas) wired into `ItemCanvas` and the intercepting routes (ADR-007 mechanics stay intact). This is the platform hook the Songs/Papers modules need; ship with default-only plus one trivial proof so the seam is exercised. **(2026-06-13, ADR-041: done.** `ItemCanvas` split into a thin **shell** (owner check, item load, guards, breadcrumb → resolves + renders the type's canvas) and the default **`MarkdownCanvas`** (editor + panels + chrome, extracted verbatim). Pure policy `canvasIdForType` in `src/lib/canvas-registry.ts` (default `"markdown"`, no component imports → testable), wiring `Record` in the shell with a `?? MarkdownCanvas` fallback. Proof: `link` → `LinkCanvas` (URL chip composed over the default). Footer-fields extracted to a pure `footerFieldsFor` pinning the ADR-018 invariant. `tsc` + `next build` clean; **16/16** in `verify-canvas-seam.mts`; in-browser confirmed link-chip + unchanged note canvas, no console errors. Registration API deferred to M6.)
- [x] **M6 — Module registration boundary** (the capability, not the modules — ADR-042). How a module registers its `{type, canvas, exporters, integration}` onto core (rather than reaching into it), with per-user enable as a later config flip. **(2026-06-13, ADR-043: done.** `src/lib/modules.ts` is the pure policy/contract half — `ModuleManifest`/`ModuleTypeDef`/`ExporterDef`/`IntegrationDef`, the registry (`registerModule`/`allModules`/`isModuleEnabled`), and the resolvers core dispatches through (`canvasIdForType`/`canonicalFormatForType`/`exportersForType`/`moduleForType`); kept import-pure so it's node-testable. **Core is the first module** (`coreModule` — the 5 system types, only `link` bespoke), so the whole app resolves type behavior through this one boundary, unchanged. `src/lib/module-wiring.tsx` is the impure half (canvasId→component, `?? MarkdownCanvas` fallback). Canonical-body-format-per-type now resolves (Tyler decision #1, code form); the per-user enable seam is owner-threaded, default-on. Proven by `referenceModule` (exported example) + `verify-module-registry.mts` **38/38**; `canvas-registry.ts` folded in + deleted, `verify-canvas-seam.mts` still **16/16**. `tsc` + `next build` clean; in-browser link-chip + default-note both route through the boundary, no errors.) The deliverable was the **boundary itself**, designed against Tyler's PR #1 specs (esp. Papers) and proven with a minimal reference registration — **building the modules (Papers, Songs, …) was explicitly not part of this slice.**

**✅ Phase M complete (2026-06-13).** The Markdown-epoch foundation rework is done: M1–M3 (editor selection + the markdown cutover, ADR-038/040), M5 (the per-type canvas seam, ADR-041), M6 (the module-registration boundary, ADR-043). The platform now supports markdown-canonical bodies, a bespoke canvas per type, and modules contributing `{type, canvas, exporters, integration}` onto core. The modules themselves are post-foundation work (ADR-042); next is **Phase 3, Tier 1 — the Build surface UX.**

**Out of scope of Phase M — and of the foundational/core build generally (ADR-042):** **the modules themselves.** Phase M delivers the *capability* for modules to plug in (the per-type canvas seam M5 + the registration boundary M6); the actual modules — **Papers, Songs, Sermons/Lessons, Discipleship** — are post-foundation, non-core work built on top of the finished capability (mostly Tyler's lane; "module internals — move fast, solo" per CLAUDE.md). That includes their module-level pieces: the Word/`.docx` exporter (Papers — a JS docx renderer like Tyler's `ty-additions/msm-render.js`, or pandoc on his local path); ChordPro rendering and PCO push (Songs); the per-module stage models and canvases. Also out of scope here: Obsidian-style `^block-id` block anchors (a Phase 3 feature — `explorations/block-linked-action-items.md`, M1 just keeps the door open).

---

## Phase 1: Core (build first, live in it)
The goal: a usable single-user tool Brandon can capture into and write in, with the export safety net working.

- [x] Repo scaffold (Next.js on Vercel, Drizzle, Neon via pooler, Clerk, env config) (`/health` green in production 2026-06-12; GitHub push → auto-deploy verified working)
- [x] Data model: `users`, `types` (seed 5 system rows), `items` (incl. `properties` JSONB), `relations`, `attachments`, `revisions`, `views`, `error_log` (see `schema.md`) (migration ran clean on Neon 2026-06-12; all 8 tables, 5 type rows, 1 user row verified)
- [x] Index plan in place (incl. FTS generated `tsvector` column) (all 22 indexes verified on Neon 2026-06-12, incl. `items_search_gin` and `items_properties_gin`)
- [x] Auth: Clerk + Microsoft sign-in; API-token scheme for machine access (done 2026-06-12: Brandon's first Microsoft sign-in landed and `users.clerk_id` is backfilled, verified against Neon; production route protection and machine tokens verified earlier same day)
- [x] Item CRUD (owner-scoped; list queries exclude `body`) (2026-06-12: full REST surface under `/api/items*` + `src/lib/items.ts`; verified against Neon incl. owner scoping, no-body list SQL, cycle guard)
- [x] Block editor (BlockNote): slash commands, headings, lists, checkboxes, quotes, dividers, code; bold/italic/highlight/text colors (2026-06-12: lazy-loaded via `next/dynamic`, verified in-browser incl. autosave; minimal `/items/[id]` host page until the canvas slice)
- [x] Markdown serialization (color/highlight → inline HTML `<mark>`/`<span>`; single mapping table) (2026-06-12: pure server-safe serializer in `src/lib/markdown.ts` + pinned color table in `src/lib/colors.ts`; 22 serializer checks pass; Obsidian eyeball check pending on `scripts/sample-export.md`)
- [x] Paste images inline (stored to R2) (2026-06-12: presign flow + `/api/attachments` + storage interface; R2 provisioned, CORS policy set, live paste confirmed by Brandon — slice 5 fully closed)
- [x] `@`-mention to other items (auto-creates a `relations` row) (2026-06-12: mention inline node + picker; diff-sync to `relations` role `mention` on every body save; verified end-to-end in-browser against Neon)
- [x] Entity pages (related items grouped by type) (2026-06-12: both-directions body-free query in `src/lib/relations.ts` + `GET /api/items/[id]/related`; entity items render a grouped Related section on `/items/[id]`; suggested edges render gray/dashed; 17/17 checks against Neon + browser-verified)
- [x] Parent/child subtasks: recursive tree reads, cycle guard, progress rollup, soft-delete cascade (2026-06-12: subtree + ancestors reads in `src/lib/subtasks.ts` + `GET /api/items/[id]/subtree`; Subtasks checklist with done-toggles, inline add, n-of-m rollups, and ancestor breadcrumb on `/items/[id]`; 21/21 checks against Neon + browser-verified; cycle guard and cascade were slice 4)
- [x] Item canvas: center modal default + expand to full screen; top/bottom field zones, horizontal top strip (PRD §4.13) (2026-06-12: intercepting-route modal at `src/app/@modal`, shared `ItemCanvas`, per-type strip defaults in `src/lib/canvas-fields.ts`; browser-verified end to end)
- [x] Today / dashboard view (batched single fetch; fixed layout, widgets come in Phase 2 per PRD §4.11) (2026-06-12: `/` is now Today — quick capture, today's meetings, due/overdue tasks, recent; one batched fetch in `src/lib/today.ts`, `LEDGR_TIMEZONE` defines the day, ADR-008; interim type-grouped list moved to `/items`)
- [x] Navigation shell: floating bottom bar on mobile (home locked to slot 1, user-assigned slots, badge-count support); desktop bottom-bar vs right-sidebar tested behind the same slot model (PRD §4.12, open Q9) (2026-06-12: slot table in `src/lib/nav.ts` + Nav/NavShell; both desktop candidates live behind an in-nav toggle, ADR-009 — Q9 stays open until Brandon picks)
- [x] Inbox view (untriaged items) (2026-06-12: explicit `items.inbox` flag set by quick capture, cleared by triage, ADR-010; `/inbox` with retype/triage/trash controls; live count badges the nav slot)
- [x] Per-type lists with simple filters (2026-06-12: `/tasks` `/meetings` `/notes` `/links` `/entities` + tab strip, filters carried in the URL over the `ViewFilter` shape a future `views` row stores, ADR-011; Tasks nav slot; Trash stays on `/items`)
- [x] Full-text search (Postgres FTS) filtered by type/entity/date (2026-06-12: `websearch_to_tsquery` over the stored tsvector with `ts_headline` snippets, `/search` + `GET /api/search`, ADR-012; Search nav slot, Ctrl/Cmd+K)
- [x] Quick capture (global affordance, desktop shortcut, title-only) (2026-06-12: nav "New" button + `q` shortcut open the capture modal — title, type, optional due/urgency, always `inbox: true` — ADR-013; share target rides the PWA-shell slice)
- [x] Backlinks panel (traverse `relations` both directions; suggested vs confirmed render) (2026-06-12: `RelatedPanel` on every item canvas — grouped by type, confirm/reject on suggested edges, "+ Relate" typeahead; first relations write path `POST/PATCH/DELETE /api/items/[id]/relations`, ADR-015; entity-at-capture joined the capture modal)
- [x] Soft delete + Trash (30-day purge); revision snapshots + restore (2026-06-12: cascade soft-delete restores as a unit, daily purge cron at `/api/machine/purge`, debounced snapshots capped at 50; Trash *UI* comes with the list views slice)
- [x] PWA shell (installable, responsive) (2026-06-12: manifest + generated icons + conservative SW — shell/static only, never item data, offline fallback page, pinned-cache seam for Pulpit Ready — + GET share target landing URLs as link items and text as tasks, all `inbox: true`, ADR-016; verified on a production build incl. offline navigation and all three share shapes; Brandon installs on his devices)
- [x] OneDrive export (nightly cron + on-demand; `/Export/{type}/{year}/{slug}-{id8}.md` + YAML frontmatter; archive path) (2026-06-12: engine + Graph app-only target + 06:30 UTC cron + `POST /api/export`, ADR-017; 21/21 checks against Neon with the local target; Azure app registration completed 2026-06-12, OneDrive end-to-end live)
- [x] Pulpit Ready action (immediate export + verified offline pin + print-styled PDF) (2026-06-12, ADR-019: self-contained /items/[id]/print render pins into ledgr-pin-v1 under both URLs, cache.match-verified “cached ✓”, print-to-PDF via @media print; OneDrive export leg live after Azure registration 2026-06-12)
- [x] `/health` endpoint (DB, last export timestamp; Todoist/Graph once they exist) (2026-06-12: reports `lastExportAt` — clean runs only — and `lastExportRunAt` from `job_state`; DB check since slice 1; Todoist/Graph checks ride their Phase 2 slices)
- [x] Structured JSON logging + correlation ids; toggleable debug mode (2026-06-12, ADR-020: shared logger, error_log capture surfaced in /health, DEBUG_MODE env toggle)
- [x] Weekly `pg_dump` to OneDrive; restore tested once before Phase 2 (2026-06-12, ADR-021: GitHub Actions weekly dump → artifact now + OneDrive after §1b; restore-test job ran green, all table counts matched)

**Not in Phase 1:** integrations (calendar/Todoist/email), view builder, embedded query views, widget dashboard, MCP server, Build surface (custom-type builder, workflow/wiki templates), sharing.

---

## Phase 2: Integrations + sharing

- [x] Microsoft Graph auth: interactive delegated OAuth (MFA) + app-only client credentials for unattended jobs; mailbox-scoped via Application Access Policy (2026-06-13, ADR-022: shared `src/lib/graph/client.ts` app-only token/fetch, `onedrive.ts` refactored onto it, `/health` `checks.graph` canary; interactive delegated = the existing Clerk+Microsoft sign-in, no new refresh token; 14/14 in `verify-graph-auth.mts` incl. a live token grant; **mailbox `Calendars.Read` + Application Access Policy ✅ granted + verified live 2026-06-19, runbook §1c**)
- [x] Calendar sync (poll next 14 days, default 6h via GitHub Actions + "sync now"); auto-create meeting items; `ms_event_id` dedupe; reschedule/cancel handling (2026-06-13, ADR-023: `calendarView` per-occurrence meetings via the slice-21 app-only client, `CalendarSource` interface, full-window poll, write-only-on-change, cancel flags `properties.calendar.canceled` (never deletes), attendees stored structured + FTS-searchable, calendar arrivals skip the Inbox; `GET /api/machine/calendar-sync` + `POST /api/calendar/sync` + 6h GitHub Actions; 24/24 in `verify-calendar-sync.mts` + a live 403-clean Graph probe; **✅ LIVE 2026-06-19: §1c granted + `LEDGR_CRON_TOKEN` repo secret set; calendar syncing against the live mailbox (`lastCalendarSyncAt` clean)**)
- [~] Matchers config + engine (attendee email → series id → title regex → `pg_trgm` fuzzy); setup wizard sampling; learn-by-confirmation; suggested/confirmed states (2026-06-13, ADR-024: `matchers` table + `pg_trgm` (migration 0004), engine with fixed kind precedence + fuzzy-as-last-resort gating + confirmed-wins accumulation, `addMatchEdge` never downgrades a confirmed edge, runs on meeting create via a `onCreated` seam, minimal `/api/matchers`; 16/16 in `verify-matchers.mts` against real pg_trgm. **Engine + config + suggested/confirmed states done. §1c landed 2026-06-19 (live calendar), so the setup-wizard + learn-by-confirmation UIs are now UNBLOCKED; the remaining work is UI code (not a Brandon-step). See next_steps.** **Rerouted 2026-06-27 (ADR-123):** the rule SOURCE moves from this `matchers` table onto **templates** (`templates.match_config`), and "learn by confirmation" becomes pin-as-a-template-rule, paired with an always-on email+fuzzy-title person suggester. The matchers table/engine go dormant. EM1 (schema + rule source) shipped; EM2–EM4 build the suggester, auto-apply, and pin UX. See next_steps "Auto-match events to people via templates".)
- [x] Meeting prep templates (open tasks for the person, last 3 meetings, agenda headings; action-item → task promotion) (2026-06-13, ADR-025: live canvas panel — `getMeetingPrep` assembles the related person's open tasks + last 3 meetings + default agenda deterministically from confirmed edges, reusing `viewItemsQuery`; `promoteActionItem` turns an action item into a task related to the meeting + its people, flowing back into prep; not body-seeded. 10/10 in `verify-meeting-prep.mts`. **Panel UI built + wired; §1c now live (2026-06-19), so a real meeting-with-person can be eyeballed.**)
- [x] Todoist sync: push dated tasks, completions sync back (webhook + polling fallback), inbox pull-in (offline capture), recurrence delegated to Todoist, conflict rule (Ledgr canonical) (2026-06-13, ADR-026: `src/lib/todoist/` engine behind a `TodoistClient` interface — push dated tasks, three-way due reconcile (Ledgr-canonical content, Ledgr-wins-on-conflict, Todoist-only date syncs back), completion both ways, lost-due unlink, inbox pull-in; HMAC-verified webhook triggers an idempotent sync + 3h polling backstop; `/health` canary. 14/14 in `verify-todoist-sync.mts` (stub client). **Live path needs Brandon-steps: `TODOIST_TOKEN`, `TODOIST_CLIENT_SECRET`, webhook registration, `LEDGR_CRON_TOKEN` — runbook §1d.** Recurrence-occurrence completion logging (Sync API) deferred.)
- [x] Email-in (Outlook "Ledgr Import" folder via Graph `messages/delta`; mark-read + move; note/`task:` prefix; attachments to R2) (2026-06-13, ADR-027: `src/lib/email/` engine behind a `MailSource` interface — `messages/delta`, HTML→text-paragraph BlockNote (no parser dep), `task:`→task/else note, `inbox:true`, `properties.email.messageId` dedup + move-out, delta token in `job_state` (advances only on clean run), attachments via the new server-side `storage.putObject`; 30-min cron + `POST /api/email/import`; `/health` canary. 20/20 in `verify-email-in.mts` incl. a real R2 putObject roundtrip. **✅ LIVE 2026-06-19: `Mail.ReadWrite` granted + `Ledgr Import` folder created; email-in importing (`lastEmailImportAt` clean).**)
- [x] View builder (custom views + layouts: list/table/board/calendar/agenda) (2026-06-13, ADR-029: stored View Definitions reuse the existing `ViewFilter`/`ViewSort` jsonb shapes + a `ViewGrouping`; one `ViewRenderer` dispatches the five layouts; owner-scoped CRUD store, system views immutable; builder UI + `/views`; 25/25 in `verify-views.mts`)
- [x] Interactive embedded query views (editable filter, inline edit/check-off, create-inherits-filters, remove = un-relate) (2026-06-13, ADR-030: an interactive panel on entity canvases over a shared `GET /api/items/query`; check-off PATCHes optimistically, add creates+relates to the host, remove un-relates without deleting; 7/7 in `verify-embedded-views.mts`)
- [x] Widget dashboard (drag-and-drop View-Definition cards; item-count-driven heights with equal-height option; badge counts; fill-screen desktop, vertical-scroll mobile; PRD §4.11) (2026-06-13, ADR-031: a nullable `views.dashboard_order` is the whole config (migration 0005); native HTML5 drag-reorder, no DnD lib; `countViewItems` badges share `viewWhere`; equal-height in localStorage; `/dashboard` distinct from Today; 9/9 in `verify-dashboard.mts`. In-browser drag check pending.)
- [x] Push notifications (morning agenda, meeting-prep-ready) (2026-06-13, ADR-034: Web Push hand-rolled over `node:crypto` (VAPID JWT + RFC 8291 encryption, no `web-push` dep) behind a `PushSender`; `push_subscriptions` table (migration 0006), Gone-prunes-self; daily agenda on the Vercel cron + hourly prep-ready on GitHub Actions, both over the machine-token door; SW v3 push/notificationclick; `PushToggle` on Today; `/health` canaries. 25/25 in `verify-push.mts` incl. an encryption round-trip + JWT verify. **✅ VAPID keys set 2026-06-20; push configured (PushToggle live on Home).**)
- [x] Public share links (read-only, print-friendly, PDF download) (2026-06-13, ADR-035: an unguessable 192-bit token → the shared print render with no Clerk on the path; `share_tokens` table (migration 0007), revocation is a `revoked_at` stamp; the print document shell is now shared by `/items/[id]/print` and `/share/[token]`; CDN-cacheable with ~60s revocation, `noindex`; `ShareLink` control on every canvas. 18/18 in `verify-share.mts`; `verify-print` still 23/23.)
- [x] Provider-interface discipline confirmed for auth + scheduler (keeps Phase 4 cheap) (2026-06-13, ADR-036: audit found no gaps — Clerk confined to 4 seam files, storage to `src/lib/storage`, all 9 machine endpoints token-gated so any scheduler (Vercel cron / GitHub Actions / local cron) reaches them identically, DB portable via Drizzle. Enforced by `verify-provider-seams.mts` (8/8) so a future boundary break fails loudly; documented in runbook §8.)

**Possible late-Phase-2 ride-along:** the Meetings module's manual "Add-to-template" slice (from planning rhythms).

---

## Phase 3: Build surface + templates + Claude layer + planning rhythms

> **⚠️ Reordered 2026-06-13 (Brandon).** After Phase M, **building the UX (the Build surface) is the next priority** — moved to the top. Templates follow from those UX decisions. The Claude layer (MCP, scheduled tasks) and Notion migration moved **later**. Planning rhythms stay last, alongside the parked items.

**Tier 1 — Build the UX (next priority after Phase M):**
- [x] Build surface shell (Work/Build toggle in the main menu; PRD §4.10) **(2026-06-13, ADR-044: a standalone floating Build-mode button — Brandon's design over a nav toggle — with path-derived on/off state, plus a `/build` home surfacing the building blocks: Types & properties, Views, and "soon" placeholders for Templates/Workflows + Work-surface wiring.)**
- [x] Configurable nav slots from Build (the middle nav becomes owner config, not the hardcoded `NAV_SLOTS`). **(2026-06-15, ADR-056, Tyler: `navSlots`/`mobileNavSlots` on `users.settings` (destination or tools-group slots, max 5 / mobile max 4; Home/New/More locked); shared icon library `src/lib/nav-icons.ts`; live editor on `/build/surface` (preview + native drag-reorder + icon picker + Desktop/Mobile split). Closes the "nav-slot config deferred to its own slice" note below. `verify-nav-and-deletes.mts` 32/32.)** Also this slice: **in-context delete confirmation is now the app standard** (`ConfirmButton`, replacing `window.confirm`) and **a type's delete can take its items too** (`deleteTypeWithItems`, `?withItems=1`) — ADR-057.
- [x] Build-mode restructure: a fixed left sidebar (DATA/INTERFACE/MAINTAIN), `/build` home → System (Model) Overview, universal ⌘K command palette, "Build tools" in the Work-nav picker. **(2026-06-15, ADR-063, Tyler: `src/lib/build-nav.ts` is the hardcoded sidebar taxonomy + single source of truth for the picker category + palette section index; `BuildSidebar.tsx` (Types dropdown, glowing "Build Mode" wordmark, Back to Work, mobile drawer) swaps in for the Work bar in `/build*`; `/build` rewritten as the counts + needs-attention overview; `CommandPalette.tsx`/`command-index.ts` (destination|action union, context-aware ranking) replaces `SearchModal`; `/build/views` producer/manager split from the Work-side `/views` consumer; `/build/{hygiene,import,claude}` stubs. No migration. `verify-build-mode.mts` 31/31. The "two modes, one rule" principle is now in CLAUDE.md.)**
- [x] Custom type & property builder UI (writes `types.property_schema`; resolves custom-type identity, open Q6). **Includes the type/kind authoring + selection UX.** **(2026-06-13, ADR-044: `src/lib/types.ts` registry store + `/api/types{,/[key]}` + `TypeBuilder` over `/build/types`; `property_schema` is an ordered `PropertyDef[]` (text/number/date/checkbox/url/select/multi_select); custom properties render + edit on the default canvas via `CustomProperties` (`MarkdownCanvas` is now async). Migration 0008 adds `show_in_quick_capture`. The parked type/kind UX (`explorations/type-and-kind-ux.md` §1–3) all landed: entity Kind is a dropdown of existing kinds + "New kind…", quick capture is data-driven + opt-in, the capture relate-picker is renamed "Relate to…" with an inline entity-kind picker. Q6 working resolution: types are first-class/creatable, entity kinds stay a reused vocabulary (not auto-graduated). User types need no `modules.ts` change — they fall back to the default markdown canvas. `verify-types.mts` 37/37.)**

**Tier 2 — Templates (follow from the Build-surface UX decisions above):**
- [x] Workflow & wiki templates ("New Workflow"/"New Wiki" guided creation → type + properties + views; on-the-fly tweaks; wire into Work as widget/nav slot; retire = archive, never delete; PRD §4.14) **(2026-06-13, ADR-046, slice 35: a generator — not a stored entity, so its output is ordinary types + views and "retire" is just unpin. `src/lib/structure-templates.ts` pure `planWorkflow`/`planWiki` + presets + impure `applyStructurePlan` over createType/createView/pinView; `POST /api/build/structures`; `/build/new` chooser + `StructureBuilder`; `/build/surface` pin/unpin page; both `/build` "soon" cards now real. Enabled by a contained view-engine extension — board grouping by a custom select property (the workflow's "Stage"): pure helpers in `src/lib/view-grouping.ts`, `ViewGrouping` widened to `{propertyKey}`, column order from the type's options. "Wire into Work" = the dashboard-widget pin (built); nav-slot config deferred to its own slice. `verify-structures.mts` 38/38, `verify-views` extended; in-browser board-by-stage confirmed.)**
- [x] Per-type item templates (task/meeting/note templates storing property choices + starter canvas content; the meeting-prep template of Phase 2 becomes one instance; Brandon, 2026-06-12) **(2026-06-13, ADR-045, slice 34: owner-scoped `templates` table (migration 0009) keyed to a type, storing a `{format,text}` starter body + `property_defaults`; `src/lib/templates.ts` store + `createItemFromTemplate` (rides the item-create path, `inbox:false`); `/api/templates{,/[id],/[id]/apply}`; `TemplateBuilder` over `/build/templates` reusing the Tiptap editor in controlled mode + a property-defaults editor off the type's schema; template-aware "+ New ▾" menu via a self-fetch in `NewItemButton` (zero page churn). The Phase-2 meeting-prep agenda is now an expressible instance. `verify-templates.mts` 27/27; in-browser end-to-end (build → list → apply seeds the default) confirmed.)** **REDESIGNED 2026-06-20 (ADR-093, TPL1–TPL5): templates are now real prototype items applied by `cloneItemSubtree` (carry full body/subtasks/properties/relations), authored in the normal canvas, with `{{token}}`/`{{ask:}}` variables + date rules, apply-to-existing (fill/overwrite), a per-type default + chooser on "+ New", MCP parity, and a `new-from-template` dashboard widget. Reverses ADR-045's config-table shape. `verify-templates` 105/105.**

**Relations chunk — typed relation properties + create-on-miss (ADR-067, core; both agreed):** un-defers the `relation` property kind and makes inline create-on-miss the way you add links without leaving the item. Phased R1–R4. **✅ DONE (2026-06-15, branch `relations-typed-properties`); in-browser eyeball confirmed 2026-06-17.**
- [x] **R1 — the `relation` property kind (data model + builder).** Add `relation` to `PROPERTY_KINDS`; extend `PropertyDef` with `targetType` + `cardinality` (`single`|`many`) and validate them; the `TypeBuilder` authors a relation field. Values store as `relations` edges with `role` = the field key (not `items.properties`). Any type, user-added. **(Done: `PROPERTY_KINDS` now 8; `parseRelationTarget` validates shape-only + defaults (targetType null=any, cardinality many); relation field keys can't be the reserved roles `mention`/`related`; `TypeBuilder` renders a target-type dropdown + cardinality select inline per property row, fed by `availableTypes` (listTypes) from both type pages; `CustomProperties` skips relation kinds. `verify-types` extended, all PASS.)**
- [x] **R2 — the typed relation input on the canvas.** A relation field renders as a link box (typeahead filtered to its `targetType`, chips, cardinality enforced app-side) reading/writing edges over the relations API. Create-on-miss here is **eager and typed** (the box knows the type, e.g. Author → person). **(Done: `RelationField` (client) + `RelationProperties` (server, batches initial values via the new directional `outgoingRelationsByRole`) render after the scalar Properties panel; `relations.ts unrelateItems` + the DELETE endpoint gained a role param so a field clears only its own edge; single-cardinality replaces. `verify-relations-typed` PASS.)**
- [x] **R3 — the `unmarked` placeholder type + generic create-on-miss.** Seed a hidden `is_system` `unmarked` type with a glyph label (migration + `seed.mjs`). Free-text `@`-mention (`mention-suggestion.ts`) and the generic + Relate picker (`AddRelation`) gain a "Create '<name>'" row that makes an `unmarked` item with `inbox = true`, links it, and stays in place. Inbox triage = retype + clear inbox. **(Done: migration 0018 + seed.mjs insert the hidden `◌` type (applied to Neon); create-on-miss in `AddRelation`, `mention-suggestion` (mention edge stays body-owned — only the item is created + the mention node inserted; the save-time diff-sync writes the edge), and untyped `RelationField`. Typed fields create their `targetType` (no Inbox); untyped → `unmarked` + Inbox.)**
- [x] **R4 — Related panel polish.** Show unmarked related items under the glyph group; surface the relation field's `role` as a section heading (Attendees, References) where one is set. **(Done: `RelatedPanel` renders the host type's relation fields as labeled sections (schema order, authoritative `outgoingRelationsByRole` set, role-scoped removal threaded through `RelatedRow`/`RelationActions`), then the plain type groups with `unmarked` under its glyph and sorted last.)**

**Tier 3 — Claude layer + migration (moved later):**
- [x] MCP server (search/read/create/update items, list by entity/date; personal API token) **(2026-06-13, ADR-047, slice 36: a hand-rolled Streamable-HTTP endpoint at `POST /api/mcp` — no SDK dep (Principle 5, the ADR-034 hand-roll precedent), stateless, Clerk-bypassed and gated by the existing machine-token scheme with a new `mcp` scope (owner resolved by UPN like the other machine jobs). Six tools — `search_items`/`list_items`/`get_item`/`create_item`/`update_item`/`list_types` — each a thin wrapper over the libs the REST API uses, so the MCP surface can't drift from the app contract or skip owner-scoping; create/update reuse `parseItemPayload`. Pure protocol split from impure tools/dispatch (the M5/M6 discipline); `parseItemPayload`/`asUuid` moved to a node-safe `item-input.ts`. `/health` `checks.mcp` canary. `verify-mcp.mts` 50/50 against Neon + an in-browser HTTP smoke of the real route (handshake, 202/405/400, create→get round-trip). Setup: runbook §1f.)** **Extended 2026-06-17 (ADR-071): `propertyPatch` on `update_item`, relation `targetType`/`cardinality` in `list_types`, and 6 new tools (`relate_items`/`unrelate_items`, `list_views`/`run_view`, `list_templates`/`apply_template`) → 12 tools; `verify-mcp` extended, ALL PASS on Neon.** **Templates parity 2026-06-20 (ADR-093, TPL5): `list_templates` now reports `askLabels`/`isDefault`/`applyConfig`; `apply_template` gained `targetId`+`mode` for apply-to-existing (fill | overwrite). `verify-mcp` 71/71.** **Workspace shaping 2026-06-23 (ADR-102): the model can now SHAPE the workspace, not just items — 8 new tools (12 → 20) over the validated Build libs (`describe_workspace` + `create_type`/`update_type` + `create_view`/`update_view` + `create_dashboard`/`add_widget` + `update_nav`), plus the orientation guide as an MCP `resources` capability (`src/lib/mcp/guide.ts`). Additive; safety in the parsers; no config delete tool. `verify-mcp` 101/101 on Neon + a live `/api/mcp` HTTP round-trip.** **OAuth shim 2026-06-26 (ADR-117): a stateless OAuth 2.1 layer (discovery + DCR + PKCE auth-code + refresh, all HMAC-signed via `LEDGR_OAUTH_SECRET`, no DB/dep) so the MCP can be added as a custom connector in claude.ai web + the Claude mobile apps (consumer connectors are OAuth-only). `/api/mcp` accepts an OAuth access token alongside the static `LEDGR_API_TOKENS` bearer (additive; Claude Code/Desktop untouched). `verify-oauth-mcp` 37/37 + a full live discovery→authorize→token→MCP round-trip.**
- [x] Scheduled Claude tasks (morning briefing, weekly health check) over the same API **(2026-06-14, ADR-052, slice 37: the deterministic half. The "morning briefing" already exists as the daily agenda count-summary push (slice 30); this adds the missing sibling, the weekly health check — `GET /api/machine/health-check` + `.github/workflows/health-check.yml` (weekly Mon 13:00 UTC). `gatherHealth()` extracted to `src/lib/health.ts` so the job reads the same `/health` canaries in-process; a pure `evaluateHealth` applies fixed rules (DB-down→critical, 7-day captured errors, configured-failing Graph grant, a *stalled* — not merely unconfigured — cron) and pushes Brandon only on failure (PRD §6.2), recording findings to `job_state`/`checks.healthCheck` (never `error_log`, no feedback loop). No model in the loop — Principle 3 holds; an AI-authored briefing would be a separate principle-amending decision. `verify-health-check.mts` pure evaluator 15/15; tsc + build clean; DB-backed half pending a live Neon run.)**
- [ ] Selective Notion migration (full export to OneDrive archive first; import active items; map relations; share-to-app)

**Tier 4 — Planning rhythms + parked items (last):**
- [ ] Planning rhythms (configurable rituals; deterministic modules; AI-assembled agenda is the only model step)
- [x] Inline action-item → linked task: promote a body line/bullet to a task whose title is the line text, linked back to the meeting and to that line (`explorations/block-linked-action-items.md`; Brandon, 2026-06-13). **DONE (2026-06-18, ADR-090):** Obsidian-style trailing `^id` block anchors (rides as plain text, stripped from share/print/export/FTS, dimmed in the editor) + per-line hover "→ task" on checkbox lines → popup pre-filled with the line text (title) and its de-indented sub-bullets (body) → task `inbox:false` related to meeting+people, storing `properties.source.blockRef`. `ensureAnchor`/`scrollToBlockId` primitives shipped; the read-side "✓ task" badge (links to the task, suppresses re-promote), the `#^id` deep link (scroll + flash), a "Copy link to this line" toolbar button (any line), and a task→source back-link all shipped the same session — public-share `#^id` anchors are the only remaining follow-up. Resolves the ADR-037 note (markdown has no native block ids → the `^id` marker is the markdown-friendly mechanism). verify-block-anchor 32/32 + verify-meeting-prep 11/11; in-browser on live Neon.
- [x] Richer dashboard widgets + flexible layout: widget types beyond the list preview (layout-faithful cards, stat/count cards), per-widget settings, resizable/draggable grid. **DONE (2026-06-15, ADR-064/065, slices 1–7):** customizable dashboards — multiple named, resizable/draggable (react-grid-layout v1.5.3) grids of widgets (view-compact/view-faithful/stat/action kinds, display-only gear settings), a dashboard-level focus that re-scopes all widgets, the dashboard as a first-class nav destination, and Set-as-Home/Today assignment (`/` + `/today` render an assigned dashboard, fixed Today layout as fallback). Supersedes the single-dashboard ADR-031 (its `views.dashboard_order` model retired). Views gained an "in use as a widget" badge + filter. `verify-dashboards` 19/19; migrations 0016/0017. **In-browser eyeball confirmed 2026-06-17.** (`explorations/dashboard-widgets.md` + `flexible-surfaces.md` Part 1 realized; Part 2 — prose blocks interleaved with widgets — still open.) **Canvas extension SHIPPED (2026-06-27, ADR-111 DC1–DC4 built + a nested-list widget, ADR-120, non-core/solo):** per-widget appearance (header/border/background/accent/view-mode collapse), a dashboard "stage" (full-bleed color/gradient/image background + scrim/blur + title/density — `dashboards.appearance`, migration 0034; video + byte-upload deferred behind the parser seam), any-item `embed` widget (sticky notes), tab/stack/section `container`, and a `tree` (nested-list) widget — N parents from a view, each with its children by `parent_id` hierarchy OR a relation role. `verify-dashboard-canvas` 26/26 + `verify-dashboards` 19/19; tsc/eslint/`next build` clean. MCP `add_widget` enum pinned to the base 4 kinds (contract held). DC5 (journal/daily-note mode) still deferred. Plan: `explorations/dashboard-canvas.md`.
- [~] Flexible interactive surfaces (Brandon first-use feedback, 2026-06-14; `explorations/flexible-surfaces.md`). Two scales: **(near-term, self-contained) per-view column/property visibility — DONE (2026-06-14, ADR-049):** a `columns` jsonb on the View Definition (migration 0010) + a `ViewBuilder` picker + `ViewRenderer` (list/table/agenda) honoring it, with property-column labels resolved from the type schema; answered the "items list only shows the created date" pain. **(future, still open) custom composable pages with interactive embeds** — entry forms, action buttons, property-chosen embedded views — which should converge with the dashboard-widgets engine rather than land piecemeal. **Also DONE (2026-06-17, ADR-072): list-filter by a custom select/multi_select property** (`ViewFilter.propertyFilters`, index-backed `properties @>`, in `ViewBuilder` + the `/tasks` and `/list/[type]` bars) **and kanban drag-and-drop** (a native-HTML5 `BoardDnd` over the pure `boardDropPatch`, gated by `ViewRenderer.boardDraggable` so dashboards stay read-only). **Mobile touch drag (long-press): K1 DONE 2026-06-19** (isolated touch path beside the untouched mouse HTML5 path, no DnD dep — `useBoardTouchDrag` + pure `board-touch-drag.ts`; non-passive touchmove, ~400ms arm, rect hit-test; `verify-touch-drag` 19/19, browser-verified drop+PATCH + scroll/tap no-ops). **K2 DONE 2026-06-19:** floating drag ghost (clone follows the finger) + proximity-ramped edge auto-scroll (pure `edgeAutoScrollVelocity`); `verify-touch-drag` 27/27, browser-verified ghost-follow + 192px auto-scroll. **Mobile kanban drag complete (K1–K2);** real-device feel is the only remaining eyeball.
- [x] Templates that preset people/relations (Brandon first-use feedback, 2026-06-14) **(DONE 2026-06-14, ADR-050: a `relation_defaults` jsonb on `templates` (migration 0010) — `{targetId, role}` edges written on apply via `relateItems`, resilient to a stale target; `TemplateBuilder` "Related items" picker fed by `listEntityOptions`. The "pastors meeting with the usual attendees" ask. The broader `relation` property kind stays folded into `explorations/entity-vs-custom-type.md`; presetting core structured fields (urgency/due-offset) is the noted follow-on in `explorations/template-property-defaults.md`.)**
- [x] Item-view UX push (Brandon, 2026-06-16/17). **Feature A — inline editing (DONE 2026-06-16, ADR-068, non-core):** hover-pencil to fix a related item's title in place, right-click Rename / Edit-in-builder on type + field labels (labels-only "punch through the line"), × to clear a property value. **Feature B — arrangeable, field-level, per-type item canvas layout (DONE 2026-06-17, ADR-069, CORE):** the default `MarkdownCanvas` becomes a free 2D `react-grid-layout` grid, every field/property/relation its own card (body/title/Related stay single blocks), stored per type in a new nullable `types.canvas_layout` jsonb (migration 0019; null = today's stacked render, never forced), per breakpoint (arrange `lg`, sm single-column view-only), each card flow (auto-height) or fixed. Pure `src/lib/canvas-layout.ts` + `verify-canvas-layout` 36/36; arrange via full-page `?arrange=1`; `PATCH /api/types/[key]/layout`; per-key `propertyPatch` jsonb merge so per-property cards don't clobber. **Realizes PRD §4.13's per-type field placement** (subsumes the planned "B2") and is the item-canvas slice of `explorations/flexible-surfaces.md`. Grouping/collapsible cards + bespoke-canvas arrangeability deferred. In-browser cycle verified against live Neon.

**§1c landed 2026-06-19, so this is now unblocked (UI dev work, not Brandon-step-gated):** the deferred matcher setup-wizard + learn-by-confirmation UIs (see Phase 2).

**Editor parity / completeness (carried from the M3 markdown cutover, ADR-040 — runs parallel to the tiers, not tier-ordered):** the Tiptap editor was stood up with the core path (text, headings, lists, colors, mentions, task-list checkboxes) but a few BlockNote-era affordances didn't get ported. The R2 attachment infra (`/api/attachments`, presign, `src/lib/attachments.ts`, the storage `putObject` interface) is all intact, so these are re-wiring jobs against an existing backend, not rebuilds.
- [x] **Inline image paste — REGRESSION fix.** Was Phase-1-complete (paste image → R2 → inline), dropped by the M3 cutover; the `markdown-editor/` components have zero paste/drop/image handling. Re-wire a Tiptap paste/drop handler onto the existing R2 presign flow and insert a markdown image (`![](url)`). Brandon confirmed it no longer works (2026-06-13). **(2026-06-15: done. `uploadImage` re-wires the proven `/api/attachments` presign → R2 PUT handshake; Tiptap `handlePaste`/`handleDrop` + a toolbar Image picker upload and insert `![](url)` at the cursor, only when an image is present so text paste falls through; controlled hosts pass no uploader. `LedgrImage` (`@tiptap/extension-image`, inline) binds the markdown hooks. `tsc`/`next build`/eslint clean; `verify-editor-media.mts` 17/17; in-browser eyeball confirmed 2026-06-17.)**
- [x] **Tables.** GFM table syntax (`| col |`). Server render is **already covered** — markdown-it's default preset has GFM tables on (`markdown-render.ts`), so this is **editor-side only**: the first-party `@tiptap/extension-table` (+ row/cell/header nodes), a toolbar insert/edit affordance, and CSS. Low resilience risk (markdown is canonical; tables serialize to standard GFM, no data lock-in; same `@tiptap/*` vendor already in use). The real work is verifying `@tiptap/markdown` round-trips a table to GFM and back identical. (Brandon, 2026-06-13.) **(2026-06-15: done. `LedgrTable` (`@tiptap/extension-table`) binds `renderMarkdown`/`parseMarkdown` — parse rebuilds the row/cell subtree from marked's single table token, render flattens JSON back to GFM via the pure `tableToGfm`; toolbar Table insert (3×3 + header). Round-trip proven through real `marked` in `verify-editor-media.mts`; full in-editor round-trip confirmed in-browser 2026-06-17.)**
- [ ] **Paste / embed documents (files).** BlockNote had file blocks; the markdown canvas doesn't. Decide the markdown representation (a link to the R2-stored file vs an inline embed) and wire paste/drop → R2 → markdown. (Brandon, 2026-06-13 — recorded for later; lower priority than image paste.) **Open direction (Brandon + Tyler, 2026-06-15):** prefer **converting docs to markdown on ingest** (pandoc-style) over storing heavy binaries (e.g. large PDFs) in R2 — keeps content searchable/portable and fits the ~$0/quota goals. Needs its own small design pass (what converts deterministically, what happens to the original binary, scanned PDFs).
- [x] Minor: a **strikethrough toolbar button** (Strike already round-trips via StarterKit; just no button). **(2026-06-15: added alongside the image/table work.)**
- [x] **Floating table of contents** (new, not BlockNote parity; Brandon, 2026-06-26). A Notion-style outline auto-built from the body's H1–H3: a right-edge hover rail on desktop, a button→bottom-sheet on phone/modal, with active-section tracking and click-to-jump. Per-type on/off + levels in Build → Types, stored in `users.settings.tocByType` (no migration). **(2026-06-26, ADR-114: done — `FloatingToc` mounted once in `ItemCanvas`, reads the live `.ledgr-prose` DOM; tsc/eslint clean; browser-verified page/mobile/modal + the Build panel. PR pending.)**

**Build-meeting decisions (2026-06-14 Brandon + Tyler) feeding Phase 3 + the post-foundation modules — tracked in `next_steps.md` "Build-meeting follow-ups":**
- [ ] Per-property **"include in share?"** toggle; Share/Print renders the canvas + opted-in fields (ADR-062).
- [x] Quick Capture defaults to a **catch-all type**, not `task`. **(2026-06-17, ADR-072 — the hidden `unmarked` catch-all, shown "Unsorted"; captures stay `inbox:true` for triage.)**
- [ ] **Notes module attachable to every type**; relations are the primary organizing principle (ADR-061). Post-foundation module work.
- [ ] **Bible/passage relational DB + RefTagger auto-tagger** (ADR-060) — the keystone module; book→chapter→verse, verse atomic. Post-foundation module work.
- Polish carried from the meeting (mouse click-drag canvas scroll, Songs lyrics paste-convert, Papers section drag-and-drop + page-count, highlight color everywhere, chord-width jitter) — see `next_steps.md`.

---

## Toward 1.0 (committed scope + decisions, 2026-06-17)

Brandon's toward-1.0 pass settled four things and lined up one build chunk. Decisions logged as ADR-073/074/075.

**Settled decisions:**
- **Tasks go fully native (ADR-073).** Drop Todoist as the engine; Ledgr owns tasks end to end. Todoist stays an optional adapter behind the `tasks` seam (Tyler keeps it). Reverses PRD §4.6/§5.2 (ADR-037 precedent).
- **Microsoft-only for v1.0 (ADR-074).** Google/iCloud calendar/email adapters deferred post-1.0 (built/proven in Tyler's instance when he adopts). Seam discipline stays. Answers PRD Q7 for v1.0.
- **No confidential tier (ADR-075).** Existing single-user + Clerk + owner-scoped posture is sufficient; declined for v1.0.

**🥇 Native Tasks chunk (ADR-073) — ✅ COMPLETE (2026-06-17, T1–T6, ADR-076–081).** Tasks now fully replace Todoist on Brandon's instance: native recurrence, scheduling/reschedule/overdue-roll, a Top-3 focus layer, reminders via a published ICS feed + web push, offline-outbox capture, and the `tasks` seam (native default, Todoist optional). Pairs with `explorations/calendar-time-blocking.md` (the calendar surface, still open).
- [x] T1 — Native recurrence engine (rule on task; complete → advance next; deterministic; no stacking). **DONE 2026-06-17, ADR-076.** Model (C) per-date completion log; first-class `scheduled_date` column (migration 0021); hand-rolled RRULE subset (no dep, Principle 5); virtual/materialized occurrence modes via the shared `cloneItemSubtree` primitive; fixed vs after-completion anchor; recurrence-aware completion in `updateItem`; `RecurrenceControl` + scheduled field on the task canvas. `verify-recurrence.mts` 60+ checks PASS. Un-defers ADR-026.
- [x] T2 — Scheduling + rescheduling. **DONE 2026-06-17, ADR-077.** Reschedule shortcuts + a hand-rolled NL date parser (`nl-date.ts`, no dep) on the scheduled field + capture; a deterministic overdue auto-roll (`scheduling.ts`, rolls effective-overdue non-recurring tasks to scheduled=today) as a Today button + `/api/tasks/roll-overdue` (+ optional machine cron); defer/start = future scheduled hidden from Today. `verify-scheduling.mts` PASS.
- [x] T3 — Daily focus layer ("Top 3" / in-focus). **DONE 2026-06-17, ADR-078.** Day-stamped `properties.focus` marker (no column; `focus.ts`); a "Today's Focus" zone atop Today + a star affordance on task rows; soft cap 3; focused tasks shown once. `verify-focus.mts` PASS. (Follow-ups: canvas/list star, a focus widget, drag-reorder.)
- [x] T4 — Reminders without Todoist. **DONE 2026-06-17, ADR-079.** A published read-only ICS feed (`/api/ics/<token>.ics`, per-owner token in settings, hand-rolled `ics.ts`, no dep): all-day VEVENTs, RRULE passthrough for recurring, default 9am VALARM + per-task override, CDN-cached/Sunday-proof; generate/rotate from User Settings. Web-push half = the existing ADR-034 agenda push. `verify-ics.mts` PASS.
- [x] T5 — Offline/quick capture without Todoist. **DONE 2026-06-17, ADR-080.** An offline outbox (`outbox.ts`, localStorage) — capture enqueues on network failure and an app-wide `OutboxSync` flushes on reconnect/visibility/load; the PWA share-target (ADR-016) already exists. `verify-outbox.mts` PASS + in-browser end-to-end.
- [x] T6 — Seam + retire Todoist on Brandon's instance. **DONE 2026-06-17, ADR-081.** A `tasks` provider seam (`provider.ts`): native default, Todoist opt-in via `TASKS_ADAPTER=todoist`; the Todoist sync routes skip cleanly when native; `/health` `checks.tasksAdapter`; `.env`/runbook/PRD §4.5/§5.2 reversal notes; Todoist code untouched (Tyler keeps it). `verify-tasks-seam.mts` PASS. **Native Tasks chunk (ADR-073) complete.**

**🔧 Tasks Polish chunk — finishing touches on Native Tasks (S1–S6, started 2026-06-18; Tyler greenlit all).** Brandon's polish pass: fully integrate due/scheduled, configurable statuses, a recurrence completions calendar, NL quick-add, relative subtasks. Decisions in `next_steps.md`.
- [x] S1 — Both dates in sort/filter/board + subtask rows. **DONE 2026-06-18.** `scheduledDate` added across the view engine (sort/filter/group/column), the Tasks list **Date (due|scheduled) + When** selectors, and subtask rows (scheduled + due, UTC-correct). Non-core. `tsc`/eslint clean; in-browser verified. **Extended 2026-06-24 (ADR-109, CORE/additive):** a first-class **`plan`** date = `COALESCE(scheduled, due)` across the same four field enums, made the **default** for tasks (filter window, calendar/agenda placement, builder) so the whole app is scheduled-primary/due-secondary and never breaks on a missing due; the `/tasks` list + the Overdue/Today/Upcoming starter widgets now key off `plan`. **Also restored** the per-type DnD "Customize layout" on the task type (regressed by ADR-108): `TaskCanvas` delegates to `MarkdownCanvas`'s grid when arranging or a `canvas_layout` is saved, else the compact rail (default). `verify-views` extended green.
- [x] S2 — Configurable statuses (category-backed, per-type + inherit) + done-checkbox/glyph. **DONE 2026-06-18, ADR-082 (CORE).** `items.status` → text key + denormalized `status_category` enum (migration 0022); per-type `types.status_schema` (null = inherit); `StatusSchemaEditor` + `/api/types/[key]/statuses`; done-checkbox via `/api/items/[id]/complete`; kanban shows colored custom columns; `/tasks` + view filters category-aware. `verify-statuses` 26/26. **Extended 2026-06-24 (ADR-106):** a per-type **display mode** (`types.status_mode` = none/checkbox/select, migration 0032) over this same category plumbing — `task` defaults to a done/undone checkbox, multi-status is opt-in, and the view builder hides Status for `none` types. Presentation only; the category stays the source of truth. `verify-statuses` now 35/35.
- [x] S3 — Recurrence completions calendar + occurrence carve-out (month grid toggles the per-date log; "edit this occurrence" carves a detached copy). **DONE 2026-06-18, ADR-083.** `RecurrenceCalendar` (virtual-only) + `POST /api/items/[id]/occurrence` `{toggle|carve}`; pure log helpers in `recurrence.ts`; carve = detached `cloneItemSubtree` one-off, series skips + advances. Non-core. `verify-recurrence-calendar` 39/39 + in-browser.
- [x] S4 — NL quick-add: parse dates + recurrence + urgency from the task title (strip-and-confirm). **DONE 2026-06-18, ADR-084.** `parseTaskTitle` in `nl-date.ts` (date→scheduled, `by <date>`→due, recurrence→RRULE, p1-4/!1-4→urgency); CaptureModal live Detected preview + ✨ Apply + parse-on-save. Non-core. `verify-nl-task` 36/36 + in-browser.
- [x] S5 — Relative subtasks: stored offset (N days after parent scheduled), set by calendar-pick; recomputed per occurrence. **DONE 2026-06-18, ADR-085.** `properties.relativeSchedule.offsetDays` + derived `scheduled_date`; `SubtaskSchedule` control back-calculates the offset; `recomputeRelativeChildren` on parent-date change / recurrence advance / materialize. Non-core. `verify-relative-subtasks` 26/26 + in-browser.
- [x] S6 — Loose ends: focus stars on canvas/list/board + Top-3 widget; per-task reminder picker; ICS EXDATE; "planned" marker. **DONE 2026-06-18, ADR-086.** ICS EXDATE (drops completed/skipped occurrences); `ReminderControl` (→ `properties.reminder.minutesBefore`); focus star on the item canvas; "planned" badge on the Tasks list; `ViewFilter.focusedToday` + a "Today's Focus" starter widget. Deferred: list/board-row stars, focus-zone drag-reorder, the status popover. Non-core. `verify-ics`/`verify-views` extended + in-browser. **🎉 Tasks Polish chunk (S1–S6) complete.**

**Other committed 1.0 work:** selective Notion migration (Phase 3 Tier 3, above); the §1c-gated matcher UIs (Phase 2); the **alpha → v1.0 production flip** (✅ **DONE 2026-06-26, ADR-115**: migration-caution in force; data is production data. The no-Saturday-deploys rule was later reframed to general production-mindfulness, ADR-119).

**Post-import list ergonomics (from the production data load):**
- [x] **List paging past the 200-row cap** — Load-more + true count + window growth (✅ **DONE 2026-06-26, ADR-116**).
- [x] **Multi-select + bulk item operations** — a reusable selection layer (provider + row checkbox + floating action bar) on every list, with bulk Delete / Set property·status / Move; batch endpoint `/api/items/batch`. "Every list includes multi-select" is now a working convention. Bulk Archive deferred to the future archive feature (✅ **DONE 2026-06-26, ADR-118**).

**🎙️ Meeting recording chunk — Tyler pre-approved 2026-06-18 (`explorations/meeting-recording.md`).** Transcript-as-pivot, upload/paste-only for v1, minutes/action-items via a **Claude-over-MCP workflow** (no in-app LLM call). v1a (paste-first) then v1b (transcription seam + storage). 3 core touches (transcription seam, audio-retention column, Principle-3 interpretation) pre-approved; ADR each at build.

**v1a — paste-first (✅ COMPLETE 2026-06-18, ADR-087):**
- [x] V1a-1 — Data layer: the `transcript` child type (migration 0023 + seed; `minutes` select none/draft/done) + the "Transcripts awaiting minutes" saved view (`scripts/seed-meeting-views.mts`). `verify-meeting-transcripts.mts` 16/16 on Neon.
- [x] V1a-2 — The Transcript panel on the meeting canvas: paste-to-create (writes the child + a confirmed `transcript`-role edge so it's MCP-discoverable), list with minutes badge + word count, opens the transcript for full editing. Classic + grid card. In-browser verified.
- [x] V1a-3 — The Claude-over-MCP minutes automation: `docs/meeting-minutes-automation.md` (the prompt + manual/scheduled use + the meeting↔transcript traversal) surfaced on Build → AI & MCP; the **Principle-3 interpretation** ratified (ADR-087, the core element).

**v1b — convenience transcription (✅ COMPLETE 2026-06-18, ADR-088/089):**
- [x] V1b-1 — the `transcription` provider seam + AssemblyAI adapter (ADR-088, core): null-safe/env-selected, submit+poll, pure response mapping, no SDK; `/health` `checks.transcription`. `verify-transcription` ALL PASS.
- [x] V1b-2 — audio upload (`audio/*`/`video/*`, 2GB cap) + auto-transcribe (ADR-089): `startAudioTranscription`/`advanceTranscription`, client-poll (`TranscriptionPoller`) + cron backstop (`transcription-poll.yml`); fills the diarized transcript → awaiting-minutes view. Compression deferred (a seam knob). `verify-transcription-flow` 20/20.
- [x] V1b-3 — audio retention (ADR-089, core schema touch): `attachments.purge_after` (migration 0024), stamped now()+30d on transcript-produced, reclaimed by the daily purge; `DELETE /api/attachments/[id]` = delete-now. `verify-audio-retention` 10/10.

In-app recording stays deferred. **Live audio→transcript eyeball gated on a configured `ASSEMBLYAI_API_KEY` (runbook §1i)** — pipeline proven against a fake provider + fixtures + in-browser (panel renders, upload hidden without a key, `/health` `transcription: none`).

---

## Unified UI/UX refresh (decided 2026-07-03, full brief `explorations/ui-refresh.md`)

Full-pass refresh across item view, lists/lenses, dashboards, and nav; non-core/solo (token layer + mobile interaction standard get an ADR + a CLAUDE.md convention when they land). UI-only — no schema, no new tables, no route changes. Seven slices:

- [x] S1 — semantic token layer + neutral-ramp variables (light-mode-ready; mechanism only, theme deferred) + type scale / 8px rhythm / quieter borders / badge caps. **DONE 2026-07-03, ADR-141.** Two-tier tokens in `globals.css` (tier 1: `--color-neutral-*` → `--n-*` runtime vars, dark = Tailwind verbatim = zero change; tier 2: `bg-surface-*`/`border-line`/`text-ink-*`/`rounded-card`), `ui-*` type scale, `badgeCount()` (`src/lib/format-count.ts`), `.light` flip + dev proof at `/dev/theme`. tsc/eslint clean; dark pixel-identical + light proof + unstyled-flash check green in dev-auth.
- [ ] S2 — full-width desktop lists + per-lens default columns (reuses ADR-049) + nav search slot + richer rows
- [ ] S2b — desktop peek panel (spike-first): `ItemCanvas` `peek` variant docked to the content region, modal fallback when nav config leaves too little width
- [ ] S3 — item-canvas rhythm: chip field strip under title, empty sections collapse, Rich/Source + arrange into ⋯ menu
- [ ] S4 — shared long-press/right-click `RowMenu`; retire the always-visible per-row Trash button; relative dates
- [ ] S5 — `SwipeRow` (right = complete, left = schedule; no dependency) wired into list + tasks + views list layout
- [ ] S6 — mobile shell: fixed icon-only bottom bar + pull-up launcher grid, bottom-sheet item view, capture sheet, swipeable tab strips
- [ ] S7 — dashboard + home polish: compact mobile stat cards, Recently-Touched cleanup, widget chrome on tokens

---

## Phase 4: Packageable local / self-hosted build (exploratory)

- [ ] Gated on a genuine alternative-deployment motivation (not resilience, already covered by export + Pulpit Ready)
- [ ] Swap/stub external deps behind provider interfaces (Clerk → local single-user, R2 → local FS, scheduler → local cron, Graph/Todoist → off or stubbed)
- [ ] DB is already portable (Drizzle connection-string change)

---

## Later / ideas parking lot
Meeting capture + AI processing (PRD §4.15: Whisper or Teams-transcript + Anthropic API summary and suggested tasks; designed-for now, promoted when PRD Q10 resolves), pulpit mode (large-type distraction-free render), staff accounts (schema ready, product deferred), calendar time-blocking (schedule tasks onto the calendar à la Morgen: manual drag-to-calendar, a deterministic slot-finder + overdue-rescheduler, and an AI "slot N hours over 2 weeks" front-end over the placer; largely core, `explorations/calendar-time-blocking.md`), synced blocks, formulas/rollups, gallery/Gantt layouts, email-out, tiered attachment storage (cold-demotion, gated on a real R2-quota trigger; chosen variant is delete-from-R2-and-rehydrate-from-OneDrive), human-readable item URLs (slug + id, UUID stays canonical so existing links/export/offline keep working; non-core; `explorations/human-readable-item-urls.md`).

---

## Success criteria (PRD §9)
- Brandon stops opening Notion for new items within 2 weeks of Phase 2 completing
- A 1:1 with Roger preps in one click and is conducted entirely in the app
- A sermon is written in the app and preached from it (or its export) at least once
- Zero data-loss incidents; export verified restorable
- Maintenance incidents ≤ 5/year, each resolved under an hour with Claude Code
- Monthly cost stays ~$0
