// Drizzle schema for Ledgr, implemented against schema.md (repo root).
// The data model slice (next_steps.md step 2) defines users, types, items,
// relations, attachments, revisions, views, and error_log here.
// Intentionally empty until that slice.
export {};
