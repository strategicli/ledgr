# COLLAB.md — heads-up board (Brandon + Tyler)

The two-person coordination surface for Ledgr. **Two sections, current state only, overwrite in place — no archive.** Each person keeps their block to "where I am and what I'm doing next" so the other isn't guessing. Pair it with a quick Discord/Telegram ping for anything time-sensitive.

**Where things go:**
- **Plans / availability / "what I'm touching this week"** → here.
- **Decisions** (anything architectural) → `decisions.md` as an ADR.
- **The core-change contract** (what needs both-agree before it merges, and what doesn't) → CLAUDE.md, "Building together." Read it before touching anything foundational.

Rule of thumb: a change to **core** (data model, the canonical body format, the type/canvas model, the module boundary, the provider interfaces, the cross-cutting invariants, the MCP/API contract, the nine principles) needs **both-agree + an ADR**. Everything else, move fast solo.

---

## Brandon — current

- **Availability:** _(e.g. "around this week, evenings")_
- **🟢 Heads-up (Tyler) — Service worker fix v6→v7 SHIPPED (2026-07-09, ADR-152; near-core SW/PWA but a pure correctness bug fix — peek + flag). No migration, no dependency.** Prod's `sw.js` navigate branch was missing a `return`, so hard navigations (direct open/reload of a URL, notification taps) fell through and called `event.respondWith()` a second time → `InvalidStateError: respondWith() already called` at `sw.js:147`, resolving the navigation as a network error. It surfaced on freshly imported notes (the NSX migration) but it's app-wide and pre-existing (shipped with v6); the note bodies are schema-valid. v7 adds the one missing `return` (the four fetch branches are mutually exclusive again) and the `VERSION` bump drops stale shell caches + reclaims clients on `activate`, so installed PWAs self-heal on next load. No change to the caching model (ADR-016), the pinned-cache seam, or push. **For your instance:** if you deployed v6 you have the identical bug — take the one-line fix. Full record: ADR-152.
- **🟢 Heads-up (Tyler) — The Desk BUILT (2026-07-06, ADR-146, branch `desk-workspace`; NON-core/solo — additive surface, peek + flag). One new dependency; no schema, no body-format change, no MCP-contract change.** A desktop-only multi-panel workspace at `/desk` where items, saved views, and (read-only) dashboards open in resizable, tabbed panels, and the arrangement survives closing the app. **The tree is Ledgr's own versioned format** (`src/lib/desk/layout.ts`, fractions not pixels) behind a `DeskShell` seam; **`react-resizable-panels@^2.1.9`** only renders the splits (we don't use its autoSaveId). Note the two-libraries-two-jobs split: RGL still owns dashboards/arrange grids, RRP owns only the Desk's docking tree. **One-writer-per-item** is structural (a client doc store): the focused panel mounts the reused, untouched `ItemEditor`; every other panel is a live read-only `MarkdownPreview` — the save path (debounce/PATCH/revisions) is untouched, the only editor change is an additive optional `onLiveChange` tap. Persistence: live layout in localStorage (`desk:layout`) + a per-device Recent auto-snapshot ring (`desk:recent`); **named workspaces sync via a new `users.settings.deskWorkspaces` key** (parse-with-default like `navSlots`, **no migration**). Reachable from anywhere: the ADR-142 RowMenu and inline @mentions/item-links gain "Open in Desk"/"Open beside" (shared `DeskSendMenu`, desktop-gated). Gated at 640px (below it a plain tab list); registered in the destination picker only (ADR-063 opt-in, no default-slot change). **For your modules:** nothing changes — any item/view/dashboard opens in a panel for free; no data-model, canvas-seam, or MCP touch. One refactor to note: the dashboard per-widget fan-out moved from `DashboardView` into `src/lib/dashboard-resolve.ts` (shared with the panel); the `/dashboards/[id]` page renders unchanged. lint + build clean each slice; dev-auth preview pass across all of S1–S5. Full record: ADR-146.
- **🟢 Heads-up (Tyler) — Collapsible toggle block + collapsible headings SHIPPED (2026-07-05, ADR-145; CORE — extends the canonical body format; you agreed 2026-07-05 before merge). No migration, no dependency.** The markdown editor gains (1) a **toggle block** = a `<details><summary>…</summary>\n\n…\n\n</details>` disclosure, and (2) **collapsible headings** (view-only fold, nothing serialized). **The one core touch:** the body dialect now allows a `<details>` block — rendered by the existing `html:true` markdown-it path (so print/share/export get a native disclosure for free), parsed in-editor by a custom block tokenizer that rebuilds the subtree. **Additive + back-compat:** a body with no toggle is byte-identical; any reader without special handling shows a native disclosure widget. Also new: the **first `/` slash-command menu** (Heading 1/2/3 + Toggle; hand-rolled over `@tiptap/suggestion` like the `{{` menu, its own `PluginKey`), and two **default-on** settings (`collapsibleHeadingsEnabled`, `toggleBlocksEnabled`) riding `users.settings` (no schema). The toggle NODE type is always registered so existing bodies parse; the settings only gate the creation affordances. **For your modules:** nothing changes — every markdown canvas gets the block + fold + slash menu for free; no data-model, canvas-seam, or MCP-contract change. `scripts/verify-toggle-markdown.mts` 13/0 (pure shape + full MarkdownManager round-trip) + dev-auth browser pass; tsc/eslint clean. Full record: ADR-145.
- **🟢 Heads-up (Tyler) — Unified event People model BUILT (2026-07-05, ADR-144; NON-core/solo — no tables/columns/enums change, roles + types are instance DATA).** Events now render ONE People card (For-group / Here / OUT / ghosts / mentioned) replacing the MeetingPrep-People + Attending-field + Linked-here-persons split; RelatedPanel claims persons+groups on events; MarkdownCanvas hides person-target relation fields on events. New role vocabulary on the generic edges: `attending` (attendance, event→person), `absent` (explicit OUT), `group` (event→group), `members` (group roster). **Your instance:** without a `group` type row or person-relation field on events, the card just shows empty strata and everything else keeps working — run `scripts/setup-groups.mts` (adjust the tag list) if/when you want groups. One shared-behavior change to note: `attendeeEmail` match rules now only fire on events with ≤3 attendee emails (person-1:1 rules were claiming group meetings — live misfire in Brandon's prod). Template pin now pre-relates people with role `attending` (was `related`).

- **🟢 Heads-up (Tyler) — Unified UI/UX refresh QUEUED (2026-07-03, full brief `explorations/ui-refresh.md`; NON-core/solo — peek + flag, not a both-agree gate). No schema, no new tables, no route changes.** A full-pass refresh across item view, lists/lenses, dashboards, and nav, from a live audit at 1440px + 375px. Seven UI-only slices (S1 tokens → S7 dashboard polish; see the brief). **Two pieces will touch shared visual conventions your modules inherit, so flagging them even though neither is on the frozen-core list:** (1) a **semantic token layer** in `globals.css` — `bg-surface-1`/`border-line`/`text-ink-muted` etc. registered in `@theme`, plus the neutral ramp (`--color-neutral-*`) routed through variables so a future `.light` class flips it without touching the ~2,790 existing `neutral-*` call sites (dark values identical to today → **zero visual change now**; light mode itself deferred to a later slice, this pass ships only the mechanism + one dev-flag proof screen). New/refreshed components should adopt the semantic tokens; your existing `neutral-*` usage keeps working untouched. (2) a **mobile interaction standard** — a no-dependency `SwipeRow` (right = complete, left = schedule), a shared long-press/right-click `RowMenu` that retires the always-visible per-row Trash button, and a fixed icon-only bottom bar + pull-up launcher grid (nothing removed from reach — the full destination set is one swipe up). Both get an **ADR + a CLAUDE.md working-convention line** when they land (like multi-select/ADR-118 did), so there's one documented pattern to build lists and rows against. **Nav stays position-flexible** (top/bottom/left/right/split/combined); every new affordance is positioned relative to the nav frame, never a hardcoded edge. **For your modules:** nothing changes until you adopt the tokens or wire a list through the new row components; your list pages get the refreshed defaults for free. Reviewed as a visual plan (link in the brief). Flag anything on the token names or the row-interaction contract before S4–S6 land. **UPDATE 2026-07-03 — S1 (the token layer) SHIPPED, ADR-141.** Semantic classes now live: `bg-surface-{0,1,2,3}`, `border-line`/`border-line-strong`, `text-ink`/`-ink-muted`/`-ink-subtle`/`-ink-faint`, `rounded-card`, the `ui-title`/`ui-section-label`/`ui-row`/`ui-meta` type scale, and `badgeCount()` in `src/lib/format-count.ts`. **Zero visual change now** — `--color-neutral-*` route through `--n-*` vars whose dark values equal Tailwind's, and a `.light` class flips them (proof at `/dev/theme`, dev-only). Adopt the semantic classes in new/refreshed UI; your existing `neutral-*` usage keeps working and flips for free. The `neutral-N → semantic` map is the comment block atop `globals.css`. S2–S7 follow in-branch.
- **🔴 Heads-up (Tyler) — Live item tokens (LT1) needs your ack before merge (CORE, ADR-139, branch `live-item-tokens-lt1`).** New `{{item.*}}` / `{{parent.*}}` / `{{item.children}}` / `{{item.related.<roleOrType>}}` tokens that live in the canonical markdown body and resolve from the item's CURRENT state at every server render (print, Save Offline, share, live Preview) — the "put the title/due-date/related-people in the body once and never edit them twice" ask. This **extends the canonical body format** (a body may now contain live tokens that are derived, not literal), so it's core → both-agree + ADR. **Deliberately additive + back-compat:** unknown tokens pass through untouched, apply-time template vars (`{{today}}`, `{{ask:}}`) are left alone (different layer, different resolve-time), a body with no tokens renders byte-identical, and nothing is written to the DB (resolution is render-only; the token stays the source). Pure resolver `src/lib/item-tokens.ts` (mirrors `template-vars.ts`), DB context in `src/lib/item-tokens-service.ts`, wired into the print/share/render-markdown routes; related/children emit the existing `[@Title](ledgr://item/id)` mention-link so they render + print-flatten for free. `verify-item-tokens` 58/58 + body regressions green; tsc/eslint/`next build` clean. Follow-on slices (LT2 editor chips, TPL6 apply-time gaps, LT3 export/MCP parity) are non-core and ride on top. **Peek `explorations/live-item-tokens.md` + ADR-139; flag anything on the grammar (it's the contract).**
- **🔴 Heads-up (Tyler) — AI Memory improvements SHIPPED 2026-07-02 (ADR-137 addendum, branch `ai-memory-improvements`; touches the machine/MCP contract text, ADDITIVE-ONLY + still gated behind `aiMemoryEnabled` — peek + flag).** From a review of the ADR-137 memory subsystem: design was sound, but it had an activation gap (nothing reliably told a connected AI the memory tools existed) and no maintenance loop (the protocol taught *when to remember* but never *how to revise*). Four additive changes, **no schema / migration / new tool / protocol-version bump**: (1) **owner-aware connect-time instructions** — `server.ts` `buildInstructions(ownerId)` appends a short "call `get_memory_stumps` at session start / file with `remember` / read the protocol" paragraph **only when `aiMemoryEnabled`**; **off ⇒ instructions are byte-identical to before** (verified), so your vanilla clients are unchanged. This is the real fix — connect-time instructions are the one surface every MCP client shows the model, unlike tool descriptions/resources. (2) a **"Maintain the store" section** in the memory-protocol resource (update-over-duplicate, enrich links, keep a confirmed seasonal alive, correct-in-place / flag-for-owner-removal, prefer few dense memories). (3) **per-horizon aging** in `memory.ts` — the one 45-day always-on window split into `seasonal: 45` / `episodic: 10`. (4) **`remember` partial-link hardening** — new exported `assertOwnedItems()` validates every `about` id up front so a bad id fails the whole call instead of creating an unlinked memory. Also a one-off live-data fix (a seeded memory title had a double-escaped `&amp;`). **Recorded gating boundary:** off hides the tools/resource/instructions/sidebar, but existing `memory` items stay reachable via generic `search_items`/`get_item` (they're ordinary hidden-type items) — deliberate, not data-level hiding. `verify-mcp` extended (+13 memory checks: gating both ways, instructions addendum on/off, partial-link rejection creating no memory, always-on link carry, seasonal-survives / episodic-ages-out) — **ALL PASS** on dev Neon; tsc/eslint clean. Deferred (defer-by-hiding): AI-initiated `forget`, aging-by-recall, auto-consolidation; a privacy pull-only flag was declined as overbuild (consistent with ADR-075). Full record: ADR-137 addendum.
- **🔴 Heads-up (Tyler) — architecture flyover (2026-07-01) found a `WidgetKind` naming collision + a grid-breakpoint mismatch; asking for your input before touching it (core-ish, pairs with your parked A/B/C widget-engine decision). NOTHING HAS SHIPPED on this — no branch, no PR, just a finding.** A structural review (`docs/architecture-refinements-2026-07.md`) found `src/lib/widgets.ts` and `src/lib/dashboard-widgets.ts` both export a type named `WidgetKind` with completely different members (`"property"|"collection"|"relation"|"derived"` vs `"view"|"stat"|"action"|"text"|"tree"|"embed"|"container"|"image"`) — confusing but harmless today since nothing imports the wrong one. More concretely load-bearing: `src/lib/canvas-layout.ts` breaks the item-canvas grid at `md: 480px` while `src/components/dashboards/RglInner.tsx` breaks the dashboard grid at `md: 768px`, even though both store the identical per-breakpoint `{x,y,w,h}` composition layout shape — so a layout saved on one system doesn't mean the same breakpoint on the other. **Why this needs you:** it's the direct prerequisite for whichever letter you pick on the parked A/B/C widget-engine decision (`next_steps.md`, "Wire the widget engine across types") — cheaper to sort out before that rollout than after. **Proposed, not done:** (1) rename the two enums apart (e.g. `RecordWidgetKind`/`DashboardWidgetKind`) — low-risk, no behavior change; (2) share one `WidgetItemRow`/result type between `record-widgets.ts` and the dashboard data path; (3) pick one `md` breakpoint (or parameterize the grid config) — this one has real implications for anyone with saved layouts, so it's the one I most want your read on before anyone touches it. Four other (lower-stakes, solo-safe) findings from the same review shipped separately this session — mcp/tools.ts split into per-tool modules, items.ts split into reads/mutations, NavShell.tsx glyphs+hover-state extracted, a couple of quick-win cleanups — see the doc for the full list and status.
- **🟢 Planner — drag-to-schedule calendar (ADR-131, CORE — Tyler agreed). Phase 1 SHIPPED 2026-06-29, PR merged to main.** The `calendar` view layout made interactive: a month grid + a multi-day time-grid where tasks are dragged to plan when to do them (writes `scheduled_date` + floating `properties.scheduledTime`; per-view "place by due" toggle). **The one core touch: a new additive `views.display` jsonb (migration 0038)** holding the calendar's interactive options (mode/dayCount/slotMinutes/placeBy/work-hours), tolerant-parsed in `views.ts`. Everything else is UI (solo): `src/components/planner/*`, `src/lib/planner-grid.ts`, the `/planner` route + a Tasks "Planner" tab + a Planner nav slot. Meeting (`meetingAt`) calendars stay the read-only static grid. Reuses the view engine, `scheduled-time.ts`, the board touch-drag, the PATCH path, and the ICS feed (drags reflect to Outlook for free). `verify-planner` 38/38. **Heads-up:** `views.display` is on the frozen-core list — additive/back-compat (`null` = defaults), so your existing calendar views are unchanged. Run `npm run db:migrate` (0038).
- **🟢 Heads-up (Tyler) — Planner daily-use polish (branch `planner-improvements`, 2026-07-02; NON-core/solo, UI only — no schema, no migration, no ADR; peek + flag only if something looks off).** Four additive slices on the Planner (ADR-131/133), no touch to `views.display` or any core seam: **complete-in-place** (a check on every task chip/block/rail item → optimistic `status` PATCH + Undo toast), **click-to-create** (empty time-grid slot / month day double-click → new task with date+time prefilled, via the existing `POST /api/items`), an **orientation/rendering pass** (now-line + scroll-to-now, month/year header, time prefixes, `formatRange` on blocks, past-day dimming, "+N more" opens the day in Multi-day), and **drag feedback + overlap** (drop-time label, live resize duration, move Undo toast, and `src/lib/planner-overlap.ts` so overlapping blocks split the column instead of hiding each other). All new files under `src/components/planner/*` + the one pure lib; writes go through the existing owner-scoped PATCH/POST routes. Verified in dev-auth preview; tsc/eslint clean. **For your instance:** nothing changes until you open the Planner; no data-model or contract change.
- **Working on:** **Auto-match events to the right people via templates (EM1–EM4, ADR-123, CORE — needs Tyler ack before merge): EM1 SHIPPED 2026-06-27, branch `events-em1-template-rules`.** From Brandon's "events should arrive knowing who they're about, so tasks/notes/prayer connect." Three layers: a guessing layer (attendee email + fuzzy title/details → person suggestions), confirm-then-pin, and the rule **routed through a template** (a pinned match = an `event` template with the people pre-related + recurring content). EM1 is foundation, no behavior change: new nullable **`templates.match_config` jsonb** (`{condition, autoApply}`, **migration 0035**) + a partial index; the rule SOURCE moves from the empty `matchers` table onto templates, reusing the matcher engine's condition vocabulary verbatim (`src/lib/calendar/event-rules.ts`). `verify-event-rules` 32/32; tsc/eslint/`next build` clean. EM2–EM4 (suggester + Tier-B suggestions on Add, Tier-A auto-apply, pin-as-rule UX) follow, one PR each. See the 🔴 below. Before that: **Dashboard canvas (ADR-111 DC1–DC4) + a nested-list (parent + children) widget — BUILT 2026-06-27 (ADR-120; NON-core/solo), branch `dashboard-canvas`.** The dashboard becomes a customizable canvas: (DC1) per-widget appearance — show/hide header + border, a background color, an accent edge, and a view-mode collapse chevron (the forced height-1 lives in `RglInner` view-mode-only, so the stored expanded height is never clobbered); (DC2) a dashboard "stage" — full-bleed background (curated color/gradient swatches + image URL) with scrim + blur sliders + title-visibility/density toggles (a new nullable `dashboards.appearance` jsonb, **migration 0034**, `null` = today's plain dashboard; video + byte-upload deferred behind the parser seam); (DC3) an `embed` widget rendering ANY item's body through the same autosaving `ItemEditor` (sticky note = embed + color + header off); (DC4) a `container` widget (tabs/stack/section) holding child widgets, one-level nesting. **Plus the new `tree` (nested-list) kind** Brandon asked for: N parent items from a backing view, each with its children under it, children sourced EITHER from the `parent_id` hierarchy OR a relation role (so "projects + their tasks" works via role `project`, since project↔task is a relation, not parent_id), hide-completed default on, "+N more" overflow. **Appearance + the new-kind fields ride the existing `widgets` jsonb (tolerant parsers, no migration); only the stage column is new.** MCP `add_widget` enum **pinned to the original 4 kinds** (the canvas kinds are UI-built, kept OFF the frozen machine/MCP contract). `verify-dashboard-canvas` 26/26 + `verify-dashboards` 19/19 green on Neon; tsc/eslint/`next build` clean. See the 🟢 below. Before that: **Multi-select + bulk item operations — BUILT 2026-06-26 (ADR-118, core-adjacent/additive), branch `feat/multi-select-bulk`.** From Brandon's "I want to edit 50 tasks at once and can't." A reusable selection layer (`SelectionProvider` + `SelectCheckbox` + a floating `BulkActionBar`, `src/components/selection/`) on every row-based list (`/tasks`, `/notes`, `/links`, `/events`, `/inbox`, `/list/[type]`, and the shared `ViewRenderer` list/table/agenda via a `selectable` prop), plus a user-auth batch endpoint `/api/items/batch` (`PATCH {ids,patch}` / `DELETE {ids}`) that loops the SAME `updateItem`/`softDeleteItem` as the single-item routes — no new item contract, no migration, no dependency. Actions: Delete · Set property·status · Move (set parent); **Archive deliberately not built** (the Set… field list is the seam for the future archive feature). "Every list includes multi-select" is now a CLAUDE.md convention. See the 🔴 below. Before that: **MCP OAuth shim — NOW BUILDING (ADR-117, CORE/machine-MCP contract; Tyler agreed 2026-06-26).** So the Ledgr MCP can be added as a custom connector in claude.ai web + the Claude mobile apps (used from Brandon's phone). Consumer Claude connectors are OAuth-only (no bearer/header field; Anthropic #112 "not planned"), so this adds a minimal, **stateless** OAuth 2.1 shim that **wraps, not replaces**, the static bearer model: discovery (`/.well-known/oauth-protected-resource` + `…-authorization-server`), stateless DCR, a Clerk-gated `/api/oauth/authorize`, a PKCE `/api/oauth/token`, all signing HMAC tokens with a new `LEDGR_OAUTH_SECRET` (node:crypto, **no DB tables, no dependency** — that's what keeps it off the shared schema). `/api/mcp` accepts an OAuth access token **in addition to** the existing `LEDGR_API_TOKENS` bearer, so **Claude Code/Desktop and the HTTP-API/clipper/cron callers are untouched** (additive). Revocation = rotate `LEDGR_OAUTH_SECRET` (kills only Ledgr OAuth tokens; not the static-token clients, not non-Ledgr connectors). Full record: ADR-117. **Before that: Effective plan date default + task canvas arrange restored (ADR-109): SHIPPED 2026-06-24, branch `fix/effective-plan-date-and-task-canvas-arrange`.** From Brandon's "due vs scheduled" question. (A, CORE/additive) tasks now default app-wide to the effective plan date (`scheduled ?? due`, the new `plan` field) for filter windows, sort, calendar/agenda placement, the `/tasks` list, and the Overdue/Today/Upcoming widgets: scheduled-primary, due-secondary, never breaking when due is unset; the MCP/view date enums gain a `plan` value (additive). (B, non-core) the per-type DnD "Customize layout" works again on tasks (it regressed in ADR-108): `TaskCanvas` delegates to `MarkdownCanvas`'s grid when arranging or a `canvas_layout` is saved, else the compact rail (the default). No migration. See the 🔴 below. Before that: **Per-type status DISPLAY MODE (ADR-106, CORE; Tyler agreed): SHIPPED 2026-06-24 (slice 1).** Tasks default to a done/undone **checkbox**, multi-status is opt-in per type, and the view builder stops offering Status for types that don't use it — a thin presentation layer over the ADR-082 category plumbing (no boolean redesign; the category already *is* the boolean). New `types.status_mode`; **run `npm run db:migrate`** (0032). Next slice: the **archive axis** (cross-cutting `items.archived_at`, its own ADR). See the 🔴 heads-up below. Before that: **Events & calendar matching (E1–E4, ADR-094, CORE; Tyler OK'd the core): NOW BUILDING.** E1 (rename `meeting`→`event`) SHIPPED: migration 0027 renames the type key via insert/repoint/delete (28 items repointed); user-facing surfaces renamed (type+label, `/events` route+tab, MCP tool examples, push text, starter widget), internal names kept (`meeting_at` col, `src/lib/meetings/`, `MeetingPrep`/`MeetingTranscripts`, `/api/meetings/*`, the `transcript` role, the canvas cards). `tsc`/eslint/`next build` clean, 15/15 affected verifies green. E2 (tag type), E3 (calendar list + click-to-add), E4 (configurable task-pull rule) follow, one PR each. See the 🔴 below. Before that: **Track changes — version history + diff + undo SHIPPED 2026-06-20, chunk COMPLETE (non-core, no migration/dependency/ADR).** The user-facing surface over the revision snapshots every save already writes. A pure `src/lib/diff.ts` (word-level, no dep — Principle 5), a `GET /api/items/[id]/revisions/[revisionId]` body read (`getRevision`), and a `HistoryPanel` on both canvas paths (lazy; list → "Show changes" word-diff vs. any version → "Restore" = the item-view undo, which force-snapshots current first so it's undoable). **The one shared-file touch: a `history` card in `src/lib/canvas-layout.ts`** (so it renders in both classic + grid). `verify-revisions` 38/38 + in-browser. **Source-tagging (which-channel-wrote-it) was scoped then DROPPED — Brandon: provenance doesn't matter, undo is the point — so no `revisions.source` column / no core touch.** See the 🟢 heads-up below. Before that: **Templates redesign — TPL1–TPL5 SHIPPED 2026-06-20, chunk COMPLETE (ADR-093, CORE; reverses ADR-045 "templates aren't items").** Templates are now real prototype items applied by `cloneItemSubtree`. TPL1: `items.is_template` + registry reshape + the owner-scoped exclusion sweep (migrations 0025/0026). TPL2: a "Template" banner on the prototype canvas (rename/set-default/duplicate/delete), **"Save as template"** on any item, duplicate, per-row index actions — riding an additive `cloneItemSubtree({isTemplate})` option. TPL3: variable resolution (`{{today+7d}}`/`{{nextSunday}}`/`{{title}}` + `{{ask:Label}}` prompts) over the cloned tree + structured due/scheduled date rules (none|offset-from-apply|fixed) in `apply_config`, authored from the banner. TPL4: a per-type **default** template applied by "+ New" + a chooser (4a), and **apply-to-existing** — an "Apply template…" merge onto a started item, fill-blanks ("change only the unchanged") or overwrite (4b). TPL5: **MCP parity** (`list_templates` now reports `askLabels`/`isDefault`/`applyConfig`; `apply_template` gained `targetId`+`mode` for apply-to-existing) + the **`new-from-template` dashboard widget** made addable + configurable with a real template picker. See the 🔴 below. Before that: **Meeting Recording chunk — COMPLETE 2026-06-18 (v1a paste ADR-087 + v1b audio ADR-088/089).** v1a: a `transcript` child type + Transcript panel + "Transcripts awaiting minutes" view + the documented Claude-over-MCP minutes automation. v1b: the `transcription` provider seam + AssemblyAI adapter (CORE), audio upload → auto-transcribe (client-poll + cron backstop), and audio retention (`attachments.purge_after`, CORE). In-app recording stays deferred. See the 🟢 heads-ups below. Before that: **Tasks Polish chunk — finishing touches on Native Tasks (S1–S6, 2026-06-18); COMPLETE (S1–S6 shipped).** S1: scheduled date in sort/filter/board + subtask rows. S2: configurable category-backed statuses (ADR-082, CORE). S3: recurrence completions calendar + occurrence carve-out (ADR-083, non-core). S4: NL quick-add from the task title (ADR-084, non-core). S5: relative subtasks (ADR-085, non-core; touches clone/updateItem). S6: focus/reminder/ICS loose ends (ADR-086, non-core). Details in the heads-up below and `next_steps.md` "NOW BUILDING". Before that: **Toward-1.0 papercuts + MCP additions (ADR-071/072)** — Quick Capture → catch-all default, list-filter by a custom property, kanban drag-and-drop, and 6 new MCP tools (relate/unrelate, list/run views, list/apply templates) + `propertyPatch`/relation metadata. Before that: **Feature B — arrangeable, field-level, per-type item canvas layout (ADR-069, CORE)** (B1; the sibling to Feature A — details in the heads-up below). Before that: **Feature A — item-view inline editing (ADR-068, non-core)** — hover-pencil related-title edit, right-click Rename / Edit-in-builder on labels, × to clear a property. Before that: the Relations chunk (ADR-067, branch `relations-typed-properties`); editor parity (image paste fix + GFM tables + strike); nav v6; scheduled Claude tasks (ADR-052); per-view columns + templates-preset-relations (ADR-049/050). (Tier 3 — MCP — ADR-047; Tier 2 — templates — ADR-045/046; Tier 1 — Build surface — ADR-044; Phase M — ADR-040/041/043.)
- **Next:** **Events & calendar matching: E2 (`tag` type), E3 (calendar list + click-to-add, reverses ADR-023), E4 (configurable event task-pull rule) — per ADR-094, one PR each.** Before that: Track changes chunk COMPLETE (TC1 shipped; TC2 source-tag dropped); Templates chunk complete (TPL1–TPL5). Before that: **Tasks Polish chunk is done (S1–S6).** Open next: the still-open **flexible-surfaces Part 2** ("custom composable pages," converging with your dashboard-widgets engine — Feature B's item-canvas grid is the same `react-grid-layout` engine, so they should share patterns) and **inline action items** (`explorations/block-linked-action-items.md`). The **selective Notion migration stays deferred** until closer to 1.0. Still open for a joint ADR: **OneDrive MD archive vs. git-versioned export**. Also queued from the 6.14 meeting: the per-property "include in share?" toggle (ADR-062), Quick Capture default-to-catch-all, and the polish batch. Feature B follow-ups (later slices): grouping/collapsible container cards, pin-to-fixed polish, opening Chord/Paper canvases to arrangeability. **(Inline action items is now DONE — see the 🔴 below.)**
- **🟢 Heads-up (Tyler) — Calendar feed + event timeline are now bespoke LENSES; events unified onto `/list/event` (ADR-136, core-adjacent — extends the shared `Lens` union from ADR-105; peek + flag). No migration, no dependency.** Brandon's calendar-import feature (browse upcoming Outlook meetings, click **Add** to pull each in as an `event`) had gone missing — a routing split, not a deletion: the feed + Upcoming/Past timeline lived only on the bespoke `/events` page (no lens strip), while the destination picker sent `event` to the generic `/list/event` (lenses, no feed). Fix: unify onto `/list/event` and make both surfaces lenses. **The shared touch (why you're flagged):** the `Lens` union in `src/lib/list-lenses.ts` gains two new *kinds* — `{kind:"calendar"}` (the external un-promoted feed + Add) and `{kind:"timeline"}` (the `meetingAt` Upcoming/Past grouping). **Both are event-only defaults** (`defaultLenses(typeKey)` now prepends them for `event`; every other type keeps the four generic sorts unchanged), and their bodies are event-module internals (MS calendar adapter + timeline). `parseLens` round-trips them; the Build → Types tabs editor hides/reorders/re-adds them. **Related panels are unaffected:** bespoke lenses are list-page-only, so `relatedLensCandidates()` filters them out of the related-group default + picker (a calendar feed inside "Linked here" makes no sense). **For your modules:** nothing changes — your types still get the four generic sort defaults; only `event` gets the two new kinds. Bespoke `/events` is now a `redirect("/list/event")` (defer-by-hiding). Verified in dev-auth preview on the prod DB (strip = Calendar·Timeline·Recent·Newest·A→Z·Most linked, Calendar default with 12 real events to Add; `/list/person` shows only the four sorts, no leak); tsc/eslint clean. Full record: ADR-136.
- **🔴 Heads-up (Tyler) — cross-device edit guard SHIPPED (ADR-134, CORE/machine-REST contract; ADDITIVE + back-compat, peek + flag). No migration, no dependency.** From Brandon's question: open a meeting on device 1, edit it on device 2, then type on device 1 without reloading — the stale full-body PATCH silently clobbered device 2's edits, with no indication. Two additive pieces: **(1)** the body autosave now sends an optional **`expectedBodyDigest`** (a tiny pure non-crypto digest of the body it last synced, `bodyDigest` in `src/lib/body.ts`) and `updateItem` returns a new **409 (`ItemError("conflict")`)** when a *real* body write's digest no longer matches the stored body — so a stale overwrite is refused, not silent. **Content-based on the body, deliberately NOT an `updated_at` token**, so sibling writes to status/properties on the same item never trip a false body conflict. The editor surfaces the 409 as a "Keep mine / Reload" banner (Keep mine = resend without the token; the clobbered version stays in revision history). **(2)** a refresh-on-focus check via a new lightweight **`GET /api/items/:id/version`** (`getItemVersion` — one `updated_at`, no body) raises an "Updated on another device" banner when a backgrounded canvas tab refocuses. **Additive + back-compat:** a caller that sends no `expectedBodyDigest` (MCP, batch ops, the field-only writers) keeps last-write-wins exactly as before — your integrations are untouched. **Scope boundary (defer-by-hiding):** the hard guard is on the markdown `ItemEditor` (notes/tasks/meetings); chord/paper bodies advance the focus baseline but don't send the token yet (focus banner only), and title clobber stays last-write-wins under the focus banner — all additive to extend later. `scripts/verify-concurrent-edit.mts` green on Neon + the full 409 round-trip eyeballed in dev-auth preview; tsc/eslint clean. Full record: ADR-134.
- **🔴 Heads-up (Tyler) — Notification Center NOW BUILDING (ADR-129, CORE — new `notifications` table; you agreed 2026-06-28). Migration 0037.** A place where all notifications live (read + unread), markable read/unread, archivable — **one row per event** (3 tasks notifying at once = 3 entries), persisted over the existing Web Push transport (ADR-034), which is fire-and-forget today. **The core bit:** a new dedicated **`notifications` table** (NOT notifications-as-items — read/unread doesn't fit the `status_category` enum, and as items they'd pollute `items`/FTS/Discover; same call as `revisions`/`item_relatedness`): `owner_id`, `kind`, `title`, `body?`, `url?`, `related_item_id?` (FK items, cascade), `state` (unread|read|archived) + `read_at`/`archived_at`/`created_at`; index `(owner_id, state, created_at)`. A pref-gated `recordNotification(...)` writes a row wherever a push fires. **Per-source on/off toggles** in `users.settings.notificationPrefs` (no schema, navSlots posture, default all on) — one switch gates both the row and the push. UI: a `/notifications` Work page (Unread/All/Archived + per-row + multi-select bulk per ADR-118), a **bell + unread badge** in the Work nav (reuses Inbox's `badgeEligible` plumbing), **and a PWA app-icon badge** (Badging API, SW-increment + client authoritative). 30-day archived purge (matches Trash). **For your modules:** nothing changes until a source notifies; any module event can call `recordNotification` later. Phase 1 = persistence + center + prefs + both badges; Phase 2 = task-due/calendar-soon/sync-error sources; Phase 3 = MCP tools. **Peek at ADR-129 + flag.** Run `npm run db:migrate` (0037) once it lands.
- **🔴 Heads-up (Tyler) — auto-match events to people via templates, EM1 SHIPPED (ADR-123, CORE — touches matching, `match_state` semantics, relations, and the `templates` schema; needs your ack before any slice merges). Run `npm run db:migrate` (migration 0035).** Brandon's design: stop forcing a binary confirmed/suggested. Instead (1) a **guessing layer** suggests who an event is about from attendee email (exact) + fuzzy title/details (e.g. "Roger/Brandon 1:1" → the Roger person); (2) confirm a suggestion, then **pin** it; (3) a pinned match is **routed through a template** — an `event` template that pre-relates the people (via its prototype edges, ADR-050) AND carries recurring content, so future matching events apply it (people confirmed + content). **EM1 (this slice, foundation only — no behavior change yet):** new nullable **`templates.match_config` jsonb** = `{ condition: MatcherCondition; autoApply: boolean }` (migration 0035) + a partial index `templates_match_idx`. The rule SOURCE moves from the **empty** `matchers` table onto templates — the matcher engine's condition vocabulary (`MatcherCondition` kinds, `CONDITION_RANK`, `validateCondition`) is **reused verbatim** from `src/lib/matchers/`; only the source changes. New `src/lib/calendar/event-rules.ts` (`listEventRules` / `matchEventToTemplate`, kind precedence, fuzzy gated, oldest-template tie-break); `matchConfig` flows through template CRUD (`src/lib/templates.ts` + new `src/lib/templates/match-config.ts`, strict validation). **`matchConfig: null` = a plain content template (unchanged), so your existing templates are untouched.** **Promotion stays MANUAL** (cron does not auto-promote; templates apply on a manual Add) — no firehose. **For your modules:** nothing changes until you set a `match_config` on an `event` template; the `matchers` table/store/engine are deprecated **dormant** (left in place, reversible, 0 rows). EM2 adds an always-on person suggester (`suggest-people.ts`, token-containment fuzzy with owner-exclusion) writing **suggested** edges on Add; EM3 wires Tier-A auto-apply + retires the dormant matcher engine; EM4 adds the pin-as-rule UX. `verify-event-rules` **32/32** on Neon; tsc/eslint/`next build` clean. **Peek at ADR-123 + ack before I merge EM1.**
- **🟢 Heads-up (Tyler) — Dashboard canvas SHIPPED (ADR-111 DC1–DC4 + a new `tree` widget, ADR-120; NON-core/solo, additive. Run `npm run db:migrate` — migration 0034).** The Work-surface dashboard (the move-fast-solo side, like ADR-064/065). **One new nullable column: `dashboards.appearance` jsonb** (the "stage" — background/scrim/blur, title-visibility, density); `null` = today's plain dashboard, so your dashboards are untouched until you set one. **Everything else rides the existing `dashboards.widgets` jsonb** via tolerant parsers (no migration): a cross-cutting per-widget `appearance` (header/border/background/accent/collapsible), plus three new widget kinds — `embed` (renders any item's body through the autosaving `ItemEditor`, the one place a widget reads a `body`), `container` (tabs/stack/section holding child widgets, one-level nesting), and `tree` (a backing view's parents each with their children, sourced from the `parent_id` hierarchy OR a relation role like `project`). **The machine/MCP contract is unchanged** — `add_widget`'s `kind` enum is now **pinned to a local `MCP_WIDGET_KINDS` (view|stat|action|text)** rather than `WIDGET_KINDS`, so the new UI-only kinds don't leak onto the frozen contract; your MCP integration sees no new kinds. **For your modules:** any saved view of your type can be added as a List/Count/Nested widget; any of your items can be embedded; nothing in the data model, body format, or canvas seam changed. New libs: `src/lib/dashboard-tree.ts` (batched, body-free, owner-scoped child fetch), plus the `src/components/dashboards/*` additions. `verify-dashboard-canvas` 26/26 + `verify-dashboards` 19/19 on Neon; tsc/eslint/`next build` clean. Full record: ADR-120 (it also notes ADR-111 DC1–DC4 landed; DC5 journal mode + video/byte-upload deferred). Peek + flag.
- **🟢 Heads-up (Tyler) — the "no deploys Saturday night" rule is reframed, not gone (ADR-119, non-core; peek + flag if you disagree).** ADR-115 told you the no-Saturday-deploys rule was in force for the shared codebase; this walks that back. Brandon preaches only 2-3×/year, so a blanket weekend/Saturday no-deploy window was ceremony more than protection, and even a bad deploy is recoverable (git revert + soft-delete + revisions + the weekly backup). **New posture:** there's **no fixed weekend/Saturday no-deploy window**; instead, be deliberate with production (prefer additive/reversible changes, don't ship large risky changes carelessly) and use judgment on the rare sermon-sensitive weekend. **Principle 4 "Sunday-proof" (the offline OneDrive/PDF fallback) is untouched** — this is only about deploy *timing*. Migration caution (ADR-115) also stands. Updated in CLAUDE.md (status callout + working conventions), runbook §0, PRD §6.2. Your instance is your call regardless. Full record: ADR-119.
- **🔴 Heads-up (Tyler) — multi-select + bulk item ops on every list (ADR-118, core-adjacent; additive, peek + ack). No migration, no dependency, no item-contract change.** Brandon asked to edit many items at once. Added a reusable client selection layer (`src/components/selection/`: `SelectionProvider` holding the selected set + ordered in-view ids for shift-range/select-all, `SelectCheckbox` a per-row island that renders nothing without a provider, `BulkActionBar` the floating bar) + a user-auth **`/api/items/batch`** (`PATCH {ids,patch}` loops `updateItem`; `DELETE {ids}` loops `softDeleteItem`; per-id `errors`, cap 200/request, client chunks). **Actions:** Delete (Trash, cascades) · Set (status / select·multi_select property / due·scheduled date via `propertyPatch`) · Move (set `parentId`). Config from `bulkConfigForType` (`src/lib/bulk-config.ts`) — status-less types hide the status/date setters; mixed-type Inbox shows only Move + Delete. **The shared touch (why you're flagged):** `ViewRenderer` gained an opt-in **`selectable`** prop (off by default), so view lenses (`ViewLensBody`) and `/views/[id]` get checkboxes; **board/calendar layouts, dashboard widgets, and Trash are deliberately excluded** (defer-by-hiding). **For your modules:** every module type's `/list/[type]` and any saved view of your type gets multi-select for free; **"every list surface includes multi-select" is now a CLAUDE.md working convention** — wire new lists the reusable way (provider + row checkbox + bar), never per-page bulk logic. **Archive is NOT built** — the Set… field list is the documented seam for the future archive feature. tsc/eslint clean; in-browser eyeball + a `verify-items-batch.mts` pending. Full record: ADR-118.
- **🔴 Heads-up (Tyler) — list paging: every plain list now pages past the 200-row cap (ADR-116, core-adjacent; additive, peek + ack). No migration, no dependency, no contract change.** Brandon's Notion import (ADR-115) pushed types past 200 live items and exposed that `queryViewItems` defaulted to and hard-capped at `VIEW_LIMIT = 200` with no paging UI, while the list subtitle counted the capped array (so 743 notes read "200 notes"). **The shared touch (why you're flagged):** `VIEW_LIMIT` stays the 200 default page size; a new **`VIEW_MAX = 2000`** hard ceiling is what `viewItemsQuery` clamps to now (was `VIEW_LIMIT`), so a render can grow its window while a backstop remains; rows stay body-free and owner-scoped. Added a pure `parseListWindow(?show=)` and a shared server component `LoadMore` (a `scroll={false}` link that bumps `?show=` one page at a time, preserves the lens/rev/filter params, and shows an honest "Showing X of Y" via `countViewItems`). Wired into `/notes`, `/links`, `/list/[type]`, `/events`. **For your modules:** every module type's `/list/[type]` gets Load-more plus the true count for free. **Unaffected:** MCP clamps its own input to 200; your dashboard `itemLimit` now clamps to 2000 (a raise, not a break). **Not yet paged (same cap, follow-up):** the `/items` browse and `/tasks`/dashboard view widgets. `verify-list-paging` ALL PASS on Neon (clamp, true count, 205-row window) plus a LoadMore static-render check; tsc/eslint clean. Full record: ADR-116.
- **🔴 Heads-up (Tyler): Brandon's instance flipped to v1.0 PRODUCTION (ADR-115, 2026-06-26); the alpha posture (ADR-039) is over for the shared core.** Brandon's Notion import landed (the ledgr#84 machine-API endpoints), so his instance now holds real, hard-to-reproduce data and is in daily use. **What changes for you:** we share one schema, so **migration caution is re-armed on anything core** — schema changes need real migrations (additive, then backfill), no `DELETE FROM`/reseed of live owner data, back-compat matters (a destructive migration on the shared core would hit Brandon's live data). (The "no deploys Saturday night" rule this originally announced has since been reframed to general production-mindfulness, no fixed weekend window — see ADR-119 / the 🟢 heads-up above.) **Your instance flips on your own timeline** (separate single-tenant deploy): flip it when you're in real use and add your own note. Nothing in the data model or contracts changed here, this is a posture flip. Full record: ADR-115 (CLAUDE.md status line + runbook §0 updated).
- **🔴 Heads-up (Tyler) — task date default is now the effective plan date (`scheduled ?? due`) (ADR-109, CORE/ADDITIVE; peek + flag). No migration.** A new **`plan`** value (`COALESCE(scheduled_date, due_date)`) joins the view engine's `DATE_PROPERTIES`/`SORT_FIELDS`/`GROUP_FIELDS`/`COLUMN_FIELDS` and is the **default** date field for task/any views (filter window, calendar/agenda placement, and the builder offers it first). So a date window with no `dateField` now resolves to `plan` (was `dueDate`), and the `/tasks` list plus the Overdue/Today/Upcoming starter widgets key off it: the whole app reads scheduled-primary, due-secondary, and an unset due date never hides a task. **Additive to the MCP/view contract:** `list_items` `dateField` and `create_view` accept the new `plan` value; the existing values and your saved views are unchanged (name `dateField: dueDate` for a pure-deadline view). **Also (non-core):** the task DnD "Customize layout" works again, since `TaskCanvas` delegates to `MarkdownCanvas`'s grid when arranging or a saved `canvas_layout` exists, else the ADR-108 rail (the default); module types are unaffected. Pre-existing grid limitation noted in ADR-109 (no focus/time/reminder card yet). `verify-views` extended green on Neon, `verify-canvas-seam`/`-layout` green, tsc/eslint/`next build` clean. Full record: ADR-109.
- **🔴 Heads-up (Tyler) — Per-type status DISPLAY MODE SHIPPED (ADR-106, CORE — adds a `types` column + changes the `task` default; ADDITIVE, peek + flag). Run `npm run db:migrate` (migration 0032).** New nullable **`types.status_mode`** = `none` | `checkbox` | `select`, the user's Build → Types choice for how a type shows completion. **It is PRESENTATION ONLY over the ADR-082 category plumbing** — `items.status`/`status_category` and the **machine/MCP status contract are unchanged**; `status_category === 'done'` is still the one source of truth for "done". `checkbox` reuses the existing recurrence-aware `toggleItemDone`/complete endpoint, so your checkboxes are unchanged. Resolved by `resolveStatusMode` (`src/lib/status.ts`) — **read the StatusMode block there before touching status; do NOT collapse status into a boolean `done` column to "simplify" it** (the category already is the boolean; a boolean would break recurrence-advance, the archive/in-progress buckets, and multi-status types). **Defaults backfilled:** `task` → checkbox (the new binary default), any type with a custom `status_schema` (`project` + user types) → select, **everything else → none** (status is opt-in; person/note/link/event show no status affordance). **⚠️ For your module types:** any type that *inherited* the default 3 statuses but never defined a custom `status_schema` defaulted to **`none`** — so **`hiring_candidate`, `paper`, `song` now show no status**. If a module type wants completion tracking, set its mode in Build → Types → Completion tracking (or seed `status_mode`); a type with its own `status_schema` already became `select` automatically. The done-checkbox is wired on the **task canvas** (`TaskCanvas`); the view builder gates Status by the selected type's mode. **Deferred (noted in ADR-106):** threading `status_mode` through `ViewRenderer` so a `none`-type *table* view drops its default Status column (the common list/agenda/calendar surfaces are already clean); a status control on the **default canvas** for non-task checkbox/select types; MCP `list_types` exposure. The **archive axis** (`items.archived_at`, hide-but-recoverable, orthogonal to done) is the next slice + its own ADR. `verify-statuses` 35/35 + `verify-types`/`verify-views` green; tsc/eslint/`next build` clean; in-browser on Neon (Build control, view-builder gating, task checkbox toggle, route round-trip). Full record: ADR-106.
- **🟢 Heads-up (Tyler) — list lenses: every type's list page now has a customizable tab strip (ADR-105, NON-core; no migration/dependency). One additive shared-file touch in `views.ts`.** The `/tasks` tab pattern generalized to the generic `/list/[type]` plus Notes and Links: each renders a strip of "lenses" — four virtual sort defaults (Recent · Newest · A→Z · Most linked, each reversible via an active-tab direction toggle) plus owner-added tabs. A tab is a **sort** lens OR a **view ("widget") lens** that renders a saved view through the existing `ViewRenderer` (board/calendar/agenda/table as a tab). Customized in Build → Types → edit (`ListTabsEditor`) and stored in **`users.settings.listTabs`** (navSlots/favorites posture — no schema). **The shared touch:** `queryViewItems` gained an internal `ListSort` superset — `mostLinked` (a correlated `count(*)` over confirmed `relations`) + a property sort (`properties->>key`, guarded numeric cast). **Additive: the public `ViewSort`/`parseSort` and the MCP `create_view` shape are unchanged**, so your saved views/sorts are untouched; surfacing most-linked/property sort in `create_view` later would be a small extension. View-lens render reuses the `DashboardView` pattern in a self-contained `src/lib/view-render.ts` (`resolveViewLens`/`applyTypeScope`) — **no dashboard code touched.** **For your modules:** every module type's list gets the strip + the four defaults for free, and any of your saved views can be added as a widget tab. **Events and Tasks keep their bespoke tabs.** `verify-list-lenses` 30+ green on Neon; tsc/eslint/`next build` clean; in-browser eyeballed (strip + reverse + most-linked on Notes and `/list/person`, the Build panel listing real saved views as widget options). Full record: ADR-105.
- **🔴 Heads-up (Tyler) — bug fix: viewing an item no longer bumps its edit date; a body PATCH whose text is unchanged is now a no-op write (ADR-104). Touches the shared `updateItem` and the revisions/`updated_at` invariant, so flagging it; behavioral only, no schema/migration/MCP-shape change.** The Tiptap body editor fires one programmatic `onUpdate` on mount (a normalize pass, not a user edit), so merely opening an item PATCHed `{body}` byte-identical to what's stored, `updateItem` wrote it unconditionally, and since `items.updated_at` is `$onUpdate(now())` every view bumped the edit date and snapshotted a junk revision (against the ~50 cap). Fix is two guards: `updateItem` now skips the body write (and its revision snapshot, mention re-sync, and the `updated_at` bump) when the body text/format matches what's stored, returning the item untouched if that was the only field (covers editor + MCP `update_item` + REST); and `ItemEditor` skips scheduling a save for an unchanged body, so the phantom PATCH never fires and the save indicator doesn't flash on open. **If any module code relies on a body PATCH always writing/snapshotting even when the text is identical, it's now a no-op** (only real changes write). `verify-items`/`verify-revisions`/`verify-body-contract`/`verify-mcp` all green; tsc/eslint clean. Full record: ADR-104.
- **🔴 Heads-up (Tyler) — AI shapes the workspace over MCP SHIPPED (ADR-102, CORE — the machine/MCP contract; ADDITIVE-ONLY; you pre-approved the direction in `explorations/ai-workspace-shaping-over-mcp.md`, peek + flag).** The MCP server can now *shape* the workspace, not just file/read items (Brandon's "the AI is FOR them" — a user says "make me a place to track sermons" / "set up my main toolbar" and the model makes Ledgr-correct moves). **8 new tools (12 → 20), each a thin wrapper over the same owner-scoped Build libs the REST routes use** (the ADR-047/071 discipline — **safety lives in the `parse*` validators**, so the model can't persist an illegal config): `describe_workspace` (read-before-write summary snapshot), `create_type`/`update_type`, `create_view`/`update_view`, `create_dashboard`/`add_widget`, `update_nav`. **No config delete tool** (shape by create/update only; destruction stays in the Build UI). **The one protocol-surface addition: a new MCP `resources` capability** (`resources/list`/`resources/read` + an empty `resources/templates/list` in `server.ts`; the stable orientation guide in new pure `src/lib/mcp/guide.ts`, `ledgr://guide/workspace-shaping`) — still additive (existing clients/tools + the transport version are unchanged). Brandon's design calls: `describe_workspace` = **summaries + drill-down** (full property/filter detail stays in the existing `list_types`/`list_views`; no `body`, Principle 8); config-write safety = **conversational confirm + soft-delete + no-delete-tool**, no new infra (Track-changes/TC1 covers content, not config). **For your modules:** the shaping tools cover your module types/views/dashboards for free (they key off the same registry + owner-scoped libs; a module-specific tool would still be a later `tools.ts` extension). `verify-mcp` **101/101** on live Neon + a live `POST /api/mcp` HTTP round-trip (both capabilities, 20 tools, the guide reads, `describe_workspace` returns the real workspace); tsc/eslint/`next build` clean; **no schema/migration/protocol-version/dependency change.** **One aside:** my dev DB was 2 migrations behind `main` (0030/0031) — `npm run db:migrate` if yours is too. Full record: ADR-102.
- **🔴 Heads-up (Tyler) — repo moved to the `strategicli` GitHub org and made PUBLIC (ADR-101; infra/collab, not a code-core change).** Why: Vercel's free Hobby plan won't deploy a *private org* repo, so to keep both our deploys free we chose public over Vercel Pro / a GitHub-Actions deploy workaround. **Action for you:** repoint your remote (`git remote set-url origin https://github.com/strategicli/ledgr.git`) and reconnect/import the repo in *your* Vercel (you're an org Member, so Hobby + a public repo deploys fine; set your own env vars). **Secrets were scanned clean before the flip** (no `.env` ever committed; full-history gitleaks across all refs = 0 findings) and **Secret Scanning + Push Protection are now ON** (a future accidental secret-commit gets blocked). Public exposes code + the planning docs (the PRD names the church/role), not keys, so nothing needed rotating. Reversible later (private + the Actions deploy route or Pro). Full record: ADR-101.
- **🔴 Heads-up (Tyler) — weekly DB backup now goes to OneDrive ONLY, not a GitHub artifact (ADR-103; security follow-on to ADR-101's public flip). The shared workflow `.github/workflows/backup.yml` changed.** A public repo makes GitHub Actions artifacts downloadable by any signed-in user, and the `backup` job had been storing a full `pg_dump` as a 60-day artifact — i.e. the whole DB was effectively downloadable (the pre-flip secret scan checked committed code, not artifacts, so this was missed). Fixed: the `upload-artifact` step is removed, OneDrive `/Ledgr/Backups/` is the sole destination, and a **missing Graph secret now FAILS the run** (was warn-and-skip). Brandon's two exposed alpha-DB artifacts were deleted, and the four Graph secrets are now set as repo Actions secrets. **Action for you:** the scheduled job runs on the repo's `DATABASE_URL_UNPOOLED` (Brandon's DB); when you wire up your own DB backup, supply `GRAPH_TENANT_ID`/`GRAPH_CLIENT_ID`/`GRAPH_CLIENT_SECRET`/`ONEDRIVE_EXPORT_UPN` or the run fails loudly — there is no artifact fallback anymore. Full record: ADR-103.
- **🟢 Heads-up (Tyler) — handoff triage (2026-06-22), docs only, nothing built.** A session re-grounded a stale-branch handoff (a prior session ran 139 commits behind `origin/main`; its ADRs/doc edits are dead and ignored) and captured Brandon's carried-forward intent into the repo, **verified against current main first**. New: a **"Handoff triage backlog (2026-06-22)"** section in `next_steps.md` (all post-1.0 — Brandon's bar is ~2 items out), and two new parked explorations — `explorations/two-way-relations.md` (derived reverse relation section; **CORE if built**) and `explorations/ai-workspace-shaping-over-mcp.md` (MCP write/orientation tools; **CORE if built**) — plus notes appended to `dashboard-widgets.md`/`project-items.md`/`mobile-swipe-navigation.md`. **Two CLAUDE.md working conventions now ADOPTED (2026-06-23, Brandon's norms — peek + flag if you disagree):** "scope the UI when you build something big" (a big feature must be legible to the other builder without a walkthrough; no unlabeled bespoke controls) and "defer by hiding, not deleting" (hide a not-ready feature behind a default/setting, don't rip it out — the feature-side analog of soft-delete). Both live in CLAUDE.md → Working conventions. No code, no schema, no ADR; nothing on the 1.0 sprint changed.
- **🔴 Heads-up (Tyler) — Events & calendar matching COMPLETE (ADR-094, CORE; you OK'd the four decisions). E1–E4 shipped; run `npm run db:migrate` (migrations 0027 + 0028 + 0029; E4 has none).** The FK `items.type→types.key` has no ON UPDATE CASCADE, so 0027 inserts `event` (carrying the old row's attributes), repoints `items`/`templates`/`views.filter`, then drops `meeting`; idempotent + a no-op on a fresh DB (seed.mjs now seeds `event`). It's **one umbrella `event` type** ("meeting-ness" = the prep panel + people, shown when an event has people). **Internal names were kept (no churn for you):** the `meeting_at` column, `src/lib/meetings/`, `MeetingPrep`/`MeetingTranscripts`, `/api/meetings/*`, the `transcript` relation role, the `meetingPrep`/`meetingTranscripts` canvas cards. Only user-facing surfaces + the MCP tool *examples* renamed. **If your module code hardcodes `type === "meeting"` or seeds a meeting fixture, switch it to `event`.** **E2 (shipped, migration 0028):** an ordinary `tag` type + a built-in `tags` relation field (targetType=tag, many) on task/event/note, so tagging is the ADR-067 chip box + create-on-miss (which makes a `tag`); tags are edges (role `tags`), people stay their own type. **If you add a `tags` field elsewhere, the targetType is `tag`.** **E3 (shipped, migration 0029):** calendar is now a pull-from list — a `calendar_events` cache; the sync upserts all events, auto-promotes matched ones (injected `shouldPromote`/`onPromoted`, so the engine stays matcher-agnostic), and feeds the rest as one-click Adds on `/events` (`listCalendarFeed`/`promoteCalendarEvent`, `POST /api/calendar/events/[id]/add`). **The sync result is now `promoted`/`cached` (was `created`) — if your code reads the calendar run result, update it.** **E4 (shipped):** a configurable per-event task-pull rule — `properties.taskPull` = OR'd groups of seeds (a seed = an item id or the `@people` sentinel), each group ANY/ALL; default = the people on the event (union). `getMeetingPrep` resolves open tasks through it; `TaskPullControl` edits it via `propertyPatch`; templates carry it in properties. New `src/lib/events/task-pull{,-service}.ts`. **🎉 The Events chunk (E1–E4, ADR-094) is COMPLETE — PRs #51–#54, all live.** Peek at ADR-094 + flag anything.
- **🟢 Heads-up (Tyler) — Track changes (version history + diff + undo) SHIPPED (non-core; no migration, no dependency, no ADR).** A `HistoryPanel` over the revision snapshots saves already write. New pure `src/lib/diff.ts` (word-level diff: prefix/suffix trim → LCS, size-guarded — reusable from any surface, e.g. a future AI-apply "show changes" preview); a `GET /api/items/[id]/revisions/[revisionId]` (`getRevision` — the by-id body read; the list route stays metadata-only, no `body` in list queries); `HistoryPanel` (lazy — no revisions query on item open). **The one shared-file touch: a `history` card added to `src/lib/canvas-layout.ts`** (vocab + `STATIC_LABELS` + `SPECS` + `kindOf`), gated nowhere — it renders for every type on both the classic and grid canvas, so your module types get version history for free wherever they render through `MarkdownCanvas`. `verify-revisions` 38/38 + `verify-canvas-layout` updated/green + in-browser on Neon. **Source-tagging was considered + dropped** (Brandon: provenance doesn't matter, undo is the point), so no `revisions.source` column — nothing core changed.
- **🔴 Heads-up (Tyler) — Templates redesign TPL1–TPL4 SHIPPED (ADR-093, CORE — reverses ADR-045 "templates aren't items"; you ratified the direction). Run `npm run db:migrate` (migrations 0025 + 0026).** Templates are now real prototype items, applied by `cloneItemSubtree`. What landed in TPL1 (the core data change):
  - **`items.is_template boolean not null default false`** (migration 0025). Covers the WHOLE prototype subtree, not just the root: a template's subtasks are template rows too. The invariant lives in `createItem` — `isTemplate = explicit ?? parentIsTemplate ?? false` (a child created under a template parent inherits it, so subtasks-in-the-canvas need no special-casing). **`cloneItemSubtree` never copies the flag**, so applying a template yields real (`is_template=false`) items automatically — your recurrence-materialize caller is unchanged, and a TPL2 "duplicate template" just passes `isTemplate:true` to the root.
  - **`templates` reshaped to a thin registry** (migration 0026): `{id, owner_id, type, name, prototype_item_id → items.id ON DELETE CASCADE, is_default, apply_config jsonb, created_at}`; the old `body`/`property_defaults`/`relation_defaults` columns are dropped — that content now lives on the prototype item (its body/properties/edges; the clone's carryRelations re-creates the preset edges). Partial unique `(owner_id, type) WHERE is_default`. **Alpha reseed (ADR-039):** migration 0026 does `DELETE FROM templates` before the NOT NULL — re-author any templates in the UI; your instance is unaffected until you migrate.
  - **Exclusion discipline** (the both-agree bit): every owner-scoped enumeration now carries `is_template = false`, paired inline with `isNull(deletedAt)` (no central predicate). Covered via the `viewWhere`/`today.live`/`needsExportWhere`/`overdueWhere` helpers + list/inbox/Build-counts/search/relations/mentions/ICS/push/todoist/trash. **By-id reads stay visible** (getItem/subtree/clone — the authoring/apply path). **FK nuance:** the `deleteType` guard (`countLiveItemsOfType`) KEEPS templates counted, so a type with a prototype can't be deleted into an `items.type` FK violation; only the Build *display* count excludes. **If you add a query over `items`, add `eq(items.isTemplate, false)` next to the soft-delete filter** or it'll surface template content.
  - **MCP:** `list_templates` now returns `{prototypeItemId, isDefault}` (not propertyDefaults/relationCount); `apply_template` deep-copies the prototype. No protocol/dependency change.
  - **TPL2 (non-core, additive — peek the clone option):** the one shared touch is **`cloneItemSubtree` gained an additive `isTemplate` override** (root-only; descendants inherit via createItem) so "Save as template" / "Duplicate" produce a template-flagged subtree — backward-compatible, your recurrence-materialize caller passes nothing and still gets real clones. New lib: `createTemplateFromItem` (clone an item's subtree → hidden prototype + registry row), `duplicateTemplate` ("Copy of …", never default), `getTemplateByPrototype` (banner lookup). New endpoints `POST /api/templates/from-item` + `/api/templates/[id]/duplicate`. UI: a `TemplateBanner` on the prototype canvas (ItemCanvas, both variants) + a `SaveAsTemplateButton` in the modal header & full-page chrome (real items only; a template's modal hides the generic Trash — its delete is the registry-aware banner) + index row actions (set-default/duplicate/delete). `verify-templates` **64/64** on Neon; tsc/eslint/build clean; **in-browser verified** (banner + save-as-template end-to-end → new prototype, set-default uniqueness live, no console errors). Supersedes `explorations/template-property-defaults.md`.
  - **TPL3a (non-core, additive — variable resolution):** a new **pure, client-safe `src/lib/template-vars.ts`** resolves `{{tokens}}` in titles + bodies at apply time — dates (`{{today}}`, `{{today+7d}}`/`-3d`/`+2w`/`+1m`/`+1y`, `{{tomorrow}}`, `{{nextSunday}}`…`{{saturday}}`, `{{now}}`) with optional `:format` (iso/long/short/us/day), `{{title}}` echo, and `{{ask:Label}}` fill-in prompts. Reuses your `nl-date.ts`/`recurrence.ts` (no dep). Apply (`createItemFromTemplate(ownerId, id, {answers, now})`) deep-clones then resolves over the whole cloned subtree (root title first so `{{title}}` echoes it); the prototype keeps its raw tokens. New `GET /api/templates/[id]/vars` (the ask-labels) + `POST …/apply` and **MCP `apply_template` now take `answers`**; "+ New" shows a prompt form (`TemplateApplyDialog`) when a template has `{{ask:…}}`, else applies straight through. `verify-template-vars` **33/33** (pure) + `verify-templates` extended (apply resolves dates/offsets/ask/title-echo over the tree, prototype untouched) all green on Neon; **in-browser end-to-end** ("+ New ▾" → pick → "Passage?" form → item titled "Sermon for Jun 28" with the answer + resolved date in the body). TPL4/TPL5 remain. **For your modules:** `resolveVars`/`scanAskLabels` are reusable from any client/server surface.
  - **TPL3b (non-core, additive — date rules):** the registry `apply_config jsonb` now holds **per-field date rules** for the applied item's dated fields — `{ dueDate?, scheduledDate?: { mode:"none"|"offset"|"fixed", days?|date? } }` (typed `ApplyConfig`/`DateRule` + `resolveDateRule`/`parseApplyConfig` in `template-vars.ts`). The clone clears the prototype's own dates, so these rules are the source: on apply, `offset` = apply-date + N days (0 = apply day), `fixed` = an absolute day, `none`/absent = empty. **Setting the root's scheduled cascades to relative subtasks (ADR-085)** via `updateItem` — so a template's relative subtasks land correctly off the applied date. Authored from a "Dates" panel in the Template banner (PATCHes `apply_config`); `updateTemplate` persists it; `parseTemplateInput` accepts it. `verify-template-vars` **50/50** + `verify-templates` extended (offset/fixed/none on apply, relative-subtask recompute, config round-trip) green on Neon; tsc/eslint/build clean; **in-browser** (banner "Dates" → Scheduled "Days after apply" 3 → apply → item scheduled = today+3). No schema/migration (the column shipped in TPL1).
  - **TPL4a (non-core — create UX):** a per-type **default** template (the `is_default` you ratified in TPL1) is now applied by the primary **"+ New"** click; a **▾** opens a chooser (Blank + every template, default ★, each with a subtask-count/starter-body preview). New `listTemplatesForPicker(ownerId, type)` (one query, default-first, live-prototype join + subtask count) behind `GET /api/templates?…&preview=1`; `NewItemButton` is now a split button; the TPL3 `{{ask:…}}` dialog rides every apply path. No schema change. `verify-templates` extended (picker default-first + preview + owner-scoping) green; tsc/eslint/build clean; in-browser verified.
  - **TPL4b (non-core — apply-to-existing):** `applyTemplateToExisting(ownerId, templateId, targetId, {mode, answers, now})` merges a template into an already-started item. **fill** sets only empty scalars, skips a non-empty body, adds only properties the target lacks (via `propertyPatch`), sets due/scheduled only if empty, adds the template's subtasks the target doesn't have (deduped by resolved title), adds missing relations — "change only the unchanged". **overwrite** replaces scalars/body/shared-props/dates but still only ADDs subtasks/relations (never deletes the target's). Tokens + date-rules resolve into the merged content (`{{title}}` echoes the target's final title); new relative subtasks recompute off the target's scheduled. Same-type / not-template / not-trashed / owner guards. `POST /api/templates/[id]/apply` gained `{targetId, mode}` (else create-new); `ApplyTemplateButton` (picker + fill/overwrite + `{{ask}}` fields) in the modal header + full-page chrome. **For your modules:** any same-type item can have a template merged onto it. `verify-templates` +18 (fill/overwrite field-by-field, token+relation merge, dedup, guards) green; tsc/eslint/build clean; in-browser verified.
  - **🔴 TPL5 (SHIPPED — the chunk is COMPLETE; touches the MCP/API contract, additive — ratify):** MCP parity + the dashboard widget. **`list_templates`** now reports each template's **`askLabels`** (`templateAskLabels` — the `{{ask:Label}}` prompts, so an MCP client knows which `answers` to send), **`isDefault`**, `prototypeItemId`, and `applyConfig` (when set). **`apply_template`** gained **`targetId` + `mode`** → with `targetId` it calls `applyTemplateToExisting` (fill | overwrite) instead of creating, mirroring `POST /api/templates/[id]/apply` (the lib already had it; the tool just needed the args). **This is additive parity** — new optional args + output fields, the original 12 tools' shapes unchanged — within ADR-093's ratified scope and your ADR-071 tool set, **not a new core decision; peek + flag if you disagree.** The **`new-from-template` dashboard widget**: its settings/parser/`ActionWidgetBody` render existed since the dashboards slices but were **unreachable** — TPL5 added an **"Actions"** section to `AddWidgetMenu` (+ `handleAddAction` in `DashboardClient`) so it's addable, and replaced the settings popover's raw template-UUID paste with a **template `<select>`** from `/api/templates` (which also sets the widget's `targetType` from the chosen template); an unconfigured widget shows a "Pick a template in settings" guard instead of silently creating a blank item. **For your modules:** the MCP tools cover your module-type templates for free; the widget picks any of your templates. `verify-mcp` 71/71 + `verify-dashboards` 21/21 + `verify-templates` 105/105 green; tsc/eslint/`next build` clean; in-browser on Neon (add → guard → pick → one-click apply → new item with resolved body + subtask). No schema/migration/protocol/dependency change.
- **🔴 Heads-up (Tyler) — Block anchors + promote-from-canvas SHIPPED (ADR-090, CORE: extends the canonical body-format contract; you agreed this session). No migration, no dependency.** Pulled the parked `block-linked-action-items.md` forward (the `^block-id` anchor M1/ADR-038 "kept the door open" for never actually existed — now built). **The core bit:** a line can carry a trailing **Obsidian-style `^id` marker** in the body markdown (plain text, no schema change) as a stable back-reference target. `markdownToHtml`/`markdownToText` **strip it** (share/print/export/FTS stay clean — verified against the print route); the editor **dims** it. **For your modules:** any markdown body gets anchors for free; **if a module ever renders body markdown outside `markdownToHtml`, run `stripBlockAnchors` (`src/lib/editor/block-anchor.ts`)** so markers don't leak. The promote UX (non-core): a checkbox line's hover **"→ task"** → popup pre-filled with the line text (title) + de-indented sub-bullets (body) → task `inbox:false` related to meeting+people, storing **`properties.source.blockRef`**. Reuses `promoteActionItem`. **Round 2 (same session) shipped the rest:** the read-side **"✓ task" badge** (links to the task, replaces the button so a line can't be double-promoted — `promotedBlockRefs` server query → pushed into plugin state), the `#^id` **deep link** (scroll + a plugin-managed flash decoration), a **"Copy link to this line"** toolbar button (anchors the cursor's line + copies `/items/<id>#^<id>`, **any line/type** — your modules' markdown bodies get it for free), and a **task → source-line back-link**. Only **public-share `#^id` anchors** (`/share/<token>#^id`) remain open. `verify-block-anchor` 32/32 + `verify-meeting-prep` 11/11 + in-browser on Neon (also fixed a pre-existing `verify-meeting-prep` fixture that didn't set `status_category` post-ADR-082). Also shipped (non-core polish): removed the meeting "Agenda" frame, "Meeting prep"→"People", heading icons via a shared `SectionHeading`. Full record: ADR-090.
- **🟢 Heads-up (Tyler) — Meeting Recording v1a SHIPPED (ADR-087; you pre-approved the feature + all 3 core touches). Run `npm run db:migrate` (migration 0023 inserts the `transcript` type) + `npx tsx scripts/seed-meeting-views.mts` for the awaiting view.** Paste-first transcripts → minutes via a **Claude-over-MCP workflow, not an in-app LLM call**. What landed:
  - **A `transcript` system type** (migration 0023 + `seed.mjs`): is_system, visible, `show_in_quick_capture=false`, with a `minutes` select (`none`/`draft`/`done`). **NOT added to `coreModule`** — an unregistered seeded type already falls back to the default markdown canvas (same path your `paper`/`song` would take if unregistered), so the core module contract is untouched.
  - **A transcript is a child AND a related item** (`src/lib/meetings/transcripts.ts` `createTranscript`): `parent_id = the meeting` (cascade) **plus** a confirmed `relations` edge meeting→transcript, role `transcript`. The edge is deliberate — it makes "list recent meetings → pull their transcripts" one hop for the MCP graph (`get_item(meeting).related` / `list_items(relatedTo)`), since parent/child is invisible to relations. **If a module ever models a sub-document of a parent, this child+edge pattern is the one to copy** so the LLM can traverse it.
  - **The "needs minutes" signal is a `select` property** (`minutes` on the transcript), so the **"Transcripts awaiting minutes"** saved view is a plain `propertyFilters:[{minutes:none}]` (the ADR-072 `properties @>` path) — no engine change.
  - **The minutes step is the existing 12 MCP tools, no new tool** (`run_view`→`get_item`→`update_item`→`create_item`→`update_item` with `propertyPatch`). The prompt is documented (`docs/meeting-minutes-automation.md`) + surfaced on Build → AI & MCP.
  - **🟢 CORE you pre-approved — the Principle-3 reading (ADR-087 point 5):** a *scheduled* external Claude-over-MCP automation producing **staged** outputs (draft minutes, Inbox tasks, transcript left `draft`) is consistent with "no AI in Ledgr's own cron" because Ledgr's backend stays model-free and **nothing auto-commits.** This amends how Principle 3 reads — peek + flag if you disagree.
  - **One shared-file touch:** `src/lib/canvas-layout.ts` gained a `meetingTranscripts` card in the meeting vocabulary (the panel renders in both the classic and grid canvas). `MarkdownCanvas` gates the panel on `item.type === "meeting"`. `verify-meeting-transcripts.mts` 16/16 + in-browser on Neon; `tsc`/build/eslint clean.
- **🟢 Heads-up (Tyler) — Meeting Recording v1b SHIPPED too (ADR-088/089; the other 2 pre-approved core touches). Run `npm run db:migrate` (migration 0024 adds `attachments.purge_after`).** Audio upload → auto-transcribe, all deterministic plumbing over the seam (no model in Ledgr's loop; minutes stay Claude-over-MCP). Decisions settled with Brandon at build: **defer compression** + **client-poll + cron backstop**.
  - **🟢 CORE — the `transcription` provider seam (ADR-088):** `src/lib/transcription/{types,provider,assemblyai}.ts`, **null-safe + env-selected exactly like `getStorage()`/`tasksAdapter()`** (enabled by `ASSEMBLYAI_API_KEY`; `TRANSCRIPTION_ADAPTER` selects, default assemblyai, `none` disables). Submit+poll job model (long files transcribe over minutes). AssemblyAI first adapter, **no SDK** (two fetch calls; pure `mapTranscriptResponse`). `/health` `checks.transcription`. **This is a new provider seam — peek + flag.** Swap/add adapters (Deepgram, a local WASM Whisper) without touching callers.
  - **🟢 CORE — `attachments.purge_after` (ADR-089, migration 0024):** nullable timestamptz; audio is transient — a completed transcription stamps it `now()+30d`, the daily purge (`/api/machine/purge`) reclaims the bytes (R2 + row). Null = keep (every non-audio attachment). **Peek at the schema touch + flag.** `deleteAttachment` + `DELETE /api/attachments/[id]` = delete-now.
  - **Non-core plumbing:** `audio/*`/`video/*` allowed at a 2GB cap (`maxFileBytesFor`); `transcription-service.ts` (`startAudioTranscription`/`advanceTranscription`, provider **injected** so it's node-testable with a fake — no key); routes `POST /api/meetings/[id]/transcribe`, `GET /api/transcription/[id]/status`, `GET /api/machine/transcription-poll`; `.github/workflows/transcription-poll.yml` (15-min backstop); `AudioUpload` + `TranscriptionPoller`. The audio path **rejoins the v1a minutes flow** — a finished transcript enters the awaiting-minutes view exactly like a pasted one.
  - **For your modules:** the seam is generic — if a module ever needs speech-to-text it goes through `getTranscription()`. Nothing about the core type/canvas/relations contract changed beyond the additive column. `verify-transcription` (pure) + `verify-transcription-flow` 20/20 + `verify-audio-retention` 10/10, all on Neon; tsc/build/eslint clean. **Live audio eyeball gated on an `ASSEMBLYAI_API_KEY`** (runbook §1i) — Brandon's instance is paste-only until he sets one, so the upload control is hidden there for now.
- **🔴 Heads-up (Tyler) — Tasks Polish chunk underway (finishing touches on Native Tasks, 2026-06-18). You greenlit all of it; S2 is the CORE one to peek at when it lands.** **S1 (shipped, non-core, no migration):** `scheduledDate` is now a first-class sort field / date-filter field / board+agenda group bucket / list+table column across the view engine (`views.ts`, `view-grouping.ts`, `ViewRenderer`, `ViewBuilder`, `BoardDnd`), plus a **Date (due | scheduled) + When** selector on the Tasks list and scheduled shown on subtask rows (UTC-correct). Additive; your module types pick up the scheduled dimension for free. **S2 (SHIPPED — ADR-082, CORE; run `npm run db:migrate` for migration 0022):** configurable statuses — `items.status` is now a text *key* + a denormalized `items.status_category` enum (not_started/in_progress/done/archived) every hot query / the checkbox / recurrence key off (open/done/archived preserved + backfilled). Per-type **`types.status_schema`** (null = inherit the default 3; the In Progress category ships empty, plumbing ready). Pure engine in `src/lib/status.ts`. A `StatusSchemaEditor` on the type edit page → `PATCH /api/types/[key]/statuses` (validates + re-syncs item categories). The done-checkbox flips via a new **`POST /api/items/[id]/complete`** (`toggleItemDone`, recurrence-aware) so client checkboxes need no schema; the **kanban shows every custom status as a colored column in schema order**; `/tasks` + the view builder filter by `ViewFilter.statusCategory`. **This touches the status field + the machine/MCP status contract — peek at ADR-082 + flag.** Your module types get configurable statuses for free (give the type a `status_schema`, or inherit). `verify-statuses` 26/26 + in-browser on Neon. **S3** recurrence-completions calendar + per-occurrence carve-out, **S4** NL quick-add (title → date/recurrence/urgency), **S5** relative subtasks, **S6** focus/reminder/ICS loose ends follow.
- **🟢 Heads-up (Tyler) — S6 SHIPPED (ADR-086, NOT core; no migration). The Tasks Polish chunk (S1–S6) is complete.** The loose ends from ADR-077/078/079: **ICS EXDATE** (a recurring task's feed now drops its `completeInstances ∪ skippedInstances` via `EXDATE` — `ics.ts`/`ics-data.ts`); a **per-task reminder picker** (`ReminderControl` → `properties.reminder.minutesBefore`, which the feed already honored); a **focus-today star on the item canvas** (reused `FocusStar`); a **"planned" marker** on the Tasks list (scheduled-no-deadline); and a **Top-3 focus dashboard widget** via a new **`ViewFilter.focusedToday`** flag (index-backed `properties @> {focus:{date:<today>}}`, today resolved at query time since the marker auto-clears) + a "Today's Focus" starter widget. **For you:** `focusedToday` is a new non-core ViewFilter key (additive; your module-type tasks can use it); the canvas focus-star + reminder are classic-path only (a custom grid layout can add cards later). Deferred: list/board-row focus stars, focus-zone drag-reorder, the optional colored status popover. `verify-ics`/`verify-views` extended + in-browser. **No data-model/contract change.**
- **🟡 Heads-up (Tyler) — S5 SHIPPED (ADR-085, NOT core but touches two primitives you ride; no migration).** **Relative subtasks** — a subtask stores `properties.relativeSchedule = {offsetDays:N}` (N days from the parent's scheduled date), its `scheduled_date` column holds the derived concrete day. Set by calendar-pick (`SubtaskSchedule` control back-calculates the offset). **Two shared touches to know:** (1) **`cloneItemSubtree` now derives a relative child's `scheduled_date`** from the new clone's scheduled day + the carried offset (instead of resetting to null) — so your **materialized occurrences AND the subtree-aware template apply** get correctly-dated subtasks for free; the offset is NOT stripped (it rides the clone). (2) **`updateItem` recomputes relative children whenever a task's `scheduledDate` changes** (and the recurrence-advance / S3 calendar paths call the same `recomputeRelativeChildren`) — direct child-writes, depth-capped, no re-entrancy. Pure helpers in client-safe `src/lib/relative-subtask.ts`; the recompute in `src/lib/relative-subtask-service.ts`. `SubtaskNode` gained a `relativeOffset` field (the subtree query now selects `properties`). `verify-relative-subtasks` 26/26 + in-browser. Nothing in the data-model/contract changed.
- **🟢 Heads-up (Tyler) — S4 SHIPPED (ADR-084, NOT core; no migration).** **Natural-language quick-add** — type a task title like "Call Bob tomorrow p1 every week" and the date/recurrence/urgency are pulled out, the title left clean. New pure **`parseTaskTitle(title, today)`** in `src/lib/nl-date.ts` (extends the ADR-077 parser; reuses `makeRecurrence`/`parseNaturalDate`) → `{title, scheduledDate, dueDate, recurrence, urgency, detections[]}`. Grammar: **date → scheduled**, **`by <date>` → due**, recurrence → RRULE (`every day/week/…`, `daily/weekly/…`, `every N units`, `every <weekday>`, `every weekday`), urgency `p1..p4`/`!1..!4`. Wired into the global **CaptureModal** (type=task only): a live "Detected" chip preview + a ✨ Apply (strip-and-confirm) + parse-on-save. **For your modules:** the parser is reusable from any client surface (it's pure/client-safe); a module that wants smart capture for its task-like type can call `parseTaskTitle` the same way. Nothing in the data model/contract changed. `verify-nl-task` 36/36 + in-browser on Neon.
- **🟢 Heads-up (Tyler) — S3 SHIPPED (ADR-083, NOT core; no migration).** A **completions calendar** on a recurring (virtual) task + the **inverted occurrence edit** — both are operations over the *existing* recurrence model (ADR-076), so **no schema, no `properties.recurrence` shape change, no MCP/seam change.** What's there: a `RecurrenceCalendar` month-grid where every occurrence date is individually checkable in any order (toggles the per-date completion log); and **carve-out** — "edit this occurrence" deep-clones the series prototype (`cloneItemSubtree`) into a fresh **DETACHED** one-off for that date (no `occurrence` role, so the materialized create-next machinery never fires on it), the series skips the date + advances, you land on the new item. This replaces ADR-076 §9's deferred "this & future" RRULE-split. New pure helpers in `src/lib/recurrence.ts` (`isOccurrence`/`toggleCompleteInstance`/`addSkippedInstance`/`instanceState`), service fns in `recurrence-service.ts` (`toggleOccurrenceCompletion`/`carveOccurrence` — recompute scheduled+status server-side), and one endpoint **`POST /api/items/[id]/occurrence` `{action:"toggle"|"carve", date}`**. New `recurrenceCalendar` canvas card (ADR-069 vocab). **Your module-type tasks inherit the calendar wherever they render through `MarkdownCanvas` and `item.type === "task"`** (same gate as the Repeat control). `verify-recurrence-calendar` 39/39 + in-browser on Neon.
- **🔴 Heads-up (Tyler) — MCP tool additions (ADR-071, CORE — the machine/MCP contract). Additive only; ratify.** The six MCP tools (ADR-047) predated properties/typed-relations/views/templates maturing; I closed the gap. **Two patched tools:** `update_item` now takes **`propertyPatch`** (atomic per-key merge, ADR-069 — set one custom property without clobbering the rest), and `list_types` now reports a relation field's **`targetType` + `cardinality`** (ADR-067), not just select options. **Six new tools, all thin wrappers over the owner-scoped libs** (the ADR-047 discipline holds — no drift, no skipped scoping): `relate_items`/`unrelate_items` (add-or-confirm / remove an edge between two existing items; optional `role`; `suggestedOnly` to reject a match), `list_views`/`run_view` (discover + run a saved view by its own filter+sort), `list_templates`/`apply_template`. **6 → 12 tools.** No protocol/schema/dependency change; the original six are untouched. **When you add a module type, all 12 tools cover its items for free** (they key off `items.type` + the registry; a module-specific tool would still be a later `tools.ts` extension). `verify-mcp` extended (12-tool list, propertyPatch merge, relate/unrelate, views, templates, owner-scoping) — ALL PASS on Neon. Full record: ADR-071.
- **🟢 Heads-up (Tyler) — view + capture papercuts (ADR-072, NOT core), touch two shared surfaces you build on.** Three of the toward-1.0 papercuts: (1) **Quick Capture now defaults to the catch-all `unmarked`** ("Unsorted" in the picker), not `task` — captures stay `inbox:true` for triage. (2) **List-filter by a custom property** — `ViewFilter` gained `propertyFilters` (index-backed `properties @>` containment over `items_properties_gin`, scoped to select/multi_select); a dropdown per filterable property in `ViewBuilder` + on the `/tasks` and `/list/[type]` bars (`prop_<key>` URL params, via new `propertyFilterOptions`/`propertyFiltersFromParams` helpers). `/list/[type]` now runs through `queryViewItems`. **If a module ships a stage-style type, its select properties become list filters automatically** — the filter counterpart to the board-grouping you already get. (3) **Kanban DnD** — a new client `BoardDnd` (native HTML5 drag, no dep) makes board cards draggable between columns when the grouping is status/urgency/single-select; the drop→PATCH map is the pure `boardDropPatch` in `view-grouping.ts`. **`ViewRenderer` gained a `boardDraggable` prop — dashboards leave it false, so a board widget stays read-only** (no clash with your dashboard grid). No migration, no dependency. `verify-views` extended — ALL PASS.
- **🟡 Heads-up (Tyler) — corrected the `person` type's `is_system` flag (ADR-070, CORE but drift-only). Run `npm run db:migrate`.** A live query showed `person` was the only base type with `is_system = false` (task/meeting/note/link/unmarked were true; paper/song/hiring_candidate correctly false). Your migration 0013 (entity→person, ADR-055) meant person to be `is_system = true`, but its `INSERT … ON CONFLICT DO NOTHING` kept a pre-existing `false` row. That left person **user-deletable** (the delete guards in `src/lib/types.ts` key off `is_system`, and it's the relational hub per ADR-055/060) and badged as custom in Build. Fix is **migration 0020** — an idempotent `UPDATE "types" SET "is_system" = true WHERE "key" = 'person'`, i.e. drift-correction to your already-agreed intent, not a new decision. **No seed change** (`seed.mjs` already inserts person `is_system=true` — its loop hardcodes it for every system type). No schema-shape change; `ListTabs`' `systemTypeKeys` backstop stays. Brandon's live DB already corrected; yours converges on `db:migrate`.
- **🟢 Heads-up (Tyler) — item-view inline editing (Feature A, ADR-068) is NOT core** (UI over existing APIs, no schema/migration change), but it touches files you build on. New leaves `src/components/relations/InlineTitle.tsx` (related-title pencil edit) + `src/components/build/InlineLabel.tsx` (right-click rename a type/field label → inline, or "Edit in builder…"). Renames go through a new **`PATCH /api/types/[key]/rename`** over `renameTypeLabel`/`renamePropertyLabel` (`src/lib/types.ts`) — **labels only; the key/role is immutable**, so values + relation edges are untouched (`verify-inline-rename.mts` proves a typed edge survives a field rename). Wired into `RelatedRow`/`RelationField`/`RelatedPanel`/`RelationProperties`/`CustomProperties` (now take a `typeKey` prop, threaded from `MarkdownCanvas`/`ChordCanvas`). Ride-along: a × to clear a scalar property value. Your module canvases get the label-rename + title-edit for free wherever they reuse these panels. In-browser eyeball pending.
- **🔴 Heads-up (Tyler) — Feature B is BUILT (2026-06-17, ADR-069, CORE — you'd pre-blessed the direction). B1 done end-to-end; in-browser verified against live Neon.** The default `MarkdownCanvas` can become a free 2D `react-grid-layout` grid (your v1.5.3 engine), **field-level** (every system field / custom property / relation field is its own card; **body, title, Related stay single blocks**), **per type**, **per breakpoint** (arrange `lg`; `md`/`sm` auto-derived; sm single-column, view-only), each card **flow** (auto-height) or **fixed** (pinned, scrolls). **What to know for your modules / coordination:**
  - **Schema/contract changes (additive, run `npm run db:migrate`):** **migration 0019** adds nullable **`types.canvas_layout` jsonb** — **null = today's stacked render, untouched** (the common case, zero risk; your module types are unaffected until someone arranges them). `TypeDefinition` gained `canvasLayout` (tolerant-parsed, bad shape → null). **`CanvasProps` gained `arrange?: boolean`** (true only on the full-page `?arrange=1`; module canvases ignore it). The shape + all pure logic live in client-safe **`src/lib/canvas-layout.ts`** (vocab / `defaultLayout` reproducing today's order / tolerant parse / `reconcile`-on-read / responsive) — the `dashboard-widgets.ts ↔ dashboards.ts` split you'll recognize.
  - **New API:** **`PATCH /api/types/[key]/layout`** (`{layout}` or `{layout:null}` to reset) over `setTypeCanvasLayout` — focused, like the ADR-068 rename route, so it can't clobber a builder edit. Owner-guarded.
  - **One `items` behavior addition (no shape change): `propertyPatch`** on `ItemPatch`/`parseItemPayload` → atomic Postgres `properties || $patch::jsonb` in `updateItem`. Needed because each scalar property is now its own card and a wholesale `{properties}` PATCH would clobber siblings. `CustomProperties` now sends `propertyPatch`; classic render still correct. **If your code writes a single property key, prefer `propertyPatch` over re-sending the whole object.**
  - **Reuse seams:** `ItemEditor` gained a **`slot` prop** (`title`/`body`/`full`) so title and body render as independent autosaving cards. The grid (`ItemRglInner` behind `ItemLayoutGrid`, `dynamic ssr:false`, mirrors your `DashboardGridLayout → RglInner`) takes a **`Record<CardId, ReactNode>`** — your async server panels pass straight through the RSC boundary; the grid only positions them. A scoped **`.canvas-card-content`** CSS rule strips the panels' full-width canvas chrome inside a cell.
  - **Bespoke module canvases (Chord/Paper) are OUT of scope** — Feature B is the **default canvas only**. Yours are unaffected; they opt into arrangeability at the module level in a later slice. **B2 (top-strip/footer field placement, PRD §4.13) is subsumed** — field-level placement delivers it. Deferred: grouping/collapsible container cards (the `CardMeta` shape leaves room). The spike (`/scratch/layout`) graduated into `ItemRglInner` and was deleted. Full record: ADR-069; `verify-canvas-layout` 36/36.
  - **Post-build refinements (2026-06-17, Brandon feedback) — a couple touch shared files you build on:** (a) **new app-wide save signal `src/lib/save-status.ts`** (`beginSave`/`endSave`/`useSaveStatus`, a module singleton) + a floating `SaveStatusIndicator` in `ItemCanvas`; ItemEditor's inline "Saved" badge is gone — **if your canvas autosaves, call `beginSave()`/`endSave(ok)` to feed the same indicator.** (b) `ItemEditor` title is now an auto-grow wrapping `<textarea>` (was an input). (c) `CustomProperties`/`RelationProperties` gained a `hideHeading` prop (drops the "Properties"/"Relations" heading on per-field cards); `CustomProperties` text-kind is now a wrapping textarea. (d) Per-field cards show no header label in arrange (just handle/pin/hide); the flow/fixed pin is monochrome text. (e) Full-page canvas widened via `.canvas-wide` (your module canvases that render through `MarkdownCanvas` panels inherit the wider page automatically; bespoke canvases set their own width). (f) **Each viewing surface maps to a breakpoint** (`GRID_BREAKPOINT_PX = {lg:1024, md:480, sm:0}`; the grid is `w-full` so it follows its container): the full-page expand fills the browser → `lg`/Desktop, the modal panel (~768) → `md`/Tablet, a phone → `sm`/one-column. The modal is the default 3xl (NOT widened for layouts — an earlier pass did that, reverted); a classic/non-customized item keeps a readable column on the full page. (g) The arrange editor has a **Desktop/Tablet/Phone width switcher** so all three layouts are arrangeable from the full page (Tablet ≈ 700px matches the modal; the grid remounts on switch so WidthProvider re-measures cleanly; the flow measurer writes height to the active breakpoint only). None of this changes the core type/canvas contract.
- **🔴 Heads-up (Tyler) — Relations chunk is BUILT (2026-06-15, ADR-067, CORE — you'd already confirmed the design). Branch `relations-typed-properties`, R1–R4 done.** Un-defers the **`relation` property kind**: typed relation fields (a Book's Author, a Meeting's Attendees) **addable to any type by the user** in the builder (a "Links to" type dropdown + "How many" cardinality, inline per property row). **A relation field's value is `relations` edges with `role` = the field key, NOT `items.properties`** — one source of truth for links, and typed links show in the generic Related panel for free. Plus **inline create-on-miss** on one rule: typed box → make that type (no Inbox); free-text `@` or generic +Relate or an untyped field → make a hidden **`unmarked`** item (glyph label `◌`, never shows the word) flagged for the Inbox, retyped at triage. **What to know for your modules / coordination:**
  - **Schema/contract changes (additive, no `relations`-shape change):** `PROPERTY_KINDS` widened to 8 (`relation`); `PropertyDef` gained `targetType` (null=any) + `cardinality` (single|many); **migration 0018 seeds the hidden `unmarked` system type** (also in `seed.mjs`) — run `npm run db:migrate`. A relation field key **can't be the reserved roles `mention`/`related`** (a guard in `parsePropertySchema`).
  - **New lib surface you can reuse:** `outgoingRelationsByRole(ownerId, itemId, roles[])` in `src/lib/relations.ts` (directional, role-bucketed reads — the typed-field value); `unrelateItems` + the relations DELETE endpoint take an optional `role` for role-scoped removal. A relation field's links are just edges, so your module types get typed relation fields for free (declare them in the type's `property_schema`).
  - **Create-on-miss respects ADR-015:** the free-text `@` path only creates the item + inserts the mention node; the save-time diff-sync writes the edge, so it never fights the body-owned mention sync.
  - Generalizes your "Bible passages = first use of the relation kind" note — the kind is now built for everyone. Full record: ADR-067; slices logged in `roadmap.md`/`next_steps.md`. **In-browser eyeball still pending; the `◌` glyph is Brandon's to repick.**
- **🟢 Heads-up (Tyler) — editor parity (2026-06-15) is NOT core**, but it touches two files you also build on: **`src/components/markdown-editor/extensions.ts`** (added `LedgrImage`/`LedgrTable` binding `@tiptap/markdown`'s `renderMarkdown`/`parseMarkdown` hooks; re-exports `TableRow`/`TableHeader`/`TableCell`) and **`MarkdownEditor.tsx`** (paste/drop image upload + Image/Table/Strike toolbar buttons + an optional `uploadImage` prop). The encode/decode logic is in node-testable pure libs `src/lib/editor/{image-markdown,table-markdown}.ts` (the colors.ts/mention-markdown.ts pattern). **Two new deps:** `@tiptap/extension-image` + `@tiptap/extension-table` (same `@tiptap/*` vendor; roadmap-pre-blessed, low resilience risk — tables/images serialize to standard GFM/`![]()`). Your Songs/Papers canvases are unaffected (they declare their own canvases); if a future markdown-based canvas wants images, the `uploadImage` prop is the seam. `verify-editor-media.mts` 17/17; nothing about the core types changed.
- **🔴 Heads-up (Tyler) — the 6.14 build meeting produced THREE accepted CORE ADRs (both of us agreed in the meeting). Numbered 060–062 because your entity→person ADR (055) and the nav/type batch (056–059) took those numbers first. Logged in `decisions.md`:**
  - **ADR-060 — pre-built Bible/passage relational DB.** Ship the canon as a relational structure, **book → chapter → verse, verse atomic**; a passage range links each verse; sub-verse rejected. Plus a deterministic RefTagger-style auto-tagger (cron, no AI). This is the keystone module everything ties into — it's now decided (model + approach); the **build is module-level** (whoever's instance wants it). **Shape:** a bespoke `passage` type related to via the `relations` table — **not** the deferred `relation` property kind (your ADR-055 confirmed item-to-item relations already cover this), so the old "passage as an `entity.kind`" framing in the exploration is superseded.
  - **ADR-061 — notes are a module attachable to every type; relations (not files/folders) are the primary organizing principle.** A "project" is just relations, not a type. Dovetails with your ADR-055 (relations, not a meta-type column, are how things connect). The in-app org half of `storage-organization.md` is settled by this; the export→MD-file-tree mapping is still the open part we'll talk through.
  - **ADR-062 — Share/Print renders the canvas, with a per-property "include in share?" toggle.** Core-adjacent (a per-`PropertyDef` flag, like `show_in_quick_capture`). Your module types get the flag for free once it's built.
- **🟢 Resolved (your ADR-055, same evening as the 6.14 meeting) — entity vs. custom type.** The meeting re-flagged the confusion; you closed it that night by retiring `entity` → bespoke `person`, dropping `kind`, and making the related list universal. `explorations/entity-vs-custom-type.md` is marked resolved. The meeting docs are reconciled to match (the Bible hub is a `passage` type, not an entity kind; the `relation` property kind stays deferred).
- **🟢 Heads-up (Tyler) — the weekly health check (ADR-052) is NOT core** (a cron over existing canaries + the existing push sender). Worth knowing: **`gatherHealth()` now lives in `src/lib/health.ts`** (the `/health` route is a thin wrapper, the `HealthReport` reads every canary). If a module adds an integration with a freshness canary, add it to `gatherHealth` and, if a stall should page, a rule in `src/lib/health-check.ts`. The check is deterministic on purpose (Principle 3 — no model in a cron).
- **🟡 For our next sync (Tyler) — define v1.0 + pick a version scheme (answers your "v0.5 or v0.6?" note).** My proposed v1.0 bar, philosophically: **"v1.0 = Brandon can fully replace his Notion workflows with Ledgr."** Top priorities inside that line: meeting notes + transcription, task tracking, web-clipped note archive, and sermon writing/preaching. Everything *new* beyond Notion (calendar sync, daily/weekly planning, promote-tasks-from-notes, auto-assign-people-to-meetings) layers on in later versions, not v1.0. **I know that's very me-centered** right now, so: what do *you* need in the v1.0 line? Add your bar and we'll merge them. On the version *number*: the PRD's "v0.18" is a doc-revision number, not a product release, so there's no product version yet, which is why your question has no answer. Proposal: adopt SemVer-shape (`MAJOR.MINOR.PATCH`) mapped to the roadmap we already keep, **patch = a slice/ADR, minor = a tier/phase completed, major = 1.0 (the Notion-replacement bar) and later big reshapes (ADR-037 scale).** Then "bump the minor?" just means "did we finish a phase?" Worth 10 min at the sync.
- **🔴 To ratify (Tyler) — toward-1.0 decisions I settled 2026-06-17 (ADR-073/074/075, all CORE). Peek + flag, especially the `tasks` seam:**
  - **🔴 Tasks go fully native — ADR-073. RESOLVED → direction (B).** Dropping Todoist as the engine; Ledgr owns tasks end to end (native recurrence, rescheduling, a Top-3/in-focus layer, reminders via a Ledgr-published **ICS feed** + the existing web push, offline-outbox capture). **Per-instance behind the seam: I run native as the default `tasks` adapter; Todoist stays an optional adapter so you keep it (or Google Tasks) — no rewrite for you.** Reverses PRD §4.6/§5.2 (ADR-037 precedent). Build = the **Native Tasks chunk T1–T6** (`next_steps.md`/`roadmap.md`).
    - **🟢 The WHOLE Native Tasks chunk (T1–T6) is BUILT — ADR-076–081, PRs #17–#22.** Tasks are fully native on my instance; Todoist is now an **optional adapter** behind the new `tasks` seam (your path, unchanged). **The seam is the core change you were asked to ratify — peek + flag.** What landed, all additive (run `npm run db:migrate` — one migration, 0021):
      - **T1 recurrence (ADR-076):** migration **0021** adds `items.scheduled_date` (nullable, the planned date ≠ `due`); the rule + completion log live in **`properties.recurrence`** (no column); a hand-rolled RRULE subset (`src/lib/recurrence.ts`, **no `rrule` dep** — Principle 5, off-ramp documented); **`cloneItemSubtree`** (`src/lib/clone.ts`) — the shared deep-clone-with-reset primitive materialized occurrences use and **a subtree-aware templates extension should adopt**; **`updateItem` intercepts open→done on a recurring task** to advance instead of marking done (so your module-type tasks + the MCP `update_item` tool + REST all recurrence-advance for free). Model (C): one item + per-date log, no spawned clones, no overdue stacking; per-task virtual/materialized + fixed/after-completion.
      - **T2 scheduling (ADR-077):** reschedule shortcuts + a hand-rolled NL date parser (`nl-date.ts`, no dep) on the scheduled field + capture; a deterministic overdue auto-roll (`scheduling.ts`); future `scheduled` = deferred (off Today). **T3 focus (ADR-078):** a day-stamped `properties.focus` marker + a "Today's Focus" zone + a star on task rows (no column). **T4 reminders (ADR-079):** a published read-only **ICS feed** at `/api/ics/<token>.ics` (per-owner token in `settings.icsToken`; hand-rolled `ics.ts`, no dep; recurring tasks emit the stored RRULE verbatim) + the existing ADR-034 web push. **T5 offline capture (ADR-080):** a localStorage outbox (`outbox.ts`) flushed by an app-wide `OutboxSync`.
      - **🔴 T6 — the `tasks` seam (ADR-081), the core ratify point:** `src/lib/tasks/provider.ts` → `tasksAdapter()` = **`native` (default)** or **`todoist`** (opt in with **`TASKS_ADAPTER=todoist`** + `TODOIST_TOKEN`; falls back to native if unconfigured). The three Todoist sync entry points (machine cron / "sync now" / webhook) **skip cleanly when native** (`{skipped:true, adapter:"native"}`, 200 — no 503 noise). `/health` gains `checks.tasksAdapter`. **Your Todoist code is untouched** — set `TASKS_ADAPTER=todoist` on your instance and it runs exactly as before (Phase-4 packageable). `.env.example` + runbook §1d + PRD §4.5/§5.2 reversal notes updated. **Peek at ADR-081 + flag anything; the seam is additive and your path is the opt-in adapter.** Verifiers: `verify-recurrence`/`verify-scheduling`/`verify-focus`/`verify-ics`/`verify-outbox`/`verify-tasks-seam` all PASS; `verify-health-check`/`verify-provider-seams` green.
  - **🟡 Microsoft-only for v1.0 — ADR-074. RESOLVED → defer Google/iCloud.** v1.0 ships Microsoft-only for calendar + email-in; the Google/iCloud adapters land in *your* instance when you adopt (the seam discipline stays, only the extra adapters wait). Answers PRD Q7 for v1.0; resolves the provider-seam half of `explorations/provider-seam-calendar-email.md`.
  - **🟢 No confidential tier — ADR-075. RESOLVED → declined for v1.0.** The single-user + Clerk + owner-scoped posture is enough for my content; if you want a stricter tier for discipleship content in your instance, that's a future ADR.
  - **🟡 Still open for our next sync — the calendar event-model + time-blocking** (NOT resolved above): today calendar sync makes *every* event a `meeting` item (ADR-023) — generalize into a general **event** type (meeting = has people/prep, time-block = linked to a task)? Plus the time-blocking front-end (manual drag, a deterministic slot-finder/overdue-rescheduler, an AI "slot 40 hours before August"). `explorations/calendar-time-blocking.md`. The native-tasks **ICS feed** (ADR-073) is now the calendar-surface + reminder path here, replacing the old "lean on the Todoist feed" idea.
- **🟢 For-discussion (Tyler) — new exploration doc, no code:** `explorations/storage-organization.md` parks a question I want us to wrestle together — how each type is organized in-app (sub-pages, folders, tags, archive) and how that lays out as exported MD files for Obsidian-style local use. The crux is mapping Ledgr's relation graph to a file tree: one primary parent → folder path, everything else → `[[wiki-links]]`, and whether that link set is enough to rebuild the graph from files alone. It's **core** (relations/schema), so it's both-agree + ADR before anything builds. Nothing to do now, just read when you have a minute and we'll talk.
- **🟢 Approved (Tyler) — meeting recording/transcription/minutes; pre-approved 2026-06-18, building next (`explorations/meeting-recording.md`).** Upload-only for v1, transcript-as-pivot (paste OR upload→transcribe), audio is transient (compress-on-ingest + purge-after-confirm), and the minutes+action-items step is a **Claude-over-MCP workflow, not an in-app LLM call** (fires manually or scheduled; reuses the ADR-047/071 tools; outputs staged as drafts / Inbox suggestions). **Three CORE touches (all pre-approved; an ADR each at build):** (1) a new `transcription` provider seam, (2) an audio-retention schema column on `attachments`, and (3) a Principle-3 interpretation (a *scheduled* Claude automation over MCP producing staged suggestions is consistent with "no AI in Ledgr's own cron" so long as nothing auto-commits). v1a (paste-first) needs almost no new infra; v1b adds the transcription seam + storage. Full detail + cost table in the doc.
- **🟡 Heads-up (Tyler) — feedback features (2026-06-14), one core-adjacent. Migration 0010 adds two nullable jsonb columns:**
  - **`templates.relation_defaults` (ADR-050, core-adjacent — the relation concept):** templates can now preset relation edges (e.g. a meeting's usual attendees), written on apply via `relateItems`. Your module types get this for free — it's owner data over the existing relations table, no module work. The broader **`relation` property kind** is still deferred (folds into `entity-vs-custom-type.md`). Shipped solo per your core-waiver (you're not on core yet).
  - **`views.columns` (ADR-049, non-core):** a View Definition can choose which fields/properties its list/table/agenda show (`ViewColumn[]`, null = old defaults). If a module ships a stage-style type, its properties show up as selectable columns automatically (labels resolved from the type schema).
- **🔴 Heads-up (Tyler) — Tier 3 touched CORE: the MCP/API contract (ADR-047). Shipped solo per Brandon's Tier direction (same waiver as M6/slices 33–35). What it means for you:**
  - **The MCP server is `POST /api/mcp`** — hand-rolled Streamable HTTP, **no `@modelcontextprotocol/sdk` dependency** (Principle 5; the ADR-034 web-push precedent). Pure protocol in `src/lib/mcp/protocol.ts`, dispatcher in `server.ts`, tools in `tools.ts`, owner in `owner.ts`; the route is a thin shell. Stateless, Clerk-bypassed, gated by the **existing machine-token scheme (ADR-004) with a new `mcp` scope** — no token-format change. If you stand up your own instance, the same hand-roll is the pattern; compare against your Savor MCP if you have one.
  - **The six tools are thin wrappers over the core libs** (`search`/`views`/`items`/`relations`/`types`), so they stay owner-scoped and can't drift from the REST contract. **When you add a module type, its items get `search_items`/`list_items`/`get_item`/`create_item`/`update_item`/`list_types` for free** — the tools key off `items.type` + the type registry, no per-module MCP work. (A module-specific MCP tool, e.g. "render this song to ChordPro," would be a later extension to `tools.ts`.) The "make me a workflow" MCP path (Tier 3) will call the slice-35 `/api/build/structures` endpoint.
  - **Refactor you'll see:** `parseItemPayload`/`asUuid` moved from `@/lib/api` to a new **pure `@/lib/item-input.ts`** (api.ts re-exports them, so existing imports are unchanged) — needed because `api.ts` pulls `next/server` + the Clerk chain, which can't load in a node verify script. Import item-input directly from any node-context code (tools, verify scripts).
- **Heads-up (Tyler) — Tier 2 added one core thing (a new `templates` table) + a non-core view-engine widening; Brandon directed shipping Tier 2 solo, same waiver as M6/slice 33. For you:**
  - **New `templates` table (migration 0009):** owner-scoped item templates, `{id, owner_id, type → types.key ON DELETE cascade, name, body jsonb ({format,text}), property_defaults jsonb, created_at}`. `src/lib/templates.ts` is the store; `createItemFromTemplate` just calls `createItem` with the body + defaults pre-filled (`inbox:false`). The Phase-2 meeting-prep agenda is the forerunner — your module types get item templates for free (no module-side work; it's owner data over the existing type). `TemplateBuilder` reuses the Tiptap editor in *controlled* mode (`LazyMarkdownEditor` + stable `initialMarkdown` + `onChange`, `itemId` optional) — handy if a module canvas ever needs an editor not bound to an item.
  - **`ViewGrouping` widened (non-core, views):** a board can now group by a custom select property — `{ propertyKey: string }` alongside `{ field }`. The pure grouping logic now lives in **`src/lib/view-grouping.ts`** (`groupValueFor`/`orderedGroups`/`dueBucket`, node-testable). Column order comes from the type's option list, resolved by the view page. Relevant if a module ships a stage-style type — the board "just works" via the generated `{propertyKey}` view.
  - **Workflows/wikis are generators, not entities:** `/build/new` → `POST /api/build/structures` → `src/lib/structure-templates.ts` emits a type + views (a workflow = a `stage` select + a board grouped by it). The MCP "make me a workflow" path (Tier 3) can call this same endpoint. "Surface on Work" = pin the view (dashboard widget); **nav-slot config is still the `NAV_SLOTS` stand-in** (a later slice).
- **(slice 33 heads-up, still relevant) Tier 1 touched core (the `types` table + the type model). Two things:**
  - **The `types` table gained a `show_in_quick_capture` column (migration 0008)** and `property_schema` now has a concrete shape: an ordered **`PropertyDef[]`** = `{key, label, kind, options?}`, kinds `text|number|date|checkbox|url|select|multi_select` (parsed in `src/lib/types.ts`). Per-item values live in `items.properties`.
  - **The ADR-043 split is intact:** a user type is just a `types` row and falls back to the default markdown canvas (no `modules.ts` change). For your modules nothing changes — you still register a `ModuleManifest` *and* seed a `types` row (the DB owns label/icon/`property_schema`/enumeration; the registry owns canvas/format/exporters). New reusable bit: `src/components/build/CustomProperties.tsx` renders a type's `property_schema` editably on the default canvas — your module canvas can compose it or roll its own.
- **(Earlier heads-up, still relevant) the M6 boundary** — what you implement to add a module (Papers/Songs/…):
  - **`src/lib/modules.ts`** (pure, the contract). A module is a `ModuleManifest = { id, label, enabledByDefault, types: ModuleTypeDef[], exporters: ExporterDef[], integration?: IntegrationDef }`. `ModuleTypeDef = { key, label, canonicalFormat, canvasId, icon? }` — `canonicalFormat` is **your decision #1, accepted**: a type declares its own canonical body format (`"chordpro"` for Songs, `"markdown"` for Papers), resolved by `canonicalFormatForType`. `ExporterDef = { id, label, forType, fileExtension, render }` — `render` is your deterministic markdown→docx / ChordPro→chart (heavy renderers live in *your* module file, never imported by core's pure registry, so keep `pandoc`/`docx` there). `IntegrationDef = { id, label, direction }` for Savor-pull / PCO-push.
  - **`src/lib/module-wiring.tsx`** (impure) maps your `canvasId` → your React canvas component. Add yours alongside `markdown`/`link`.
  - **Register** with `registerModule(manifest)`; resolvers (`canvasIdForType`, `canonicalFormatForType`, `exportersForType`, `moduleForType`) then dispatch to it. **Core is itself the first module** (`coreModule`), so your module sits beside core, not bolted on.
  - **Per-user enable** is the `isModuleEnabled(moduleId, ownerId?)` seam — default-on today, owner-threaded so the per-instance enable flip is a later table lookup, no call-site change.
  - **Worked example:** `referenceModule` (exported from `modules.ts`) shows a full four-slot manifest; `scripts/verify-module-registry.mts` shows it resolving. Copy that shape.
  - **Scope (ADR-042):** the foundation delivered the *capability*; **building the modules is your lane** ("module internals — move fast, solo"). Adding a type also means a `types` DB row (see `scripts/seed.mjs`) — the registry owns *code behavior*, the DB still owns label/icon enumeration.
  - Still current: **markdown-it** is the core server-side markdown→HTML path (`src/lib/markdown-render.ts`, `html:true`, `ledgr://`→`.mention`, heading-shift) your docx/chart renderers derive from; bespoke Tiptap `TextColor`/`Highlight`/`LedgrMention` are the only editor path (compare against your Savor versions).
- **Last updated:** 2026-06-20 (**Track changes — version history + diff + undo SHIPPED** (non-core; no migration/dependency/ADR). A `HistoryPanel` over existing revision snapshots: pure `src/lib/diff.ts` word-diff, `getRevision` by-id body read, lazy panel on both canvas paths, restore = undoable item-view undo. One shared touch: a `history` card in `canvas-layout.ts`. Source-tagging dropped by decision (provenance doesn't matter; undo is the point). `verify-revisions` 38/38 + in-browser. Before that, same day: **Templates redesign TPL1–TPL5 COMPLETE — ADR-093, CORE** (templates are real prototype items applied by `cloneItemSubtree`; migrations 0025/0026). Before that: **Block anchors + promote-from-canvas SHIPPED — ADR-090, CORE** (Obsidian-style `^id` body markers as stable back-reference targets — plain text, no schema change; stripped from share/print/export/FTS, dimmed in the editor; a checkbox line's hover "→ task" → popup pre-filled with the line text + de-indented sub-bullets → task `inbox:false` related to meeting+people, storing `properties.source.blockRef`; round 2 same session added the read-side "✓ task" badge, the `#^id` deep link, a "Copy link to this line" button for any line, and a task→source back-link — public-share anchors the only follow-up) + **meeting-view polish** (removed the vestigial "Agenda" frame, "Meeting prep"→"People", heading icons via a shared `SectionHeading`). `verify-block-anchor` 32/32 + `verify-meeting-prep` 11/11 + in-browser on Neon. **No migration, no dependency.** Before that: **Meeting Recording chunk COMPLETE — v1a ADR-087 + v1b ADR-088/089.** v1b adds the **`transcription` provider seam** + AssemblyAI adapter (CORE; null-safe/env-selected like storage, submit+poll, no SDK), **audio upload → auto-transcribe** (2GB AV cap, client-poll `TranscriptionPoller` + 15-min cron backstop, compression deferred), and **audio retention** `attachments.purge_after` (CORE schema touch, migration 0024; stamped now()+30d on transcript-produced, reclaimed by the daily purge; delete-now via `DELETE /api/attachments/[id]`). The audio path rejoins the v1a awaiting-minutes flow. `verify-transcription`/`-transcription-flow` 20/20/`-audio-retention` 10/10 on Neon; tsc/build/eslint clean; live audio eyeball gated on an `ASSEMBLYAI_API_KEY` (runbook §1i). **Run `npm run db:migrate`.** Two 🟢 CORE heads-ups above (the seam + the column) to peek/flag. Earlier today: **Meeting Recording v1a SHIPPED — ADR-087** (`explorations/meeting-recording.md`): paste-first transcripts as a `transcript` child type (migration 0023; child + a `transcript`-role edge so the MCP "meetings → transcripts" hop is first-class), a `minutes` select + the "Transcripts awaiting minutes" view, and the **Claude-over-MCP minutes automation** documented (`docs/meeting-minutes-automation.md`) + on Build → AI & MCP. The CORE bit is the **Principle-3 reading** (scheduled external Claude-over-MCP producing *staged* suggestions is fine; backend stays model-free; nothing auto-commits). `verify-meeting-transcripts` 16/16 + in-browser. v1b (transcription seam + audio retention) next. **Run `npm run db:migrate` + `npx tsx scripts/seed-meeting-views.mts`.** Before that: **Meeting-recording design converged + Tyler pre-approved**: upload-only v1, Claude-over-MCP minutes, the next build chunk; 3 core touches pre-approved, an ADR each at build (see the 🟢 approved bullet above). Plus: **Tasks Polish chunk COMPLETE — S1–S6 shipped.** **S6 (ADR-086, NOT core, no migration):** the ADR-077/078/079 loose ends — ICS **EXDATE** (recurring feeds drop completed/skipped occurrences), a per-task **reminder picker** (`ReminderControl` → `properties.reminder.minutesBefore`), a **focus star** on the item canvas, a **"planned" marker** on the Tasks list, and a **Top-3 focus dashboard widget** (new `ViewFilter.focusedToday` + a "Today's Focus" starter widget). Deferred: list/board-row stars, focus-zone drag-reorder, the status popover. `verify-ics`/`verify-views` extended + in-browser. **S5 (ADR-085, NOT core, no migration):** relative subtasks — `properties.relativeSchedule.offsetDays` (N days from the parent's scheduled date) with a derived `scheduled_date`; a `SubtaskSchedule` calendar-pick back-calculates the offset; `recomputeRelativeChildren` re-derives on parent-date change / recurrence advance / occurrence materialize (clone derives, doesn't reset). Touches `cloneItemSubtree` + `updateItem` (additive) — your materialized occurrences + templates get dated subtasks for free. `verify-relative-subtasks` 26/26 + in-browser. **S4 (ADR-084, NOT core, no migration):** natural-language quick-add — `parseTaskTitle` in `nl-date.ts` pulls date→scheduled / `by <date>`→due / recurrence→RRULE / `p1..p4`→urgency out of a task title; CaptureModal gets a live Detected preview + ✨ Apply + parse-on-save. `verify-nl-task` 36/36 + in-browser. **S3 (ADR-083, NOT core, no migration):** a recurrence **completions calendar** (month-grid; tick any occurrence date in any order → the per-date log) + the **inverted occurrence edit** (carve "this occurrence" into a fresh detached `cloneItemSubtree` one-off; the series skips + advances; replaces ADR-076 §9's "this & future"). New pure helpers in `recurrence.ts`, `toggleOccurrenceCompletion`/`carveOccurrence` in `recurrence-service.ts`, one endpoint `POST /api/items/[id]/occurrence`, a `recurrenceCalendar` canvas card. `verify-recurrence-calendar` 39/39 + in-browser. **S2 (ADR-082, CORE — run `npm run db:migrate` for migration 0022):** configurable category-backed statuses — `items.status` text key + denormalized `status_category`, per-type `types.status_schema`, a Build status editor, the done-checkbox via `POST /api/items/[id]/complete`, colored custom kanban columns; verify-statuses 26/26 + in-browser. S1 (non-core): scheduled date in sort/filter/board + subtask rows. (S3–S6 now shipped, see above.) Before that: **Native Tasks chunk T1–T6 COMPLETE — ADR-076–081, PRs #17–#22** — tasks now fully replace Todoist on my instance: native recurrence (model C, migration 0021 `scheduled_date`, hand-rolled RRULE, no dep), scheduling/reschedule/overdue-roll + NL dates, a Top-3 focus layer, reminders via a published ICS feed + web push, an offline-outbox capture path, and the **`tasks` provider seam** (native default, Todoist optional via `TASKS_ADAPTER=todoist`). 🔴 **The seam (ADR-081) is the core ratify point — peek + flag; your Todoist path is unchanged, just opt-in now.** All six verifiers + regression green; tsc/build/eslint clean; in-browser verified each slice. Heads-up above has the full contract. Earlier same day: **Toward-1.0 decisions ADR-073/074/075** — tasks go fully native (drop Todoist as the engine; native recurrence/reschedule/Top-3/ICS+push/offline; Todoist optional behind the `tasks` seam), Microsoft-only for v1.0 (Google/iCloud deferred to your instance), no confidential tier (declined for v1.0); the **Native Tasks chunk T1–T6** is lined up and the in-browser verification backlog is cleared. Ratify flags above. Earlier same day: **Toward-1.0 papercuts + MCP additions, ADR-071/072** — Quick Capture defaults to the catch-all `unmarked`; list-filter by a custom select/multi_select property (`ViewFilter.propertyFilters`, index-backed `properties @>`, in `ViewBuilder` + the `/tasks` and `/list/[type]` bars); kanban drag-and-drop via a native-HTML5 `BoardDnd` over the pure `boardDropPatch`, gated by `ViewRenderer.boardDraggable` so dashboards stay read-only; and 6 new MCP tools (relate/unrelate, list/run views, list/apply templates) + `propertyPatch` on update_item + relation metadata in list_types (6→12). `verify-views` + `verify-mcp` extended, ALL PASS on Neon; tsc + eslint clean; in-browser eyeball pending. Tyler heads-ups above. Before that: **Feature B — arrangeable, field-level, per-type item canvas layout, ADR-069, CORE — BUILT B1 end-to-end + a round of Brandon feedback refinements**: migration 0019 `types.canvas_layout`, pure `src/lib/canvas-layout.ts` + `verify-canvas-layout` 36/36, the `react-grid-layout` item grid (`ItemRglInner`/`ItemLayoutGrid`) with flow auto-height + pin + hide palette, `MarkdownCanvas` classic-vs-grid dispatch + node map, full-page `?arrange=1`, `PATCH /api/types/[key]/layout`, atomic `propertyPatch` jsonb merge, `ItemEditor` slot prop, `CanvasProps.arrange?`; in-browser cycle verified on live Neon; spike deleted. Refinements: removed repeated category labels, a floating `save-status.ts` indicator, wrapping title + text props, monochrome flow/fixed pin, full-page widened to ~1024px (wider than the modal, consistent with arrange), and Arrange no longer commits a layout just by opening it. **Tyler heads-up above has the contract + refinement details.** Before that: item-view inline editing — Feature A, ADR-068 — shipped: related-title pencil edit, right-click label rename, × to clear a property; non-core. Before that: Relations chunk BUILT end-to-end — ADR-067, branch `relations-typed-properties`, R1–R4: the `relation` property kind authored in Build, the typed canvas link box, the hidden `unmarked` placeholder type + migration 0018, inline create-on-miss everywhere, and the Related-panel role sections. tsc + build + eslint clean; verify-types + verify-relations-typed all pass; in-browser eyeball pending. Before that: editor parity shipped — inline image paste regression fix + GFM tables + strike; Notion migration deferred; OneDrive-vs-git archive question raised for joint ADR; `calendar-time-blocking.md` + `tasks-todoist-vs-native.md` parked for our next sync, plus a v1.0-definition + version-scheme proposal. The 6.14 build meeting is in the docs — ADR-060/061/062; reconciled to your entity→person ADR-055.)

## Tyler — current

- **🔴 Heads-up (Brandon) — I reviewed & AGREED to ADR-149 (passage hub as a reference dimension, proposed as ADR-134); build may start, cleanup as fast-follow. Full review is appended to the ADR-149 entry in `decisions.md`.** Short version: I'm in on the reference-dimension model (we never author *onto* a verse, so lightweight refs beat 31K empty rows; verse-level lookup survives fine via the integer/range resolver, and ranges come out cleaner). **My two calls:** (1) **keep the ADR-060 auto-tagger**, but its edges land *suggested* + confirm, and it gets its own edge role so the body-sync (`syncPassageRefs`) never deletes tagger rows; (2) **build the sermon Primary Passage field in-scope**, so the seam must plug into ADR-067 typed relation fields (a 5th surface beyond the four). **Before/as you build:** the ADR was renumbered **134 → 143** (134 = the cross-device edit guard, 135–142 filled after my review) + the branch rebased onto current main; the seam also touches **search/⌘K + the MCP tools** (not just the four listed); the `@/ref` "scoped like `@/type`" line is net-new work (no scope-token parsing exists yet, and create-on-miss must be off for the passage scope); **pin the canon** (66-book Protestant, English versification); and for the commentary import, check Neon headroom + flip the faithfulness check to exact-match. Full list in the ADR block. Branch name `passage-hub-lite-adr-134` kept as-is.
- **Availability:** around; still **dev-only** (no prod instance yet — `prod-tyler` branch staged, see [[deploy-and-shared-main]]).
- **🟢 Heads-up (Brandon) — pushed a batch to `main` 2026-07-01 (two commits `f06657d` + `b34fc0a`, rebased cleanly onto your architecture refactor; one conflict, the contain-route imports — I took your `createItem` from `item-mutations` + kept `getItem` from `items`). All NON-core / additive, tsc+eslint+`next build` green.** Two areas: **(A) Project widget-homepage polish** — collection cards now show a capped preview (default 5) via a hover **count gear** + a **"Showing N of M →" drill-down** at a new route `/items/[id]/collection/[key]` (multi-select per ADR-118); **done tasks sink** to the bottom; the progress bar dropped the opaque "pts" label + moved above People/Status; **jotted meeting notes** get a Notes card on the event canvas and bubble to the meeting's project so they show in Docs (the contain route + a new `homeParentRecord` in `relations.ts`; new `meetingNotes` card in the shared `canvas-layout.ts`); **Mindmap** added as an opt-in project block. **(B) Icon system now has TWO families** — your hand-rolled stroke glyphs (`NAV_ICONS`) plus a licensed **filled** "AI Agent" set (`src/lib/ai-icons.ts`, 25 icons) selected as **`ai:<name>`**; `NavGlyph` renders `ai:` refs filled, the Build picker (`NavIconPicker`) got **tabs** (General / AI Agent), and the icon sanitizers widened to **`isIconRef`** (stroke key OR `ai:` ref) across `settings`/`dashboards`/`favorites`/`nav-slot-options` so an AI pick persists. Also **17 new house-style stroke glyphs** (education + workflow: id-card, certificate, graduation-cap, roadmap, badge-check, hierarchy, folder-open, image, external-link, …). **Shared files I touched: `canvas-layout.ts` (additive card), the contain route, and the four icon sanitizers** — all additive/back-compat; nothing of yours should break. Full detail in `next_steps.md` + [[project-type-widget-model]] / [[icons-svg-not-emoji]].
- **🔴 Re: your `WidgetKind`/breakpoint flag — seen it, agree it's worth sorting before the A/B/C widget-engine rollout.** My read: (1) rename the two enums apart — yes, do it, zero-risk; (2) share one result type — yes; (3) **the `md` breakpoint (canvas-layout 480 vs dashboard 768)** is the real one — I lean **parameterize the grid config** (each surface passes its own breakpoints) over forcing one number, so neither surface's saved layouts shift, but I want to look at how many real saved layouts exist first. This also intersects the **Overview after-action** design I just wrote up (`explorations/overview-after-action.md`) — flagged there. Let's sync before either of us touches the breakpoint.
- **🟢 Overview = a living after-action report (designed, not built; `explorations/overview-after-action.md`).** Two decisions that touch shared ground: **milestones should be BOTH dated deadlines AND manually stampable** (a stamp = completion + a chapter boundary) — `milestone` is a system type, so that's **CORE, needs a joint ADR** with you; and AI narration runs **on my Claude subscription over MCP (a scheduled automation), never an embedded API** — Ledgr just exposes the work queue. Deterministic substrate (activity log + a recap field) ships first, no AI. Peek when you have a moment.
- **🔴 Heads-up (Brandon) — Project Type chunk (PJ1–PJ11) + a Project UI redesign are INTEGRATED with `origin/main`; PR #139 open, NOT deployed (CORE — schema + type/canvas model; you pre-agreed ADR-111 and dropped the per-slice gate for this chunk).** Merged current `main` (through your PR #138 — AI Memory, calendar lenses, edit guard) into the PJ branch; conflicts resolved cleanly (journal, decisions, next_steps). **Migrations — this affects your instance too (we both deploy from `main`):** your **`0040_memory_type` stays at 0040**; the PJ migrations renumber to **`0041_solid_leech` … `0045_pursuit_type`** (activity_events + relations.home, items.composition/next_action + types.default_widgets, project widget-home, milestone type, pursuit type). Their `when`s were rewritten to sit AFTER memory_type — Drizzle applies where `when > max(applied)`, so otherwise a DB already at memory_type would silently skip them. SQL is byte-identical to what ran on dev; memory_type is untouched. **⚠️ There's no migrate-on-deploy, so BEFORE #139 merges + deploys, each instance must `npm run db:migrate` against its prod (applies 0041–0045 after memory_type) — or the deployed code 500s on the missing tables/columns.** **ADR renumber:** the Project Type ADR moved **133 → 138** (you'd taken 133 for the Planner); ADR-133–137 kept as-is. **Project UI:** `/list/project` is now a card grid; the single-project page is a header strip (status/people/progress) + a drag-reorderable card grid (Tasks/Milestones/Docs/Meetings) + "+ Add section" (retires the Customize gear); weighted-points progress. tsc + `next build` + eslint + verify scripts all green on the merged tree — nothing of yours broke. **Please review PR #139** before we land it on `main`.
- **🔴 NOW BUILDING — Project Type & widget-composed homepages (ADR-111, CORE; you agreed to proceed 2026-06-29). PJ1–PJ11 ALL COMPLETE + merged to LOCAL main (2026-06-29) — the whole widget-composed Type chunk. Tyler dropped the both-agree merge gate; FYI not a request. ✅ Now INTEGRATED with origin/main + a Project UI redesign — PR #139 (see the heads-up above for the migration renumber + deploy order). Follow-up still open: rebind PJ7 Digest persistence to your notifications table (ADR-129/130).** The PRD (`ledgr-project-type-prd.md`) reframes Types as **widget-composed**: a record page = widgets bound to the record, the same widget running in two scopes (unscoped on a Dashboard, bound to a record on a Type page). We're **reusing your dashboard widget engine** for it (record page = a dashboard focused on the record), and — my scope call — making it a **universal per-type homepage capability**, not Project-only (Project is just the first adopter; later types author their homepage in Build). Slice series **PJ1–PJ11**, one PR each; usable project page ships at PJ6. **PJ1 (the spine, migration 0034 — run `npm run db:migrate`):** (1) **`relations.home boolean`** + a partial unique `(source_id) WHERE home` — the containment residence bit (home=primary residence vs referenced), orthogonal to role/match_state; default false leaves every existing edge untouched, so your tags/mentions/typed-fields/`task→project` edges are unaffected. Containment runs through home edges (`setHome`/`relateItems({home})`), `parent_id` stays subtasks-only. (2) **`activity_events` table + `activity_kind` pgEnum** — the append-only owner-scoped log Recent Activity / Digest / Story-weave read from; emitted in `createItem`/`updateItem`/`setHome` (record_created/status_changed/task_completed/task_added/…), gated to "tracked" subjects (`project` for now) so inbox items don't flood it. `last_reviewed_at` is derived (no column). **For your modules:** nothing changes until a type opts into the homepage; `relations.home` defaults false so all existing edges/queries behave exactly as before. Decisions I locked with myself you should sanity-check: dashboard engine over canvas_layout; Digest push-first (email later — outbound email isn't built); fresh-install project statuses = Planning/Active/On Hold/Done. PJ2 added `items.next_action_task_id`/`next_action_text`/`composition` + `types.default_widgets` (migration 0035) + `initialStatusKey`, and reset the project status default (fresh = Planning/Active/On Hold/Done; existing instances keep their buckets, Ongoing→in_progress mapping fixed — your instance is non-destructively migrated). **If your project type had no items yet, it adopted the new PRD statuses; if you had projects, your buckets are preserved + fixed.** Full record: **ADR-111** (PJ1 + PJ2 addenda). All verifies green; tsc/eslint/build clean.
- **🟢 Note "date taken" = an editable `items.note_date` column (ADR-110). ✅ Approved + MERGED to main (2026-06-25). Run `npm run db:migrate` (migration 0033).** A note's displayed date is now *when I took it* (editable, defaults to the creation day), distinct from `created_at`/`updated_at`. Property-vs-column fork went column because "when I took this note" is the natural sort/group key for notes (+ future "notes from last week" views) — a real hot column, hence the both-agree core touch. **What landed:** `items.note_date timestamptz` (nullable) + `items_note_date_idx` (migration **0033**, additive + a one-time backfill of existing notes to their creation day, alpha/ADR-039); UTC-midnight calendar day exactly like `scheduled_date`/`due_date` (ADR-008). `createItem` defaults it to the app-tz creation day **for notes only** (other types stay null). **Amends ADR-018:** the `note` type gains its one baked-in strip field, label "Date taken" (a plain date input in `FieldStrip`; renders on classic + grid canvas). Save path: `ItemInput`/`ItemPatch` + `parseItemPayload` + `updateItem` + `listColumns` all carry it (body-free rule intact). **Merge notes:** renumbered from a provisional ADR-100 → **ADR-110** (your Web clipper owns ADR-100) and migration 0032 → **0033** (collided with your `status_mode`/ADR-106). `TaskCanvas` took your ADR-108 version (its rewrite dropped the `StripValues` block my one-liner targeted — nothing lost); the notes-list date display was grafted into your ADR-105 list-lenses page. `verify-note-date` 11/11 + `verify-canvas-layout` updated; `next build`/eslint clean. **For your module types:** the column is note-scoped, so a type that ignores it just sees null.
- **🔴 Tasks redesign BUILT on branch `tasks-ui` (7 slices) — peek before main-merge.** Priority P1–P6 (ADR-096, you ratified) + canvas tabs are already on main. On `tasks-ui`: project type, four-tab Tasks page (Today/Inbox/Upcoming/Projects), editor chrome + SVG toolbar, **configurable editor toolbar** + **configurable Quick Add** (settings.editorToolbarHidden/quickAddHidden), capture-card inline token highlighting, and the **bespoke two-pane task canvas**. **Two CORE touches to peek:** (1) priority urgency→smallint (ADR-096, ratified); (2) **`task` now declares a bespoke canvas** — `coreModule` task `canvasId` DEFAULT→"task" (uses the ADR-041 seam, doesn't change it). Also **removed the cross-type ListTabs strip** from all list pages (open question logged in next_steps re: how to find all of a type). All green (tsc/eslint, verify-priority 28/28, verify-project 7/7). Remaining: capture-card cosmetic polish (SVG chips, the Image-#4 chip-row layout, "…" kebab).
- **🔴 Heads-up (Brandon) — recurrence engine extended: monthly ordinal weekdays + day-of-month (ADR-099, CORE, on `tasks-ui`). You gave permission for this; flagging the contract. No migration, no dependency.** Amends ADR-076's RRULE subset so the engine can express "first/third Sunday of the month," "the third Thursday," "first and second Thursday," "3rd/last of the month" — my real recurring Todoist workflow needed these. **What changed in the shared engine (`src/lib/recurrence.ts`):** `RRuleParts` gained two MONTHLY-only optional fields — `byDayOrdinal` (ordinal `BYDAY`: `1SU`, `3TH`, `1TH,2TH`, `-1FR`) and `byMonthDay` (`BYMONTHDAY`: `3`, `-1`=last day). `parseRRule`/`formatRRule`/`enumerateOccurrences`/`describeRule`/`makeRecurrence` all handle them; everything stays **standards-shaped RFC-5545, so the ICS feed still emits the stored string verbatim** (`ics.ts`/`ics-data.ts` untouched). **Weekly `byDay` is unchanged** — ordinals are parsed/emitted only for `FREQ=MONTHLY`, so your existing rules are unaffected (back-compat; round-trip verified). NL quick-add (`nl-date.ts`) got the grammar (+ `every other …` → `INTERVAL=2`). **`RecurrenceControl` has no picker UI for these yet but preserves them** (it carries the monthly fields through on rebuild so editing interval/anchor doesn't drop them) — a dedicated ordinal/day picker is a follow-up. `verify-recurrence` +19 / `verify-nl-task` +14 ALL PASS (incl. live-Neon service); `verify-recurrence-calendar`/`verify-ics` green; tsc/eslint clean. **Peek at ADR-099 + flag anything; your module-type tasks inherit the new shapes for free.**
- **🔴 For Brandon — CORE proposal: P1–P6 priority replacing the 4-level `urgency` (ADR-096, on branch `tasks-build`).** Part of a Todoist-style Tasks redesign (PRD: `explorations/tasks-redesign.md`). Replace `items.urgency` (critical/high/normal/low) with a 1–6 priority — 1 highest … 6 lowest, colors P1 red/P2 gold/P3 purple/P4 blue/P5 green/P6 none — shared from one `priority.ts` vocab (checkbox, Today grouping, board, NL quick-add). Task data-model change → both-agree + ADR. **ADR-096 written; the vocab is built; schema migration + ripple are next.** Decision: **keep the column named `urgency` internally** (becomes a `smallint` 1–6) to dodge a ~30-file rename — surfaced everywhere as "Priority P1–P6"; migration `critical→1/high→2/normal→4/low→6` (alpha). **NOTE — your Events E2 already shipped the built-in `tags` field, so the labels half of the Tasks redesign is DONE; I'll build on it (not rebuild).** Peek at ADR-096 + flag before it merges.
- **🟢 Canvas tabs — MERGED to main (ADR-095, PR #55).** Marker-based (`<!-- tab: Title -->`) sections of the one markdown body; auto-on for `note`, attachable via a `tabs` capability (ADR-051); readers flatten to `## Title`. Built then merged after your go-ahead.
- **🟢 Confirmed (Brandon) — `/build/views` vs `/views` split is good.** You asked me to confirm the producer/consumer split (your 🟡 in the ADR-063 block); I'm happy with it (I think I proposed that shape originally). Move the actual builder routes (`/views/new`, `/views/[id]/edit`) under `/build/views` proper whenever you like — nothing of mine depends on where they sit.
- **🅿️ Parked — my user-defined API keys work (ADR-066/067, branch `tyler/user-defined-api-keys`).** Those 2 commits were accidentally on my local `main`; I moved them to a branch and reset `main` to yours. **Rescope needed before re-merge:** your ADR-067 is typed relations and your migration chain runs to 0026, so mine renumbers (migration `0018_steep_wolverine` → `0027`, regen snapshot; the API-keys ADR moves off `067`) and the `/build/claude` page reconciles against your Automations section. Not urgent; parked in `next_steps.md`.
- **🎯 My v1.0 bar (answers your "what do YOU need in 1.0" ask):** **"Ledgr replaces Todoist + Apple Notes for me, and is my creative + dev workspace."** Concretely: (1) native tasks cover my Todoist usage — engine's done, I need to close two gaps (cross-cutting labels, and how my Todoist projects/sections map) + validate against my real workflow; (2) notes replace Apple Notes via one `note` type organized by relations (your ADR-061) with saved-view lenses ("meeting notes," "song notes"); (3) Papers (done); (4) finish the Songs module so I can author songs; (5) a Sermons bespoke type (I have sermons to upload); (6) a dev-work `project`/`app` bespoke type as a command center for my 5+ apps. Books + an `idea` type are post-1.0. **Both our bars now render as live progress bars on the Changelog page** ("Toward 1.0" — `src/lib/v1-goals.ts` is the source of truth; bump an item's status as work lands).
- **🟢 Projects resolved (not a gap).** My Todoist "projects" are just flat Kanban task-buckets; what I actually want is a project *hub* that also gathers the notes/files/meetings tied to it and tracks progress. That's a bespoke `project` type — openable, canvas = a board of its related tasks + the attached notes/files/meetings. My 5+ dev apps are the first use (status, repo, live URL, stack), but it's general. Fits your ADR-061 (the *tracked* project is the openable case; loose ones stay emergent-from-relations). My lane unless the canvas reuses something core — I'll flag if so.
- **🟡 For Brandon — proposal (core-touch): make a task `labels`/tags field a BUILT-IN.** Mapping my Todoist to native tasks, the one real mechanical gap was cross-cutting labels (I lean on Bulletin/Comms/Media/Web/Teaching Prep/watch/read/assignment…). It's just a label property + a view filter, but I think *every* user wants this, so rather than each of us adding a custom property I'd bake it into the seeded `task` type — which is `coreModule` + seed, so both-agree + ADR. **One design fork to settle:** Todoist labels grow freely (type a new one to create it), which a fixed-option `multi_select` doesn't model well. Shape options: **(a)** a new free-tag property kind, **(b)** `multi_select` with inline option-add, or **(c)** tag-items via relations (most "Ledgr" — each label is its own filterable item/page). I lean (b) or (c). Thoughts?
- **🔴 CORE-TOUCH to ratify (Brandon) — external HTTP API `/api/machine/items` + new `api` token scope (2026-06-15, ADR-066).** Additive to the machine/API contract: a plain REST door for app integrations/crons to write items in (single or `{items:[...]}` batch) and read them out, token-authed (`api` scope), Clerk-bypassed like the other `/api/machine/*` jobs. Writes reuse `parseItemPayload`/`createItem` so it can't drift from `/api/items`. Trigger: wiring up Savor (my journalling app) — its cron will POST new entries as items; the item `type` is the integrator's choice, so it lands in whatever type I set up (likely a bespoke `journal` type, or `note`). New `resolveMachineOwner()` (`LEDGR_API_OWNER_UPN` knob, same fallbacks as MCP). MCP/cron/webhook contracts unchanged. Also renamed the Build sidebar entry "Claude & MCP" → **"AI & MCP"** (client-agnostic) and built out its page (MCP connection status/instructions + this API section). Peek + flag anything; ratify the contract addition.
- **🔴🔴 OVERLAP FLAG (Brandon, READ FIRST) — I built customizable dashboards (2026-06-15, ADR-064/065), which your block lists as YOUR top next focus.** Heads-up before you start so we don't double-build — let's sync. It's **non-core** (views/dashboards), so not ADR-blocked, but it's a big surface. End to end (slices 1–7):
  - **Multiple named dashboards**, each a resizable/draggable **react-grid-layout** grid. Widget kinds: **view** (compact list OR layout-faithful via `ViewRenderer` reused verbatim), **stat** (count), **action** (quick-capture / new-from-template / link). A per-widget **gear** edits display-only overrides (never mutates the view). A **dashboard-level focus** item re-scopes every widget at once. New `src/components/dashboards/*`, `src/app/dashboards/*`, `src/lib/dashboards.ts` + `dashboard-widgets.ts`, `dashboards` table (migrations 0016/0017).
  - **🔴 CORE-TOUCH to ratify (shared surfaces):** I **retired the single-dashboard model** — dropped **`views.dashboard_order`** (migration 0017 — run `npm run db:migrate`), removed `pinView`/`unpinView`/`setDashboardOrder`/`listDashboardViews`, deleted `/api/dashboard` + the `/dashboard` page + `DashboardGrid` + `PinButton`. **`/dashboard` → `/dashboards`** everywhere (nav-slot-options, command-index, Today link). Your structure-template auto-pin now calls `addViewToDefaultDashboard` (adds the generated view to a "Home" dashboard). Peek + flag anything.
  - **Home/Today:** any dashboard can be **Set as Home** and/or **Set as Today** (`users.settings.homeDashboardId`/`todayDashboardId`, additive jsonb, tolerant parse). `/` and the new **`/today`** render the assigned dashboard via a shared `DashboardView`, falling back to the fixed `TodayHome` layout — opt-in, no forced cutover. The start of your "dashboard becomes the Work home" direction.
  - **Dashboard = a nav destination** (icon-pickable in Build → Navigation; `NAV_DEST_KINDS` gained `dashboard`). **Views** gained an "in use as a widget" badge + a Views/Widgets filter (`usedViewIds`).
  - **One dep:** **`react-grid-layout@1.5.3`** (pinned — npm `@latest` is the v2 hook-rewrite without `WidthProvider`). Scoped CSS + dark-theme handle override in `globals.css`.
  - **Open follow-ups (not built):** create-a-view inline from Add-widget (reuse `ViewBuilder` + an `onSaved` prop); ~~richer action-widget pickers (type/template dropdowns vs text keys)~~ **✅ DONE (Brandon, 2026-06-20, ADR-093 TPL5): action widgets are now addable (new "Actions" section in `AddWidgetMenu`) and the gear's raw text keys are now a template dropdown (`/api/templates`, also sets `targetType`) + a type dropdown (`/api/types`)**; a switcher/rename/reorder on `/dashboards`. Your **flexible-surfaces Part 2** (prose blocks interleaved with widgets) is the natural next layer on this engine — plan together.
  - **Verify:** `verify-dashboards.mts` 19/19; `verify-structures`/`verify-views` updated + green; regression suite green; `tsc` + full `next build` + eslint clean. **In-browser eyeball pending** (the grid is the part scripts can't cover).
- **Prev:** **Build-mode restructure — DONE (2026-06-15, ADR-063).** Build mode now has a fixed left sidebar (DATA / INTERFACE / MAINTAIN), `/build` home became the System (Model) Overview, there's a universal ⌘K command palette in both modes, and the Work-nav destination picker grew a "Build tools" category. **Mostly non-core (chrome + new Build pages + a search component); shipped solo.** Two things for you, both small:
  - **🟡 `/build/views` producer/consumer split (confirm, please).** You built the original `/build` "Views" card pointing at `/views` (no `/build/views` existed). I split it: **`/views` stays Work-side** (browse/open/run — consumer) and new **`/build/views`** is the builder/manager (producer, the INTERFACE → Views sidebar entry). I left the actual builder routes (`/views/new`, `/views/[id]/edit`) where they are and just link into them from the manager, so nothing of yours moved — only the framing changed. The view engine + definitions are untouched shared surfaces. Fine by you? If so we can later move the builder routes under `/build/views` proper.
  - **🟢 New single source of truth: `src/lib/build-nav.ts`** — the hardcoded Build sidebar taxonomy (groups + entries + routes), which also feeds the destination picker's "Build tools" category and the command palette's section index. If you add a Build/Maintain section, add it here and it shows up in all three. The sidebar is a system surface (no DB row, not user-configurable — the opposite of the Work nav).
  - **🟢 `SearchModal` → `CommandPalette`** (`src/components/search/CommandPalette.tsx`, pure ranking in `src/lib/command-index.ts`). Result model is a `destination | action` union; only destinations are produced now (the action seam is there for "New Sermon" / "Clean up unused" later). If you want a module surfaced in the palette, its types/views/templates already appear via `/api/command-index`; a bespoke *command* would populate the action variant.
  - **🟢 User Settings is the one deliberate both-places entry** — reachable from the Work kebab *and* the Build sidebar; `/settings` keeps the Work nav (it doesn't trigger Build chrome) and its H1 is now "User Settings". The deeper "where should settings live" question I left alone; flag if you'd rather house it under `/build/settings`.
  - **Verify:** `scripts/verify-build-mode.mts` 31/31 (build-nav taxonomy + isBuildPath, command-index ranking + mode-aware hrefs, the Build-tools category); `tsc` + full `next build` + eslint all clean. **Pending:** in-browser eyeball — enter Build, watch the Work bar swap to the glowing "Build Mode" sidebar, expand Types, ⌘K from both modes, drop a Build tool into a Work slot.
  - **🟢 Follow-up polish (same session), a couple touch shared files:** the "Points to" picker is now a browse-by-category + filter combobox (`NavSlotEditor`), with a flip-up-when-low fix and a "Tools group" tooltip; the nav **count badge sits on the icon** again (`CountBubble`/`IconWithCount` in `NavShell`); User Settings gained the **nav spacing control** (mirrors the More menu) and a context-aware `BackButton`; the preview bar shows the real New/More glyphs (no more misleading dim). **One additive shared-file change to know: `src/lib/settings.ts` gained `highlightGradient: string \| null`** (gradient accent options — `HIGHLIGHT_GRADIENTS`; jsonb, no migration, tolerant parse). `--accent` stays a solid (text/borders/glows); new `--accent-gradient` drives accent *fills* (checkbox check, count badge) and defaults to the solid, so nothing of yours changes unless a gradient is picked.
- **Prev:** **Configurable nav slots + in-context delete standard — DONE (2026-06-15, ADR-056/057).** The middle nav is now owner config edited from `/build/surface`; every delete confirmation is an in-context `ConfirmButton` popover (no more `window.confirm`).
  - **🟢 Nav slots (ADR-056) — NOT core** (chrome/UI, same lane as the v6 redesign). **One shared-file touch to know:** `src/lib/settings.ts` gained `navSlots` + `mobileNavSlots` (additive jsonb on `users.settings`, no migration, tolerant `parseSettings` — same pattern as `railSize`/`navDensity`). `NavShell.tsx`/`Nav.tsx` rewired to read config; the old `src/lib/nav.ts`/`NAV_SLOTS` **deleted**. New `src/lib/nav-icons.ts` shared icon library + `NavGlyph`. Your nav is configured in Build now — nothing for you to do.
  - **🔴 Delete-a-type-with-its-items → Trash, recoverable (ADR-057 confirm + ADR-058 soft-delete) — CORE-TOUCH to ratify.** Types now join the soft-delete model: **`types` gained a `deleted_at` column (migration 0014, additive nullable — run `npm run db:migrate`).** `softDeleteTypeWithItems` (`src/lib/types.ts`) sends the type + its live items (+ descendants) to Trash with a shared `deleted_at`; `restoreType` (`POST /api/types/[key]/restore`) brings them back; restoring a single item auto-revives its type; the Trash page has a new "Types" section; `purgeExpiredTrash` hard-purges expired soft-deleted types. `listTypes`/pickers hide deleted types, `getType` still resolves them (labels), `assertTypeExists` blocks creating into one. (My first cut hard-deleted the items — you rightly flagged that it broke our "soft-delete only" rule, so this replaces it.) It's still a core-touch over `items`/`types` semantics — peek at ADR-058 and flag anything.
  - **🟢 `ConfirmButton` (`src/components/ui/ConfirmButton.tsx`) is the new delete-confirm standard** — reach for it instead of `window.confirm` anywhere you ask before deleting. Already wired into TypeBuilder/ViewBuilder/TemplateBuilder/CollabNotes.
  - **🔴 Hide-a-type (ADR-059) — CORE-TOUCH to ratify.** **`types` gained a `hidden` boolean (migration 0015 — `npm run db:migrate`).** A hidden type still exists and works but drops out of quick capture, +New menus, list tabs, and nav options (`listTypes()` excludes hidden by default; `listTypes({includeHidden:true})` is the Build → Types page only). Eye toggle on each Build → Types row. Lets you turn off a built-in you don't use (e.g. Link) without deleting it. The Types page also got a subtle "Create new type" footer button + a card linking to the bespoke-tool catalog.
  - **🟡 For-discussion (Brandon) — should `Link` be a built-in at all?** My take: **no** — I'd make the built-ins just **Meeting, Note, Person, Task**, and turn Link into a **bespoke capability** a user adds if they want it (like Chord Chart / Paper Workspace). That's a change to `coreModule` + the seed, so it's core/both-agree — flagging it rather than doing it. The new `hidden` flag is the interim (I can just hide Link). What do you think?
- **Prev:** **Changelog + shared collab notes — DONE (2026-06-14, ADR-054).** A Changelog page in the kebab "More" menu (3/4 live commit list with file/line counts) + a 1/4 shared markdown notes scratchpad we both read/write/clear. **Not core.** Cross-deploy sharing uses **GitHub as the shared medium** (commit history via REST; notes in committed `COLLAB_NOTES.md` via Contents API). New `src/lib/github/client.ts`; `checks.github` in `gatherHealth`. **To turn it on, set `GITHUB_TOKEN` (runbook §1g);** until then a clean "not connected" state. Optional `GITHUB_NOTES_BRANCH=collab-notes`.
- **Prev:** **Papers module — DONE (2026-06-14, ADR-048).** Second workflow module after Songs. `paper` type, markdown-canonical; tabbed canvas (Quote Bank · Outline · Draft); deterministic MSM citation engine (Full/Short/Ibid) + click-to-cite footnotes; one-click MSM `.docx` export (ported from `ty-docs/msm-render.js`). All in `src/lib/papers/` + `src/components/paper-editor/` + `PaperCanvas`. `verify-papers.mts` 16/16; registry/canvas/songs verifiers, `tsc`, `next build`, eslint all green; `paper` type seeded.
- **Heads-up (Brandon):** two additive touches to shared files — `src/lib/modules/register.ts` (+`paperModule`) and `src/lib/module-wiring.tsx` (+`paper` canvas). Core types unchanged. **One thing to know:** the MSM `.docx` is binary, and `ExporterDef.render` returns a string, so I shipped it as a dedicated route (`GET /api/items/[id]/render-docx`) rather than widen the exporter contract — the core module contract is untouched (no ADR needed). New runtime dep: `docx`. If we ever want binary exporters as a first-class module slot, that's a core change for us to agree on later.
- **Next:** **Song import (PDF / chord chart → ChordPro)** — bulk-add my chord-chart library. Spec'd in `explorations/song-import.md` (deterministic text-parse, reviewed in the chord canvas; new dep `unpdf` when I build it). Then Sermons/Lessons, Discipleship; Savor/Atlas integrations; iOS wrapper.
- **🟡 For you (Brandon) — Hiring module:** when you get a chance, build out the **Hiring** bespoke module. You already have it shaped in **Notion**, so the fastest path is to use that Notion data to shape the type + stages and get most of the way there. (Module internals are solo/your lane — no ADR needed; the M6 shape above is all you need.)
- **🟡 Bespoke-model backlog to discuss (Brandon + Tyler):** I dumped a batch of module ideas into `next_steps.md` → "Bespoke module backlog" — **People / relational growth, Bible books·chapters·verses (the relational hub), Sermons & Bible Lessons, Songs↔passages, Presentations, Books (snowflake), Quiet-time journals (Savor pull)**. Most are solo module work, but **Bible passages is flagged core-ish** (first real use of the `relation` property kind → touches `relations`/search, likely needs an ADR) and it's the keystone the others tie into. Worth a conversation on ordering. Also: the **bespoke-tool catalog** spike (branch `spike/bespoke-tool-catalog` — a "Bespoke tools" catalog in Build that attaches a module's behavior to a user-named type, decoupling behavior from the type key) is **now merged to main (2026-06-14, ADR-051)** — Brandon reviewed and approved it; `types.capability` via migration 0011, regenerated from the spike's 0010 to avoid the collision with the per-view-columns/relation-defaults 0010.
- **Last updated:** 2026-06-15 (customizable dashboards shipped end-to-end, ADR-064/065 — multiple resizable react-grid-layout dashboards of view/stat/action widgets, per-widget gear overrides, dashboard focus, dashboard-as-nav-destination, Set-as-Home/Today; retired the single-dashboard model + dropped `views.dashboard_order` via migration 0017 — run `npm run db:migrate`. **OVERLAP FLAG: this was on Brandon's next-focus list — sync before building more on it.** In-browser eyeball pending.)
