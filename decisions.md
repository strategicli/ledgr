# decisions.md: Ledgr Architecture Decision Log

A running log of decisions made *during the build*. The PRD's §10 decisions log is frozen product intent; this file captures the implementation choices that come up as code gets written (library picks, schema judgment calls, trade-offs Claude Code and Brandon settle in the moment).

**Why a separate log:** so the reasoning behind a choice survives past the session that made it. When future-Brandon (or Claude Code) wonders "why did we do it this way," the answer is here, not lost in chat history.

## How to use this
- Add an entry when you make a real architectural or tooling choice, not for routine code.
- Keep entries short: context, the decision, and why (plus what was rejected, if it matters).
- Never rewrite history. If a decision is reversed, add a new entry that supersedes the old one and note it.
- Number sequentially. Date each entry.

### Template
```
## ADR-NNN: <short title>
**Date:** YYYY-MM-DD
**Status:** accepted | superseded by ADR-NNN | reversed
**Context:** what prompted the decision.
**Decision:** what we chose.
**Why / alternatives:** the reasoning; what was rejected and why.
**Affects:** files/areas touched.
```

---

## Decisions inherited from the PRD (frozen, not re-litigated here)
These are settled in PRD §10. Listed only as pointers so this log is self-orienting; don't duplicate or re-debate them:
stack (Next.js/Vercel, Neon, Drizzle, Clerk, R2, BlockNote), DB-canonical one-way export, everything-is-an-item, deterministic-by-default, Todoist for recurrence + offline + notifications, BlockNote JSON canonical body, soft-deletes + revisions in v1, generic page-to-page relations, single-parent containment, table-backed types, baseline encryption posture, GitHub Actions for sub-daily crons, packageable-local gated to Phase 4. See PRD §10/§11 for the full list and the still-open questions.

---

## Build decisions (newest at the bottom)

## ADR-001: Supporting docs split into five files
**Date:** 2026-06-11
**Status:** accepted
**Context:** PRD is complete; needed working docs to drive a Claude Code build.
**Decision:** CLAUDE.md (concise pointer/operating manual), schema.md (implementable data model), roadmap.md (phase checklist), next_steps.md (near-term queue), runbook.md (operations), decisions.md (this log).
**Why / alternatives:** CLAUDE.md kept as a pointer rather than self-contained so it can't drift from the PRD. A single mega-doc was rejected as harder for Claude Code to load selectively.
**Affects:** repo root docs.

## ADR-002: Repo scaffold choices
**Date:** 2026-06-12
**Status:** accepted
**Context:** First code slice. create-next-app defaults plus the project's non-negotiables needed concrete picks.
**Decision:**
- Next.js 16 (App Router, TypeScript, Turbopack) scaffolded with create-next-app at `C:\dev\ledgr`. Code lives outside OneDrive on purpose: node_modules and `.next` in a synced folder cause churn, file locks, and slow installs. The project docs are copied into the repo root so GitHub backs them up and Claude Code sessions in the repo have them.
- Tailwind CSS v4 kept from the scaffold default: one well-trodden dependency instead of a bespoke CSS system, consistent with the boring-stack rule.
- Drizzle ORM over the Neon serverless HTTP driver (`drizzle-orm/neon-http`). `src/db/index.ts` is a lazy singleton that refuses any `*.neon.tech` connection string whose host lacks `-pooler`, turning the pooler rule from a convention into a runtime guard. Local Postgres (Phase 4) is exempt from the guard.
- Clerk wired behind a thin `AuthProvider` interface (`src/lib/auth/`): server code calls `authProvider.getCurrentUser()`, the layout wraps `AppAuthProvider`, and both fall back cleanly when no Clerk key is configured. This is the Phase 4 local-mode seam made real, and it also lets the repo build on a fresh clone with no secrets.
- Clerk middleware lives in `src/proxy.ts` (the Next 16 convention replacing `middleware.ts`) and excludes `/health`, since machine endpoints authenticate with scoped API tokens, never Clerk.
- Dependencies pinned exactly (`--save-exact`) per the "updates batched intentionally, never auto" rule.
- `/health` reports DB reachability plus a placeholder `lastExportAt`; raw error detail is gated behind `DEBUG_MODE` so failures stay legible without leaking connection details.
**Why / alternatives:** The Neon HTTP driver was chosen over node-postgres/TCP because it is built for serverless functions and pairs naturally with the pooled endpoint. Requiring Clerk keys at build time was rejected; the no-key fallback keeps the auth seam genuine rather than decorative. Skipping Tailwind was rejected as trading one dependency for a pile of hand-rolled CSS.
**Affects:** `package.json`, `src/db/`, `src/lib/auth/`, `src/proxy.ts`, `src/app/health/`, `drizzle.config.ts`, `.env.example`, runbook §1.

## ADR-003: Schema implementation judgment calls
**Date:** 2026-06-12
**Status:** accepted
**Context:** Implementing schema.md in Drizzle (slice 2) surfaced the deferred decisions schema.md flagged, plus a few implementable-form details.
**Decision:**
- **Entity `kind` is a real column on `items`** (`text`, nullable), not a key in `properties`. It is a hot, filterable field ("show all people"), and hot fields are columns. Plain `text` rather than a Postgres enum so adding a kind never needs a migration.
- **FTS uses an app-maintained `body_text` column** plus a `GENERATED ALWAYS AS (...) STORED` `tsvector` over `title + body_text`, GIN-indexed. Generating the tsvector straight from the BlockNote JSONB was rejected: `body::text` would index structural noise (`"type":"paragraph"`, etc.) and pollute results. App code extracts plain text from the BlockNote doc on save (same save path that snapshots revisions).
- **Child tables hard-cascade:** `relations`, `attachments`, `revisions` have `ON DELETE CASCADE` to `items`, so the 30-day Trash purge (the only hard delete) cleans up edges and snapshots in one statement. Soft-delete cascade to child *items* stays app-level (slice 4).
- **`users.email` is unique and `clerk_id` is nullable-unique**, so the seed is idempotent (`ON CONFLICT DO NOTHING` on email) and the row can exist before Clerk is wired in slice 3.
- **Migrations:** `drizzle-kit generate` produces SQL into `drizzle/`; `scripts/migrate.mjs` applies it via `drizzle-orm/neon-http/migrator`, and `scripts/seed.mjs` seeds via the same Neon HTTP driver. Both are plain Node (`--env-file-if-exists`), reuse existing deps, and enforce the pooler guard. No `tsx`/`dotenv` added (boring-stack rule).
- `matchers` (Phase 2) deliberately not created.
**Why / alternatives:** Each call follows an existing principle (hot-fields-are-columns, no-new-deps, purge must not strand rows). A separate `entities` table for `kind` was rejected (violates everything-is-an-item).
**Affects:** `src/db/schema.ts`, `drizzle/0000_*.sql`, `scripts/migrate.mjs`, `scripts/seed.mjs`, `package.json` (db:* scripts), schema.md corrected per its own header rule.

## ADR-004: Machine API tokens as hashed env entries, no DB table
**Date:** 2026-06-12
**Status:** accepted
**Context:** Slice 3 needs the machine-to-machine auth scheme (MCP, cron, webhooks) the PRD requires to be scoped, revocable, and separate from Clerk. The obvious designs were a `api_tokens` DB table or an env-var scheme.
**Decision:** Tokens live as comma-separated `name:scope1+scope2:sha256hex` entries in the `LEDGR_API_TOKENS` env var. `scripts/make-token.mjs` generates a raw token (`lgr_` + 48 hex chars, shown once, held only by the caller) and its env entry. `src/lib/auth/machine.ts` verifies `Authorization: Bearer` headers by SHA-256 + constant-time compare and returns `{name, scopes}`. Machine routes live under `/api/machine/*`, are excluded from Clerk protection in `src/proxy.ts`, and verify their own token per-handler; `/api/machine/ping` is the diagnostic route (any valid token; echoes the caller's own identity). Revocation = remove the entry, redeploy.
**Why / alternatives:** Single user with a handful of long-lived tokens does not justify a table, issuance UI, or query on every request (fast-and-cheap rule); env entries are zero-dependency and the hash-only storage means neither a DB dump nor an env dump leaks a credential. A DB table becomes the right answer if tokens ever need per-token expiry, usage audit, or self-serve issuance; nothing in the route contract would change, only `verifyMachineToken`'s lookup.
**Affects:** `src/lib/auth/machine.ts`, `src/app/api/machine/ping/`, `src/proxy.ts`, `scripts/make-token.mjs`, `.env.example`, runbook §1/§3.

## ADR-005: Trash/restore semantics, purge-cron auth, and a dev-only TS runner
**Date:** 2026-06-12
**Status:** accepted
**Context:** Slice 4 (item CRUD) had to make the PRD's soft-delete promises concrete: "soft-deleting a parent takes its children to Trash with it so the unit restores together" (§3.5/§4.6), a 30-day purge, and debounced revision snapshots. It also needed the first real scheduled job and a way to verify app code outside an HTTP request.
**Decision:**
- **Deletion units via shared `deleted_at`.** Cascade soft-delete stamps the item and every live descendant with the same `deleted_at` in one recursive-CTE UPDATE. Restore matches on that shared timestamp, so the unit round-trips together while a child trashed earlier in a *separate* delete keeps its own timestamp and stays in Trash. No extra `deletion_group` column needed; the timestamp already encodes the group.
- **Purge is the only hard delete** (`purgeExpiredTrash`): items with `deleted_at` older than 30 days are deleted (child tables cascade per ADR-003) after a detach UPDATE clears `parent_id` on any non-expiring item still pointing at an expiring one (the restored-out-of-a-unit edge case). Two statements without a transaction is accepted: a stray detach is harmless and the delete retries next run.
- **Vercel cron authenticates as a machine token.** `vercel.json` schedules `GET /api/machine/purge` daily (08:00 UTC); Vercel sends `Authorization: Bearer $CRON_SECRET`, and `CRON_SECRET` holds a raw `cron`-scoped token from the ADR-004 scheme. The platform cron walks through the same door as GitHub Actions or any future local scheduler — no second auth path, which keeps the provider-interface discipline (CLAUDE.md) intact. Purge failures insert into `error_log` with a correlation id (no silent failures).
- **Cycle guard at write time.** `parent_id` updates reject self and descendants (recursive CTE check), since one cycle would hang every recursive tree read; the cascade CTEs additionally use `UNION` (not `UNION ALL`) so even bad data can't recurse forever. The fuller subtasks slice still owns recursive reads + rollup.
- **`tsx` added as a devDependency** to run `scripts/verify-items.mts`, which exercises the real `src/lib/items.ts` against Neon. ADR-003 declined `tsx` when plain-Node `.mjs` scripts sufficed; this slice is the first whose verification must import app TypeScript (with `@/` aliases). Dev-only, zero runtime footprint, and it's the seam a future test runner would use. Duplicating the logic in `.mjs` was rejected as verifying a copy, not the code.
**Why / alternatives:** A `deletion_group_id` column was rejected as a second source of truth for what the timestamp already says. Vercel's implicit `CRON_SECRET` comparison alone (without the token scheme) was rejected because it would create an unscoped, unnamed credential outside the rotation runbook. Restoring a child out of a still-trashed parent intentionally keeps `parent_id` (the Trash view shows everything, and restoring the parent later reunites the tree); detach-on-restore was rejected as destroying hierarchy.
**Affects:** `src/lib/items.ts`, `src/lib/body-text.ts`, `src/lib/api.ts`, `src/app/api/items*`, `src/app/api/machine/purge/`, `vercel.json`, `scripts/verify-items.mts`, `package.json` (tsx), runbook §1/§2a/§3.

## ADR-006: Editor slice judgment calls (serializer, storage, mentions, dev auth stand-in)
**Date:** 2026-06-12
**Status:** accepted
**Context:** Slice 5 (BlockNote editor + markdown serialization + image paste + @-mentions) forced four implementation choices the PRD leaves open.
**Decision:**
- **The markdown serializer is hand-rolled, pure JSON-walking code** (`src/lib/markdown.ts`) with no `@blocknote` import, fed by the single color mapping table in `src/lib/colors.ts` (PRD §4.1). BlockNote's own `blocksToMarkdownLossy` was rejected: it drops colors/highlights (the whole point of §4.1's encoding) and would drag the editor bundle into the OneDrive-export cron and Pulpit Ready paths, which must stay server-side and editor-free. The color table pins BlockNote's default palette hexes so an editor upgrade can't silently change what exported documents mean. Highlights emit both the class (`hl-*`, the stable theming hook) and an inline `background-color` style so the exact color renders even with no CSS snippet; mentions export as `[@Title](ledgr://item/<uuid>)`, a stable URI for the future importer/link-rewriter.
- **R2 via `aws4fetch`** (~6KB, zero deps) behind the `StorageProvider` interface (`src/lib/storage/`): presigned PUT from the browser, bytes never proxy through the app. The full AWS SDK was rejected on Principle 5. The attachment **metadata row is created at presign time**, before the upload: an orphaned row from an abandoned upload is harmless, an untracked R2 object would leak quota. Per-file cap 100MB; per-user quota ~10GB enforced by summing `attachments.size_bytes`. Keys are `ownerId/attachmentId/filename` so per-user accounting and cleanup are prefix operations.
- **Mentions sync as `relations` rows with role `'mention'`**, diffed on every body save (create, update, revision restore): rows appear when a mention is added, disappear when it's removed, and rows with any other role are never touched, so the auto-sync can never destroy a manual link. `'mention'` rather than the schema example `'references'` precisely because the sync deletes what it owns; an auto-managed role must not be shared with hand-made edges. Targets are owner-scoped on insert; self-mentions and dangling ids are dropped silently.
- **A dev-only auth stand-in** joins the provider seam (`src/lib/auth/index.ts`): with no Clerk key, `NODE_ENV=development`, and `DEV_USER_EMAIL` set, the app resolves that email as the signed-in user. This is the Phase 4 local single-user mode in miniature and exists so editor UI (and every future UI slice) can be exercised locally without a Microsoft sign-in. Alongside it, **`resolveOwner`'s email backfill now only fills an empty `clerk_id`**, never overwrites an existing link; the old behavior would have let any second provider identity with a matching email silently steal the row.
**Why / alternatives:** Each choice keeps a non-negotiable intact: editor-free exports (rule 3/8), no new heavyweight deps (rule 5), deterministic mention plumbing with no model in the loop (rule 3), and a genuine auth seam (provider-interface discipline). A `mentions` join table separate from `relations` was rejected (relations *is* the unified edge table).
**Affects:** `src/lib/colors.ts`, `src/lib/markdown.ts`, `src/lib/storage/`, `src/lib/attachments.ts`, `src/lib/mentions.ts`, `src/lib/items.ts`, `src/lib/owner.ts`, `src/lib/auth/index.ts`, `src/app/api/attachments/`, `src/components/editor/`, `src/app/items/[id]/`, `package.json` (@blocknote/*, aws4fetch), `.env.example`, runbook §1, `scripts/verify-editor.mts`.

## ADR-007: Item canvas via intercepting routes; hardcoded field-zone table
**Date:** 2026-06-12
**Status:** accepted
**Context:** Slice 8 (item canvas, PRD §4.13) needs the center modal as the default open with the URL still routable, a full-screen expand, and per-type field zones, all without forking the editing core.
**Decision:**
- **The modal is a Next.js intercepting + parallel route** (`src/app/@modal/(.)items/[id]` rendered through a `modal` slot in the root layout). Client-side navigation to any `/items/[id]` link intercepts into a center modal over the launching list; a document load (refresh, deep link, the ⤢ Expand control) renders the full page at `src/app/items/[id]`. Expand is therefore a plain `<a>`, not `<Link>`: a soft navigation to the same URL would stay intercepted. Both forms render one shared `ItemCanvas` server component; ItemEditor is untouched except a `fields` slot.
- **Edit-loss seams closed at unmount:** ItemEditor flushes pending autosave edits in an unmount cleanup (keepalive PATCH), since closing the modal inside the 1.5s debounce window would otherwise drop the last edit; the Modal calls `router.refresh()` on unmount so the list underneath shows modal-made changes the moment it closes.
- **The top strip is a hardcoded per-type table** (`src/lib/canvas-fields.ts`): task status/due/urgency, meeting when/status, link url/status, entity kind/status, note status, unknown types status only. PRD §4.13's user-configurable field placement is a Build-surface feature; this table is the seam it replaces. Non-strip fields render read-only in a collapsed Fields footer.
- **Status/urgency enums moved to `src/lib/item-enums.ts`** (re-exported from `items.ts`) because the client-side strip needs the value lists and importing `items.ts` would drag the DB layer into the browser bundle.
- **Esc closes the modal only when unclaimed** (`!e.defaultPrevented`), so BlockNote popovers (slash menu, mention picker) keep their own Esc and the modal closes on the next one (verified in-browser).
**Why / alternatives:** Client-managed modal state (open-in-place without a route) was rejected: the URL wouldn't be routable, breaking refresh, sharing, and the Notion-default contract. A separate modal editor component was rejected as forking the editing core the PRD says carries over unchanged. Per-user opening-mode preference (side panel, new tab) is deferred; the renderer is shared, so adding modes later is chrome, not a rewrite.
**Affects:** `src/app/layout.tsx`, `src/app/@modal/`, `src/app/items/[id]/page.tsx`, `src/components/canvas/` (ItemCanvas, Modal, FieldStrip), `src/components/editor/ItemEditor.tsx`, `src/lib/canvas-fields.ts`, `src/lib/item-enums.ts`, `src/lib/items.ts`.

## ADR-008: "Today" is computed against an explicit app timezone; due dates and meeting times use different day boundaries
**Date:** 2026-06-12
**Status:** accepted
**Context:** The Today view (slice 9) needs day boundaries, but the server runs in UTC on Vercel and the PRD never pins a timezone. Worse, the two date fields encode time differently: `meeting_at` is a real instant (the canvas strip round-trips local↔UTC), while `due_date` is a calendar day stored as UTC midnight (the strip's date input slices the ISO date).
**Decision:** A `LEDGR_TIMEZONE` env var (IANA name, default `America/New_York`) defines "today"; `src/lib/today.ts` computes the day's bounds with hand-rolled `Intl.formatToParts` math (a guess-and-correct pass that survives DST, no date library). Meetings filter on the timezone's real midnights; due-date comparisons use plain UTC midnights matching the storage encoding. `getTodayData` batches the screen's three queries (meetings, due/overdue open tasks, recent) in one `Promise.all`, all body-free `listColumns`.
**Why / alternatives:** Deriving the timezone from the browser was rejected: day-scoped *queries* run on the server, and a cron (morning agenda, Phase 2) has no browser. Normalizing both fields to one encoding was rejected as a rewrite of working slice-8 semantics; the two-boundary rule is small and contained in one file. A date library (Luxon, date-fns-tz) was rejected under rule 5 for what is ~30 lines of `Intl`. The env var becomes a per-user column if Ledgr ever goes multi-user.
**Affects:** `src/lib/today.ts`, `src/app/page.tsx` (Today replaces the interim home; the type-grouped list moved to `/items`), `src/components/today/QuickCapture.tsx`, `.env.example`, runbook §1.

## ADR-009: Navigation shell as a hardcoded slot table; desktop bar-vs-sidebar resolved by a live toggle
**Date:** 2026-06-12
**Status:** accepted
**Context:** PRD §4.12 wants user-chosen nav slots (Home locked to slot 1, badge counts) with mobile settled on a floating bottom bar and desktop deliberately undecided (open Q9: same bar vs right sidebar), to be settled by trying both.
**Decision:** `src/lib/nav.ts` is a hardcoded slot table (Home, Inbox+badge, Items), the same Build-surface seam pattern as `canvas-fields.ts`; a server `Nav` wrapper resolves the owner (no nav signed out) and fills badge counts, and a client `NavShell` renders the chrome. Both desktop candidates are built behind the same slot model and a toggle button *in the nav itself* flips between them, persisting to localStorage via `useSyncExternalStore` (server renders the bar default; the stored preference applies after hydration). Mobile always gets the bottom bar. Badges re-render on every `router.refresh()`, which all mutation components already call.
**Why / alternatives:** A DB-backed preference was rejected for a single-user A/B trial: localStorage costs nothing, needs no schema, and the loser gets deleted along with the toggle once Q9 is decided (the keeper may then become a Build-surface setting). Building only one candidate was rejected because the PRD explicitly calls for a try-both decision. Icons are three hand-rolled SVGs, not an icon library (rule 5).
**Affects:** `src/lib/nav.ts`, `src/components/nav/` (Nav, NavShell), `src/app/layout.tsx`.

## ADR-010: Inbox membership is an explicit boolean column, set on arrival, cleared only by deliberate triage
**Date:** 2026-06-12
**Status:** accepted
**Context:** The Inbox (PRD §4.2) holds "items that arrived untriaged, awaiting type/entity/date assignment," but Phase 1's only arrival path is quick capture and the PRD doesn't define what makes an item leave.
**Decision:** A real `items.inbox` boolean (not null, default false) with a partial index (`owner_id where inbox and deleted_at is null`). Arrival paths set it (quick capture today; email-in, Todoist pull-ins, and the share target reuse it in Phase 2). It clears only when a control says so: the Inbox row's "✓ Triaged" button or any explicit `inbox: false` PATCH. The nav badge is `countInbox`, riding the partial index.
**Why / alternatives:** A derived definition ("task with no due date or entity") was rejected: a deliberately dateless someday-task would squat in the Inbox forever, and assigning a date would silently "triage" items behind Brandon's back, violating deterministic-and-predictable behavior. A `properties` JSONB key was rejected because the flag is hot (the badge counts it on every page render) and hot fields are columns (ADR-003). Auto-clearing on any edit was rejected for the same predictability reason; if real use shows the explicit click is friction, auto-clear-on-date/entity-assignment is a one-line loosening, and the reverse migration would be impossible to get right.
**Affects:** `src/db/schema.ts`, `drizzle/0001_*.sql`, `src/lib/items.ts` (input/patch/list/`countInbox`), `src/lib/api.ts`, `src/app/api/items/route.ts`, `src/app/inbox/`, `src/components/inbox/TriageControls.tsx`, `src/components/today/QuickCapture.tsx`, `src/lib/nav.ts`, schema.md.

## ADR-011: Per-type lists ride a ViewFilter shape a `views` row can store; the URL carries the filter
**Date:** 2026-06-12
**Status:** accepted
**Context:** Slice 12 needed per-type list pages now, but PRD §4.2 makes every view a stored View Definition in Phase 2; building pages against ad-hoc query params would mean a rewrite when the view builder lands.
**Decision:** `src/lib/views.ts` defines `ViewFilter` (type, status, urgency, kind, due window, entityId) and `ViewSort`, exactly the shapes a `views` row's `filter`/`sort` jsonb will store, plus one query builder over the body-free `listColumns`. Pages (`/tasks`, `/meetings`, `/notes`, `/links`, `/entities`) parse URL search params into that shape and `FilterBar` selects write params back, so the URL *is* the filter state (shareable, back-button-friendly, zero client list state). The entity filter is an EXISTS over `relations` in both directions, **confirmed edges only** (suggested edges stay out of trusted queries, PRD §3.3). Due windows reuse ADR-008's UTC-midnight calendar encoding; date sorts push nulls last (undated tasks sink). `/items` stays as the all-type sweep and the Trash's home; a tab strip links the six pages; Tasks joins the nav slots.
**Why / alternatives:** Client-side filter state was rejected (server-rendered + URL is simpler and matches every existing page). Filtering through suggested edges was rejected: a provisional calendar match must not silently shape a task list. Creating real seeded `views` rows now was rejected as premature; the shape is the contract, the storage rides in with the view builder.
**Affects:** `src/lib/views.ts`, `src/app/{tasks,meetings,notes,links,entities}/`, `src/components/lists/` (ListPage, ListTabs, FilterBar), `src/app/items/page.tsx`, `src/lib/nav.ts`, `src/components/nav/NavShell.tsx`.

## ADR-012: Search binds raw input through `websearch_to_tsquery`; snippets via `ts_headline` over capped `body_text`; the date filter is `updated_at`
**Date:** 2026-06-12
**Status:** accepted
**Context:** Slice 13. PRD §4.2 wants full-text search filtered by type/entity/date but pins neither query syntax, nor snippets, nor which date "date" means.
**Decision:** `GET /api/search` binds the user's raw string through `websearch_to_tsquery('english', …)` (Google-ish syntax: words, quoted phrases, OR, -exclusions; never throws on any input), matches the stored generated tsvector (ADR-003) on `items_search_gin`, ranks with `ts_rank`. Filters: type, entity (confirmed edges, both directions, same fragment as ADR-011), and an `updated_at` day-window in `LEDGR_TIMEZONE`. Results are body-free list rows plus a snippet `ts_headline` computes **in the database** over `left(body_text, 4000)` with `[[..]]` markers the client renders as `<mark>`; markerless headlines (title-only hits) are dropped as noise. The client debounces 300ms and aborts stale requests.
**Why / alternatives:** The snippet is a deliberate, bounded brush with body content on a list-shaped read: the no-body rule's point is never shipping whole bodies to lists, and an ~18-word db-computed excerpt honors that while making results scannable; returning no snippet was rejected as a worse product for zero real savings. `plainto_tsquery` was rejected (no phrases or negation). Filtering on due/meeting dates was rejected for v1: "that note from March" means recency of touch, and the per-type lists already filter their own date fields.
**Affects:** `src/lib/search.ts`, `src/app/api/search/route.ts`, `src/app/search/page.tsx`, `src/components/search/SearchClient.tsx`, `src/lib/nav.ts`.

## ADR-013: Quick capture is a nav-mounted modal with `q` / `Ctrl+K` shortcuts; captures always arrive `inbox: true`
**Date:** 2026-06-12
**Status:** accepted
**Context:** Slice 14 (PRD §4.4): a global "new item" affordance on every screen with a desktop shortcut, title-only creation, date/urgency/entity optional inline.
**Decision:** `NavShell` (already on every signed-in page) owns a "New" button and the global keydown: `q` opens the capture modal (inert while typing — inputs, selects, contentEditable), `Ctrl/Cmd+K` navigates to `/search`. The modal: title (Enter submits and closes), type select defaulting to task, optional due date and urgency. It always posts `inbox: true`, even with fields set: per ADR-010, leaving the Inbox is a deliberate act, never a side effect of capture-time detail. Its Esc handler claims the key in the **capture phase** (preventDefault + stopPropagation) so a capture opened above the item-canvas modal (which closes on any unclaimed Esc at document level, ADR-007) closes alone, one layer per press. Entity assignment at capture is deferred until a relations write path exists (the backlinks slice builds it).
**Why / alternatives:** `q` follows Todoist's quick-add key (Brandon's existing muscle memory; browser `Ctrl+N` can't be intercepted), `Ctrl+K` is the Notion-default for search. Auto-triaging a capture that has a date set was rejected per ADR-010's reasoning. The PWA share target is deliberately deferred to the PWA-shell slice, which owns the manifest it requires.
**Affects:** `src/components/capture/CaptureModal.tsx`, `src/components/nav/Nav.tsx`, `src/components/nav/NavShell.tsx`.

## ADR-014: The FTS document covers title, body, url, kind, and property values — weighted, always on, no toggle
**Date:** 2026-06-12
**Status:** accepted
**Context:** Brandon asked what search combs through and wanted broader coverage ("search more, not less"), locked on or behind a toggle. The original tsvector (ADR-003) covered only `title + body_text`: a link's URL, an entity's kind, and custom `properties` values were invisible to search.
**Decision:** The generated `search` column (migration 0002) is now a weighted concatenation: title at `A`, `body_text` at `B`, then url + kind and `properties` string values (via `jsonb_to_tsvector('english', …, '["string"]')`) at `C`. URLs and kinds pass through `regexp_replace([^a-zA-Z0-9]+ → space)` first, because Postgres tokenizes a URL as whole host/path lexemes and "youtube" would never match `www.youtube.com` otherwise. Coverage is always on — no toggle. **Status, urgency, and dates stay out deliberately:** searching "done" should find items that *say* done, not every completed task; enums belong in filters (the list pages have them, and the search page can grow them if wanted). The weights make `ts_rank` order title hits above body hits above metadata hits with zero query-side work.
**Why / alternatives:** A toggle was rejected: it adds a control nobody would turn off, and false-positive risk is handled by ranking instead. Re-deriving the column meant a drop/re-add of the generated column; **drizzle-kit did not regenerate the dependent GIN index** (dropping the column drops it silently), so the migration file gained an explicit `CREATE INDEX items_search_gin` — check this any time a generated, indexed column is rebuilt.
**Affects:** `src/db/schema.ts`, `drizzle/0002_*.sql`, schema.md (index plan + gaps list), `scripts/verify-lists-search.mts` (coverage + weighting checks).

## ADR-015: Relations write path — un-relate hits the pair in both directions; mention edges belong to the body; relating over a suggested edge confirms it
**Date:** 2026-06-12
**Status:** accepted
**Context:** Slice 15 (PRD §4.9): the backlinks panel needed the first relations write path (relate, un-relate, confirm/reject for suggested edges), and the deferred entity-at-capture rides it. The `relations` table is direction-ful (source → target) and role-ful, but the panel the user acts on is direction-blind and dedupes a pair's edges into one row.
**Decision:** `POST/PATCH/DELETE /api/items/[id]/relations` over `relateItems`/`confirmRelations`/`unrelateItems` in `src/lib/relations.ts`. (1) **Un-relate matches the gesture, not the storage:** it deletes every edge between the pair in both directions ("remove = un-relate, never delete", PRD §4.9). (2) **Mention edges (role `mention`) are body-owned and untouchable here:** the write path refuses to create or delete them, because mention diff-sync (slice 5) re-derives them on every body save — a manually deleted mention edge would silently resurrect. Mention-only rows render an `@` marker and no remove control; removing the @-mention in the body is the remove gesture. (3) **Relate upserts on the (source, target, role) unique and flips suggested → confirmed:** relating *is* the confirm gesture and POST is idempotent. Confirm (PATCH) flips every suggested edge between the pair; reject (DELETE `?suggested=true`) deletes only suggested ones, leaving confirmed edges alone. Manual relate writes source = the item the user is on → target = the picked item, role `related` (PRD §3.4 tag direction). Quick capture POSTs the item, then best-effort relates it to the picked entity: a failed relate never blocks the capture, which already landed in the Inbox where triage catches the missing link.
**Why / alternatives:** Deleting only the directed edge was rejected: the panel collapses both directions into one row, so a ✕ that removed half the pair would read as a no-op. Letting un-relate take mention edges was rejected for the resurrect problem. Addressing edges by relation id from the client was rejected: the UI never surfaces rows, it surfaces pairs.
**Affects:** `src/lib/relations.ts`, `src/app/api/items/[id]/relations/route.ts`, `src/components/relations/` (RelatedPanel, RelationActions, AddRelation — replacing `src/components/entity/RelatedItems.tsx`), `src/components/canvas/ItemCanvas.tsx`, `src/components/capture/CaptureModal.tsx`.

## ADR-016: PWA shell — hand-rolled conservative service worker; static offline page; GET share target; stand-in auth stays dev-only
**Date:** 2026-06-12
**Status:** accepted
**Context:** Slice 16 (PRD §4.5): installable PWA, app shell loads offline, share target completes quick capture. The SW ecosystem default (Workbox/next-pwa) brings a dependency and generated caching logic; the PRD explicitly promises no offline writes and no offline item data.
**Decision:** `public/sw.js` is ~80 hand-written lines: precache `offline.html` + icons, cache-first for hashed `/_next/static/` assets, network-only navigations that fall back to a **pinned cache, then the offline page** — the pinned cache (`ledgr-pin-v1`) is empty until Pulpit Ready (PRD §4.7) fills it, so that slice adds a pin protocol, not new routing. The SW never caches item data or API responses. The offline page is a self-contained static file (`public/offline.html`, inline styles, no chunks) so it renders with nothing else cached. Registration is production-only. Icons are generated by `scripts/make-icons.mjs`, a zero-dep PNG encoder over node:zlib drawing the L mark from the theme palette; one full-bleed 512 serves both `any` and `maskable`. The share target is GET to `/capture/share` (Android; iOS has no share-target support and stays on Todoist capture): a shared URL lands as a `link` item titled from the share's title, else its text minus the URL, else a bounded fetch of the page's own `<title>` (4s/64KB), else the hostname; bare text lands as a `task`; both always `inbox: true` (ADR-010). The floating nav gained `safe-area-inset-bottom` padding for standalone iOS (`viewport-fit=cover`).
**Why / alternatives:** next-pwa/Workbox rejected (rule 5: a dependency to generate behavior we mostly must *suppress*). Caching item pages for offline reads rejected: PRD §4.5 promises best-effort offline for *recently viewed* items at most, and a stale Today view is worse than an honest offline page; Pulpit Ready's verified pin is the one offline-content promise and it gets its own slice. POST share target rejected (GET keeps the share a plain navigation; no SW fetch-event handling of form data). During verification the dev auth stand-in was confirmed to be **NODE_ENV=development-gated by design** — production verification of signed-in flows uses a stand-in *build* (`NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY= … npm run build && npm start`), not a weakened gate.
**Affects:** `public/sw.js`, `public/offline.html`, `public/icons/`, `scripts/make-icons.mjs`, `src/app/manifest.ts`, `src/app/capture/share/page.tsx`, `src/app/layout.tsx`, `src/app/apple-icon.png`, `src/components/pwa/PwaRegister.tsx`, `src/components/nav/NavShell.tsx`.

## ADR-017: OneDrive export — app-only Files.ReadWrite.All; ExportTarget interface; one updated_at>exported_at comparison drives everything
**Date:** 2026-06-12
**Status:** accepted
**Context:** Slice 17 (PRD §5.4). The PRD left the Graph file scope open (app-only `Files.ReadWrite.All` vs a stored delegated token, §6.3/Q4) and pinned the tree shape but not slugs, incremental bookkeeping, or how deletes/renames reconcile files.
**Decision:** (1) **App-only client credentials.** No stored refresh token to go stale (delegated refresh tokens die after ~90 days of disuse; this job must run unattended for years — it is the disaster-recovery path), no MFA in a cron, and it matches the PRD §5.1 split. The cost — tenant-wide file permission — is accepted and documented (runbook §1b): Brandon is the tenant admin, the secret lives only in Vercel env, and the app's Graph surface is exactly the export writes. (2) **The engine writes through an `ExportTarget` interface** (putFile/deleteFile): OneDrive via plain fetch (token cached per lambda, upload sessions past 4MB, one throttle retry), local-FS for verification and Phase 4. There is deliberately no moveFile: every selected item gets re-put and its old path deleted, because (3) **one comparison, `updated_at > exported_at`, drives the whole lifecycle** — edits, renames, retypes, soft delete (→ `/_archive/`), restore (→ back), and `status: archived` all bump `updated_at`, so content rewrites and relocations are never separate cases. `items.export_path` remembers where the previous file went; `attachments.exported_at` marks immutable byte copies done forever (bytes come off the R2 public URL, no new storage method). (4) **Paths are `{type}/{year}/{slug}-{id8}.md`** — year from created_at in `LEDGR_TIMEZONE`, slug from the title, the 8-char id suffix making paths collision-proof and rename-stable without a lookup. (5) **Run state in a new `job_state` table** (key/jsonb; Phase 2 delta tokens land here too): `/health` reports `lastExportAt` only from runs with zero errors and zero remaining, plus `lastExportRunAt` for every attempt — the stalled-export canary. Runs cap at 100 items; the remainder is counted and logged, never silent. The machine cron (06:30 UTC) resolves its owner by `ONEDRIVE_EXPORT_UPN`; `POST /api/export` is the user-authed export-now twin Pulpit Ready will call.
**Why / alternatives:** Delegated token rejected as above. Graph SDK rejected (rule 5; the surface is two endpoints). Per-item export columns over a separate export-log table: the stamp is 1:1 with the item and joins free. Suffix-only-on-collision slugs rejected: detection needs global state and renames would shuffle other items' names. The export reads full bodies in batch — the no-body rule governs *list* queries; export is the bulk "item opened" case.
**Affects:** `src/lib/export/` (engine, target, onedrive, local), `src/db/schema.ts` + `drizzle/0003_*`, `src/app/api/machine/export/`, `src/app/api/export/`, `src/app/health/route.ts`, `vercel.json`, `.env.example`, runbook §§1, 1b, 2, 2a, 3, 4, `scripts/verify-export.mts`.

## ADR-018: Baked-in fields stay core to their type — status, due date, urgency, and subtasks are task-only in the UI; the columns stay
**Date:** 2026-06-12
**Status:** accepted
**Context:** Brandon flagged that Status appeared on every type's canvas (meetings, notes, links, entities) and that the Subtasks section rode along everywhere too. Long-term, users add fields they want via custom properties; the *baked-in* fields should be only the ones core to the type. The `items` table carries status/due_date/urgency for all rows (everything-is-an-item, rule 2), and `status: archived` drives the export's `/_archive/` move for every type.
**Decision:** UI-level discipline, no schema change. (1) The canvas strip table (`canvas-fields.ts`) now gives status/dueDate/urgency to tasks only; meeting keeps `meetingAt`, link `url`, entity `kind`, note nothing; custom types get an empty strip until the Build surface lets them declare one. (2) The collapsed Fields footer hides task fields on non-tasks even when legacy data set them. (3) The Subtasks section renders only on tasks (children under any parent still work — trees, breadcrumbs, and `parent_id` are untyped; only the built-in checklist affordance is task-scoped). (4) Quick capture shows due/urgency only while type = task and drops them if the type changes. (5) Export frontmatter writes `status:` only for tasks. The status *column* stays on all items because `archived` is the archive mechanism for every type; archiving a non-task currently has no UI and will need its own affordance (an Archive action, not a status select) when it matters.
**Why / alternatives:** Dropping the columns for non-tasks rejected: one-table typed items is rule 2, and archive semantics ride status. Refusing task fields in the PATCH API rejected for now: the canvas is the only writer, and a hard API refusal would complicate future per-type property config for no present safety gain. A "project" treatment for subtask-having items is parked in `explorations/project-items.md`, not built.
**Affects:** `src/lib/canvas-fields.ts`, `src/components/canvas/ItemCanvas.tsx`, `src/components/capture/CaptureModal.tsx`, `src/lib/export/engine.ts`, `src/app/items/page.tsx`, `src/app/meetings/page.tsx`.

## ADR-019: Pulpit Ready — one self-contained print render serves the pin, the offline copy, and the PDF; pins verify by cache round-trip
**Date:** 2026-06-12
**Status:** accepted
**Context:** Slice 18 (PRD §4.7): one-tap export-now + verified offline pin + print-styled PDF. The PRD pinned the three legs but not the mechanism. Constraints: nothing Sunday-critical may depend on the network (rule 4), no new dependencies (rule 5), the SW must never cache item data on its own (ADR-016).
**Decision:** (1) **A new route `GET /items/[id]/print` renders the document as one self-contained HTML page**: inline CSS, no /_next chunks, no hydration, dark on screen / black-on-white under `@media print`. The renderer (`src/lib/print-html.ts`) is a pure JSON-walker like the markdown serializer — no @blocknote import, all text HTML-escaped, unknown blocks degrade to their text, block headings shift down one level (the title owns h1, clamped at h6), mentions render as styled names (ledgr:// means nothing on paper). (2) **The pin is page-side, not SW-side:** PulpitReady fetches the print HTML and `cache.put`s it into `ledgr-pin-v1` under *both* the print URL and `/items/[id]`, so an offline navigation to the item itself serves the clean document. Document images (sermon slides) are pinned too, cors-then-opaque best-effort, reported as "n/m image(s)". **"cached ✓" appears only after a `cache.match` round-trip re-reads the stored copy** and finds the document in it — verified, never best-effort. A pinned item shows "pinned for offline ✓" on revisit, with a remove control. (3) **The PDF leg is the print view + the browser's print-to-PDF** (a Print/PDF button is the page's one inline script). A server-generated PDF would need a rendering dependency (puppeteer/react-pdf) against rule 5; the print stylesheet gets the same result through the engine the device already has, and works offline because the pinned copy carries its own styles. (4) **SW v2:** non-navigation and cross-origin GETs now fall back to the pin cache when the network fails (so pinned images serve offline); the SW still only ever *reads* the pin cache. (5) The export leg POSTs the existing `/api/export` (incremental, exports everything pending — a superset of this item); a 503 from the unconfigured OneDrive target reports honestly and never blocks the pin.
**Why / alternatives:** Pinning the real app page rejected: it depends on JS chunks and renders stale interactive UI; a flat document is the honest offline artifact and doubles as the print source. Per-item export endpoint rejected for now: the incremental pass is cheap and already idempotent. A message protocol to have the SW do the pinning rejected: the page has full Cache API access, and keeping the SW read-only preserves ADR-016's "never caches item data" invariant.
**Verification:** 23/23 renderer checks (`scripts/verify-print.mts`); in-browser: pin → both cache keys present, verified ✓ message, export 503 reported, print view renders (title, mention, dark serif), task/note canvases correct, zero console errors; SW v2 registered manually (registration is prod-only) → precache v2 present, pinned entry matches a request for `/items/[id]` with and without query (the exact match call the SW fallback makes). The full dead-server navigation was verified for the offline page in slice 16 with identical fallback code; end-to-end pin-while-offline on the installed phone PWA is listed as a Brandon-step.
**Affects:** `src/lib/print-html.ts`, `src/app/items/[id]/print/route.ts`, `src/components/canvas/PulpitReady.tsx`, `src/components/canvas/ItemCanvas.tsx`, `public/sw.js` (v2), `scripts/verify-print.mts`.

## ADR-020: Observability — one shared JSON-line logger, error_log over Sentry, debug mode stays an env toggle
**Date:** 2026-06-12
**Status:** accepted
**Context:** Slice 19 (rule 9, roadmap "Structured JSON logging + correlation ids; toggleable debug mode"). Logging had grown ad hoc: hand-rolled `console.error(JSON.stringify(...))` in four places, errorLog inserts duplicated in both cron routes, user-facing API 500s logged but never captured, and nothing surfaced captured errors. The error-capture open decision (error_log table vs free Sentry tier) was still open.
**Decision:** (1) **`src/lib/log.ts` is the one logging module:** `createLogger(source)` mints a correlation id and emits one JSON line per event (`{ts, level, source, correlationId, message, ...fields}`) to stdout/stderr, which Vercel's log pipeline ingests as-is. (2) **Error capture is the `error_log` table, not Sentry:** `captureError(source, err, opts)` logs and best-effort inserts (stack/details into the jsonb `detail`); it never throws, so capture failing can't escalate a logged failure into a crash, and a DB-down failure still has its console line. Sentry rejected: a dependency + external service (rule 5) duplicating a table that already existed in the schema, for a single-user app whose log volume a free tier dwarfs. (3) **Every 500 now carries its correlation id** (`errorResponse`, both export routes, purge), so a user-visible failure greps straight to its lines and row. (4) **/health surfaces capture:** `errors.last24h` count always; the 5 most recent messages only when `DEBUG_MODE=true`, which also unlocks real exception text in the DB check. Debug mode stays an env toggle for now; the PRD's per-session UI toggle lands with the Build surface. (5) Machine-token parse warnings keep their local console line: `log.ts` imports the DB layer, and `machine.ts` is middleware-adjacent code that must not.
**Why / alternatives:** A per-request logger threaded through all routes rejected as ceremony: user API routes only need the error path (the capture in `errorResponse`), while crons narrate their runs and already hold a logger. Pino/winston rejected (rule 5: `JSON.stringify` is the whole job). Indefinite error_log retention accepted for now; revisit if the table ever matters at single-user scale.
**Verification:** 13/13 in `scripts/verify-logging.mts` against Neon (line shape, insert + detail jsonb, explicit message/detail form, 24h window query, swallow-on-insert-failure, cleanup); live `/health` returns `errors.last24h: 0`.
**Affects:** `src/lib/log.ts`, `src/lib/api.ts`, `src/app/api/machine/{purge,export}/route.ts`, `src/app/api/export/route.ts`, `src/app/health/route.ts`, runbook §2, `scripts/verify-logging.mts`.

## ADR-021: Weekly backup runs in GitHub Actions; dumps are role-portable; artifacts now, OneDrive when the registration exists; restore proven in a throwaway container
**Date:** 2026-06-12
**Status:** accepted
**Context:** Slice 20 (roadmap "Weekly pg_dump to OneDrive; restore tested once before Phase 2"; runbook §4). Vercel functions can't run pg_dump, the Azure registration (OneDrive leg) doesn't exist yet, and the restore procedure was an untested stub — which rule 4 treats as no backup at all.
**Decision:** (1) **The backup is a GitHub Actions workflow** (`.github/workflows/backup.yml`, Fridays 05:00 UTC — a fresh dump before every Sunday, clear of the Saturday-night no-deploy window). This is the scheduler interface already chosen for sub-daily Phase 2 jobs: a real shell with the postgres 17 client (PGDG install + PATH override; the runner's preinstalled v16 aborts on Neon's v17). (2) **Dumps are `--format=custom --no-owner --no-privileges`:** Neon's roles (`neon_superuser`) exist nowhere else, and the first restore attempt proved grants make dumps non-portable. (3) **Storage is a workflow artifact today, OneDrive `/Ledgr/Backups/` tomorrow:** the artifact (private repo, 60-day retention, ~8 weekly dumps) makes backups real *now*; the OneDrive upload (Graph client-credentials via curl, upload session) activates when the §1b secrets land as repo secrets and skips with a visible warning until then. (4) **The restore test is a workflow job, not a document:** `restore-test` dumps production, restores into a Postgres 17 service container, and diffs per-table row counts — ran green 2026-06-12 (9 tables, all counts matched), closing the "tested once before Phase 2" gate. A container stands in for "fresh Neon DB" deliberately: the risk being tested is dump restorability, not Neon provisioning. (5) **Failures surface twice:** GitHub's failure email, plus `POST /api/machine/report-error` (new cron-scope endpoint → captureError → error_log → /health) once a `LEDGR_ERROR_TOKEN` repo secret is issued; the endpoint is the generic error intake all future Actions jobs reuse. New repo secret: `DATABASE_URL_UNPOOLED` (pg_dump wants the direct connection, not pgbouncer).
**Why / alternatives:** Dump-via-Vercel rejected (no pg_dump binary, 60s/4.5MB limits). Uploading the dump through a Ledgr endpoint to OneDrive rejected (request body cap; Graph secrets in GitHub are one more copy but rotate with §3's existing procedure). Neon's own PITR rejected as the primary path (free-tier window is thin and provider-internal; the PRD wants an off-provider copy). Storing dumps in the repo itself rejected (history bloat, secrets-adjacent).
**Verification:** restore-test run green end to end (dump 14KB, PGDMP magic check, restore `--exit-on-error`, 9/9 table counts matched); backup run green (artifact `ledgr-backup-2026-06-12` stored, OneDrive step skipped loudly); report-error rejects missing/bad tokens (401), captureError path covered by ADR-020's 13/13.
**Affects:** `.github/workflows/backup.yml`, `src/app/api/machine/report-error/route.ts`, GitHub repo secret `DATABASE_URL_UNPOOLED`, runbook §4 (+§2a implicitly), roadmap Phase 1.

## ADR-022: Microsoft Graph auth — unattended jobs are app-only on one registration; "delegated for calendar" is the existing Clerk sign-in; the canary is the token grant
**Date:** 2026-06-13
**Status:** accepted
**Context:** Slice 21, the Phase 2 opener (roadmap "Microsoft Graph auth: interactive delegated OAuth (MFA) + app-only client credentials for unattended jobs; mailbox-scoped via Application Access Policy"). `next_steps` framed it as "interactive delegated OAuth for calendar reads ... design token storage so refresh-token death is a visible /health condition," which reads as *storing a delegated refresh token for the calendar poll*. That collides with PRD §5.1, which is explicit and frozen: **unattended jobs (calendar poll, email-in, export writes) use app-only client-credential tokens so MFA never blocks the cron**; the interactive delegated flow is the app sign-in (Clerk + Microsoft), which shipped in Phase 1. CLAUDE.md/schema.md both say PRD intent wins on a conflict.
**Decision:** (1) **No new delegated/refresh-token storage.** Every unattended Graph job authenticates app-only via client credentials, exactly like the OneDrive export (ADR-017): no refresh token to die after ~90 days of disuse, no MFA in a cron, and it must run unattended for years (it carries the disaster-recovery export and, in Phase 2, calendar prep). The "interactive delegated OAuth (MFA)" the roadmap names is the **existing Clerk + Microsoft sign-in** — already built — so slice 21 adds no second interactive path. (2) **One registration carries everything.** The existing `ledgr-export` registration gains the Exchange application permissions (`Calendars.Read` now, `Mail.Read` at email-in) alongside its `Files.ReadWrite.All`; one secret, one consent, one rotation, one health check. A second registration was rejected (more secrets and rotation surface, no security gain at one tenant admin). (3) **Token acquisition and the throttled fetch move to a shared client** (`src/lib/graph/client.ts`): `getGraphCredentials`, `getAppToken` (module-scope cache, `.default` scope covering all granted app permissions), `graphFetch`/`graphGet` (one 429/503 retry, typed `GraphError` with `kind: not_configured | auth | request`). `onedrive.ts` was refactored onto it with no behavior change (it keeps only path-building and upload sessions). (4) **The canary is the token grant, not a refresh token.** `checkGraphAuth()` attempts a client-credentials grant; a success proves the secret is valid and unexpired (the real app-only failure mode), surfaced as `/health` `checks.graph` (`{configured:false}` until set up, `{ok:true}`/`{ok:false,detail}` after). It deliberately does **not** call a resource endpoint — a `Calendars.Read` 403 before the permission is granted would wrongly read as broken auth. Graph health never changes overall `/health` status (the DB is what "healthy" means; Graph down must not make the app look down — Sunday-proof). (5) **Mailbox scoping is a Brandon-step, Exchange-only.** App-only `Calendars.Read`/`Mail.Read` are tenant-wide by default and must be restricted to Brandon's mailbox by an **Application Access Policy** (Exchange Online PowerShell, runbook §1c). That policy is Exchange-only, which is *why* the export's `Files.ReadWrite.All` stays tenant-wide (ADR-017 already accepted this). `getGraphMailboxUpn()` resolves the mailbox UPN (`GRAPH_MAILBOX_UPN` || `ONEDRIVE_EXPORT_UPN`) for the calendar/email slices.
**Why / alternatives:** A stored delegated token for the calendar poll was rejected on the same grounds ADR-017 rejected it for export: unattended longevity beats it, and the refresh-death failure mode is worse than secret-expiry (which is already on the rotation calendar). Calling a Graph resource in the health check was rejected (a pre-grant 403 is a false negative). Putting calendar on its own registration was rejected (rotation/consent sprawl).
**Verification:** 14/14 in `scripts/verify-graph-auth.mts` — config detection, a **live** client-credentials grant against the Microsoft identity platform (the export secret is in `.env.local`), token-endpoint hit-count proving the module cache, the bad-secret canary returning `{configured:true, ok:false, detail:"...invalid_client"}` and a typed `GraphError(kind:auth)`, the `not_configured` path, and a `Calendars.Read` probe that returned **403 as expected** (permission + Application Access Policy are the pending Brandon-step). `tsc --noEmit` clean after the onedrive refactor.
**Affects:** `src/lib/graph/client.ts` (new), `src/lib/export/onedrive.ts` (refactored onto it), `src/app/health/route.ts` (graph check), `.env.example` (`GRAPH_MAILBOX_UPN`), runbook §§1, 1c, 2, 3, `scripts/verify-graph-auth.mts`.

## ADR-023: Calendar sync — calendarView per-occurrence meetings; full-window poll; calendar arrivals skip the Inbox; cancel flags, never deletes
**Date:** 2026-06-13
**Status:** accepted
**Context:** Slice 22 (PRD §5.1): poll the next 14 days, auto-create meeting items, dedupe on `ms_event_id`, handle reschedule and cancel. The PRD pinned the behavior but not: which Graph endpoint, delta vs full poll, whether calendar arrivals go to the Inbox, how cancellation is stored, or how to avoid write amplification. Matchers (entity/template matching, the actual "matching configurable rules") are the *next* slice (23), so this slice had to decide what an unmatched event becomes.
**Decision:** (1) **`calendarView`, not `/events`** — it expands recurring series into individual occurrences, so each 1:1 instance is its own meeting (you prep them separately). The Graph event `id` of each occurrence is the dedupe key stored in `items.ms_event_id`. UTC is requested via `Prefer: outlook.timezone="UTC"` so no zone math is needed; body as text for a clean preview. (2) **Full 14-day window poll each run, not delta.** The window is the bound (14 days of events is tens of rows, trivial), a delta token over a *moving* window is finicky, and the 6h cadence is the real efficiency lever (runbook "calendar 6h, not 30 min"). "Incremental everything" is honored by writing only changed events, not by delta transfer. (3) **Calendar-created meetings do NOT enter the Inbox.** Unlike a bare capture or a forwarded email (no type, no date), a calendar event arrives fully-formed as a `meeting` with a time — it belongs in Meetings/Today immediately, and flooding the Inbox with every recurring standup would violate "fast for the user." Entity association is matchers' job (suggested edges, slice 23) and manual relate, **not** Inbox triage. (This diverges from the email-in arrival→inbox pattern deliberately; it's a one-line change to revisit if it feels wrong in use.) (4) **A `CalendarSource` interface** (`listEvents(windowDays)`) fronts Graph, exactly like `ExportTarget`: the `GraphCalendarSource` is production, a stub verifies the engine against Neon with no creds, and it's the Phase 4 seam. The engine never sees Graph JSON; the source normalizes to a `CalendarEvent`. (5) **Reschedule updates `meeting_at`; cancel flags `properties.calendar.canceled = true` and never deletes** (prep survives, PRD §5.1). Cancellation lives in `properties`, not a column: it isn't a hot filter in v1, so keeping it off-schema avoids a migration (hot-fields-are-columns, ADR-003). An event cancelled before we ever synced it creates no tombstone. (6) **Write only on real change.** `unchangedSince` compares title, `meeting_at`, the canceled flag, and Graph's `lastModified`; an unchanged poll writes nothing — which matters because any write bumps `updated_at` and would re-export the item (ADR-017). (7) **`properties.calendar` stores attendees structured** (`{name,email}[]` + a flat `attendeeEmails`) plus organizer/location/joinUrl/webLink/seriesMasterId/bodyPreview, so (a) matchers read attendee emails directly and (b) those string values fall into the FTS document (`jsonb_to_tsvector`, ADR-014) — "Roger" now finds his meetings. Writes **merge** into `properties` so a matcher-written key (matched template, default urgency) survives a later sync. (8) **Two routes, same engine:** `GET /api/machine/calendar-sync` (cron-scoped token, called 6h by `.github/workflows/calendar-sync.yml`) and the user-authed `POST /api/calendar/sync` ("sync now"). A Graph **403** (Calendars.Read / Application Access Policy not yet in place, §1c) is returned as a **503 "not configured"** with a warn log, *not* a captured error, so `error_log` isn't spammed every 6h before setup; `/health` `lastCalendarSyncAt` staying null is the canary. (9) **Soft-deleted meetings are never resurrected or duplicated:** the dedup lookup includes trashed items, and the engine skips writes to them.
**Why / alternatives:** `/events` rejected (returns series masters, not instances). Delta sync rejected for v1 (moving-window complexity for no real gain at this volume). Inbox-on-arrival for calendar rejected (recurring-meeting noise; see (3)). A `canceled` column rejected (migration for a non-hot field). Routing the create through `createItem` rejected: it can't set `ms_event_id` (a machine column), so the engine does owner-scoped raw writes, exactly as the export engine sets `exported_at` — machine columns are written by their owning subsystem.
**Verification:** 24/24 in `scripts/verify-calendar-sync.mts` against Neon with a stub source under a throwaway owner — create, idempotence (no `updated_at` churn), reschedule, cancel/uncancel (item survives, flag flips), dedupe, trashed-item protection, properties-merge (matcher key survives), **attendee FTS** (a unique attendee name is found via `websearch_to_tsquery` over the generated search column), owner scoping, job_state. Plus a **live** `GraphCalendarSource.listEvents(14)` against real Graph returning a clean `GraphError(kind:request, status:403)` — proving the URL/params/`Prefer` header/pagination/error handling are correct; it will return events once §1c is done. `tsc --noEmit` clean.
**Affects:** `src/lib/calendar/` (types, graph-source, sync, owner — all new), `src/app/api/machine/calendar-sync/route.ts`, `src/app/api/calendar/sync/route.ts`, `src/app/health/route.ts` (calendar canary), `.github/workflows/calendar-sync.yml`, runbook §§1c, 2, 2a, `scripts/verify-calendar-sync.mts`.

## ADR-024: Matchers — fixed kind precedence with fuzzy as last resort; engine-made edges suggested by default; matchers run on meeting create only
**Date:** 2026-06-13
**Status:** accepted
**Context:** Slice 23 (PRD §5.1): the deterministic rules that map a calendar event to entities/templates. The `matchers` table was deliberately deferred in Phase 1 (ADR-003); this slice creates it and the engine. PRD pins the condition order (attendee-email → series-id → title-regex → fuzzy, fuzzy only when nothing better matched) and that attendee/fuzzy matches land `suggested`, but leaves: how series-id/regex trust is set, whether multiple rules accumulate, when matchers re-run, and how a suggested auto-match interacts with an already-confirmed edge.
**Decision:** (1) **`matchers` table + `pg_trgm`** via migration 0004 (extension added by hand — drizzle-kit doesn't emit `CREATE EXTENSION`, same as ADR-014's GIN index). `condition` and `action` are validated jsonb; a bad regex is rejected at **save time** (`validateCondition`), never left to throw mid-sync. (2) **Fixed kind precedence, fuzzy gated.** `CONDITION_RANK` (attendee 0, series 1, regex 2, fuzzy 3) plus the user `priority` orders matched rules; **fuzzy candidates are evaluated only when no attendee/series/regex rule matched** (PRD's "last resort, external guests / room-only invites"). Fuzzy uses Postgres `similarity()` (default threshold 0.3), no new dependency. (3) **All matched rules accumulate** (a meeting can pick up several entities/tags), and for the same entity **confirmed wins over suggested**; template/urgency take the highest-precedence rule that sets them. (4) **Trust by condition kind:** attendee-email and fuzzy → `suggested` (PRD), series-id and title-regex → `confirmed` (a specific series is unambiguous; a regex is user-authored). A rule's `action.matchState` overrides. (5) **`addMatchEdge` never downgrades:** a suggested auto-match over an existing confirmed (or manually-related) edge leaves it confirmed (`ON CONFLICT … case when excluded.match_state='confirmed' or relations.match_state='confirmed' then confirmed else suggested`); a confirmed auto-match upgrades a suggested edge. (6) **Matchers run on meeting CREATE only**, wired through a `runCalendarSync` `onCreated` callback the routes supply (the sync engine stays matcher-agnostic and verifies independently). Not on every update — re-running would resurrect a suggestion the user rejected (the same hazard mention-sync avoids). The matched template + matcher ids are recorded in `properties.match` for the prep-template slice (24) and learn-by-confirmation. (7) **Edges are `meeting → entity`, role `related`** (same key as a manual relate, so they merge and the backlinks panel collapses the pair). urgency is carried in the result but **not written to meetings** (no UI surface, ADR-018). (8) A minimal user-authed API (`GET/POST /api/matchers`, `DELETE /api/matchers/[id]`) so rules are manageable; the full Build-surface UI is Phase 3.
**Scope / deferred:** the **engine, config, CRUD, and suggested/confirmed states are complete and verified**. The **setup wizard** (sample recent/upcoming events → pick matches → write first rules) is deferred — it needs live calendar reads, which are §1c-blocked. **Learn-by-confirmation** (confirming a coincident match offers to save a standing rule) is deferred — it's a UI affordance on the slice-15 confirm path; the data backbone (matched matcher ids in `properties.match`, the matchers API) is in place. Both are tracked as a follow-up; the roadmap line stays `[~]`.
**Why / alternatives:** Auto-resolving attendee emails to entities by a stored entity email was rejected for the PRD's explicit condition→action rule model (the rule names the entities). Re-running matchers on every sync was rejected (resurrects rejected suggestions). Making series/regex `suggested` too was considered but rejected — they're deterministic/user-authored, and forcing a confirm on every instance of a series Brandon already set a rule for is friction; `action.matchState` covers the cautious case.
**Verification:** 16/16 in `scripts/verify-matchers.mts` against Neon with **real pg_trgm** under a throwaway owner — kind precedence, fuzzy-as-last-resort gating (a regex hit suppresses a would-match fuzzy rule), confirmed-wins accumulation, save-time regex rejection, edge writing with the right `match_state`, no-downgrade of a confirmed edge, `properties.match` recording, delete + owner scoping. `tsc --noEmit` clean; calendar verification still 24/24 after the `onCreated` seam.
**Affects:** `src/db/schema.ts` (+`matchers`), `drizzle/0004_*` (+pg_trgm), `src/lib/matchers/` (types, store, engine — new), `src/lib/relations.ts` (`addMatchEdge`), `src/lib/calendar/sync.ts` (`onCreated`), both calendar routes, `src/app/api/matchers/` (new), schema.md, `scripts/verify-matchers.mts`.

## ADR-025: Meeting prep is a live canvas panel, not body-seeded; action-item promotion flows the task back into prep
**Date:** 2026-06-13
**Status:** accepted
**Context:** Slice 24 (PRD §5.1): meeting prep — the person's open tasks, the last 3 meetings with them, agenda headings — plus action-item → task promotion. The open design question (next_steps) was whether prep is rendered **into the meeting body on creation** or shown as a **live panel**.
**Decision:** (1) **A live panel on the meeting canvas, not body-seeding.** `getMeetingPrep` assembles prep on every canvas render from current data; nothing is written into the body. Body-seeding was rejected: it goes stale the moment a task is added/completed, it risks clobbering Brandon's own notes, and it couples prep to calendar-create timing. The body stays purely the user's writing; the panel always reflects now. (2) **Deterministic assembly, confirmed edges only** (a provisional calendar match must not shape prep, PRD §3.3). It reuses the tested `viewItemsQuery` entity filter per related entity (entities per meeting are few) and merges/dedupes: open tasks (type task, status open, related to any of the meeting's entities), recent meetings (related meetings, newest first, this meeting excluded, capped at 3). All body-free `listColumns`, owner-scoped. (3) **Agenda is a short default** (`Agenda / Discussion / Action items / Follow-ups`) shown as hints; the matched template name (from `properties.match`, slice 23) renders as a chip. Per-named-template agendas are the Phase 3 item-template system; this is its forerunner. (4) **Action-item → task promotion** (`promoteActionItem`, `POST /api/items/[id]/promote-task`): creates an open, non-inbox task and relates it (confirmed) to the meeting **and** to each of the meeting's entities — so the promoted task immediately appears in that person's open-tasks prep next time. A deliberate promotion, not an untriaged arrival (so `inbox: false`, ADR-010). (5) The panel renders only for `type === "meeting"` (canvas discipline, like Subtasks for tasks).
**Why / alternatives:** Body-seeding rejected (staleness, clobber risk). Writing the agenda into the body on demand was deferred to a future "insert agenda" affordance — the panel shows the headings, Brandon types what he wants. A dedicated prep API for a client panel was unnecessary (the canvas is a server component; prep is one server call).
**Verification:** 10/10 in `scripts/verify-meeting-prep.mts` against Neon — confirmed-only entity gathering, open-tasks scoping (done/other-entity/suggested-linked all excluded), recent-meetings exclusion-of-self + cap + newest-first ordering, empty-prep fallback, promotion (trimmed/open/non-inbox task related to meeting + person), the promoted task reappearing in the next prep read, and owner scoping. `tsc --noEmit` clean (type-checks the new `.tsx`). **The panel UI is built and wired into the canvas; a live in-browser check is pending — it needs a meeting with a related person, which pairs with the §1c live-calendar Brandon-step.**
**Affects:** `src/lib/meetings/prep.ts`, `src/lib/meetings/promote.ts` (new), `src/app/api/items/[id]/promote-task/route.ts`, `src/components/meetings/MeetingPrep.tsx` + `PromoteTask.tsx` (new), `src/components/canvas/ItemCanvas.tsx`, `scripts/verify-meeting-prep.mts`.

## ADR-026: Todoist sync — batched engine behind a client interface; three-way due reconcile; webhook triggers an idempotent full sync
**Date:** 2026-06-13
**Status:** accepted
**Context:** Slice 25 (PRD §5.2): push dated tasks to Todoist, sync completions + date changes back, pull in Todoist-inbox tasks (offline capture), delegate recurrence to Todoist, Ledgr canonical for content. The PRD pins behavior but not: where the push fires (on save vs. on sync), how the conflict rule is implemented concretely, or how the webhook relates to polling.
**Decision:** (1) **A `TodoistClient` interface** (REST v2 HTTP client in prod; a stub verifies the engine with no token) — same provider discipline as `CalendarSource`/`ExportTarget`. (2) **Batched sync, not inline-on-save.** The push/pull engine runs on the cron (polling fallback), the webhook, and "sync now" — never inline in `createItem`/`updateItem`. Saves stay fast and offline-safe; an external call on every keystroke-save would be fragile. The webhook makes the push prompt enough for notifications; tightening the poll or adding an inline fire-and-forget push is a noted refinement. (3) **Push = dated open tasks only** (content, due, priority, a link back in the description); undated tasks stay app-only, and a task that **loses its due is deleted from Todoist and unlinked** (honors "undated stays app-only"). Ledgr urgency maps to Todoist priority (critical→4 … low→1, none→1). (4) **Conflict rule via a three-way due reconcile.** `properties.todoist = {id, syncedDue}` records the due at last sync; comparing Ledgr / Todoist / syncedDue lets **Ledgr win when both changed** (the PRD rule) while still accepting a **Todoist-only date change** back into Ledgr. Content is Ledgr-canonical — always pushed, never pulled. (5) **Completion both ways:** a Ledgr task marked done closes its Todoist task; a linked task **gone from Todoist's active list** marks the Ledgr task done. Disappearance = completion (deletion is the same signal; accepted for v1). **Recurrence is Todoist's entirely** — a recurring task stays active after each completion, so per-occurrence completion logging would need the Sync API's completed-items endpoint; **deferred** (noted). (6) **Inbox pull-in:** active Todoist tasks in the Inbox project with no Ledgr link are imported as `inbox: true` tasks (the offline-capture path, ADR-010), and never re-imported (the link excludes them). (7) **The webhook** (`POST /api/todoist/webhook`, public from Clerk, HMAC-SHA256 verified with the app client secret) triggers **one idempotent `runTodoistSync`** rather than duplicating per-event logic — the polling engine is the single source of truth, the webhook just makes it prompt. It returns **200 even on a sync error** (Todoist retries non-2xx; the cron is the backstop, so a retry storm is worse than a missed beat). (8) `/health` gains `lastTodoistSyncAt`/`lastTodoistRunAt` (the export/calendar canary pattern).
**Why / alternatives:** Inline push-on-save rejected (fragile, slow, offline-hostile). Per-event webhook handlers rejected (logic duplicated across webhook + poll; the idempotent full sync is simpler and self-healing). Treating disappearance as completion rejecting deletion was considered but deletion-vs-completion can't be told apart from the active list alone without the Sync API; accepted for v1. A `todoist_completed` column rejected — completion is just `status`.
**Verification:** 14/14 in `scripts/verify-todoist-sync.mts` against Neon with a stub client under a throwaway owner — push-create (dated only, priority mapped), undated-skip, idempotence (zero ops on a no-change re-run), Ledgr-canonical content push, the three-way due reconcile (Todoist-only change syncs back; both-changed → Ledgr wins), completion both ways, lost-its-due delete+unlink, inbox pull-in + no-re-import, job_state. `tsc --noEmit` clean. The REST client + webhook HMAC verification are built but exercised only via the stub; the live path needs the **Brandon-steps** (token, app client secret, webhook registration, `LEDGR_CRON_TOKEN`).
**Affects:** `src/lib/todoist/` (types, client, sync, owner — new), `src/app/api/machine/todoist-sync/route.ts`, `src/app/api/todoist/sync/route.ts`, `src/app/api/todoist/webhook/route.ts`, `src/proxy.ts` (webhook public), `src/app/health/route.ts` (canary), `.github/workflows/todoist-sync.yml`, `.env.example` (`TODOIST_CLIENT_SECRET`, `TODOIST_OWNER_UPN`), runbook §§1d, 2, 2a, 3, `scripts/verify-todoist-sync.mts`.

## ADR-027: Email-in — messages/delta over a MailSource; HTML reduced to text-paragraph BlockNote; move-out + properties dedup; storage gains a server-side put
**Date:** 2026-06-13
**Status:** accepted
**Context:** Slice 26 (PRD §5.3): poll the "Ledgr Import" Outlook folder via Graph `messages/delta`, turn each message into a `note` (or `task` if subject is `task:`-prefixed), convert the body, store attachments to R2, mark-read + move so nothing double-imports. Open: how to convert HTML without a parser dependency, how to guarantee no double-import, and how to write attachment bytes server-side (the storage interface was browser-presign only).
**Decision:** (1) **A `MailSource` interface** (stub-verifiable, Phase 4 seam) fronts Graph, like `CalendarSource`. `GraphMailSource` resolves the import folder by name, auto-creates an "Imported" subfolder, walks `messages/delta` (only what's new), fetches file attachments (base64 → bytes), and `markImported` = mark-read + move. (2) **HTML → text-paragraph BlockNote, no parser dep** (`src/lib/email/html.ts`): prefer the plain-text body; else strip HTML to text (drop script/style, block-closers → newlines, `<li>` → bullets, decode the common entities), then hand-build paragraph blocks (no `@blocknote` import — same discipline as the markdown serializer). Fidelity is deliberately low; the PRD accepts imperfect conversion and the item lands in the Inbox for review. (3) **`task:` subject → task (prefix stripped), else note;** every import is `inbox: true` (untriaged arrival, ADR-010). Created via `createItem` so `body_text` is extracted (email content is full-text searchable) and a revision is snapshotted. (4) **No double-import, two ways:** the primary is mark-read + **move out of the folder** (delta never re-returns it); the backstop is a **`properties.email.messageId` guard** (GIN `@>` containment) covering a crash between create and move — a re-seen message is skipped (and re-moved). (5) **The delta token lives in `job_state` and advances only on a clean run** — an errored message must be re-seen next run, not skipped past (the dedup guard then prevents a duplicate). (6) **The storage interface gained `putObject`** (server-side write): browser uploads still use presign (bytes never proxy the app), but email attachments originate server-side from Graph with no browser — they're written with the SigV4-signed PUT and recorded as `attachments` rows. Missing R2 degrades gracefully (the item imports; bytes are dropped). (7) **A Graph 403/404 → 503 "not configured"** (Mail.ReadWrite not granted §1c, or the Outlook folder absent §5.3) with a warn, not a captured error. 30-min GitHub Actions cron + `POST /api/email/import` "import now"; `/health` `lastEmailImportAt`/`lastEmailRunAt` canary.
**Why / alternatives:** An HTML-parsing dependency (turndown etc.) rejected (rule 5; the PRD accepts lossy conversion). A `email_message_id` column for dedup rejected — `properties.email.messageId` rides the existing GIN index and avoids a migration; move-out is the real guard anyway. Proxying attachment bytes through a presigned browser-style flow makes no sense with no browser; the server-side `putObject` is the right primitive (and it's behind the same provider interface, so the local build still swaps cleanly).
**Verification:** 20/20 in `scripts/verify-email-in.mts` — a **real R2 `putObject`/`deleteObject` roundtrip** against the live bucket, then the engine under a stub source: note/task routing, `task:` strip, HTML→text body, `inbox:true`, sender in `properties.email`, dedup (re-seen message skipped, single item), graceful attachment-drop without storage, delta-token persistence and start-from-token, and **an errored run not advancing the token** (with the item surviving for next-run dedup). `tsc --noEmit` clean. The `GraphMailSource` is built but verified live only — needs the **Brandon-steps**: add `Mail.ReadWrite` to the registration (extend §1c) and create the "Ledgr Import" Outlook folder.
**Affects:** `src/lib/email/` (types, html, sync, graph-source — new), `src/lib/storage/` (`putObject` on the interface + R2), `src/app/api/machine/email-import/route.ts`, `src/app/api/email/import/route.ts`, `src/app/health/route.ts` (canary), `.github/workflows/email-import.yml`, runbook §§1c, 2, 2a, 5.3, `scripts/verify-email-in.mts`.

## ADR-028: Tool language standardizes to generic product terms; "Pulpit Ready" → "Save Offline"; the no-Saturday-deploy rule applies only once Ledgr is in real Sunday use
**Date:** 2026-06-13
**Status:** accepted
**Context:** Brandon asked, as a standing principle, to use more standardized (non-church-jargon) language in the tool's UI, giving "Pulpit Ready" → "Sync to Storage" as the example. Separately, when pushing the Phase 2 backbone he noted the "no deploys Saturday night" rule (CLAUDE.md, PRD §10) is meant to protect a *working* tool relied on for Sunday, not the build-out phase where shipping fast matters and nothing live can break.
**Decision:** (1) **UI labels prefer plain, conventional product language** over insider terms; PRD section names keep their original wording (the spec is intent), but what renders on screen reads like a general product. Recorded as a working-conventions rule in CLAUDE.md. (2) **"Pulpit Ready" → "Save Offline"** (button + component `SaveOffline`, file renamed, "saved offline ✓" status copy, comments across print/sw/markdown/export updated). Chose "Save Offline" over the suggested "Sync to Storage" because the one button does both legs — OneDrive export *and* a verified local offline pin — and "Save Offline" names the outcome a user cares about (works with no network); the export leg still reports "exported to OneDrive ✓". Behavior is unchanged. (3) **The Saturday rule is scoped** in CLAUDE.md: active from the day Brandon starts preaching/working from Ledgr (flip to be tracked here), inactive during build-out.
**Why / alternatives:** Keeping "Pulpit Ready" rejected (the principle is explicit, and the name leaks one use case into a general tool). A literal "Sync to Storage" rejected as inaccurate for the offline-pin leg. Deleting the Saturday rule outright rejected — it's correct once live; it just hasn't started applying yet.
**Verification:** `tsc --noEmit` clean; 23/23 `verify-print.mts` still green (renderer untouched beyond a comment); full `next build` clean. No grep hits for the old name in `src/` user-facing strings.
**Affects:** `src/components/canvas/SaveOffline.tsx` (renamed from `PulpitReady.tsx`), `ItemCanvas.tsx`, comments in `print-html.ts`/`items/[id]/print`/`api/export`/`markdown.ts`/`public/sw.js`/`verify-print.mts`, CLAUDE.md (two working-conventions rules).

## ADR-029: View builder — stored View Definitions with five layout renderers; the filter/sort/grouping jsonb shapes are the same ones the list pages already use
**Date:** 2026-06-13
**Status:** accepted
**Context:** Slice 27 (PRD §4.2/§4.9), the foundation the embedded views (28) and dashboard (29) build on. The `views` table and the `ViewFilter`/`ViewSort` shapes existed since Phase 1; this turns them into user-built, saved, multi-layout views.
**Decision:** (1) **A view's `filter`/`sort`/`grouping` jsonb store exactly the existing `ViewFilter`/`ViewSort` shapes** plus a new `ViewGrouping` (`{field}` over status/urgency/kind/type/due); `layout` and `date_property` are real columns. So a stored view runs through the same owner-scoped, body-free `viewItemsQuery` as the hardcoded list pages — no second query path. (2) **Hand-rolled validation** (`parseViewInput`, rule 5): unknown filter keys are dropped not rejected (an old client can't wedge a save); calendar/agenda default `dateProperty` to `dueDate`. (3) **One `ViewRenderer` server component dispatches by layout** — list, table, board (columns grouped by the group field, known enums ordered first), calendar (month grid for the current month; out-of-month/undated counted), agenda (day-bucketed sections). Task rows carry the shared check-off control. (4) **CRUD store is owner-scoped; `is_system` views reject edit/delete** (none seeded yet — the hardcoded list pages stay as-is for now). (5) Builder is a client form posting the whole definition; option lists are duplicated as plain arrays so it never imports the DB-backed module.
**Why / alternatives:** Seeding the list pages as system views now rejected — deferred to keep the slice tight; the seam is ready. A per-layout query rejected — the layouts are presentations of one row set. Month-navigation on the calendar deferred (current month only) — enough to prove the layout.
**Verification:** 25/25 in `scripts/verify-views.mts` against live Neon — parse/validate (bad layout/status/entityId/grouping rejected, unknown keys dropped, name trimmed), CRUD round-trip, owner scoping (other owner gets not_found), system-view immutability, and that the view query still carries `owner_id` and selects no body. `tsc` + `next build` clean.
**Affects:** `src/lib/views.ts` (types, validation, store, `viewWhere` refactor), `src/app/api/views/route.ts` + `[id]/route.ts`, `src/app/views/` (index, `[id]`, `[id]/edit`, `new`), `src/components/views/ViewRenderer.tsx` + `ViewBuilder.tsx`, `src/lib/nav.ts` (Views slot), `scripts/verify-views.mts`.

## ADR-030: Embedded query views are an interactive panel on an entity; remove = un-relate, create inherits the relation, check-off edits in place
**Date:** 2026-06-13
**Status:** accepted
**Context:** Slice 28 (PRD §4.9): the four embedded-view behaviors — editable filter, inline check-off, create-inherits-filters, remove = un-relate. Where they live and what "remove" means had to be pinned down.
**Decision:** (1) **The embedded view mounts on an entity canvas** and runs a filter pinned to items related to that host (confirmed relations, either direction — the existing `entityId` filter). This is the actionable surface for a 1:1 prep (success criterion). (2) **A shared `GET /api/items/query`** runs a `ViewFilter`+sort and returns body-free rows, so the panel (and the dashboard) can refetch after an inline edit without a full reload. (3) **The four behaviors:** filter selects (type/status/due) refetch in place; a task checkbox PATCHes status optimistically and drops the row if it leaves an active status filter; **"+ Add" creates an item of the filtered type and relates it to the host** (so it lands in the view — create-inherits); **"remove" un-relates** the host↔item edge (DELETE relations) and never deletes the item. (4) Kept alongside the existing `RelatedPanel` (backlinks/overview), not replacing it — different jobs.
**Why / alternatives:** Embedding a saved View Definition by reference (storing where each view is embedded) rejected for v1 — heavier plumbing; the standalone entity panel delivers the behaviors now and shares the same filter language. "Remove = soft-delete the item" rejected — un-relate is the PRD's meaning and is non-destructive.
**Verification:** 7/7 in `scripts/verify-embedded-views.mts` against Neon — created+related task appears in the host view, unrelated stays out, status toggle moves it across the status filter, un-relate drops it while the item survives. `tsc` + `next build` clean. In-browser interaction check pending (mirrors slice 24's posture).
**Affects:** `src/app/api/items/query/route.ts` (new), `src/components/views/EmbeddedView.tsx` (new), `src/components/canvas/ItemCanvas.tsx` (mount on entities).

## ADR-031: Widget dashboard — `views.dashboard_order` is the whole config (nullable = pinned + position); native drag-reorder, no DnD library
**Date:** 2026-06-13
**Status:** accepted
**Context:** Slice 29 (PRD §4.11): drag-and-drop View-Definition cards, item-count badges, content-driven heights with an equal-height option, fill-screen desktop / vertical-scroll mobile. Needed a place to store the dashboard config and a DnD approach that respects the few-dependencies rule.
**Decision:** (1) **A single nullable `views.dashboard_order` integer column is the entire dashboard config** (migration 0005): null = not a widget, a number = grid position. No separate dashboard table — one dashboard per owner in v1, and the config naturally rides the view it shows, owner-scoped. (2) **Native HTML5 drag-and-drop** (draggable + dragOver/drop), no library (rule 5); a drop persists the new order via `PUT /api/dashboard`. (3) **Badge count is `countViewItems`, sharing `viewWhere` with the list** so the number can never disagree with the view; cards show a capped preview (8) with a "+N more" link. (4) **Equal-height is a display preference in localStorage**, not server state — off = content height, on = uniform scrollable cards. (5) **A distinct `/dashboard` route**, not a rebuild of the Phase 1 Today home — pin/unpin from the view detail page; linked from Today and the Views index.
**Why / alternatives:** A `dashboards` table rejected for v1 (one per user; the column is simpler and still multi-user-ready). A DnD library (dnd-kit) rejected (rule 5; native suffices for a small grid, and mobile is scroll-only per the PRD). Replacing the Today home now rejected — the working Phase 1 home stays; merging the dashboard into it is a later call.
**Verification:** 9/9 in `scripts/verify-dashboard.mts` against Neon — pin adds in order, re-pin is a no-op, reorder persists, unpin removes (other fields survive), and the count is the true total independent of the preview limit. Migration 0005 applied to Neon. `tsc` + `next build` clean. In-browser drag check pending.
**Affects:** `src/db/schema.ts` (+`dashboard_order`), `drizzle/0005_*`, `src/lib/views.ts` (count, dashboard fns), `src/app/api/dashboard/route.ts` (new), `src/app/dashboard/page.tsx` (new), `src/components/views/DashboardGrid.tsx` + `PinButton.tsx` (new), `src/app/views/[id]/page.tsx` + `src/app/page.tsx` + `src/app/views/page.tsx` (links), `scripts/verify-dashboard.mts`.

## ADR-032: A view's date filter names which date it applies to (default due date, or a meeting's "When"), and adds a within-N-days range
**Date:** 2026-06-13
**Status:** accepted
**Context:** Review feedback on slices 27/28: a meeting view could only filter by due date, but meetings have no due date — they have "When" (`meeting_at`). So "meetings today" was impossible. Separately, the date window was a fixed preset dropdown; Brandon wanted a range ("meetings in the next N days").
**Decision:** (1) **`ViewFilter` gains `dateField`** (`dueDate` | `meetingAt` | `createdAt` | `updatedAt`, default `dueDate`); the window applies to that column. The day-boundary semantics follow the field: due dates are UTC-midnight calendar days (ADR-008), the timestamp fields use real timezone midnights — the same split `today.ts` already makes, reused via `todayBounds` + `zonedMidnightUtc`. So "overdue" on a meeting means "in the past", "today"/"week"/"none" work on `meeting_at`. (2) **`ViewFilter` gains `withinDays`** (integer 1–366): the window becomes today through today+N (exclusive), winning over `due` when both are set. The existing "week" preset is exactly `withinDays = 7`. (3) **The builder UI** pairs a date-field select with the window select (which gains a "next N days…" option revealing a number input) and **auto-switches the date field to "When" when the type is set to meeting**, so "meetings today" is one obvious path. (4) **The embedded view** derives `dateField` from the selected type (meeting → `meetingAt`), so its window works for meetings too. Backward compatible: the tasks list and everything sending only `due` keep dueDate behavior.
**Why / alternatives:** A separate `meetingDue`-style filter rejected — one date-window concept with a field selector is simpler and generalizes to created/updated. A full arbitrary from/to date-range picker deferred — `withinDays` covers the asked-for case ("next N days") with one number; calendar-range pickers can come if a real need appears (noted, not built).
**Verification:** `verify-views.mts` extended (now 33 checks, all green): meetingAt today/week/withinDays windows include/exclude the right meetings, the default dueDate field correctly *misses* meetings with no due date (the bug, now scoped away), bad `dateField`/`withinDays` rejected, `withinDays` string coerces to a number, and the generated SQL targets `meeting_at`. `tsc` clean.
**Affects:** `src/lib/views.ts` (ViewFilter, `viewWhere` date branch, `parseViewFilter`), `src/app/api/items/query/route.ts`, `src/components/views/ViewBuilder.tsx`, `src/components/views/EmbeddedView.tsx`, `scripts/verify-views.mts`. Also a clarifying hint on the meeting "Agenda" section (`MeetingPrep.tsx`) and two parked explorations (`block-linked-action-items.md`, `dashboard-widgets.md`) for the deferred review items.

## ADR-033: The builder only offers fields a view's type actually has; changing the type reconciles every "by which field" pick
**Date:** 2026-06-13
**Status:** accepted
**Context:** Follow-up to ADR-032. Brandon hit it live: a calendar view of meetings let him pick "due date" as the placement field, so it rendered blank (meetings have no due date). His point — a bad option shouldn't be offered in the first place; the choice lists should reflect what the view displays.
**Decision:** **Field choices are derived from the selected type, not fixed.** `dateFieldsFor(type)` (task→[dueDate,created,updated]; meeting→[meetingAt,created,updated]; note/link/entity→[created,updated]; any→all) drives the filter date field, the sort field, and the calendar/agenda placement field. `groupFieldsFor(type)` drops urgency + due window for non-tasks (ADR-018) and kind for non-entities. The Urgency and Entity-kind filters hide entirely for types they don't apply to. **Selecting a type reconciles every current pick** to a valid one (a meeting view's placement snaps from due date to "When"). On load, a stored definition's now-invalid picks are clamped the same way, so opening a legacy meeting calendar in the editor and saving repairs it. The server `parseViewInput` also defaults a calendar/agenda's `dateProperty` by type (meeting → `meetingAt`). Principle for future selects: **option lists update based on what the view is displaying.**
**Why / alternatives:** Leaving all fields always-offered + a runtime fallback (place meetings by When even if "due date" is chosen) rejected — it masks a nonsensical choice instead of removing it; Brandon explicitly wanted the option gone. A hard validation error on save rejected — worse UX than simply not offering the field.
**Verification:** Browser-verified with the dev auth stand-in: on a new view, selecting Type=meeting + Layout=calendar removed "due date" from the filter date field, the sort field, and the placement field (all defaulting to "When"), and hid Urgency + Entity kind; creating it rendered the meetings on the correct calendar days (the previously-blank case). `verify-views.mts` adds the meeting-calendar default check (34 checks green); `tsc` + `next build` clean. A one-off check confirmed no stored meeting calendar/agenda views still held `dueDate`.
**Affects:** `src/components/views/ViewBuilder.tsx` (type-aware field lists + `changeType` reconciliation + on-load clamping), `src/lib/views.ts` (`parseViewInput` type-aware `dateProperty` default), `scripts/verify-views.mts`.
