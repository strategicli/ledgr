# next_steps.md: Ledgr Work Queue

**What's next, not what's done.** Finished work lives in git history, the PR that carried it, and the in-app Changelog, so nothing gets logged here after it ships: it just comes off the list. Update this when something actually lands, not on every push.

Anything that stops being live moves to `next_steps_archive.md`, which holds the finished and superseded sections through 2026-09-21 (158 of them) plus everything retired since.

---

## 🟡 NEXT — Brandon's switch to the built-in password (ADR-274, by hand)

The built-in password sign-in shipped switched off everywhere; nothing changes on a copy
until its owner flips it (runbook §1o). Left for Brandon, in order:

1. Clerk dashboard → Configure → Restrictions: confirm sign-up is **Restricted** on his
   instance (Phase 0 check; it guards the Clerk door that stays open beside the password).
2. While signed in through Clerk: User Settings → Sign-in → set a password, save the kit.
3. Switch a spoke (the PC) first and live on it a few days, then the laptop hub (sign the
   phone app in again), then the Vercel copy. At each step confirm MCP, the API and the
   phone still work. Undo: User Settings → Sign-in → Switch to Clerk.

## 🟢 IN FLIGHT — self-hosted hub install plan, chunk A (Ledgr note "Ledgr install plan", 2026-09-25)

Step 1 (setup quick wins, ADR-275) landed: `/setup` in plain words, the first owner
made at the machine or by the first Clerk sign-in, a no-sign-in local copy answers
127.0.0.1 only, and the supervisor makes `LEDGR_OAUTH_SECRET` itself (runbook §1p).
Left over from step 1:
- The setup form asks for email and password only. Ledgr stores no owner name anywhere
  yet; add one when something displays it.
- `npm run local:setup-owner` is the first-run door until the installer (step 7) opens
  it for you. The tray has no "Set up" item; its Reset sign-in password lands on the
  setup form when there is no owner.
- The Clerk first-sign-in claim is proven on local Postgres; the Neon (Vercel) path runs
  the same lock through a one-transaction batch and has not been exercised on a real
  Neon database.

Step 2 (files on local disk) landed. Step 3 (private access through the bundled
Tailscale helper, ADR-276, runbook §1q) landed. Step 5 (public access through Tailscale
Funnel, only behind sign-in, ADR-279) landed.

Step 9 (keep a copy in the cloud, ADR-277, runbook §1r) landed: Build → Network pairs a
hub with a fresh cloud copy by a one-time code typed on the cloud's `/setup`, fills it
from one snapshot, lists it as a 15-minute copy, and share tokens now sync with one
public address for share links. Left over from step 9:
- Proven on two local installs (the cloud one run with `VERCEL_ENV=production` on local
  Postgres). Not yet run against real Vercel + Neon: the neon-http batch path for the
  fill, `ALTER TABLE … DISABLE TRIGGER` as Neon's owner role, the 60-second limit on a
  real fill request, and Neon's real storage figure.
- A stronger first-claim lock for a hosted copy (a setup code the Deploy Button asks for,
  chunk C) would close the two-hour window a stranger could use on an empty copy.
- "Check in now" still skips a copy set to "only when there are changes" when this machine
  has nothing to send (ADR-252 behavior, untouched); the paired copy is set without it.

## 🟢 IN FLIGHT — core and modules (ADR-272, started 2026-09-24)

One codebase, one `main`, installs differ only in which modules are on. Step 0 (the
core list, the ESLint fence, the ADR) landed with this section. The full six-step plan
is §6 of `explorations/core-and-modules.md`; each step is its own PR.

1. ~~Step 0: definitions and the fence.~~ Done.
2. ~~Step 1: the switch.~~ Done. `settings.modules`, a real `isModuleEnabled` (resolver
   seam in `src/lib/modules/enabled.ts`), and `/build/modules`.
3. ~~Step 2: fold the scattered switches onto that page.~~ Done. Five feature modules
   (`src/lib/modules/features.ts`), seeded once from the old keys by
   `seedModulesFromLegacy`; read sites use `moduleOn` / `moduleOnFor`. Build-nav's
   `gatedBy` became `module`. ~~Per-install switches (`snapshots:enabled`, `sync:mode`)
   are not on the page yet.~~ Now listed there too, under "Per-install settings".
4. ~~**Step 3: manifest slots replace the hand-written lists.**~~ Done, one PR each:
   `nav` (merged by `buildNavFor`; AI Memory moved onto its manifest, and a module
   that is off drops its Build page from the sidebar and the destination picker),
   `publicPaths` (empty until sharing, todoist and ICS become modules; the proxy takes
   every registered module's paths, on or off, and step 4 guards each route),
   the job catalog (`supervisor/jobs.json`, read by both the supervisor and the app)
   and job gating by module (a job whose module is off answers "module-off" and
   stands down), `mcpTools` (a tool no manifest claims is core), `healthCheck`
   (results under `/health` `checks.modules`; areas that are not modules yet stay in
   a marked block in `health.ts`), `hooks` (`src/lib/modules/hooks.ts`; passages is
   now a default-on module), and `requires` (`requiresViolations`, enforced on the
   Modules page and PATCH /api/settings; no module sets it yet). Done:
   `settingsPanel` (a panel id, not a schema) puts a module's options under its row.
5. **Step 4: move code under `src/modules/<id>/`**, easiest first (~~themes~~ stays
   core, see §5 of the exploration; ~~sharing~~ done, `src/modules/sharing/`;
   ~~youtube~~, ~~todoist~~, ~~email~~, ~~calendar-sync~~ (the Outlook pull only; event
   views, template match rules and the person suggester stay core), ~~onedrive-export~~,
   ~~relatedness~~, ~~snapshots~~,
   ~~passages~~, ~~desk~~, ~~ai-memory~~, ~~live-context~~, ~~agent~~, ~~MCP tool
   families~~ (stays core: each remaining family wraps a core library and has nothing
   an owner could switch off, see §5 of the exploration).
6. ~~**Step 5: lazy-load the shells**~~ done: the root layout lost 19.3 kB gzipped on
   every page, and the agent sidebar now downloads only when the agent is on (numbers
   in §7 of the exploration). Known leftovers: item-panel controls and RowMenu's Desk
   items still ship while their module is off; each needs a module-side change.
7. **Step 6: instance defaults** reviewed with Tyler; done when his install is the same
   build with a shorter default list. Rule (Brandon, 2026-09-25): defaults start from
   the plain product; Brandon's own workflow is which modules his install turns on.
8. **More modules, from the Ledgr task "Other ideas for things to move into modules".**
   ~~Batch A~~ (branch `feat/modules-batch-a`): Microsoft 365 parent module (calendar
   sync, email capture, OneDrive export require it), Meeting transcripts, Triage mode,
   Listen, and Passages off for new installs (ADR-273). **Batch B next:** move Person
   and Event out of `coreModule` into People and Events modules; Planner, the ICS feed,
   meeting transcripts and calendar sync require Events; the `@` create picker skips
   Person when People is off. Test on a data copy first. Later: Songs/Papers/Mindmap
   code move, builder tools, web clipper, push into Notification center, digest +
   Overview. Skipped: custom layouts. Parked: Google.

## 🟡 FOLLOW-UPS — Claude in Ledgr (ADR-271, shipped 2026-09-24)

Built and live-tested on the hub (sidebar, tool calls, live edit of the open note,
Delete approval, inline edit, "/" prompts, "@" items, side chat with Bring back,
phone sheet at 375px). Left for real use to decide:
- **Phone over the tunnel.** Streaming was tested on localhost only. Open the sidebar
  on the phone through the public address and confirm a 1 to 3 minute turn streams
  without buffering or dropping (plan note 4, Phase 0 step 9).
- **Token cost.** Done 2026-09-24: seven core tools load every call, the rest wait
  behind ToolSearch, the cache lasts an hour, and the counter splits new from cached.
  Watch the Settings chart in real use; if a reserve tool turns out to be needed in most
  chats, add it to `CORE_TOOLS`.
- **Fake-SDK test** for turns, reconnect, and approvals (plan note 4, Testing) was not
  built; `verify-agent.mts` covers the safety rails only.
- **Eval pass** on inline edit after a few weeks: accept rates are in
  `agent_edit_proposals`.

## 📚 LONG-TERM — no user should meet a size cap on a note (Tyler, 2026-09-22)

**Where it stands:** ADR-270 made the 100K large-body gate per tab on tabbed canvases,
so a long work split into tabs never trips it. That is a workaround, not the answer.
Tyler's feedback on ADR-125: the whole-body cap came from Brandon's niche use (read-only
imported PDFs/ebooks at 1M+ characters) and shouldn't be a hard ceiling for every user.
"A user should be able to write a book using Ledgr," untabbed if they want.

**The real fix is the editor, not the threshold.** The freeze is one ProseMirror
`contenteditable` tree re-serialized per keystroke (ADR-125's measurement). Options to
weigh:
- **Section-windowed rich editing:** split an untabbed body at `#`/`##` headings (like
  tabs, but automatic and invisible) and mount only the sections around the viewport.
  Reuses TabbedBody's "one section per editor" trick without asking the writer to tab.
- **Debounced/incremental serialization:** stop calling `getMarkdown()` on the whole doc
  each keystroke; serialize on idle or only the changed top-level nodes. May lift the
  practical ceiling well past 100K on its own. Measure first.
- **Keep the gate only for imports:** a large body that arrived by import/upload (the
  PDF-as-text case) opens in Preview; a body a person wrote keeps rich editing.

Pick after measuring where the current editor actually starts to lag (typing latency at
100K, 250K, 500K), since the 100K line was set against crash reports, not a typing test.

## 🔜 OWED — templates can be applied over MCP but not made (found 2026-09-22)

**The gap:** `src/lib/mcp/tools/templates.ts` exposes only `list_templates` and
`apply_template`. Making, editing, or deleting a template is UI-only: the routes
under `/api/templates` are `requireOwner()` session auth, so neither the MCP nor
the machine API can reach them. Found while building a "Sermon Notes" template
for Tyler: Claude could write the prototype content as a normal note
(`e8083919-…`) but had to hand back the "Save as template" click, then clean up
the source note afterward.

**The fix is wiring, not new logic.** `src/lib/templates.ts` already has every
function, so each tool is a thin owner-scoped wrapper, additive per the ADR-183
carve-out:
- `create_template` — from an existing item (`createTemplateFromItem`, with an
  option to trash the source once cloned), or from scratch (`type`, `name`,
  title, body, properties → `createTemplate` + a prototype). The from-scratch
  path is the one agents want most, since it skips the throwaway source note.
- `update_template` — rename, set/clear `isDefault` for its type, edit
  `applyConfig` date rules (`updateTemplate`). Editing the prototype's body/props
  already works through `update_item` on `prototypeItemId`, as long as
  `list_templates` returns that id (check that it does).
- `delete_template` (`deleteTemplate`) and `duplicate_template`
  (`duplicateTemplate`).
- Mirror them on the machine API (`/api/machine/templates`) so a script can
  make a template with a byte-exact body.

**Gotchas to cover in the tool descriptions:** the `{{ask:Label}}` /
`{{today:long}}` token vocabulary (`src/lib/template-vars.ts`) versus the live
`{{now.*}}` / `{{item.*}}` tokens (`item-tokens.ts`), since mixing them up bakes
the wrong date; and that canvas tabs are just `<!-- tab: Title -->` lines in the
prototype body.

---

## ▶️ TWO-CLICK JOB FOR BRANDON — turn off PR previews on the `ledgr` Vercel project

Decided 2026-09-22: neither builder reviews a PR from a preview URL, so previews are quota burn. `devledgr`
already skips correctly. `ledgr` still builds a preview on every push.

Vercel → project **`ledgr`** → Settings → Git → **Ignored Build Step** → paste:

```
[ "$VERCEL_ENV" = "production" ] && exit 1 || exit 0
```

Exit 0 skips the build, exit 1 runs it, which is backwards from intuition. This kills previews and **never**
touches `release:prod`, which is always a production build. Not settable through the Vercel MCP (read-only on
this account), and it must not go in `vercel.json`, which is shared with Tyler's project. Runbook §1j-1.

## 🔜 DB-backed verify scripts: run on their own now, one latent date bug

All ~90 pass against dev as of PR #418 (2026-09-24). `release:prod` no longer runs them (they could never block a
release and were most of its runtime); run `npm run verify:db` when you want the report. A script that proves
itself can still move into `CORE_VERIFIES` in `scripts/release-prod.mjs` as a hard gate.

**Still latent:** `verify-mcp.mts`, `verify-meeting-prep.mts`, `verify-todoist-sync.mts` compute "today" with the
machine's local date while the code uses the owner's timezone (the bug fixed in `verify-view-tokens.mts` on
2026-09-22). They pass most of the day and can fail for the ~4 hours between UTC midnight and the app zone's
midnight. Fix by reading the same clock as the code (`todayBounds(new Date(), appTimezoneSync())`).

## 🔜 OWED — the write boundary still doesn't reject a malformed scaffold

Out of the incident above. `elements` on `SurfaceDef` now DESCRIBES the shape of a
structured surface (surfaced via `list_types` and `get_item`), which is what
would have stopped the guessing. It does not enforce anything. Still owed:

- **Reject unknown/malformed keys on a structured surface write**, naming the
  accepted ones. A typo like `heading` instead of `title` currently stores
  verbatim and renders as an empty section. Brandon's standing steer applies:
  warn about orphaned data, never rewrite it.
- **A partial-edit path for `sections`/`quoteBank`.** `propertyPatch` replaces the
  whole array, so an agent appending one quote has to read-modify-write the bank,
  and a concurrent UI edit is lost. Notes have `edit_item_body` for exactly this
  reason; these need the equivalent.
- **Check whether a paper's `properties.notes` picked up a child note's body.**
  Reported from the same session: notes came back holding the text of a separate
  "Assignment Brief" child note. Nothing in the code copies a child's body into
  `properties.notes` (the 2026-09-14 work folds notes INTO `body_text`, the search
  index, not the other way), so the likeliest answer is the agent wrote it there.
  Needs a look at the real record before assuming either way.

## 🐛 OPEN — `verify-sync.mts` is FLAKY (pre-existing, found 2026-09-11)

Fails intermittently, on **two different checks**:

```
FAIL  and the last edit is what landed  (draft 3)
FAIL  the assignment survives an unrelated preference written later elsewhere  (A={} B={})
```

Confirmed **not** caused by any of this branch's work: with it stashed, 4 of 6 runs on `main` failed, showing both messages. It is in `verify:ci`, so it reddens CI at random and trains people to re-run rather than read it, which is the worse cost.

Both reads are last-write-wins races resolved by a timestamp two writes can share (`A={} B={}` is two empty settings blobs, i.e. neither write is visible). Likely fix: order the fixture's writes by an explicit sequence rather than wall-clock, or space them deterministically.

## 🐛 OPEN — `verify-mcp-tasks.mts` has 3 date-rotted failures (pre-existing, found 2026-09-11)

Not caused by ADR-253; confirmed identical on `main` by stashing. Three checks hardcode occurrence dates (`2026-09-04`, `2026-09-07`) that have now drifted into the past, so the projections legitimately no longer contain them:

```
FAIL the projection honors interval + byday — got [09-21, 09-24, 10-05, 10-08], want [09-07, 09-10, 09-21, 09-24]
FAIL get_item projects the bounded series      — got [09-11, 09-18, 09-25], want [09-04, 09-11, 09-18, 09-25]
FAIL get_item reports the next uncompleted date — got 09-11, want 09-04
```

The fix is to anchor the fixtures relative to "today" the way the other suites do, not to a literal. Left alone here to keep the ADR-253 diff honest. It is DB-backed so it isn't in `verify:ci`, which is why it rotted unnoticed.

## 🧷 KNOWN STEP — a release that ADDS a scheduled job needs `npm run local:restart`

Discovered 2026-08-31 while switching on video transcripts. The supervisor decides which jobs exist when its process starts, and the auto-update path replaces the **app build** without restarting the **supervisor above it**. So a newly added job is missing from the schedule until the supervisor itself is restarted, and its "last ran" stays empty while everything else looks correct.

It is visible rather than silent: `npm run local:status` says "the running service predates the code on disk, restart to apply it" and names the command. But nobody would predict the need, and the symptom (feature switched on, owner named, code deployed, nothing happening) points everywhere except here.

**Options if it bites again:** have the update path restart the supervisor when `LOCAL_JOBS` has changed, or have the app surface the same warning on `/build/updates` where the job list is read. Both are small; neither is urgent now that the symptom is written down.

## 🔜 NEXT (queued out of ADR-243, deliberately not bundled)

Two adjacent gaps, both found while fixing the above, both left alone because neither was the reported bug:

- **Custom property VALUES are not validated against their kind.** `items.properties` is untyped JSON, so a `multi_select` can be given a bare string and a `select` an undeclared option, and both store silently and then render as nothing. Same shape of bug as ADR-243 one layer over. A genuine tightening with no loosening to justify it, so it needs its own decision (and Tyler, per ADR-183).
- **`update_type` replaces `propertySchema` wholesale and wipes it when the field is omitted** (it also requires `label`). So "add one property" or "add one option to a multi-select" means reading the current schema and resending all of it, and a caller that just wanted to rename the type drops every field. A surgical per-field tool (add / update / remove / reorder, plus option add/remove) would fix it, purely additively. Brandon's steer if it gets built: **warn about orphaned data, never rewrite it** — report how many items still hold a removed option or an old key and let him decide.
- **`move_item_type` does not re-validate status.** Move an item to a type that lacks its current stage and the row keeps a status the new type has never heard of. Pre-existing, unrelated to ADR-243, cheap to fix alongside either item above.

## 🔎 IDENTIFIED, NOT YET STOPPED — a stale peer has hammered the cloud hub every 12 seconds since ~2026-08-23

**This is what kept the production Neon compute from ever going idle,** and it is the reason the whole ADR-239/240/241 batch exists.

**The measurement.** A probe issuing one query a minute, and never reading the table in question, saw **+5 sequential scans of `sync_peers` and +14 commits every minute, with zero writes anywhere**, sustained for hours. Inter-arrival gaps clustered hard at 12.5 seconds.

**Vercel's logs named it.** `POST /api/machine/sync` returning **409**, every 12.06 seconds, user agent `node`, unbroken. 409 from that route is the schema version gate: the caller **authenticates successfully with a valid device token**, and is then refused because its bundled schema version disagrees with the hub's. So this is a real registered peer running stale code, not an intruder and not a bad token.

**Which machine.** Only two devices hold a token. Brandon's ECC laptop was ruled out by sampling every outbound HTTPS connection on it four times a second for a full minute: zero connections to anything, its only open socket being its local Postgres. The other peer is `BranRedux Desktop` (`bcdesktop`), which Tailscale shows **active** with a live direct connection. Its last SUCCESSFUL check-in was 2026-08-23; migrations landed on the hub 2026-08-25 and 2026-08-26, which is exactly when its version stopped matching. Roughly **50,000 requests** since.

**Why it never healed.** That install's supervisor is set to auto-update every 15 minutes, and the 409 carries both versions precisely so the stale side can raise an update card. It did not update, so either its supervisor is not running and only an orphaned app process survived, or its updates are failing.

**Next step is on that machine:** update it so its version matches (which also hands it the ADR-239 backoff), or stop its supervisor. Stopping the app alone is not enough if the supervisor restarts it. **Careful before anything destructive:** that install has not pushed since 2026-08-23, so it may hold local edits that never reached the hub, and its pull cursor may now predate the hub's retained oplog — in which case it needs a re-fill, and a re-fill destroys unpushed local changes. Measure the unpushed count first.

**The gap this exposed, now half closed.** A peer stuck on 409 was invisible to the hub: `last_seen_at` only advances on a SUCCESSFUL exchange, so the Network page reported "last seen August 23" while that peer was asking 7,200 times a day. The `fix/log-sync-409-caller` branch above now names the caller on the 409 path, which is the half that mattered and would have answered this in one query instead of a day. **Still open:** the 401 path is still silent, and neither rejection is surfaced anywhere the owner looks, only in the function log. Principle 9 wants both.

## 🅿️ FOLLOW-UP — the Network page does not show parked changes (ADR-241)

The hub now parks a change it cannot apply instead of re-sending it forever, records it in the errors list and returns it to the peer, and the peer counts it on the exchange result (`parkedOps`). What is missing is the sentence on `/build/network` saying "this copy could not accept N of your changes." Today you find out through the hub's own errors surface and the weekly health check.

`HubStatus` already carries `holdReason`/`heldOpsCount` in exactly this shape, so this is a surface, not new logic: add `parkedOps` to `HubStatus`, fill it from the exchange result beside the others, and render one line on the row. Do it the first time a parked change is noticed late.

## 📡 QUEUED — the hub calls the peer instead of the peer polling (core, write the ADR with it)

**Agreed with Brandon 2026-08-30 as the follow-up to ADR-240.** "Only on changes" got outbound changes to zero-cost idling: your edits leave immediately, and a quiet hub is contacted once per cadence. It cannot do the same for INBOUND changes, because a peer cannot learn the hub has news without asking. The cadence is that floor, and the floor is the remaining cost.

**The shape.** The hub calls the peer when it has something, over the Tailscale Funnel address the peer already publishes (`NEXT_PUBLIC_APP_URL`). Nothing talks unless there is real work. "Only on changes" stays as the fallback for any peer the hub cannot reach, which is most of them.

**Why it is core.** It adds a new DIRECTION to the machine API: today every exchange is peer-initiated, and the hub holds no address for anyone. That changes the contract, needs a stored callback address per peer, and needs a story for a peer that is unreachable, moved, or hostile. Both-agree plus an ADR before it merges.

**Open questions for that ADR.** Where the callback address is stored and who may set it; whether the nudge carries a payload or only says "come and get it" (the second is far safer); how it authenticates in the hub-to-peer direction; and what a peer does when nudges stop arriving, since silence must not read as "nothing has changed."

## 🧹 QUEUED — every scheduled job should be movable, pausable, and off

**Brandon, 2026-08-30:** "we need to ensure any cron type item can be deactivated, moved, or whatever," so a copy can go quiet on a predictable rhythm without falling behind.

**The mechanism already exists and is half-wired.** `users.settings.jobOwners` names which install runs a job, and `standDownIfNotOwner` makes the others skip. Three jobs use it: export, email import, calendar sync. **Missing:** `relatedness`, `purge`, `transcription-poll`, `todoist-sync`, and the notify pair. That is why the cloud still runs relatedness daily even though ownership moved to the laptop on 2026-08-26.

**Two pieces of work.** (1) Put the guard on every job, which is a few lines each and matches the existing three. (2) Add an explicit **off** choice to the ownership dropdown, since today the only way to stop a job is to own it somewhere and let it fail.

**While you are in there:** `relatedness` reports `budgetHit: true` on every run, meaning it never finishes its scan and re-does the same work forever. `item_relatedness` is 334,292 rows and 103 MB. It should converge or be capped, not run daily against a budget it cannot meet.

## 💾 QUEUED — the production database is 869 MB and most of it is dead space

**Measured 2026-08-30, and it is not a cloud problem:** the local hub is 857 MB, essentially the same. Brandon's export being 300-400 MB is correct and consistent.

**Where it goes.** Real content is item bodies 125 MB plus revisions 158 MB, so 283 MB — which is the export. The rest: indexes on `items` 68 MB, rebuildable derived tables 144 MB (`item_relatedness` 103 MB, `passage_refs` 41 MB), sync machinery 21 MB, and roughly **290 MB of dead space**. The `items` table's overflow storage holds 414 MB to store 125 MB of live bodies; it was last auto-vacuumed 2026-08-17 and carries 4,196 dead rows. Every body edit rewrites the row and the old copy lingers.

**The fix is `VACUUM FULL` on `items`,** which takes an exclusive lock and therefore is a deliberate maintenance action, never a cron. Take a Neon branch snapshot first (the daily job already makes them). Reclaiming ~290 MB on a 500 MB free allowance is worth the outage window.

**Not the cause, but worth knowing:** only 35 items carry embedded `data:image` URIs and they total 1.7 MB, so pasted images are not what is filling this.

## 🎚️ FOLLOW-UP — the storage warning only fires on upload

**Shipped with ADR-231, deliberately half-surfaced.** `storageUsageFrom()` (`src/lib/attachments.ts`) owns the thresholds and the wording; `storageWarning` rides both MCP upload tools and the in-app upload response. So you find out you are at 85% *while already adding a file* — which is late if what you were adding is the large thing.

The helper is pure and already returns `{usedBytes, quotaBytes, fraction, level, message}`, so every option below is a **surface, not new logic**:
- **A gauge in Settings** — the obvious home, visible without uploading, but it is the one that needs real UI (`SettingsForm.tsx`).
- **A proactive notification** — the push subscriptions + `notify-prep` cron already exist, so a daily "you crossed 80%" is mostly wiring. Best matches "I'd like to know when I'm getting close," since it reaches you *before* you go to upload.
- **A line on `/health`** — nearly free, but only useful if someone looks.

Pick one the first time the upload-time warning annoys you. Not worth guessing at now.

## 📄 IDEA — "turn into markdown" on upload: keep the content, drop the bytes (Tyler, 2026-08-28)

**The ask.** A checkbox at upload time: *turn into markdown*. Upload a Word doc, a PDF, a deck — Ledgr converts it, files the markdown as the content, and you keep the thing in Ledgr without the binary eating the quota. Tyler's framing: "save on space and still add the file."

**Why it earns its place.** It is the highest-leverage move available against the 10GB cap. A text-heavy 4MB PDF becomes tens of KB of markdown — two orders of magnitude — and the markdown is searchable through the existing FTS, which the binary never was. It also answers the warning above properly: when someone sees "8.5GB of 10.0GB," the fix they actually want is not "delete things," it is "keep what the file *said* and drop the bytes."

**Forks that need deciding before building:**
1. **Replace the original, or keep both?** "Save on space" implies replace, but conversion is lossy and irreversible once the bytes are gone. Third option worth preferring: replace, but hold the original for N days — the audio-retention purge (ADR-089) already has exactly this shape and could be reused rather than reinvented.
2. **Where does the markdown land?** The item body (immediately searchable) or its own note child. Body is simplest; a long PDF would sail past `LARGE_BODY_THRESHOLD` (100k chars, ADR-125) into the source/preview path — which is arguably the correct outcome, not a problem.
3. **What does the converting?** Principle 5 says the dependency has to earn its place. `.docx` is the easy win (a small pure-JS converter). PDF is the hard one, and **a scanned PDF yields nothing from a text extractor** — that case must be detected and refused, never silently filed as an empty note.

**The privacy catch, and it is the one that matters.** ADR-231 just put attachments behind an access gate. **Markdown in an item body is not behind that gate** — it syncs between installs, exports to OneDrive, prints, and renders on a public share page. So "turn into markdown" silently moves content from the gated side to the ungated side. Fine for a recipe; for a bank statement or anything sensitive it is precisely the leak ADR-231 was built to stop. Whatever ships must not present conversion as an unqualified good: at minimum it warns, and it should probably be off by default for anything the owner treats as sensitive. (The scanned-image case is self-limiting while OCR is out of scope — but a *text* PDF of a statement converts perfectly and leaks perfectly.)

**Cheap first slice:** `.docx` only, keep the original, markdown into the body. Proves the flow without touching PDF or OCR, and `.docx` is the format most likely to be mostly text anyway.

## 🔗 IDEA — actually opening `file://` links, not just copying them (Tyler, 2026-08-29)

**The ask.** Click a `file:///…` link item and have the file open.

**What exists.** `SmartHref` (`src/components/ui/SmartHref.tsx`, 2026-08-20, ADR addendum 5): a `file:` URL renders as a copy-on-click button with a "paste it into the address bar" toast, because **every browser blocks navigating to a local file from a web page** — a security rule no web app can override. Copy-to-clipboard is the ceiling for a pure web page; "open it" requires something installed on the machine.

**The realistic paths, both opt-in per machine:**
1. **An OS-registered protocol handler** — a tiny helper (macOS: an `.app` or LaunchAgent; Windows: a registry entry + script) that registers e.g. `ledgr-open://`, receives the path, and hands it to `open`/`start`. `SmartHref` then tries `ledgr-open://…` first and falls back to copy when nothing answers. This is how Zoom/VS Code links work, and it works against the **cloud** deploy too — the handler runs client-side. The helper is a new per-machine install to maintain (weigh against Principle 5's spirit).
2. **The local supervisor opens it server-side** — on a local-peer install, server = your machine, so an endpoint shelling out to `open <path>` works with zero new installs. But it only helps installs running the supervisor, and Tyler's daily instance is the cloud one, so this alone doesn't answer the ask.

**Fork to decide:** whether the helper is worth owning, or whether the real answer is the item below — stop pointing at local files and put the file *in* Ledgr. For HTML specifically the item below is strictly better (shareable, device-independent); `file://` links remain right for things too big or too local to upload.

## 🌐 FILES AS A FIRST-CLASS CITIZEN — the three upload modes (Tyler, 2026-08-29) — **MODES 2+3 BUILT same day (ADR-236, branch `feat/files-first-class`)**

**What shipped:** mode 2 (any-file editor upload: paste/drop widened, paperclip Attach toolbar button, `/file` slash command; images embed, everything else links) and mode 3 (the pre-built `file` type, migration `0061`; `FileCanvas` with the shared `FilePanel` — open in new tab, Share copies `/files/<id>?s=<token>`, Remove behind ConfirmButton; plus the **Files card** in the tool catalog for projects/project-style pages). The presign handshake is shared at `src/components/attachments/upload.ts` and now surfaces ADR-231's storage warning as a toast. Guide updated (Images and files / Attachments / Files as items). Verified: tsc, eslint, verify-record-widgets, verify-derived-widgets, verify-editor-media, verify-attachment-urls/privacy, verify-user-guide, migration applied to the dev DB. **Mode 1 (convert to markdown) stays queued above. Follow-ups queued below the original write-up:**
- **Files panel on longform canvases** — record cards exist only on widget-home pages, so "add files to this type" currently reaches projects/pursuits/Project-style types; a note gets files inline via `/file` instead. If a Files panel on notes is wanted, it's a `LongformCanvas` byline addition reusing `FilePanel`.
- **Inline HTML/PDF preview on the file canvas** — deliberately deferred (Tyler, 2026-08-29): Open in new tab is v1; an iframe over the presigned URL has expiry/refresh fuss.
- ~~**Browser eyes owed.**~~ **DONE 2026-08-29 — Tyler tested on the preview and it works**, after two real fixes his testing surfaced: the Preview env had no `R2_*` vars, and the bucket had **no CORS policy at all** (browser uploads had never worked anywhere; runbook §1 updated, `scripts/r2-cors.mjs` now carries the two-instance superset origin list). Same-day polish from his feedback: a global **upload progress bar** (`UploadProgress.tsx`, XHR PUT so progress is real), a **view** action on share-link rows, FilePanel actions always visible (hover-reveal hid Delete — on touch, forever), and upload failures now toast instead of dying in the console.
- ~~**💡 Link items backed by an upload.**~~ **DECIDED AND BUILT same day (Tyler: "we need both").** The URL field (`FieldStrip`) now takes a **dropped file or the paperclip beside it** — upload fills the URL with the stable `/files/<id>` address, so your own PDF/page sits in a project's Links card like any web link; the File type stays the "the file is the main event" framing and its card is already a project tool. Bonus fix en route: a URL-less link's page had NO way to set its URL at all (`LongformCanvas` filtered the field unconditionally); the filter now only drops it when the chip is shown.
- **Round 2, same day (ADR-237 addendum 2):** the all-files browser moved to its own **Build → Files** page under DATA (Tyler's call — files are content, not cleanup); **every item now shows a Files section** above Export & sharing when it has files (Open / Copy link / Share / Delete + "not linked" chip — the backspaced-link answer, on the item itself); the orphan sweep gained **Recover all** (one note per file, attachment row recreated under the key's original uuid, body links it — proven live); the upload bar's third and final home is **top-center** (corner invisible, bottom collides with the mobile keyboard). The longform Files-panel follow-up above is now DONE by the footer section.
- **BUILT same day — originally as Data Hygiene tool #2: All files** (Tyler: "where would I see the files I have?"). Every attachment, size, parent-item link, a "not linked" chip when nothing in the item points at it anymore, Delete per row (`AllFilesTool`, `GET /api/hygiene/files`, `listAllAttachments`). This is also the concrete answer to "I backspaced the link — did it delete?": no; the row stays and shows up here. Upload progress moved bottom-center above the toast (the corner was missable). Also found on the rig: Tyler had hand-made his own "Files" type before the pre-built one existed — his renders the plain markdown canvas, the pre-built **File** type is the one with the panel + Share.
- ~~**🧹 Orphaned R2 bytes.**~~ **BUILT same day (ADR-237):** the trash purge now deletes the R2 bytes behind cascading attachment rows (origin-only; sync replication doesn't repeat it), and **Build → Data Hygiene** grew its first real tool — the orphaned-files scan + delete-all, reclaiming anything already stranded. Needed one core touch, flagged in `COLLAB.md` for Brandon's ack before merge: `StorageProvider.listObjects(prefix)` (additive). Still open from the same conversation: a NOTE has no surface listing its attachments (the longform Files-panel follow-up above covers it). Tyler also wants **more Data Hygiene tools next** — the ADR-063 plan list (types with no items, empty views, unapplied templates, orphaned relations, never-filled properties) is the menu.

Original write-up (kept for the decisions and caveats):

**The vision (Tyler's words, tightened).** Files stop being an image-only editor trick. Three distinct upload modes, each answering a different "why am I uploading this":

1. **Convert to markdown** — upload a doc/PDF, Ledgr converts it, the *content* is what you keep. Cheap storage, searchable. Already queued above ("turn into markdown," 2026-08-28) with its forks and the privacy catch; this list just gives it its place in the family.
2. **File linked inline in the body** — mid-note, `/file` (or drop/attach) and get a markdown link `[name](/files/<id>)` where the cursor is. For files that support a note rather than being the point of it. The editor **already has** a slash-command menu (`slash-suggestion.ts`: headings, lists, checklist, quote, code, table, divider, toggle), so `/file` is one more entry that opens the file picker, uploads through the existing presigned path, and inserts the link — plus widening paste/drop/toolbar from `accept="image/*"` to any file. (Do not repeat the 2026-08-29 mistake of quoting CLAUDE.md's "slash menu is not a v1 requirement" line as though the menu doesn't exist; that line predates the build and refers to the fuller Notion block-drag polish.)
3. **The item IS the file** — a pre-built `file` type: one uploaded file as the object, the markdown body as the owner's *description/notes about it* (what it is, why it's kept, where it came from). For files markdown can't be (HTML pages, spreadsheets, binaries). Its canvas leads with the file — name, size, type, **Open** — and carries a **Share** button that mints/reuses the item's share token and copies `/files/<id>?s=<token>`, so the file itself is handed out as a live link. This is the mode that answers "I make HTML files with Claude and want to share them as rendered pages."

(The fourth, images pasted into the body, already exists and stays as-is.)

**The serving side already works, end to end — verified in code 2026-08-29:**
- R2 is general file storage and `createAttachment` (`src/lib/attachments.ts`) has **no MIME allowlist**, only size caps; MCP `attach_file` can attach any type today. Only the in-app UI is images-only.
- `/files/<id>` (`src/app/files/[id]/route.ts`) 302s to an R2 presigned GET; R2 serves the stored content-type with no `Content-Disposition`, so `text/html` **renders as a live page**, on R2's origin rather than Ledgr's (uploaded HTML can't script against the app).
- ADR-231 already gives mode 3 its share mechanism: `?s=<token>` grants an anonymous read when the token belongs to the attachment's parent item. When the item *is* the file, "share the item" and "share the file" are the same act — the scoping fits perfectly.

**The "add files to this type" checkbox — reframed as surfacing, not capability.** At the data layer every item can already carry attachments, and that's correct ("everything is an item"; don't gate the primitive). What the checkbox should mean is the existing record-card model: a **Files card** in the type's layout — rows of attachments with upload/open/remove — that any type can add (notes, projects, whatever) and that the `file` type's layout leads with. No new primitive; it's the same move every other card made.

**Share-scoping caveat, and the dodge:** a token is per parent item, so "share" on a five-file note exposes all five. Per-*attachment* tokens would be a real access-model change (ADR territory, not obviously worth it). The dodge: the "share this exact file" button lives on `file` items (one file each, the type's whole point); the Files card on other types shares via the item like everything else.

**Slices, in order:**
1. Widen the editor upload to any file type (mode 2) + guide's Images/Attachments sections. UI-only, solo lane.
2. The `file` pre-built type + its canvas + the Share button (mode 3). A module on the shared frame, but a shipped pre-built type lands on Brandon's instance too — passage took ADR-060, so this wants a short ADR written alongside it.
3. The Files card in the record-card vocabulary (the checkbox). Widget vocabulary, solo lane.
4. Mode 1 proceeds on its own queue entry (it has its own forks and the privacy catch).

**Decided (Tyler, 2026-08-29):** the `file` canvas opens the file in a **new tab** for v1; inline HTML/PDF preview (an iframe over the presigned URL, with its expiry/refresh fuss) is a future polish item, not part of the first build. Tyler also confirmed the checkbox-as-Files-card reframing (surfacing, not capability).

## 💡 IDEA — the `app` type: interactive HTML apps as items (Tyler, 2026-08-29) — **DISCUSSION ONLY, nothing decided, Tyler wants Brandon's read first**

**The itch.** Tyler builds interactive HTML artifacts with Claude for anything that outgrows a note — a 5-page family-finance dashboard with sliders that recalculate, a YouTube-strategy site full of organized transcripts, a paper-writing surface (quote bank + outline rendered as one scrollable page), test-prep quizzes that grade themselves. Today those live as loose files or R2 uploads. He wants that *artifact feel inside Ledgr*: an item you open and it runs, related to the project it serves. This is NOT a body-format question — markdown stays canonical and stays the writing surface; interactivity needs JS, which no markdown dialect provides at any price. It grew out of, and stacks on, mode 3 above (the `file` type already serves uploaded HTML as live pages).

**The design sketch the conversation converged on:**
- **A bespoke `app` type whose body IS the HTML/JS source** (`{format, text}` as ever — a new format value, so likely core, see gates below). Body beats an R2 file because everything already works: create/edit through the existing MCP tools (no upload round trip), revisions as undo, soft delete, the digest guard, backups. ~177kb apps are nothing to Postgres; past `LARGE_BODY_THRESHOLD` they take the source/preview path, which is correct.
- **The canvas renders the app** in a sandboxed iframe, full width; source editing tucked behind an affordance. (Contrast with `/files/<id>` HTML, which deliberately runs on R2's origin with no app access — an app item runs *inside* Ledgr, so sandboxing + the bridge below are the security model, and they're the real design work.)
- **A tiny bridge, kept deliberately tiny:** `ledgr.saveState(data)`/`loadState()` (the app's own memory — quiz progress, slider values, typed drafts, stored on the item) and `ledgr.getRelated()` (read items related to this app — and *only* those; the scoping is the safety story). Same shape as Claude artifacts' `window.claude`.
- **Data lives in items, not baked into the HTML.** Transcripts, quote banks, quiz questions are ordinary markdown items related to the app; the app is a viewer. That keeps token costs low (Claude edits a small markdown item, not a giant HTML blob), keeps content searchable via FTS, and means a non-coder *operates* an app by editing normal items even though *authoring* one takes code or AI.
- **Templates make it no-code:** ship starter apps (quiz, flashcards/scripture memory, budget dashboard, tracker) where creating an app = pick template, relate your items. Cheap to offer — each template is a pre-written body.
- **The promotion ladder** (this is just the bespoke-first principle with a middle rung): catch-all item → app template → proven → promoted to a real bespoke module. Guardrail: when a template app starts wanting notifications, mobile polish, or cross-app data, that's the signal to *promote it*, never to grow the bridge into an app platform.

**Savor caution (Tyler's own case).** Importing Savor's data into Ledgr for safekeeping is right regardless. A simple "devotional" template over that data is a fine prototype. But Savor-the-daily-habit-app (phone-first, streaks, notifications) is a promotion candidate — likely the discipleship module's seat — not an iframe app; rebuilding it as a template is the bridge-bloat trap.

**Steps if it goes ahead:** (1) `explorations/app-type.md` capturing the above; (2) the Brandon conversation — a shipped pre-built type lands on his instance (precedent: `file` ADR-236, `passage` ADR-060), a non-markdown-family `format` value touches the body contract, and the bridge is new API surface, so this wants an ADR even though much is additive; (3) slice 1 = the type + render-body-as-HTML-in-sandbox, nothing else; (4) `saveState`/`loadState`; (5) `getRelated` with related-items-only scoping; (6) first two templates; (7) revisit `explorations/flexible-surfaces.md` and `dashboard-widgets.md`, which circle the same instinct.

## 🔐 DECISION NEEDED — final auth posture: Clerk dev instance vs. domain + Clerk prod vs. Auth.js + Entra (Brandon's session, 2026-08-30; queued by Tyler 2026-08-31)

**Settle the OAuth question once so it stops getting re-litigated.** Out of Brandon's "Local ledgr install" session. The actual weakness: both instances run Clerk **development** instances, which carry the session token in the URL (`__clerk_db_jwt`) instead of a protected cookie — Clerk itself calls this "not a strong security practice" (URLs land in history, screenshots, server logs). Honest risk read: **low likelihood, real impact** (pastoral notes). Nothing is currently open — the cloud copies 404/bounce strangers, MCP wants its own token, share links are 24 random bytes. The exposure scenarios (stolen laptop, pasted screenshot, malicious extension) mostly presume an attacker who already has the signed-in browser.

**The three options, costs verified against the repo 2026-08-31:**
- **A — buy a domain, full Clerk production upgrade.** ~$12/yr + ~2 focused hours + a DNS wait; own Entra app (secret expires every 24 months). Fixes it properly (session moves to an httpOnly `__client` cookie). Four traps, all real in this codebase: the publishable key is baked at build time (`src/lib/auth/keyless.ts`) so env edits need a redeploy; `src/proxy.ts` fails **closed** with a 503 on a half-applied key; the owner row won't relink itself (`src/lib/owner.ts` ~94 refuses to overwrite an existing `clerk_id` — clear the stale id by hand before first prod sign-in); SSO/Integrations/Paths don't copy from dev. Machine surfaces (MCP, share links, ICS, `/files`, Todoist webhook) never touch a Clerk session (`src/proxy.ts` public routes) so they keep working on the vercel.app address; R2 CORS needs the new origin added (`scripts/r2-cors.mjs`).
- **B — stay on the dev instance, permanently and on purpose.** Free, zero upkeep, no code, no ADR. Two-minute loose end to close either way: Clerk allowlist enforcement was never actually enabled (runbook §, waitlist mode queues rather than grants — verified zero pending) — Configure → Restrictions → Restricted + Allowlist on, **on both instances**.
- **C — replace Clerk with Auth.js + own Entra app.** No domain needed (redirect to vercel.app works); session in an httpOnly cookie on our origin. The seam is genuinely ready (`src/lib/auth/types.ts` one-method interface; `verify-provider-seams.mts` already fences Clerk to five files; a fifth provider is additive). But it keeps the 24-month Entra secret chore, adds auth code we own forever, and **auth is on the core list — Tyler + Brandon + ADR before merge** even though additive.

**Brandon's AI's recommendation: B**, revisit if a domain shows up for other reasons or a second real user appears. **The wrinkle Tyler should weigh before agreeing:** a domain IS already queued for other reasons — the ADR-228 follow-up above ("then the custom domain," runbook §1, moving attachments off rate-limited r2.dev) — which is exactly the note's own "what would change this answer" trigger. If/when that domain lands, A becomes a cheap follow-on and the note itself says take it. So the realistic shape may be: **B now + the Restrictions fix immediately, A opportunistically when the R2 domain happens**, C only if we want Clerk out of the stack for its own sake.

**Pricing checked (clerk.com/pricing, 2026-08-31): Clerk production is FREE.** The free plan includes production instances AND custom domains — 50,000 monthly retained users, unlimited apps ($25/mo Pro adds only branding removal, MFA, SAML — nothing Ledgr needs). So Option A's total cost is the domain alone, **and Tyler already owns a domain for Ledgr** (he has 3; Brandon has 1), which removes A's only real cost for both current instances. Where this landed (Tyler, 2026-08-31): **stay with Clerk.** (1) The zero-config onboarding path depends on it — Clerk dev instances use Clerk's own shared Microsoft OAuth credentials, so a new user never registers an Entra app; every library alternative (Auth.js, Better Auth) would force each user to register + rotate their own Entra secret, making setup HARDER, not easier. (2) The future hosted tier is one Clerk production instance on the product domain, free to 50k users, so the long-term direction is Clerk-shaped anyway. (3) The seam keeps this a non-lock-in: leaving Clerk stays a half-day job if it ever earns it. Net plan: **Tyler + Brandon upgrade their instances to Clerk production on their existing domains whenever convenient; other users stay on documented dev mode with the allowlist fix baked into setup; the hosted tier is the eventual fix for domainless users.**

**Mostly per-instance:** A and B are dashboard/ops choices each instance makes on its own timeline (separate Clerk instances, separate Vercel projects); only C touches the repo. If A or C ever happens: register the sign-in Entra app **separate from `ledgr-export`** (a shared secret expiry would take out export, calendar, email-in, AND sign-in at once), and note nothing watches a sign-in registration's secret — `/health` `checks.graph` covers only the export registration, so a lapsed sign-in secret fails silently until the morning you can't log in. Two calendar reminders, or a `/health` check, are cheaper than that morning.

**The future-users angle (Tyler, 2026-08-31).** This is not hypothetical: Tyler's current onboarding playbook for new users IS the hand-rolled stack — each person creates a free Neon + Vercel + Clerk + GitHub account — so **dev-mode Clerk is the de facto standard install today**, and most future users won't own a domain. Verdict: acceptable as a *documented* default, because every install is a single-user island (one user's weaker session endangers only their own data; no shared blast radius) and the per-user risk is the same low one analyzed above. Conditions that keep it defensible: (1) the setup instructions include the two-minute Restrictions/allowlist fix as a standard step; (2) the docs say plainly that dev mode puts the session token in the URL and that a ~$12 domain upgrades any instance to production auth; (3) know it's off-label — Clerk says dev instances aren't for production use (the per-instance user cap never bites since every install has one user, but it's a gray area, not a blessed path). The proper fix for users-without-domains is the hosted tier (one product domain, one Clerk production instance, real auth for everyone) — and a growing count of hand-onboarded users is itself the trigger that the hosted tier is worth building. Self-host users on the local peer path avoid Clerk entirely.

**✅ DONE — Tyler's instance upgraded to Clerk production (2026-08-31).** Prod instance `ins_3IgcRKryuIPTisGVf71O3bnw0Pn` on the Ledgr Clerk app, domain **ledgr.tylerjcollins.com** (five Clerk CNAMEs in Namecheap; DNS + SSL + mail complete), the three Vercel env vars swapped + redeployed, owner row relinked, R2 CORS updated (`scripts/r2-cors.mjs` carries the new origin). Sign-in is **email code / password to `tyler@tylercollins.me`** — the owner row's email moved off the bethany address to match the Clerk account Tyler created. **Gotcha found live, TWICE: `users.email` must match every owner-UPN env var.** Changing the row's email broke MCP owner resolution ("MCP owner not configured") and then, discovered separately, the REST API (`/api/machine/items` — Claude's API integration went down): `resolveMcpOwner()` and `resolveMachineOwner()` each match `users.email` against their own UPN env chain (`LEDGR_MCP_OWNER_UPN` and `LEDGR_API_OWNER_UPN` respectively, both now `tyler@tylercollins.me` + redeployed; MCP verified live via `describe_workspace`, API endpoint verified answering 401-to-strangers). `todoist/owner.ts` and `push/owner.ts` carry the same pattern (not set on this instance). **Queued hygiene idea from this:** five per-surface owner resolvers each re-derive "the one owner" from overlapping env chains — a single `resolveOwner()` (or a DB-side "owner" marker instead of email-matching an env var) would have made this one break instead of two, and would stop `users.email` from being load-bearing config. Cheap, solo-lane, worth doing before anything else renames an email. *(Built 2026-09-25: `resolveInstanceOwner`, ADR-274.)* **Post-flip sweep (2026-08-31, after the API break): nothing else broken live.** No Graph/Todoist/push/GitHub/sync configured on this instance, no other email- or URL-bearing env vars. Two dormant time bombs recorded: (a) **the claude.ai MCP connector + Claude Code `ledgr` entry still point at `ledgr-sandy.vercel.app/api/mcp`** — works on refresh tokens, but OAuth RE-approval needs a browser Clerk session, which now exists only on the custom domain, so re-add both at `https://ledgr.tylerjcollins.com/api/mcp` at leisure, before a forced re-approval does it in a crisis; (b) the calendar-feature owner-exclusion (`suggest-people.ts`, `templates/pin.ts`) matches `users.email` — if calendar sync is ever pointed at the bethany mailbox, the owner exclusion misses (covered by the single-`resolveOwner()` hygiene item above). Dev Clerk instance sign-up still public (previews use dev keys) — the two-minute restricted flip is handed to Tyler.

**Still open:** (1) ~~sign_up_mode~~ — flipped to `restricted`, confirmed 2026-08-31; (2) reinstall the phone PWA from the new domain (browser sign-in only works there; machine surfaces stay on vercel.app); (3) optional polish: register a Google Cloud OAuth client and enable `connection_oauth_google` with custom credentials to get the Google button back; (4) ~~`.env.production.local`~~ — updated 2026-08-31 (new pk/sk + app URL). The claude.ai connector and Claude Code entry were both re-added at `https://ledgr.tylerjcollins.com/api/mcp` the same day.

**Related decisions from the same session, recorded so they stick:** keep the Vercel copy (separate failure domain from the local peers/tailnet; a real sync peer, unlike the OneDrive export — use it for a day once or twice a year as the test); publish the closet laptop **MCP-only** via path-scoped Funnel (only `/api/mcp` routes; proven in that session), so a second public app interface never exists.

## 🐛 FLAKY — `verify-sync.mts` per-key settings check fails ~1 run in 3

**Pre-existing, unrelated to the storage work, but it makes `verify:ci` red at random.** The check is *"the assignment survives an unrelated preference written later elsewhere"* (ADR-226 per-key settings merge): `jobOwners` comes back `{}` on both peers while `accent` converges correctly. The preceding check, that the trigger logged only the moved key, passes every time.

**Measured, not argued:** same tree, same env, back to back — **4 pass / 2 fail**. It also fails on a branch off `main` containing none of the storage changes.

**Don't repeat this mistake:** a first pass at it *looked* like storage configuration mattered, from a six-condition matrix built on ONE sample per condition. That was noise read as signal, twice. Sample it 6+ times per condition or don't draw the conclusion. What actually settles causality is the import graph: `verify-sync.mts` reaches only `sync/engine`, `sync/apply`, `@/db`, `@/lib/passages/refs` — nothing storage-related.

**One probe worth keeping:** widening the deliberate `await sleep(50)` between the two competing writes to 500ms drops the rate (1 in 6) but does not eliminate it, so it is not simply the two stamps tying inside a too-tight window. Mechanism still unidentified. Worth chasing before the local-hub cutover leans harder on sync — this check guards a real reported failure, and one that cries wolf a third of the time is one people learn to skip.

## Queued — AI memory axis separation (ADR-230, built 2026-08-27, acked by Tyler 2026-08-28)

Built and green: typecheck, lint, both live MCP suites (`verify-memory` 24/24, `verify-mcp` unchanged apart from one pre-existing failure noted below). `horizon` no longer decides what loads; the always-on set is `pinned` and nothing else, ages render at read time, and the stump index is compact text instead of pretty JSON. Full reasoning and the measured before-numbers are in ADR-230. Source brief: `_Drafts/ledgr-memory-axis-fix-SPEC.md` (keep it there, it names a confidential personnel memory as an example).

- [x] **Tyler's ack.** Given 2026-08-28, relayed by Brandon; recorded in `COLLAB.md` and on the ADR. CORE because `get_memory_stumps`' response shape and default result set both change, so ADR-183's additive carve-out does not cover it. No schema change, no data mutated. Record in `COLLAB.md` and on the ADR when given.
- [ ] **Backfill the mislabeled data (spec §5.9), after the descriptions ship.** Pointless before, because write-time keeps regenerating the mess. Reclassify the 12 evergreen memories carrying time-bound titles to seasonal; review the remaining evergreens, expecting roughly a third to be project state rather than durable truth. One-time MCP calls, no migration.
- [ ] **Review the pinned set against the new bar.** Seven items today, all correctly Tier 1. Anything unpinned that gets pulled in most sessions is a pin candidate; anything pinned that a name search would find belongs in Tier 2.
- [ ] **Memory audit agent (spec §5.10) — harness, not Ledgr.** A skill plus a scheduled task reporting into a Ledgr task on the existing "🤖 AI System — Input needed" pattern. Flags: evergreen titles containing "now"/"live"/"currently"/"pending"/"not yet" or a bare year; near-duplicates (agents file independently without reading each other); pin candidates; pinned count past ~15; stale seasonals with a plausible newer replacement.
- [ ] **Unrelated, found in passing:** `scripts/verify-mcp.mts:122` asserts `resources/list` returns exactly one guide. It returns more now, so the check fails on `main` too. Stale assertion, not a regression.

**Open questions Brandon has not answered** (shipped on the spec's own recommendations): no recency window alongside pinned (add one only if something concrete gets missed); `episodic` kept rather than dropped, since retiring an enum value is a schema change; stale threshold at 90 days, a guess the store is too young to have tested; pinned ceiling documented as 15.

## ⏭️ NEXT UP — Relation picker `targetFilter`: MCP-shaped flexible connections (2026-08-17, CORE — Brandon + ADR gate BEFORE merge)

**The decision (Tyler, 2026-08-17).** Song "themes" stay in the shared `tag` type, NOT a dedicated theme/taxonomy type: the payoff of tags is cross-type discovery (open "Christmas" and see the song, the sermon, the devotional, and the notes taken on Christmas), and a split namespace kills exactly that. The friction is the *picker*, not the model — the tag namespace holds concept tags (Christmas, Faith, Prayer) and workflow labels (Bulletin, AppleCare), each noise in the other's autocomplete. And the fix deliberately generalizes past tags: **a relation-kind property may declare an optional `targetFilter: {property, values}` against any property of its target type.** Tag "facets" then need no new primitive — an ordinary owner-added select on the `tag` type (ADR-055 already killed type-plus-`kind` once; don't reintroduce it) — and the same key gives `attendees` → prefer people where relationship = staff, or a future sermon `passage` → filter by book. **Semantics: rank-first, not hard exclusion** — matching items surface first, but a name search still finds a mistyped tag.

**Exposure (Tyler, 2026-08-17): MCP-writable only, Build-readable.** Per the ADR-102 posture, this is an advanced-user feature shaped in conversation with Claude (create_type/update_type carry the key); the Build type editor does NOT grow an editing knob. But the Build side must still (a) **round-trip the key** — the editor rewrites `propertySchema` wholesale on save, so an unknown key would be silently stripped the first time anyone touches the type in Build — and (b) **display it read-only** on the property row (e.g. "Filtered: facet = concept"), so the owner can always *read* why a picker behaves filtered (the scope-the-UI convention: no invisible bespoke behavior).

**Why CORE despite MCP-only surface:** the key extends the property-def contract in `schema.md`. ADR-183's carve-out covers additive MCP *surface* (tools/args/response fields), not the propertySchema contract. Precedent for a tolerant additive relation-def key: ADR-067's `targetType`/`cardinality`. Pitch angle for Brandon: this solves picker noise in his instance too (people/meeting pickers), and it's parse-tolerant additive — his instance behaves identically until a def declares a filter.

**Build slices (in order):**
- [ ] **Slice 0 — Brandon + ADR.** Agree the contract: `targetFilter: {property: string, values: string[]}` on relation-kind defs; rank-first semantics; MCP-write/Build-read exposure. Log in `decisions.md`.
- [ ] **Slice 1 — parser.** `parsePropertySchema` (`src/lib/types.ts`) accepts optional `targetFilter` on `kind: "relation"`: validate shape only, not property existence (same posture as `targetType` — pure, runs on every read, degrades gracefully if the referenced property is later deleted). Round-trips through `rowToDefinition` and the `typeView` serializer (`src/lib/mcp/tools/serializers.ts`) so MCP reads it back.
- [ ] **Slice 2 — API.** `GET /api/items` grows a property-equals filter param (owner-scoped, no `body` in the query — the standing list rules). Properties live in jsonb, so filter via containment; result sets here are picker-sized (limit 8), so no new index expected — confirm with a quick explain on realistic data.
- [ ] **Slice 3 — picker.** `RelationField.tsx` reads `targetFilter` off the field def and passes it through; rank-first = fetch filtered matches first, back-fill remaining slots from the unfiltered query.
- [ ] **Slice 4 — MCP + docs.** `create_type`/`update_type` input schemas + descriptions document the key (`src/lib/mcp/tools/types.ts`); `describe_workspace`/`list_types` expose it; the MCP guide + the user guide (`src/lib/mcp/user-guide.ts`, ADR-189 — the owner can now *do* something new) get a section.
- [ ] **Slice 5 — Build read-only display + round-trip.** The type editor (`/build/types/[key]/edit`) preserves `targetFilter` on save and renders the read-only "Filtered: …" line on the property row. **This slice is not optional** — without it, any Build-side edit of a filtered type silently destroys the filter.
- [ ] **Slice 6 — first real use (Tyler's instance, workspace config, no code).** Add a `facet` select (concept | label) to the `tag` type, backfill the ~50 existing tags, set `targetFilter: {property: "facet", values: ["concept"]}` on `song.themes`. This is the acceptance test: theming a song stops offering "AppleCare," searching "Christmas" still crosses every type.

## Queued — finish the storage-address work (ADR-228, built 2026-08-26)

Built, green (typecheck, lint, build, 75 verify scripts, 27 new pure checks, live 302→200 proven in a browser), **acked by Tyler 2026-08-27 and merged the same day** — it touches the canonical body format and the storage seam, so it is core. Bodies now store `/files/<attachmentId>` instead of the R2 URL, which makes a provider or base-URL change an env edit with no body rewrite.

- [x] **Tyler's ack.** Given 2026-08-27, relayed by Brandon; recorded in `COLLAB.md` and on the ADR. Purely additive to the API surface (`publicUrl` unchanged, `fileUrl` added), no migration, no wire change.
- [ ] **Run the one-time migration on each install.** `npx tsx scripts/migrate-attachment-urls.mts --apply` (dry-run by default, idempotent). Local install: 1,412 items, 9,854 URLs. Take a snapshot first — it writes no revisions on purpose, so a backup is the undo. It bumps `updated_at`, so expect an export pass over those items afterwards (markdown only; attachment bytes are already stamped exported) and a sizeable sync push.
- [ ] **Then the custom domain,** which is the whole point of doing it in this order: procedure in `runbook.md` §1 ("Moving off the r2.dev development URL"). Doing the domain first would have meant rewriting every body twice.
- [ ] **Verify the offline pin path on hardware.** Reasoned through and unchanged in behavior — the service worker's catch-all branch covers same-origin images with the same PIN_CACHE fallback, and `SaveOffline` already falls back to a `no-cors` fetch, which is the path an r2.dev image took anyway since the bucket allows no cross-origin GET. But it is Principle 4, so prove it rather than reason it: Save Offline on an item with images, then load it with the network off.
- [ ] **Consider `R2_ACCOUNT_ID` cleanup.** It is documented in `.env.example` and `runbook.md` but **no code reads it** (confirmed by grep); the endpoint URL carries the account id already. Harmless, but it is a knob that looks required and is not.

## Queued — three found on Brandon's two-peer rig (2026-08-25)

Raised while standing up a prod-data hub and a dev-data hub side by side on one machine. None blocks anything; all three are Principle 9 shaped (a surface saying something that is not so).

- [ ] **`main` is RED on `verify-provider-seams.mts`, and it shipped.** `src/components/nav/NavAuthHeal.tsx` imports `useAuth` from `@clerk/nextjs` directly (ADR-216, `a574ff5`), which the auth provider seam forbids — the seam exists so a local single-user mode can stand in for Clerk (CLAUDE.md, provider-interface discipline). It went to production in the 2026-08-25 release because `release-prod.mjs` runs only the four CORE verifies, not `verify:ci`. **Checked, and it is not a live break:** `Nav.tsx` mounts the component only when `NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY` is set, which is the same condition under which `AppAuthProvider` mounts `ClerkProvider` — so it cannot render without its provider, and a key-less local peer never mounts it. What is broken is the RULE, and the rule is what keeps a local no-login mode a config change instead of a hunt. Two things to decide: route the hook through the auth seam, and whether `release-prod.mjs` should run the full suite rather than four verifies (it would have caught this before prod).
- [ ] **Sync state calls "not due yet" "offline".** With a hub on the daily cadence, `firstHealthyIndex` in `src/lib/sync/client.ts` is only set for hubs the loop actually EXCHANGED with, and a hub that is not due is `continue`d before any attempt — so for the 23h59m between daily syncs the state reads **"Offline (no hub reachable)"** while the hub row beside it is green and says "synced 5 minutes ago". Nothing is unreachable; nothing was due. Observed on the local prod hub. The fix is to treat only an ATTEMPTED-and-failed hub as unhealthy, and to say "Synced · next sync in N hours" when nothing was due. CORE-adjacent (sync engine, ADR-206), so it wants Brandon's yes; the comment directly above that line already says the misreport it now commits is "the silent misreport Principle 9 forbids".
- [ ] **Only one peer per machine can start at boot.** `STARTUP_TASK_NAME` is one constant (`"Ledgr Supervisor"`), so registering a second peer overwrites the first's scheduled task instead of adding its own, and the delete path removes whichever exists. `supervisor/README.md` explicitly supports multiple peers via `--config`. Fix: derive the task name per peer (keep the bare name for the default config so existing registrations keep working). Deferred deliberately on Brandon's call — one peer at boot is enough for his rig today.

## Queued — Apply a supervisor-changing update without being asked (Brandon, 2026-08-26)

**Half of this is done.** ADR-227 built the restart: a button in Build → Updates, a `local:restart` verb, and the peer telling you when it is running code older than what is installed. What is left is not needing to be told.

- [ ] **Auto-restart after an update that changed the supervisor's own files.** The mechanism is already there — the update path knows it just changed the checkout, and the fingerprint comparison already says whether that touched `ledgr-supervisor.mjs` or `lib.mjs`. One call to the same handoff finishes it. Held back deliberately for one reason: a restart loop on a bad update is worse than a button, so the guard has to come with it (restart at most once per update, and never twice for the same fingerprint).
- [ ] **The supervisor still runs from the checkout, not from a build worktree**, so it applies whatever the clone is sitting on rather than the tracked branch (the known ceiling from PR #329/#330). A dedicated clone per peer is the honest fix and is a bigger slice than either of the above.

## Queued — Local hub/spoke: the road to cutover (Brandon, restructured 2026-08-23)

**Where this stands.** ADR-206's phases **1 (sync spine), 2 (local runtime), 3 (alongside operation) and 4 (installer + updates) are done and proven on real hardware.** The sync engine is now the well-tested part of this build: bidirectional against production, two-spoke propagation, both guards tripped live, the staleness refusal merged (ADR-208), the FK family-delete wedge fixed (ADR-206 addendum 7), and Build → Network shipped (ADR-209). What remains is **phase 5 (hub duties on the PC) and phase 6 (the cutover flip)** — and the honest summary of risk is that none of the remaining work is about sync. It is about the PC becoming a *public, always-on, self-healing service*, plus making the install something a non-technical owner (or their AI agent) can actually complete.

The work below is ordered A→E. **A and B2 were the two cutover blockers, and both landed 2026-08-23 (ADR-210, ADR-211) — so nothing in this queue blocks cutover any more; what remains is D, which needs Brandon's machine and accounts rather than code.** B1 also landed the same day (ADR-212). B3, C and E never blocked cutover; C is the safety net for "the PC died mid-trip" and B3 is what makes a second machine cheap to add without a human pasting tokens. Sections A and B1 carry Brandon's 2026-08-23 refinements: per-hub **fallback trust** as an axis separate from cadence (automatic vs prompt-first, with the prompt showing errors and the freshness gap), and "tell me exactly what to type, and show me my own address" rather than a general Tailscale tour.

### A. ~~Per-hub behavior: cadence and fallback trust~~ — **DONE 2026-08-23 (ADR-210).**

Built and proven on the dev rig. `HubConfig` carries two independent fields (`cadence`: continuous | daily, `fallback`: automatic | prompt), parse-tolerant so a hub stored before today reads as continuous + automatic. `exchange()` no longer returns at the first success: every hub due on its own schedule gets an exchange, so **an archive hub now receives changes while the primary is up** — the bug that made "backup hub" a fiction. Pulling is the half trust gates: an emergency hub is push-only (an optional `pull: false` on `/api/machine/sync`, which skips the pull half, the `lastPulledSeq` advance and the staleness refusal) until the owner approves reading from it. When every automatic hub has failed ~15 min (`LEDGR_SYNC_FALLBACK_PROMPT_MS`) and an emergency hub exists, the loop records a pending decision with its evidence — each automatic hub's error, plus the backup's last exchange and how many of your changes it has not received — the pill turns amber, and Build → Network offers the approval. Brandon's three answers are in the mechanism: the approval **auto-clears only on a fully DRAINED exchange** with an automatic hub, so a recovering primary catches up before we stop leaning on the backup; **cadence promotion is asked separately** and lives on the approval, never the config, so it reverts with it; **15 minutes, configurable**. `PATCH /api/sync/hubs` edits either axis and reorders (promoting no longer means remove + re-add, which threw the token away). At least one hub must stay automatic, refused in the API. The recorded trap is closed: the first-push guard and the clock-skew hold are keyed **per hub** now, reached through `pushSelectionForHub` so a re-collapse is a test failure.

**Verified live** on the two-spoke rig, exactly the post-cutover topology in miniature (spoke B as a hub for spoke A): a note written on A reached the continuous mirror in ~12s; a note written on A reached the **emergency** hub by push while A refused to read from it; a note written only on the emergency hub stayed invisible on A until approval, then arrived; the prompt fired at 24s against a 20s threshold carrying the failing hub's error; approving flipped the hub to pulling and the state back to synced; restoring the automatic hub auto-cleared the approval. The all-ask-first list and approving an already-automatic hub are both refused with reasons.

**Left open, recorded in ADR-210 rather than fixed:** foreign-origin ops are still not forwarded (a change that reached only one hub travels to the other via its own origin device, which holds in the star topology but not if that device is lost); a hub we push to but never pull from pins its own oplog (section C's problem, now reachable by configuration too); and the first pull after a long-unread archive is approved may hit ADR-208's 410 and want a re-fill. `cadence: "manual"` was not built — the two values cover the real topology.


### A2. ~~Performance and efficiency, done properly — local AND remote~~ — **DONE 2026-08-24 (ADR-215).** Migration `0057` (three additive `items` indexes) needed an ack under the old process; ADR-269 retired that, so there is nothing open here.

The pass ran the way Brandon framed it: widen first, fix second, a number on everything. `scripts/perf-audit.mts` is the deliverable that outlives it — buffers + runtime per standard surface via EXPLAIN (ANALYZE, BUFFERS), read-only by construction, pointed at the live prod spoke, the cloud pooler, and the rig (`--url`, `--json`). **The widened measurement found two structural O(all-items) queries A2 didn't know about:** the related panel on EVERY item-page open (26,653 buffers — the OR-join gave the planner no index path, so it scanned and sorted all 23k items), and `mostLinked` all-types at 122,194 buffers. Both are rewritten (edges-first UNION ALL; a single grouped aggregate join): 26,653 → 3,241 and 122,194 → 3,448 buffers on the prod spoke, behavior suites passing byte-identical results. **The denormalized relation-count column is raised in ADR-215 and recommended AGAINST** — the rewrite makes the lens cost ~any other lens with no schema change, no trigger on every relation write, no counter that can drift.

The rest, all measured before/after: **nothing indexed `updated_at` at all**, so every list surface seq-scanned ~3,015 heap pages per render — migration `0057` adds two partial live-list indexes (`desc nulls last`, matching listOrderExpr byte-for-byte — a plain `desc` index does NOT satisfy it, measured) plus a trigram GIN for the per-keystroke picker ILIKE: recent lens 1,290 → 203 buffers, all-types → 11, picker 30ms → 0.5ms, badge counts → 29 as index-only scans. **Local Postgres is RAM-sized automatically** (`tunePostgres`, supervisor + restore cluster both): shared_buffers RAM/8 clamped 128MB–1GB so the whole 798MB database stays cached, rpc 1.1, work_mem 16MB; off-switch and manual overrides that always win. **Both restore paths end with VACUUM ANALYZE** (the never-analyzed planner believed 23,470 items were 7; stats alone re-planned the related panel 11× on the rig). **getSettings/listTypes wrapped in React cache()** — Nav + page each re-read both per navigation, two extra HTTP round trips per click on neon-http. Cloud measured too (read-only): same structural costs, so the rewrites and indexes pay there — the one cloud-distinctive number is the 4.5s first search after idle (Neon cold TOAST reads), accepted per runbook §5. Guards: `verify-supervisor.mts` +19, eleven reintroduced bugs each confirmed to fail. Runbook §5 got the durable rules.

**Rollout:** `0057` moves the journal tag, so ADR-206's version gate pauses sync between peers until each takes the update (same as ADR-213's rollout).

**Brandon's framing, and it is the point: be VERY THOROUGH.** Brainstorm many ways to improve speed and efficiency across both the local peers and the cloud deployment, get creative, and treat it as one deliberate pass rather than fixing whatever was last stumbled over. The measurements below are the starting evidence, not the scope.

**How this was found.** On a local spoke holding real production data, `/list/task` loaded fine on the default "recent" lens, but switching to "Most linked" took several seconds — while the same lens on Vercel/Neon was near-instant. The expectation was the opposite: local should be faster.

**What was measured (2026-08-23, on the prod spoke's local Postgres, 798MB database, 23,470 items / 18,780 relations). Do not re-derive:**

| Lens on `/list/task` | Buffers touched | Warm runtime |
| --- | --- | --- |
| recent (`updated_at`) | 3,082 | 24ms |
| title | 3,082 | 14ms |
| **Most linked** | **23,379** | 48ms |

- **`shared_buffers` is 128MB = 16,384 buffers.** One execution of Most linked touches **23,379** — more than the whole cache holds, so it evicts itself every run and can never stay warm. That is arithmetic, not a theory, and it is the primary cause.
- **Statistics had NEVER been gathered on the restored copy**: `last_analyze` / `last_autoanalyze` / `last_autovacuum` all NULL, so the planner believed `items` held **7 rows** (real: 23,470) and `relations` **0** (real: 18,780). A manual `ANALYZE` (2.8s) dropped the estimated cost 158k → 56k. It did not change the warm runtime — this query is I/O-bound, not plan-bound — but every other plan on that database was being chosen blind. **`local-restore.mjs` never analyzes**, which is the actual bug there.
- **`random_page_cost` is 4**, the spinning-disk default, so the planner systematically overprices the index scans these queries depend on. On SSD it should be ~1.1.
- `work_mem` 4MB; `items` is 454MB and `revisions` 170MB on this instance.
- **The lever is easy:** `embedded-postgres` accepts `postgresFlags: string[]`, so tuning is a `supervisor/config.json` block passed through `assembleAppEnv`'s sibling, not a code redesign.

**Candidate directions — raw material for the brainstorm, deliberately NOT a plan:**
- **Local Postgres tuning as a first-class part of the supervisor**: `shared_buffers` sized from the machine's RAM rather than a constant, `random_page_cost` for SSD, `work_mem`, `effective_cache_size`. Sized *automatically* is better than a number in a config file nobody revisits.
- **`ANALYZE` (and a first `VACUUM`) at the end of every fill path** — restore, `--from-url`, and after a large import. Cheap, and its absence is silent.
- **`mostLinked` is O(every matching row) on any backend** — a correlated `count(*)` per candidate. The cloud's larger cache hides it. A maintained relation count (denormalized column, or a trigger-updated counter) would make it O(limit). That is a schema change and wants its own ADR; it is also the only item here that helps the CLOUD as much as local.
- **The other lenses and views** deserve the same buffer-count treatment — this was found by accident on one lens, so measure them all rather than assuming the rest are fine.
- **The cloud side has its own axis**: the neon-http driver is hundreds of round trips per batch (the measured 8.46s for a 500-op sync batch is a *cloud* number). Where does that dominate outside sync?
- **A repeatable way to measure**, so this is not re-derived by hand next time: a script that reports buffers touched and runtime for the standard surfaces, local vs cloud, and can be re-run after a change.
- Revisit the runbook §5 performance rules against what is actually observed, rather than trusting they are still the right rules.

**Why it is queued rather than done:** found late on 2026-08-23 while Brandon was using the local spoke for real work. Nothing here is blocked by the Vercel build cap (all local), it is queued because it deserves one thorough pass instead of three reflexive patches.

### B. Install and onboarding a non-technical owner (Brandon, 2026-08-23)

The goal Brandon stated: **the install should not be technical, and where it unavoidably is, a locally-installed AI agent should be able to do it for the owner.** Three sub-items.

#### B1. ~~Tell the owner exactly what to type, and show them their own address~~ — **DONE 2026-08-23 (ADR-212).**

Both halves, and the second one is the part that actually removed the guesswork. **At the wizard's hub-URL prompt**, the help branches on what this machine can reach with, detected by shelling out to `tailscale status --json` rather than asking the owner to interpret anything: running → the tailnet form with this machine's own tailnet named; installed but not signed in → say `tailscale up` rather than offer an address that will not answer; absent → what to install and why, plus the alternatives *with their limits*. It prefers the MagicDNS hostname over the raw `100.x` (both work; the hostname is readable and survives a re-address). **On the hub**, Build → Network now shows "Other devices reach this instance at" — best first, Copy button on each, each labelled with what it does and does not reach — so adding a spoke is copy-from-one-screen, paste-into-another.

Two things worth keeping: a stopped or unsigned tailnet **publishes nothing** (a confidently-wrong address is worse than none), and Tailscale's own 100.64.0.0/10 range is filtered out of the LAN row, which otherwise duplicated the tailnet row under a label that said "only works on this network" — a lie. The Tailscale/Funnel split is stated in both surfaces rather than buried: a device that can join the tailnet needs no Funnel at all, and the only caller that really needs it is the claude.ai MCP connector, because that fetch comes from Anthropic's servers.

**A bug found on the way in:** `spawnSync("tailscale", …)` returns ENOENT on Windows even when the CLI *is* on PATH, because spawn does no PATHEXT resolution — so the first run reported "Tailscale is not installed" on a machine sitting on a tailnet. Both call sites ask for `tailscale.exe` on win32 now, guarded.

**Still open:** Funnel itself is deliberately not detected (its state is not cheaply readable, it is a separate step, and pretending to know would be worse than saying so), and the wizard does not yet print the address list at the end of a *hub* install — the Network page carries it, which covers the copy-paste case.


#### B2. ~~"Start when Windows starts"~~ — **DONE 2026-08-23 (ADR-211).**

The checkbox exists in both places Brandon asked for: the wizard asks (with `--startup=logon|always|none` for an unattended install), and the app carries the same toggle at **Build → Updates → Start with the computer**. **Logon vs always is now a real choice** rather than an accident — `--logon` maps to `/SC ONLOGON` and needs no elevation but waits for a sign-in; `--always` maps to `/SC ONSTART`, which is what a hub needs and generally wants an elevated prompt plus a stored credential. An absent or unrecognized scope resolves to `logon`, **never** to the one that demands elevation, which is guarded in both directions. `/RU` is passed only for the always-on scope.

The in-app toggle reuses the update path's signal file exactly as the section said to: the app writes `startup-requested`, the supervisor acts and records the outcome in `startup-state.json`. **A failure is a normal state here and is shown as one** — the reason plus the exact command to run in an Administrator prompt — because an owner who ticks a box and is not told it failed believes their hub survives a reboot.

The verbs exist: `npm run local:status` (supervisor alive? app actually answering? which build? registered at boot, read back from Windows rather than from our own record? — plus `--json` and an exit code an install agent can branch on), `npm run local:stop`, and `npm run local:startup` to read or change the registration later. They live in `supervisor/ledgr-ctl.mjs` because the supervisor itself boots Postgres and the app on run and so cannot answer a question without becoming the thing being asked about.

**The rig found two more bugs here.** First, `stop` reported success but left a stale lock, because a termination signal sent to another process on Windows is a hard kill Node cannot catch — the shutdown handler never ran, so Postgres was killed rather than shut down and the lock survived looking like a live owner. `stop` now asks through a `stop-requested` file that reaches the same handler a Ctrl-C reaches. Second, even *with* the handler running, every restart replayed WAL: **embedded-postgres stops the cluster with `taskkill /f /t`**, which is a kill, not a shutdown. `shutdown()` now asks `pg_ctl stop -m fast` first and keeps the forced path as a bounded fallback; verified by a stop logging `clean: true` and the next start showing zero recovery lines.

**Still open, deliberately:** writing and enabling the **launchd plist / systemd user unit** the same way win32 does its `schtasks` (both are still printed for hand-installation, and the tooling says so rather than pretending), `install.sh` for the non-Windows bootstrap, and the supervisor still not restarting *itself* when an update changes `supervisor/*.mjs` (listed in E; the new verbs are what make that fix testable). Note for whoever tests the registration on a fresh machine: **this machine's account cannot create scheduled tasks unelevated at all** — both scopes return "Access is denied" — so the honest-failure path is what got exercised end to end here, not a successful registration.


#### B3. A locally-installed AI agent can stand up a hub or a spoke

The target: the owner runs Claude Code (or similar) on the new machine and it completes the install, stopping only where a human genuinely must act. What blocks that today, all of it already queued below in E but re-framed here as one goal:

- **`/api/machine/bootstrap`** — until it exists, filling a new peer needs a production connection string or a dump file, neither of which an agent should be handling. With it, a device token is the only input. CORE, needs an ADR and Tyler.
- **MCP tools for sync devices** (`list_sync_devices` / `add_sync_device` / `revoke_sync_device`) — minting a token needs a browser Clerk session today, which is the one step an agent cannot do. Additive, solo-shippable. Keep the deliberate limit: the MCP tool mints **pull-only** devices only.
- **`--sync-mode` flag on the wizard** so an unattended install can choose the safe posture.
- **A `status` verb** (B2) so the agent can verify rather than assume.

**Name the irreducibly-human steps explicitly, so the agent knows to stop and ask rather than improvise:** (1) the Tailscale sign-in, and enabling the Funnel node attribute in the Tailscale admin console; (2) the first Clerk browser sign-in on a hub that serves a public surface; (3) the Windows elevation prompt and stored credential for run-when-logged-out; (4) pasting a device token, until B3's bootstrap + MCP work removes it. A written list of four is the acceptance test for "the install is not technical" — anything else the agent hits is a gap worth fixing.

### C. The sleeping-spoke remainder — paused devices and the return path

The **staleness refusal is built and merged** (ADR-208, PR #302), so a peer that has been away too long is now refused with a 410 and told a re-fill is required, rather than being handed a partial database and reporting itself synced. Verified live against the dev hub. Two thirds of the original problem remain:

- [x] ~~**A registered sleeping peer pins the hub's oplog indefinitely**, and **revoking to park a device is what destroys its ability to resume.**~~ **BOTH FIXED 2026-08-23 (ADR-213).** `hold_mode` per device (`auto` | `warm` | `cold`) governs **retention only**, leaving access to `revoked` and `pull_only` — separating those axes was the fix, since conflating them is what made "park it" and "let it come back" mutually exclusive. `auto` is the default and bounds the oplog at one retention window (14 days, per-device) with nobody having to remember anything; `warm` is a deliberate unbounded hold for a machine you know is coming back; `cold` releases now. **Going cold is announced rather than discovered:** at 70% of the window (a fraction, so it scales with a longer one) Synced devices names the device, says what happens if it does not reconnect, and offers both answers inline. The same bug in different clothes also closed: a token minted and never used sat at cursor 0 and pinned the *entire* oplog, so the clock is `last_seen_at` falling back to `created_at`.

  **Measured while designing it, worth keeping:** on a copy of production (10,372 items) the full row an op carries averages **8.3 KB** (p90 14 KB, max 1.9 MB), so a 14-day window is ~12 MB and an unbounded hold grows ~**25 MB/month**. That is what made Brandon's "let the owner keep something warm" affordable rather than reckless — and it says the real risk is a body-heavy spike (one 931 KB item writes a ~931 KB op *per save*), not normal typing.

  **The parked prod spoke** (data at `C:\ledgr-data-parked`, device deliberately not revoked) stops pinning production's oplog roughly two weeks after it was parked, and the Devices list will warn before it does. If it is meant to come back, set it to **keep its history** first.
- **The middle sync mode (reconcile-what-differs) is designed but not built** — full design in ADR-208, with both traps recorded: (a) "differs" must **not** become "the hub wins" (a differing row still resolves by the same per-field LWW as ordinary sync, so the reconcile payload has to carry per-field stamps or the rows have to merge through `mergeOps` exactly as ops do); (b) any row digest must **exclude generated and derived columns** (`items.search` is generated, `passage_refs` is derived), which is why the v1 digest is `(tbl, id, updated_at)`. A full re-fill stays the fallback and is fast (~4 minutes for 576k rows, measured), so the system's job is to *know* a re-fill is needed and offer it as a button, not to avoid it.

### D. Phase 5 hub duties, then phase 6 cutover

The remaining ADR-206 phases. **Local crons landed 2026-08-23 (ADR-214), so what is left here genuinely needs Brandon's machine and accounts rather than code:**

- **Expose the PC** (B1's Funnel work) and confirm a real HTTPS URL.
- **Point the accounts at it:** a claude.ai MCP connector and a Clerk phone sign-in against that URL. This was the old "probe 3" and it cannot be automated.
- ~~**Local crons.**~~ **DONE 2026-08-23 (ADR-214).** The seam already existed at the HTTP layer — Vercel cron and GitHub Actions both just `GET /api/machine/<job>` with a cron-scoped token — so what was missing was a **trigger**, not an interface. The supervisor is it: one 60s timer beside its existing signal polls, calling the same endpoints over loopback, authenticated by a cron token it **mints for itself per process** (in memory, only the sha256 appended to the app child's `LEDGR_API_TOKENS`, never on disk), so there is nothing to configure and no second auth path. Which jobs run is a written table with a double-run verdict per job, because a local peer runs alongside a cloud deployment still running all of them: **`purge` and `relatedness` default on** (the first because `pruneSyncOps` only ever prunes the oplog of the instance it runs on — the gap this item existed for; the second because `item_relatedness` is a per-instance cache, so Discover and Loose Ends stay empty until a peer fills its own), and **`export`, `calendar-sync`, `email-import`, `todoist-sync`, `transcription-poll`, `health-check` are off until asked for**, each writing somewhere shared. Schedules are `at: "HH:MM"` local or `everyMinutes` — no cron parser, since the workflows' weekday shaping exists only to save Neon compute. A failure is recorded in `cron-state.json`, retried in 10 min clamped to the next slot, and **POSTed to `/api/machine/report-error` exactly as the GitHub workflows do**, so it lands in `error_log` and counts on `/health`. Surfaced three ways: Build → Updates → "Scheduled jobs on this machine", `npm run local:status` (`--json` included), and the state file itself, written at startup so "no jobs here" never looks like "no supervisor". A job overdue by more than its period runs 3 min after boot, so a laptop asleep at 03:10 every night still purges. **Verified live on the dev rig** (spoke B; the prod spoke untouched): purge running through the minted token, export recording a real 503 with its detail and the `report-error` row in `error_log`, the retry clamped to the interval, and both rendering on the page and in `local:status`.
- **Ordered-hub failover** — which is section A.
- **Cutover (phase 6):** flip the MCP connector URL, flip the phone PWA URL, reorder the hub list so the cloud becomes the archive peer, **hand the exclusive jobs over** (turn `export` / `calendar-sync` / `email-import` on in the hub's `crons`, and stop the cloud running them — ADR-214 keeps them off by default precisely so this is a deliberate step rather than a collision), write the runbook entry. Reversible by reversing the same flips; the cloud keeps its copy either way (ADR-206 decision 4). Blobs stay on R2 through cutover (decision 8), so there is no blob work here.

### E. Smaller open items

- [ ] **What to test about the CLOUD hub as a backup, specifically (Brandon, 2026-08-23 — raised, deferred).** ADR-210's fallback path was proven with one local spoke acting as another's hub, which is the post-cutover topology in miniature and exercised every code path. What it did *not* exercise is the cloud peer in the emergency-hub seat, where the differences are environmental rather than logical: the 60s function ceiling on a drained catch-up after a long push-only stretch, whether a daily-cadence archive reliably trips ADR-208's 410 when the owner finally approves pulling from it (it should, and the prompt already shows how far behind it is), and Neon cold-start latency inflating a first exchange. None of these change the design; they are worth one deliberate rehearsal before relying on the cloud as the emergency hub after cutover.
- [ ] **Oplog size is the one large-import obstacle still unfixed.** ADR-206's wire format puts the whole changed row in `changed`, bodies included, so importing 500MB of bodies writes ~500MB of oplog on the spoke, pushes it, and grows the hub's oplog by the echo too, all held for the 14-day retention. On Neon's free tier that is worth knowing before, not after. The other two obstacles are done: the first-push hold now has a one-click GUI release (PR #305) and throughput was measured at 8.46s for a full 500-op batch (14% of the 60s ceiling), so **importing on a spoke is now a fine default** — the old "import on the hub instead" advice was conditional on those two and no longer applies, except for a genuinely huge body-heavy import where the oplog cost argues for the hub.
- [ ] **The update signal's target sha is decorative.** `/api/updates` writes the target commit into `update-requested`, and `applyUpdate` reads it only for the log message: it then does `git pull --ff-only origin <branch>` and builds whatever HEAD becomes. Benign when HEAD is that commit, wrong when origin has moved on — the owner consents to the commits the card showed and can get a newer set. Either honor it (build the target when it is an ancestor of the pulled HEAD, else refuse) or stop writing it.
- [ ] **The supervisor reads `branch` once, at start, so a peer pointed at a merged-and-deleted branch refuses every update until someone restarts it.** Hit twice in the 2026-08-23 evening session, from opposite ends: a rig spoke was moved onto a feature branch to test it, the branch was merged (and deleted on the remote), and every subsequent update signal failed with `fatal: couldn't find remote ref <branch>` — editing `branch` back to `main` in `supervisor/config.json` did nothing until the process was restarted. Keep-last-good handled it correctly (the peer kept serving its previous build and said why in the log), so this is friction rather than danger, but it is exactly the friction the mode toggle and the GUI hub list removed elsewhere: **config the app can change should not need a restart.** Two candidate fixes, either cheap: re-read the config's `branch` per update attempt (it is already re-read from disk nowhere, but nothing depends on it being stable), or fall back to `origin/HEAD` with a warning when the configured ref is gone. Pairs with the item below, since both are "the supervisor is holding stale state about itself."
- [ ] **The supervisor never restarts itself.** An update that changes `supervisor/*.mjs` flips the app but leaves the old supervisor running its old code until a manual restart, so supervisor fixes land a restart later than they look like they do. Documented in the README for now; pairs naturally with B2's `stop`/`status` verbs.
- [ ] **Resume the alongside soak.** The prod spoke is parked so its machine could host the dev rig (see C for the retention consequence). Unpark it when the rig is no longer needed: rename `C:\ledgr-data-parked` back and start the supervisor — its device was never revoked, so it can still resume, though it may now be far enough behind to hit the new 410 and need a re-fill.
- [x] ~~**The weekly backup was failing since 2026-08-14.**~~ **FIXED** — the chunked-fragment upload landed (`fix(backup): upload the weekly dump in fragments, not one PUT`) and two runs went green on 2026-08-23. The next *scheduled* run is the real proof, since both green runs were manual. The silence that hid it is a separate item: `LEDGR_ERROR_TOKEN` is still unset in Actions, so the report-error step exits 0 and `/health` showed `errors.last24h: 0` straight through two failures. **Set that secret** or the next silent failure is equally invisible.
- [x] ~~**`.env.local` can silently be production.**~~ **FIXED 2026-08-23 on this machine:** `.env.local` now points at the dev Neon branch (`ep-cold-smoke-ai4f98m1`, what devledgr serves) and `.env.production.local` holds the prod string, both verified by comparing `job_state` against each deployment's `/health`, and both pushed to the secrets store so every machine can pull them (the prod file had never been pushed before). `release:prod` can now complete from here. **The gotcha stands and is worth a guard:** `vercel env pull` overwrites `.env.local` with production values, so anyone running it re-breaks this silently. A guard in the `db:*` scripts that is honest about not being able to tell dev from prod by connection string alone is still unbuilt.

### Done in the 2026-08-23 sessions (for the record)

Bidirectional sync proven against production (LWW both directions, body conflicts to `revisions` with the flag, soft and hard delete propagating, echo terminating in one round on neon-http). Then, on a two-spoke dev rig against `devledgr` (spokes at `C:\ledgr-a` / `C:\ledgr-b`, ports 3000/5433 and 3002/5434, filled from the dev Neon branch): **ADR-208** the staleness refusal + push dedupe (PR #302, Tyler acked); **ADR-206 addendum 7** the FK family-delete wedge, which the rig caught within minutes of arming and which prod's trash purge would have hit on the first parent-with-children purge (PR #303); **ADR-209** Build → Network with a GUI hub list, a token per hub, and per-hub status (PR #304); the **"Send anyway"** one-click release for a held first push (PR #305). Guards proven live rather than only pure-tested: first-push hold (800 ops held, pull still flowing, released, drained), clock-skew hold (push held, pull flowing, drained on restore), three-node propagation with no echo growth, and a hub-cron write colliding with a spoke edit resolving per field. Throughput measured: **8.46s for a full 500-op batch** against Neon HTTP, 14% of the 60s ceiling.

## Queued — Gantt Chart tool for projects (Tyler, 2026-08-18)

A "Gantt Chart" **tool** for project records (the ADR-199/200 tool model: a widget on the widget homepage + likely a full drill-in page, same shape as the review timeline). Bars from the dates the items already carry — tasks (scheduled → due), milestones (target dates) — so it's a pure read over existing data, no new columns. Open questions when picked up: bar for a task with only one date (point vs. bar), whether dragging a bar edits the date (pairs with `explorations/calendar-time-blocking.md`), and dependencies (almost certainly out of scope for v1 of the tool; Ledgr has no dependency edge today). Not core (module/tool internals) unless a dependency edge gets proposed — that would be a relations-contract conversation.

## Queued — Project-like types as tools on projects: the open discussion (Tyler, 2026-08-18; parts 1–2 built 2026-08-19, ADR-204)

From Tyler's "Project Purchases" live-use session. **Parts 1 and 2 are DONE on branch `feat/widget-home-opt-in` (ADR-204):** the widget homepage is now an explicit **Project-style page** checkbox on the type editor (off by default for new custom types; types created under the old default are grandfathered on, unchecking is the one-click off), and the custom-tool card's "+ Add" works — `/api/records/[id]/contain` accepts any live type instead of the built-in allowlist, and the button toasts instead of failing silently. Deliberately NOT built from part 1: a pick-your-starting-tools step when the checkbox flips on — the page starts from the standard composition and Add a Tool shapes it; add the picker later if the default composition annoys in practice. **Also on the branch (ADR-204 addendum):** every tool card's hover gear has **Rename** — a per-record card title stored as `options.title` on the widget instance, honored by the card header and its drill-down page; empty restores the default.

What remains is part 3:

**Project-like types as tools on projects — OPEN QUESTION, worth a real discussion (Brandon included) before building.** Tyler's lean (2026-08-18): allow it — "it feels in the spirit of Ledgr" that a type being hub-shaped shouldn't disqualify it from ALSO being a tool on another record ("I don't see why a project can't be related to another project type and have it as a tool anyway"). Hub-ness and tool-ness look orthogonal; the current guard refuses built-in tool types only where the card would double. Things the discussion should settle: recursion/containment semantics (a hub inside a hub — what does its card preview show, and does progress roll up?), whether the home edge or a plain relation carries it, and how it meets `explorations/project-items.md` / the relatedRecords box (a Pursuit's projects), which already half-supports nesting project-shaped things.

## Queued — Custom layouts vs. bespoke default canvases: reconcile (Tyler, 2026-08-18)

Saving a custom layout for a type with a bespoke canvas silently swaps the whole experience: `TaskCanvas` (and the project widget home) defer to the generic `MarkdownCanvas` grid whenever `typeDef.canvasLayout != null`, so one visit to "Customize layout" on task throws away the new rail-panel canvas (ADR-202) — working as designed (ADR-069 regression fix), but a rough trade the user never saw coming. Needs design thought, not just code. Options to weigh when picked up: (a) warn/preview at the moment of customizing a bespoke-canvas type ("this replaces the built-in task page") + a one-click "back to the built-in layout" affordance (partially exists — clearing the layout restores it, but nothing says so); (b) make bespoke canvases themselves arrangeable (big); (c) scope customize-layout away from bespoke types entirely (defer-by-hiding). Also check what the guide says about Customize layout. Related: the "Select" toggle discussion below in COLLAB — both are "the control works but doesn't explain itself" cases.

## Queued — Drag-to-reposition cropper for the person image (Tyler, 2026-08-18)

The person picture box (ADR-202 addendum 4, `src/components/people/PersonImageBox.tsx`) currently center-crops uploads to a square automatically. The follow-up: a small crop step between choosing the file and uploading — drag to reposition (and pinch/wheel to zoom) the square window over the image, then commit. No dependency (Principle 5): render the image in a fixed square viewport, track pointer drags as an offset, export through the same canvas `drawImage` the auto-crop already uses (only the source rect changes). Pasted URLs skip the cropper (we don't proxy remote bytes); it's upload-only. Worth also applying to any future image-kind property if one ever exists.

## Potential launch (Ledgr as a product/company)

**Not the build queue.** Long-horizon track from the 2026-08-14/15 Tyler+Claude sessions; the full reasoning, options, and phase triggers live in `explorations/onboarding-and-multi-tenancy.md` (§9–§12). Two markets identified: the fused notes-graph + full-task-manager combination nobody ships (§10a), and the ministry vertical nobody serves (§10b). Model: free self-host tier + paid hosted tier (the Ghost/Cal.com shape). **Everything here is gated on the Brandon conversation** — his involvement is a spectrum (partner → advisor → key user), all fine, but Phase 1 memorializes whichever he picks.

- [ ] **§6 setup quick wins** (first sign-in claims the instance, deploy button + Neon marketplace, `/setup` page, solo-auth adapter). Not core, helps every path, start whenever. Item 1 is the best value-to-effort.
- [ ] **Friends/family beta box** — N single-tenant instances on the home server (per-person database + app container, reverse proxy). Keeps Principle 7 intact. Free; its job is measuring support burden per non-technical user.
- [ ] **The Brandon conversation** (trigger: before any charging) — operating agreement or IP-assignment/advisor agreement depending where on the spectrum he lands; written blessing for using his name/story in marketing either way.
- [ ] **Name decision — stay Ledgr (the sixth Ledger) or rename: OPEN discussion, both-agree** — the collision picture is confirmed, and worse than the Ledger worry (Tyler + Claude web pass, 2026-08-24): the exact spelling "Ledgr" is already taken several times over in software — LEDGR Money holds the name on the iOS App Store (plus Android and useledgr.com), a Ledgr sits on the Microsoft Store, ledgr.health does medical credentialing, and a Ledgr bookkeeping service exists — all on top of Ledger SAS (hardware wallets, broad software marks, phonetically identical). The App Store name alone is a hard block on the bare name. Fine as the private name indefinitely; decide the public one before printing it anywhere. First-pass candidates from the same session (quick web checks only, NOT a USPTO/domain clearance): **Bindery** (a bindery is where loose pages become one bound book — "everything is an item" in one system; the most open name checked, nearest neighbors are small "Binder" note apps with no dominant in-category mark), **Carillon** (church bells calling people together; collides with Carillon ERP, accounting software since 1994, plus literal bell-system software — risky), **Cairn** (trail marker; crowded namespace, nothing dominant in-category). Checked and DEAD, don't re-propose: Commonplace (two active notes products, one on the App Store), Tessera (Tessera Layers is a nested notes/journal app, same category), Vestry (vestry.life is church management software — the collision is in the target vertical), Steward (a Mac personal-database app plus Good Steward finance). The safest lane long-term is a coined name; whatever the shortlist becomes, clear it against USPTO TESS + the .com + both app stores before committing. Both-agree — the public name is Brandon's call as much as Tyler's. **Tyler's current frontrunner (2026-08-24): Almanack** — the archaic-k spelling is the ownable part; the neighbor to clear is Almanac (no k), the former docs startup at almanac.io. Unvetted bench in the same old-book vein, for the eventual deep dive: Kalendar (church kalendar = liturgical calendar, same archaic-spelling trick), Daybook (the merchant's capture-everything-then-sort record — Ledgr's own flow, keeps the ledger heritage without the mark), Compendium, Omnibus, Colophon, Octavo, Marginalia, Concordance (the Bible's cross-reference index = the relations graph in one word), Breviary, Vade Mecum. **Where the discussion stands (Tyler, 2026-08-24): staying Ledgr is a live option, not a rejected one — decide with Brandon whether to be the sixth Ledger.** Tyler's read: personal-finance apps and life-management/productivity are different enough lanes that Ledgr fits the coexistence pattern the other Ledg(e)rs already prove out (trademark rights are per-lane, and "ledger" is a weak, crowded mark, so everyone's protection is thin); the *real* constraints are namespace, not law — the bare name is unavailable on the iOS App Store and the good domains are gone — and a second word ("Ledgr Notes"-style) plausibly solves exactly those two. Counterpoints on record so the decision weighs them instead of re-deriving them: (1) a generic second word carries almost no weight in a confusion analysis — the dominant element is still LEDGR, phonetically LEDGER; (2) shared distinctive word + different generic suffixes is what a product *family* looks like, so "Ledgr Notes" can read as LEDGR Money's sibling — and LEDGR Money is the one holder on the same consumer app-store shelf; (3) joining a crowded weak-mark field forfeits the moat both ways: the sixth Ledger can't stop the seventh, and every marketing dollar partly promotes the other five. Middle path also on record: a distinctive brand with "ledger" demoted to the tagline ("a ledger for your whole life"), keeping the concept while vacating the contested slot. **The lens for judging collisions (Tyler, 2026-08-24): relatedness = same product lane (personal life-management/productivity software), NOT "it's also software."** A font editor, an EPUB tool, or a crypto product is another industry; flags from other industries are identity/SEO considerations, not legal walls. The two carve-outs to still check at the deep dive: truly famous marks can reach across industries (dilution — Tether is the only candidate where that's conceivable), and consumer apps on the same app-store shelf get judged somewhat more closely. Under this lens the only near-lane flags on the list are almanac.io (docs tool, defunct) and Tome.app (presentations, adjacent). Tyler's candidate list if renaming (2026-08-24) — his picks, none eliminated; the parentheticals are Claude's collision-research notes to weigh at the deep dive, not verdicts: **Almanack** (frontrunner; neighbor to clear is almanac.io, the former docs startup), **Kept**, **Tether** (Tyler likes it — short, modern, feels similar to Ledgr, and the meaning fits the home-base-with-offshoots framing; flag: Tether is the USDT stablecoin company, the same crypto lane that makes Ledger SAS a concern), **Node / Nodal** (both feel right to Tyler — items-as-nodes matches the relations graph; flags: Node.js dominates the bare word in software, Nodal Exchange is a far-lane neighbor), **Slate** (double meaning: the slate you write on + "slated" = scheduled; flags: Slate magazine, Slate Auto, Slate Digital), **Sigil** (flags: Sigil the EPUB editor; current usage of the word leans occult/chaos-magic outside the GoT house-emblem sense — weigh against the ministry vertical), **Glyph** (confirmed liked; flags: Glyphs the Mac font editor, Glyph pro drives), **Tome** (flag: Tome.app, the AI presentation startup), **Nexa** (flags: a Nexa cryptocurrency, Nexa3D). If the ledger-lane question stays contested, a trademark attorney's clearance opinion (~$500–1,500) is the tiebreaker — worth buying only when launch gets real.
- [ ] **Pick a license** — the public repo has **no LICENSE file** (= all-rights-reserved; source-visible but nobody may legally run it). The free self-host tier requires a real one (AGPL à la Cal.com, or fair-source). Both-agree.
- [ ] **LLC + ToS + privacy policy + Stripe** (trigger: first dollar). Pastoral care notes make the privacy policy non-optional.
- [ ] **Control plane** (trigger: first stranger) — signup → provision DB → migrate → seed owner → subdomain, unattended. The self-update work (3fbe738) is groundwork. Tenancy call: managed single-tenant (DB per customer) vs true multi-tenant (`types` owner-scoping + ADR-075 re-argument).
- [ ] **ADR-075 revisit for hosted strangers** — care notes are the sensitive core of the pastor use case; possibly encryption-at-rest, written up as a public trust page (privacy is marketing in this vertical).
- [ ] **Church-market seeding** — 10–20 known pastors on free accounts (testimony = launch asset); content from Brandon's real workflows; free for planters/missionaries. Sell the week ("Sunday-proof, whole ministry in one place"), never the database.

> **✅ THE CHECK ITSELF IS NOW FIXED (2026-08-12, ADR-188).** Both "worth fixing the check itself" notes below are answered: **there was no CI workflow at all** — all ten workflows are operational crons — which is why two type errors merged green and broke every production deploy from `main`. `.github/workflows/ci.yml` now runs **typecheck → lint → build → the 51 pure verify suites** on PRs *and* on pushes to `main` (the second breakage came by direct push). **No secrets**, verified not assumed: the build passes without `DATABASE_URL` or a Clerk key, because the db client is lazy and ADR-184's keyless fallback keys on `VERCEL_ENV`, which Actions doesn't set. `npm run ci` runs the same thing locally. `scripts/verify-ci.mjs` **discovers** the pure suites rather than reading an allowlist, so a new one is covered the moment it lands.
>
> **And it immediately paid for itself: six verify scripts were already failing and nobody knew, because nothing ran them.** All six were **stale tests, not code bugs** — note that, because the instinct on a red test is to change the code. `verify-songs`/`verify-papers` expected note's canvas to be `markdown` (it's `longform`, ADR-157); `verify-build-mode` expected `/dashboards/<id>` to be a Build path (it deliberately isn't — claiming it once made Build mode the only way to view an unassigned dashboard, and the test now says so); `verify-provider-seams` called four `/api/machine` routes unauthenticated (they gate on the ADR-117 `verifyApiToken` shim — I read all four to confirm they 401 before loosening the guard, then made it **per handler**, stricter than before); `verify-graph-auth` could only pass on a machine holding real `GRAPH_*` secrets; `verify-markdown-escape` used the pre-palette Notion red `#e03e3e`, which `textColorName` rejects, so the span parsed to an **empty doc**.
>
> **⚠️ TWO FOLLOW-UPS THIS DOESN'T COVER.** (1) **`CI / check` is not yet a required status check** on `main` — that's a GitHub branch-protection setting, not a file, so it can't ride in a commit. Until someone sets it, **the gate reports but does not block a merge.** (2) The **79 DB/server-backed suites still have no runner** and may hold more stale expectations of exactly the kind found above — a local `verify:all` against a scratch database is the obvious next step, and `npm run verify:list` shows the split.
>
> **📍 SETTLED, not a problem — where the repo lives and who can see it (Tyler, 2026-08-12).** Ledgr is at **`strategicli/ledgr`**, owned by a GitHub **organization**: Brandon moved it off his personal account so he and Tyler can both push and pull. It is **public on purpose** — a private org repo costs money and they chose not to pay. No secrets are in the repo (they live in environment settings), so world-readable code is the standing assumption, not an oversight. GitHub still forwards the old `brandonscollins/ledgr` address, which is where the "This repository moved" line on every push came from; the local remote now points at the real one. **Don't raise any of this as a finding again.**

> **🔴 FIXED IN PASSING AGAIN — `main` did not build, same failure mode as PR #214 (2026-08-12).** `src/app/page.tsx` carried a second named export, `export async function TodayHome()`, which `/today` imported (`import { TodayHome } from "@/app/page"`). A Next.js **page file may only export from a fixed allowlist** (`default`, `dynamic`, `metadata`, `generateMetadata`, …), so the extra export failed the generated route typecheck — and `next build` runs the typecheck, so **every Vercel deploy from `main` was failing**. Extracted to `src/app/_today-home.tsx` (the `_` prefix keeps it out of the router) as the default export; `/` and `/today` both import it from there. `tsc` 0 errors, `next build` ✓, both routes 200. **This is the second time an extra-export/type error has merged green — the pre-merge check still evidently does not run `tsc`/`next build`.** Worth fixing the check itself, not just the third occurrence.

> **▶️ START HERE (2026-08-13) — everything below through ADR-190 is MERGED AND DEPLOYED.** `fix/punch-list-2026-08-12` landed on `main` in `ce2218c` on 2026-08-12, and `main` has since moved to `4446ac9` with Brandon's three PRs of 2026-08-13 (#251 editor toolbar tooltips name their shortcuts, #252 the PWA share-target cold-share fix / ADR-191, #253 inline HTML spans surviving an ordered-list round-trip). **Both instances build Production straight from `main`**, so all of it is live on Brandon's deploy and Tyler's; `origin/prod-brandon` and `origin/main` are the same commit, and `origin/prod-tyler` has been deleted. CI on `main` is green.
>
> **⏭️ THE OPEN WORK IS FOUR DECISIONS AND ONE UNVERIFIED-ON-HARDWARE ITEM, not a build queue:**
> 1. **Make `CI / check` a required status check** on `main` (GitHub branch protection — a repo setting, not a file, so it can't ride in a commit). Until someone sets it the gate reports but does not block, and two type errors have already merged green and broken every production deploy.
> 2. **PR #140, the deploy-workflow proposal** (`main` = integration + per-instance prod branches), open since 2026-07-01 and sharper now that both instances deploy Production from `main`: any merge reaches Brandon's live data within a minute.
> 3. **`tyler/user-defined-api-keys`** — 2 commits from 2026-06-15 (ADR-067 per-type-scoped API keys, Build → API page, +2,552 lines) carrying migration `0018` that never landed anywhere. Revive or delete the branch.
> 4. **`purge-throwaway-owners.mjs --apply`** — see the 🧹 item below; still not run, still your call because it hard-deletes.
>
> **The three punch-list items still not clicked through on real hardware** (no browser automation in those sessions; they're covered by pure/structural guards, the served CSS, and live-DB calls, which is not the same as eyes on it). Now that they're deployed, each is worth 60 seconds on the live app: type `@` + a new name in a body and confirm the three create rows; open the `@` picker with the ⋯ kebab already open and confirm the kebab closes; add a task in a project's Tasks card and confirm the row stops saying "Adding…" almost immediately.

> **✅ DONE (2026-08-13, ADR-192, CORE additive) — `phone` and `email` property kinds.** The backlog item below is built, and **its premise was half wrong in a way worth keeping**: `CustomProperties.tsx` already rendered `tel:`/`mailto:` links and the right keyboard, but it guessed the intent from the field's **key** (`/phone|mobile|\btel\b|cell/`), so `mobile_2` got a dialer and `contact_line` did not, for no reason the owner could see — and only on the record canvas, never in a list. So this replaced a guess with a declaration rather than adding a missing feature.
>
> - **Storage is untouched:** both kinds are `text`, the value is stored **exactly as typed**, and nothing validates or reformats it. No migration, no backfill. An existing `text` field keeps behaving as it did until someone changes its kind.
> - **The non-obvious bug, found by writing the doc comment before the code:** stripping non-digits turns `(309) 555-0142 x12` into `tel:309555014212`, and the extension digits read as more of the number, so it **misdials**. `telHref` now cuts anything past a `,`/`;` and a trailing `x|ext|extension` first. The extension stays on screen to be entered after the call connects. DTMF `,,12` was rejected (pause support varies by dialer; a wrong pause misdials silently too).
> - **The legacy key heuristic stays, scoped to `text`,** because production data — the Notion import, Tyler's people — is full of contact fields typed `text`, and retyping them all is not a cost this change gets to impose. A declared kind always wins.
> - **List linking is the table layout only:** the compact row wraps the whole row in a `Link` to the item, so an anchor inside it is invalid HTML *and* would steal the row tap. `ViewRenderer` took an optional `propertyKinds` map (same shape and resolution sites as `propertyLabels`, defaulted `{}` so unadopted callers render identically).
> - **Filtering is text filtering** over the raw value, so a stored `(309) 555-0142` does not match a `3095550142` search. Making it match needs a normalized shadow value nothing writes — noted, not built.
> - `tsc` + `eslint` + `next build` clean. New `scripts/verify-contact-links.mts` (**66 checks, ALL PASS**, pure, so `verify-ci.mjs` discovered it with no allowlist edit — 55 pure suites now). `verify-types.mts` updated for the count and green against the dev DB.
> - **✅ CONFIRMED WORKING (Tyler, 2026-08-13, on the deployed instance).** The `person` type carries `phone`/`email` kinds in production (read back over MCP, so the type editor → Postgres → read path is proven end to end), and **both links were clicked on desktop and worked**. Also verified against the real stored values through the shipped helper: `tel:6073495980`, `mailto:tyler@bethanycentral.org`, with the `tel`/`email` keyboards. **Two things that value can't exercise, noted rather than claimed:** it's stored as bare digits, so neither the punctuation-stripping nor the extension cut was actually hit — set a field to `(607) 349-5980 x12` if you ever want to see the misdial guard work — and a desktop click is not a phone dialer.
> - **Found while verifying: the `organization` type's `phone`/`email` are still kind `text`.** They work anyway, via the legacy key heuristic (the back-compat path, confirmed working), but only because those keys happen to be named `phone` and `email`. Two dropdown changes at `/build/types/organization/edit` make it declared instead of lucky. The case that silently *doesn't* work is a contact field named something less obvious.
> - **No ack owed (ADR-269).** It extends `schema.md`'s property-kind list and is additive-only; the other instance behaves identically until a field declares one of the new kinds.
> - **Three of the sites the backlog note named needed no change, worth recording so nobody hunts for them:** `canvas/FieldStrip.tsx` and `lib/canvas-fields.ts` handle the built-in `items.url` **column** (a link item's address), not the property kind; `lib/item-input.ts` likewise — and properties have **no per-kind write coercion at all**, which is exactly the "store verbatim" behavior the note asked for; `lib/item-tokens.ts` stringifies props kind-agnostically, and a phone number in a `{{item.props.*}}` token should be text, not a link. The real blast radius was the builder pair, `CustomProperties`, `view-where`, and the `ViewRenderer` + four resolution sites.
>
> **Landed:** the `main` build-breaker (an extra named export in `app/page.tsx` — every Vercel deploy from main was failing); the sticky formatting bar (react-grid-layout's `cssTransforms` re-based the sticky containing block; notes were the only type on the grid path); `/items/[id]/markdown` + copy, on every type's ⋯ menu; `settings.itemOpenMode` (auto/left/right/center); tag chips on task rows + group-a-view-by-tag with fan-out; `#` = tags and `+` = project.
>
> **Two resolved by Tyler, not by code:** tags on notes were invisible because the note type's saved `canvas_layout` had `"rel:tags": {hidden: true}` (⋯ → Customize layout → hidden-cards tray restores it); tags on songs and a categories type are user-configurable through the existing relation-field mechanism.
>
> **✅ DONE (2026-08-12, ADR-185) — the `@` create-on-miss picker.** A bare `@Jake Tourtillott` with no match now offers **one typed create row per type it could be** — person, project, then "Unsorted" as the escape hatch — each wearing that type's glyph and naming the type on its right edge. A scoped `@/person Jane` still yields exactly one row (unchanged). Done as **one shared module** (`src/lib/mention-create.ts`) across **five** surfaces, not the three the note named: the editor picker, the capture title, the task card, **plus** the generic "+ Relate" box (`AddRelation.tsx`) and a null-`targetType` relation field (`RelationField.tsx`), which had the same bug in their untyped path. It also settled a pre-existing split the consolidation exposed — the three `@` pickers sent `inbox: true` even for a scoped `@/person Jane` while `RelationField` sent `inbox: !targetType`; now only the catch-all needs triage, in one flippable expression (`needsTriage`). `unmarked` is **not** retired: still the escape hatch, still where quick-capture files. 31 checks in `verify-mention-create.mts` (including a structural guard that a sixth site can't drift back to hardcoding `"unmarked"`), plus end-to-end against the live dev DB. **Not clicked through in a real browser** — no browser automation in that session. The stray "Jake Tourtillott" `unmarked` item can still be retyped from the Inbox.
>
> **✅ DONE (2026-08-12, ADR-186) — the popover collision.** Both of the note's guesses were right and they were **separate** bugs. (1) A **z-index tie across stacking contexts**: the kebab is `absolute z-50` in the page flow, the editor's mention popup was `z-index: 50` on `<body>` — same number, different contexts, so the winner was DOM-order accident, which is what the bleed-through was. Fixed by naming the five tiers the app already used as tokens in `globals.css` (`--z-sticky/chrome/modal/float/top`) and moving both mention popups to the float tier where every other body-portaled typeahead lives. **The rule: a panel portaled to `<body>` belongs at `--z-float` or above, never `--z-modal`.** (2) **Nothing coordinated the two panels** — and the reason is worth keeping: each has its own outside-click listener, but **an outside-click listener never fires for a panel that opens from a keystroke**, which is how `@` opens. `src/lib/floating.ts` is a window-event broadcast (no dependency); all seven top-level floating surfaces announce, the kebab and `Popover` also listen. Sub-popovers inside a menu deliberately don't announce, so "Save as template" still opens inside the kebab. **Found in passing:** `.ledgr-mention-popup` was `position: absolute` but placed from `getBoundingClientRect()` — viewport coords read against the document, so on a scrolled page it landed off by the scroll offset. Now `fixed`. 31 checks in `verify-layering.mts`; the tokens and the rule confirmed in the **served** CSS.
>
> **✅ DONE (2026-08-12, ADR-187) — the two "slight pause" items.** The note was right that both paths were already optimistic, and that's exactly what hid the bug: **the optimistic state stopped half way.** On add, the provisional row kept announcing itself unfinished (`opacity-70`, "Adding…") for the whole ~900ms refresh window when the task had existed since the POST resolved at ~150ms — so `persist()` now returns the real id and the row settles the moment it lands, dropping the muted look and picking up a **real** check circle bound to the real id. On complete, the circle filled instantly but the title's strikethrough read the **server** `statusCategory`, so the completion looked half-applied until the refetch — `TaskCheckCircle` now mirrors its optimistic state outward (`onOptimisticChange`, fired on click *and* on the failure revert) and `TasksWidget` applies it over the server prop. **The 500ms debounce was deliberately NOT shortened** — it exists so a triage burst costs one refetch, and a mid-burst refresh can reorder rows under the pointer; the problem was never that the refetch was late, it was that the UI kept looking pending while it waited. **Deliberately not done:** optimistically *removing* a completed row. `TasksWidget` is the only list rendering this circle and it keeps done tasks (styled `line-through`); whether a completed row should leave is a per-list filter question the shared circle can't answer, so it isn't guessed at. 19 checks in `verify-optimistic-tasks.mts`.
>
> **🧹 TOOL READY, NOT RUN — the throwaway verify owners.** Confirmed **13** of them in the dev DB (`verify-project-*` and `verify-mcp-records-*` `@example.invalid`), owning 23 items, 11 relations and 8 activity events — at most 74 rows. `scripts/purge-throwaway-owners.mjs` removes them, **dry-run by default**; it prints the blast radius and changes nothing until you pass `--apply`. **I did not run `--apply`** — it hard-deletes rather than soft-deleting (this is test residue, not owner content, so Trash isn't the right home), which makes it the one place in the app that deletes outright, so it's your call:
> ```
> node --env-file-if-exists=.env.local scripts/purge-throwaway-owners.mjs          # show
> node --env-file-if-exists=.env.local scripts/purge-throwaway-owners.mjs --apply  # delete
> ```
> It discovers owner-scoped tables and item FKs from the catalog rather than hardcoding a list that would rot, and deletes in FK order (nothing here declares `ON DELETE CASCADE`, so a naive `DELETE FROM users` just fails). Safety: `.invalid` is a reserved TLD (RFC 2606) that can never be a real address, and the pattern also requires the `verify-` prefix, so a real owner cannot match. **Root cause not fixed:** the DB-backed verify scripts create a scratch owner per run and never clean up, so this will refill. The better fix is each script deleting its own owner in a `finally` — worth doing when the 79 DB-backed suites get a runner (see the CI item above).

> **🆕 BACKLOG — the peek/full/arrange window round (Tyler, 2026-08-17; deliberately parked, do NOT fix piecemeal).** Tyler is planning a dedicated round on the item windows — the peek panel, the full-screen view, and the arrange (Customize layout) screen — because "there are a lot of changes that need to be made there." First confirmed bug for that round, with Tyler's own repro: **resizing a link type's URL field to about half width in arrange mode made the URL field disappear entirely from BOTH the peek panel and the full-screen view** — it looked like links had no place to enter a URL at all. Going back into arrange and stretching the field to full width brought it back on both surfaces. So a mid-width card is being dropped from render somewhere in the canvas-layout pipeline (`src/lib/canvas-layout.ts` stores `lg`/`md`/`sm` arrangements; `src/components/canvas/ItemRglInner.tsx` renders them — likely the resized `lg` card never propagated to / got clamped out of the `md`/`sm` layouts, but the cause is untraced, this is just where to look). Related standing item: punch-list item 2 below (Customize-layout-in-a-peek discoverability) belongs to the same round.

> **▶️ PUNCH LIST 2026-08-13/14 (Tyler) — 5 items, causes traced, 4 of 5 SPECIFIED by Tyler. NONE BUILT YET. This is the next build queue.** Each item below carries the cause, the file, and Tyler's decision where he's given one. Two findings changed what the work is, so read 2 and 5 before touching them.
>
> 1. **The project completion bar disappears in a PEEK.** ✅ Specified: Tyler clarified it's the **item peek panel** (`itemOpenMode`), not the nav rail — *"when I open a peek window, the progress bar isn't showing up."* Cause: the track is a hardcoded `bg-neutral-800` at `src/components/projects/ProjectCardGrid.tsx:55` with only the `bg-[var(--accent)]` fill drawn over it, so on a peek surface in the same neutral family the unfilled part vanishes and a 0% project looks bar-less. Fix per ADR-141: a semantic token (`bg-surface-2`/`bg-surface-3`) picked to hold contrast against the peek surface, not a raw gray. Small.
> 2. **"Customize layout" in a peek — 🔎 IT ALREADY DOES WHAT TYLER ASKED FOR; THIS IS A DISCOVERABILITY BUG, NOT A BUILD.** Tyler's ask was *"make two versions, one for the peek and one for the full page… if we use the peek more than the full page we'd never be able to use the customizable layout."* **Those two versions already exist.** `src/lib/canvas-layout.ts:44-58` stores **three** arrangements — `lg`/`md`/`sm` — and the comment maps them explicitly: full-page expand → `lg` (Desktop), **the modal/peek panel (~768px) → `md` (Tablet)**, a phone → `sm`. And `src/components/canvas/ItemRglInner.tsx:519-521` already renders a **Desktop / Tablet / Phone** switcher inside arrange mode, where choosing Tablet clamps the canvas to `maxWidth: 700` — *"≈ the modal's content width, so Tablet is WYSIWYG"* (line 589). So the peek's layout is editable today: Customize layout → **Tablet** → drag. What's actually broken: the menu item is a plain `<a href="/items/<id>?arrange=1">` (`ItemActionsMenu.tsx:227`, hard nav *"so ?arrange=1 escapes the intercept modal"*) that silently teleports you to the full page at Desktop width with no explanation, so it reads as "doesn't work here." **Recommended (needs Tyler's ok): (a) VERIFY in the app that a Tablet-mode edit actually round-trips to the peek — if it doesn't, that's the real bug and fix that instead; (b) make the peek's menu item deep-link to arrange pre-set to Tablet (the width is local state defaulting to `lg`, so this means a URL param); (c) label it so it says it opens full page.** No schema change, so **not core** — Brandon not needed. This is the ADR-189 pattern again: a feature that exists but is unreachable.
> 3. **Task tags go AFTER the title.** ✅ Decided by Tyler: *"tags should always be to the right of the task itself"* — note **always**, so every surface, not just `/tasks`. The previous session predicted this exact reversal and left the hatch: the comment at `src/app/tasks/page.tsx:74-78` says *"moving this one line below the Link is the whole change."* Do that, make the same move in `ViewRenderer`'s row layout so the two agree, and **delete the now-obsolete comment** rather than leaving it contradicting the code. One-liner.
> 4. **Clickable, changeable dates on task rows in the tab views.** ✅ Specified: Tyler wants the date clickable *"in the tabs, like say the 'Today' tab on tasks and such… click the date and change it quickly in context."* The write path already exists (`RowMenu` offers **Schedule** on any `canComplete` row), so this is affordance work, not plumbing. **The one thing still needing an explicit call: ADR-142 says row actions live in `RowMenu` and warns off always-visible per-row controls.** Tyler has effectively asked for the exception; record it as a deliberate carve-out (the date is already drawn, so this makes existing text interactive rather than adding a control) rather than letting it erode the rule silently. **Build it as ONE shared component** (`TaskDateButton`) used by `src/app/tasks/page.tsx`, both `ViewRenderer` layouts (row + table), and the planner rail — five bespoke copies is how this rots. The real feature of the five.
> 5. **Clicking off a task peek opens whatever was underneath** (*"I click to open a task, then click away, and the 'Along the Way' project opens up afterward"*). **Cause fully traced — deliberate behavior producing a surprising result, NOT an event leak.** The peek is non-modal by design (no backdrop, so the list stays live underneath), so `src/components/canvas/Modal.tsx:337-355` listens on the document and decides for itself what counts as "dismiss me." It **refuses to close** when the click lands on `a, button, input, textarea, select, label, summary, [role=button], [role=menuitem], [data-peek-row]` — intent: *"a row still re-navigates the peek to that item."* **The specific trap: `src/components/projects/ProjectCardGrid.tsx:34-75` wraps the ENTIRE project card in one `<Link>`** — padding, progress bar, whitespace, all of it is an anchor. So a click on what reads as background hits an `<a>`, the handler declines to dismiss *and* declines to swallow, and the link navigates. "Along the Way" was just the card under the cursor. Note project cards aren't even `[data-peek-row]` (only `/notes`, `/links`, `/list/[type]` carry that), so the intentional row-hopping feature isn't what caught him. **⏭️ AWAITING TYLER'S PICK — he asked to discuss and hasn't chosen:** **(A)** any outside click dismisses and swallows, `[data-peek-row]` the lone exception so triage row-hopping survives — costs two clicks to act on anything underneath, nav included; **(B)** = A but clicks inside the nav still pass through in one click (**recommended**); **(C)** just narrow the bail-out to `[data-peek-row]` so buttons/fields underneath still work — smallest, but leaves the "large link reads as background" class of surprise partly alive. Adding a backdrop would fix it trivially and was **rejected**: it would kill the live-list-underneath design intent.
>

> **✅ DONE (2026-08-14, Tyler) — 56 new nav icons (62 → 118) and the task-canvas subtask/rail cleanup. NOT CLICKED THROUGH IN A BROWSER.** Two unrelated asks in one session, both merged as one change. `tsc`, `eslint`, and the grouping invariant are clean; nothing here is core, so no ADR and no `COLLAB.md` entry.
>
> **The icons (`src/lib/nav-icons.ts`, 118 keys, 4 new picker groups).** The trigger was a new `Organization` type with no glyph, and the ask was explicitly *"so we don't have to make the icons one by one."* Added in two passes: everyday subjects (organization, church, place, briefcase, money, receipt, chart, cart, package, email, phone, chat, megaphone, quote, video, camera, mic, laptop, code, database, car, plane, gift, utensils, dumbbell, leaf, paw, cross, check, checklist, key, lock, shield, lightbulb, pin, paperclip, alert, info, sparkle), then the gaps that left (contract, invoice, store, door, floorplan, gear, repeat, sun, cloud, plant, pill, headphones, broadcast, playlist, family, open-book, scripture). New groups: **Places & Business, Communication, Media & Tech, Life**; `key`/`lock`/`shield`/`lightbulb`/`pin`/`paperclip`/`gear`/`repeat` went into Tools, the status marks into Misc.
> - **The verification method is the reusable part, worth stealing next time.** Hand-rolled SVG path strings are unreviewable as text, so each pass was rendered to a PNG grid and actually looked at, using the **Playwright chromium already cached at `~/Library/Caches/ms-playwright/`** driven as `chrome-headless-shell --headless --screenshot`. No new dependency (Principle 5), and it caught four glyphs that were fine as code and wrong as pictures: `car` read as a bus, `pin` as a blob, `quote` as the letters "GG", `utensils` as a tuning fork. All four were redrawn and re-rendered.
> - **`handshake` was attempted and DROPPED, deliberately.** Four versions, none legible as two clasped hands at 24px in a 1.8 stroke (they read as a squiggle or a mushroom). `contract` and `store` cover the vendor/partner sense. **The reason is recorded in a comment in the file** so nobody re-attempts it blind; if someone wants it, it probably needs a filled glyph, not a stroke one.
> - **The grouping invariant is checked, not assumed:** every key in `NAV_ICONS` appears in exactly one `NAV_ICON_GROUPS` entry (118 grouped, 0 ungrouped, 0 duplicated). **There is no test enforcing this**, only the comment above the constant, so a future addition can silently fall out of the picker. A tiny pure verify script would close it.
>
> **The task canvas (`TaskCanvas.tsx` + 6 files), against a Todoist reference Tyler supplied.** Five fixes, every one scoped behind a `bare` prop rather than changed globally, because all of these components are shared with the stacked default canvas where the current treatment is right:
> - **Rail 340px → 280px.** It carries short rows; the extra 60px only stole reading width from the title/body/subtask pane.
> - **Rail property labels now stack ABOVE their values** (`CustomProperties`, `RelationProperties`, `PeopleRow`, which needed a new `bare` prop). They held a fixed `w-32` (128px) label column, which on a 280px rail squeezed every value into a truncated sliver. **The wide canvas keeps the side-by-side column.**
> - **"Add subtask" brightened** to `text-ink` (was `text-neutral-600`, nearly invisible). "Add existing" went to `text-ink-muted` so the pair reads primary/secondary instead of one bright and one lost.
> - **The "SUBTASKS" header and the over-indentation were the same bug.** Both came from the `CanvasSection` wrapper, which also applies `px-2 sm:px-8 md:px-12` reading-column padding, which is what pushed subtasks far right inside an already-narrowed pane. A `bare` prop on `Subtasks` drops the whole frame; nested subtasks keep a gentler step (`ml-2 pl-2.5`, was `ml-4 pl-3`).
> - **The "two thin white lines" were two different borders**, which is why one fix wouldn't have done it: the section's `border-top` (gone with the header) and the **editor wrapper's `border-b`** at `MarkdownEditor.tsx`, now gated on the existing `compact` flag.
> - **⚠️ THAT LAST ONE HAS A WIDER BLAST RADIUS THAN THE TASK CANVAS.** `compact` is also set by `WidgetCanvas.tsx` and `dashboards/EmbedWidget.tsx`, so both lose their bottom rule too. Judged an improvement (all three are inline editors sitting above other content, not full-height documents), but it was **not** what Tyler asked for and he has not seen it. Revert to a dedicated prop threaded `ItemEditor → BodyEditor → MarkdownEditor` if either surface looks wrong.
> - **The "n of m done" rollup moved,** it lived on the header that got removed. Rather than lose it, it now rides the right edge of the add row. Drop it if that reads as clutter.
>
> **⏭️ FOLLOW-UPS, none started:** (1) **the event canvas shares `CanvasTwoPane`** and still has the wide rail and the `w-32` label column, so it now looks different from tasks for no reason a user could explain, the same class of inconsistency ADR-141 exists to prevent; (2) a verify script for the icon-grouping invariant; (3) `handshake` as a filled glyph if it's ever actually wanted. **And the honest caveat: the canvas work is code-verified only.** `tsc` and `eslint` pass and the reasoning traces to specific lines, but no browser touched it, and layout is exactly the category where that isn't proof. Sixty seconds on a real task with subtasks and a couple of relation fields would settle it.

> **✅ DONE (2026-08-14, ADR-194) — instances can update THEMSELVES: Build → Updates.** Built for Michelle (GitHub `michellecollins1212-rosanne`, account created 2026-08-14) but it answers Tyler's question too, and the two are **not the same question**. New page `/build/updates` reports two independent axes: **code** (are you running the latest Ledgr) and **schema** (has your database caught up with the code you're running). A satellite instance — one deploying from a **fork** — gets a list of what changed and an **Update now** button that calls GitHub's `merge-upstream` on its own fork, then polls until the rebuild takes over. A **source** instance (Tyler's, Brandon's) is told plainly it already receives every push, so no dead button. Satellite-ness is derived from Vercel's own git env vars, so **a satellite needs no configuration to be identified**.
>
> - **The schema axis is the one that matters for Tyler.** His Vercel deploys from `strategicli/ledgr` (confirmed: `prod-tyler` does **not** exist on origin, so main is his production branch), so his code is never behind. His **database** silently can be. Proved live mid-session: `origin/main` moved **4 commits** ahead (PRs #256 offline page, #257 export fix) while this was being built. Those carried **no migrations**, so nothing was owed, but that is luck, not a guarantee. The journal is a **static import**, so it ships in the bundle and the check needs no network.
> - **`LEDGR_SELF_UPDATE` gates the button and fails closed:** `off` (default) status-only, `safe` allows schema-free updates, `on` allows any — and **`on` is only correct with Build Command `npm run build:satellite`** (migrate, then build), where a bad migration fails the build and Vercel keeps the last working deploy live. The verify script **caught this gate failing open** before it shipped.
> - **Two ops scripts for what the button can't cover:** `npm run instance:new -- <name>` collapses the four provisioning steps (migrate → seed → owner row → verify) whose omission produces the ADR-184 "Signed in, but not recognized" screen; `npm run instances:sync` migrates each DB **first** then syncs each fork, refusing to sync one whose migration failed. Roster in gitignored `instances.local.json`.
> - **Verified:** 36 pure checks (`verify-updates.mts`, now in CI — 56 pure suites), live schema check green against dev (50/50), `verify:ci` all green, tsc/eslint/build clean. **NOT verified end to end: no fork exists yet**, so `merge-upstream`, the deploy poll, and the whole satellite code path have never run for real. No browser touched the page. Runbook §1k has the setup order.
> - **⏭️ NEXT, in order:** (1) Michelle forks `strategicli/ledgr` from her own account (a fork lands in whoever clicks it — Tyler **cannot** do it for her, and cannot invite her to the shared repo either: he is an org `member` with `push`, not `admin`); (2) her Vercel connects to **her fork**; (3) `npm run instance:new -- michelle`; (4) set her env vars, **her own Clerk app**; (5) she signs in; (6) then set `LEDGR_SELF_UPDATE` + `build:satellite` and prove the button on a real update. Also still open from 2026-08-13: **delete the pointless `prod-miles` branch** (it still exists on origin).

> **🆕 MILES'S OWN INSTANCE — decided 2026-08-13, NOT SET UP YET (Tyler + Miles Collins, GitHub `milesharrisoncollins-sketch`, account created 2026-08-07).** A third single-tenant deploy alongside Brandon's and Tyler's — the sanctioned pattern under Principle 7 (multi-user-**ready**, not multi-user). **Decision: FORK, not a branch or a collaborator invite.** Tyler already has Miles set up with Vercel and Neon.
>
> - **🆕 2026-08-17 (Tyler): Miles gets the READ-ONLY update posture.** Same install as Michelle's except the self-update write path is left off: `LEDGR_SELF_UPDATE` stays **unset** (Updates page is status-only, no button), and his `GITHUB_TOKEN`, if set at all, is Contents: **read-only** (Changelog + update status still work; only the Update button needs write). Updates reach him via `npm run instances:sync -- --only miles` (Miles adds Tyler as a collaborator on HIS fork) or GitHub's own "Sync fork" button, which `build:satellite` makes safe. The full step-by-step is `~/Downloads/ledgr-setup-for-miles.md`. As of this date his fork still does not exist (verified via the API); his GitHub account does.
>
> - **Why fork:** Tyler's account has `push` but **NOT `admin`** on `strategicli/ledgr` (verified via the API), so *he cannot add a collaborator* — only an org owner (Brandon) can. The repo is public, so Miles can fork it himself with nobody's permission, connect his own Vercel to his fork, and can't accidentally push to shared `main`.
> - **`prod-miles` branch exists on origin and is now POINTLESS** — created before the fork decision, nothing deploys from it. **Delete it** (Tyler was asked and hadn't answered before the session ended); it's clutter Brandon will also see.
> - **Setup order matters — two steps must happen BEFORE Miles ever loads the app:** (1) Miles forks from his own account (a fork lands in whoever initiates it, so Tyler can't do it for him); (2) his Vercel connects to **his fork**, not `strategicli/ledgr`; (3) **`npm run db:migrate` against his Neon URL before the first deploy is live** — empty DB + deployed code is a broken instance; (4) **seed his owner row**, or he gets the ADR-184 "Signed in, but not recognized" screen with no nav and it reads as a bug: `SEED_OWNER_EMAIL=<his email> DATABASE_URL=<his neon url> node scripts/seed.mjs`; (5) then he signs in.
> - **Env to boot:** `DATABASE_URL`, `NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY`, `CLERK_SECRET_KEY`, `NEXT_PUBLIC_CLERK_SIGN_IN_URL`. **His own Clerk app** — keys are per-app, sharing Tyler's would put him in Tyler's user pool. Everything else in `.env.example` is optional and degrades quietly; **no R2 means attachments/uploads silently don't work**, which is the one he'll notice. Warn him: since ADR-184 a missing Clerk key on a *deployed* instance is a hard **503**, not a partial app.
> - **The propagation goal ("I push, he gets it") has one real trap: CODE propagates, MIGRATIONS DO NOT.** There's no migrate-on-deploy; the rule is migrate-before-deploy against a specific `DATABASE_URL`. Syncing his fork pushes code, which triggers his Vercel deploy immediately — so **on any change carrying a migration, migrate his DB FIRST, then sync his fork.** Wrong order = his instance serves code his schema can't support.
> - **Two fork constraints to respect when automating:** GitHub disables Actions (especially `schedule`) in forks by default, and **any local commit on his `main` — including a sync-workflow file — breaks fast-forward and turns every sync into a merge**, which is how a fork quietly drifts. **Recommendation: start with the one-click sync** (`gh repo sync milesharrisoncollins-sketch/ledgr --source strategicli/ledgr`), prove his instance works, *then* automate — building automation before the instance exists means debugging two things at once. **The automation shape when wanted:** a workflow that fetches upstream, runs `db:migrate` against his `DATABASE_URL` (a secret in his fork), and **only advances his `main` if the migration succeeded**, which enforces migrate-before-deploy instead of merely documenting it.
> - **Related, now harder to keep deferring: PR #140** (the deploy-workflow proposal — `main` as integration + per-instance prod branches) is exactly this problem, open since 2026-07-01, and a third instance raises the stakes.
> - **⚠️ PARTLY SUPERSEDED 2026-08-14 by ADR-194** (the ✅ entry above). The **fork decision stands** and so does the setup order. What changed is the automation recommendation: the "start with one-click sync, automate later, and don't put a workflow in his fork" advice was right about forks and is now moot, because the automation lives **in the app** instead of in his repo — `/build/updates` lets him take an update himself, and `npm run instances:sync` covers the migration-carrying case from Tyler's side. The migrate-before-sync trap this note identified is now **enforced by the tooling** rather than documented: `instances:sync` refuses to sync a fork whose migration failed, and `LEDGR_SELF_UPDATE=safe` refuses a schema-carrying update in the UI.
>
> **🆕 POLISH — the contact link affordance needs tweaking (Tyler, 2026-08-13; deferred on purpose, detail not yet captured).** Both the phone and email links work on desktop (ADR-192, confirmed above), but Tyler's read is that **"the link button needs some tweaking."** **The specific complaint is not recorded** — this note exists so the observation survives, not because the fix is specified, so don't guess at it: ask before changing anything. Likely candidates, purely as a starting list: the link has no visible affordance until hover (it inherits the cell/row color and only gains `hover:underline`), there's no icon or glyph marking it as callable/mailable the way other actions get one, the whole value is the hit target rather than a deliberate button, and on the record canvas the filled row shows a small icon whose meaning may not read. **Scope note:** if the fix is an icon, it's SVG in the house style, never emoji, and the token layer (`text-ink-*`, not raw grays) — see the standing rules in CLAUDE.md. Two surfaces would need to stay in step: `CustomProperties.tsx` (record canvas) and `ViewRenderer.tsx`'s table cell.

> **✅ BUILT 2026-08-13 (ADR-192) — kept for the record; the ✅ entry above has what actually happened, including where this note's premise was wrong.** ~~🆕 BACKLOG — `phone` and `email` property kinds, so a person's number is tappable on a phone (Tyler, 2026-08-12).~~ Today a phone number or an address on a person goes in a `text` field and renders as dead text: on mobile you read it, then retype it into the dialer. Two new scalar kinds alongside `url` would render as `tel:` / `mailto:` links, which is the whole feature — the OS does the rest (tap to call, tap to mail), and desktop gets a click-to-mail for free. **Why it's a small change with a wide blast radius:** a scalar kind is one entry in `PROPERTY_KINDS` (`src/lib/types.ts:46`) plus every place that switches on a kind — the builder (`build/CustomProperties.tsx`, `TypeBuilder.tsx`, `StructureBuilder.tsx`), the canvas field renderers (`canvas/FieldStrip.tsx`, `lib/canvas-fields.ts`), list/view rendering + filtering (`views/ViewRenderer.tsx`, `lib/views.ts`, `lib/view-where.ts`), input coercion (`lib/item-input.ts`), and `lib/item-tokens.ts` for `{{item.*}}`. `url` is the working reference to copy at each stop. **Validation is the one real decision:** phone formats are a swamp, so store what the user typed verbatim and only normalize for the `href` (strip everything but digits and a leading `+`) rather than rejecting input — a field that refuses a valid number is worse than one that renders an odd one. Email can lean on the same light touch (`type="email"`-ish, warn not block). Touches `schema.md`'s property-kind list, so it's **core** (both-agree + ADR).

> **✅ LONG DONE — the PJ chunk shipped (PR #139, 2026-07-01) and every step below was completed; kept only as the record of how the migration collision was linearized.** ~~⏭️ NEXT (2026-07-01) — DEPLOY THE PJ CHUNK:~~ `integrate-pj` is committed and **merged with `origin/main`** (which had advanced to ADR-137 / PRs→#138: AI Memory, calendar lenses, edit guard, etc.). Migrations linearized past the `0040` collision → `0040_memory_type` then `0041_solid_leech`…`0045_pursuit_type` (whens rewritten so prod won't skip them). tsc + `next build` + eslint clean; verify scripts green on the merged tree. **Remaining, in order (needs prod creds — Tyler):** (1) `npm run db:migrate` against **prod** `DATABASE_URL` (applies the 5 new migrations after `memory_type`) — must precede code going live; (2) push `main` (or PR → merge) → Vercel deploys; (3) verify `/health` + a project page. **Dev-DB gotcha:** `ledgr_dev` already ran the old-numbered branch migrations, so `db:migrate` there will collide — reset dev or fix `__drizzle_migrations` (prod is unaffected, it never had them). Project Type ADR renumbered 133→**138** (origin took 133 for the Planner).

> **🟠 NEW-USER DEAD END — a signed-in user with no `users` row gets a nav-less scaffold (Tyler, 2026-08-06).** Found standing up Tyler's Vercel instance against a fresh database. Two distinct problems, worth separating:
>
> 1. **~~The real bug: a null owner is a dead end.~~ ✅ FIXED 2026-08-11 (ADR-184)** — and it bit again first, costing three redeploys to diagnose because the only symptom was "my user menu disappeared." `resolveOwner()` still never creates a row, but the states are no longer collapsed: `resolveOwnerState()` distinguishes `signed-out` from `unrecognized` and logs a warn (`auth.owner`) naming the identity, and `src/app/page.tsx` renders an explicit "Signed in, but not recognized — this Ledgr has no account for X" instead of the anonymous scaffold. **No redirect to `/sign-in`, deliberately:** the session is valid, so Clerk bounces it straight back and loops. The same fix closed a fail-open found alongside it — see the ADR-184 entry below. Still open from this item: the ~80 page-level `resolveOwner()` call sites each handle null their own way (see the follow-up below).
> 2. **The default nav exists but may not read as one.** Worth noting so this isn't "fixed" in the wrong place: a new owner *does* get a menu — `DEFAULT_NAV_SLOTS` (`src/lib/settings.ts:369`) gives Inbox, Notifications, Tasks, Planner, Favorites, Search when `users.settings` is `null`. But `DEFAULT_SETTINGS.navPosition` is `"bottom"`, so on a desktop browser it lands as a bottom bar while an existing user expects their left rail — easy to read as "the menu is gone." Open question: should the default be position-aware (rail on desktop, bottom on mobile), and should a brand-new instance seed a couple of type destinations so the nav isn't all built-ins?
>
> **✅ DONE (2026-08-12) — the hardcoded seed email.** `scripts/seed.mjs` now resolves `SEED_OWNER_EMAIL` → `DEV_USER_EMAIL` → Brandon's address (so his existing seeds behave exactly as before, and a correctly configured dev instance needs no new variable, since `DEV_USER_EMAIL` is already the identity the keyless dev path resolves). Also aborts on a malformed address rather than seeding an unusable owner: the failure mode here is *silent*, so a typo would otherwise cost a debugging session instead of printing an error. Documented in `.env.example`. Verified against the resolution chain + validator (8 cases, including that an empty string doesn't shadow the fallback); not run against a DB, since it's an `INSERT … ON CONFLICT DO NOTHING`.

> **🔴 FIXED IN PASSING — `main` did not build.** `scripts/verify-placement.mts` had three `check(name, ok, detail)` calls passing an `unknown` (values read back out of a `Record<string, unknown>` patch) into a `string` param, landing with the ADR-166 planner slices (`c88ad40`/`db20e89`, PR #214). `next build` runs the typecheck, so **every Vercel deploy from `main` was failing** until now. Fixed once at the shared function (`detail: unknown`, `String()` inside) rather than three call-site wrappers, on branch `feat/toc-container-relative-pin` as its own commit. Verify script still ALL PASS. **Worth a look at why this merged green** — the pre-merge check evidently didn't run `tsc`/`next build`.

> **🟡 ROTATE THE CLERK SECRET KEY when convenient (2026-08-17).** While wiring the Preview environment (see below), Tyler's `sk_test_…` Clerk secret was pasted into a Claude Code conversation transcript. Dev-tier key, low blast radius, but it's the same Clerk instance production signs in through. Tidy fix: Clerk dashboard → API keys → regenerate secret, then update `CLERK_SECRET_KEY` in Vercel's **Production and Preview** envs and redeploy. Also from that session: **Preview deployments now work** — Preview env carries `DATABASE_URL` (pointed at the DEV database, deliberately not prod, so branch migrations can't touch live data) plus the three Clerk vars, which every branch preview needs since the Build Command became `build:satellite` (it migrates first, and ADR-184 fails closed without auth keys on any deployed env).
>
> **🟡 TYLER'S INSTANCE IS MISSING ONE AUTH SECRET — was two (updated 2026-08-13).** Found while building ADR-179: `tylerjaycollins-projects/ledgr` had **none** of the `LEDGR_*` auth vars set. **Now set:** `LEDGR_APP_SECRET` and `LEDGR_API_TOKENS` (Build → API Tokens works), plus `LEDGR_OAUTH_SECRET` and `LEDGR_MCP_OWNER_UPN` as of 2026-08-10, so the MCP Generate button works and MCP runs over OAuth on the cloud instance. **Still unset: `LEDGR_CLIPPER_SECRET`,** which means the web-clipper Generate button renders as nothing on that instance — the invisible-feature trap ADR-179 was written about. Fix when wanted: `openssl rand -hex 32` → `vercel env add LEDGR_CLIPPER_SECRET production` → redeploy. Not needed for Overtone.

## ⟢ NEXT SLICE — Archive axis (cross-cutting `items.archived_at`, own ADR)

**Brandon's decision (2026-06-24):** archive is a *different axis* from done/undone — "hide from most surfaces (search, sort, lenses, automations) but keep it recoverable," and it applies to **every type**, not just tasks. Today "archived" is only a task *status category* and doesn't even hide from search (`search.ts` excludes only `deleted_at`). Build a first-class lifecycle flag parallel to soft-delete:
- **`items.archived_at`** (hot column, partial index like inbox/`deleted_at`) → **excluded by default** from search, lists, lenses, automations; an `/archive` view + unarchive action. Active → Archived → Trash (30-day purge) as three tiers.
- **Reconcile** the existing `archived` *status category* — lean toward folding it into the flag (categories become not_started/in_progress/done), decide in the ADR.
- **Optional fast-follow:** deterministic auto-archive-after-N-days-done (cron, no model — Principle 3), opt-in per type/user.
- CORE (new column + a new "exclude archived by default" query invariant) → both-agree + its own ADR before building.

## ⟢ General Ledgr upgrades (backlog, either builder)

### ⟢ Handoff triage backlog (2026-06-22) — carried forward from Brandon's 2026-06-21 call, verified against main

A fresh session re-grounded a stale-branch handoff against current `origin/main` (the prior session ran 139 commits behind and its ADRs/doc edits are dead — ignored). Each idea below was **verified against the actual code** before landing here. **Framing: Brandon is ~2 items from his 1.0 bar** (`v1-goals.ts`: Notion migration + the alpha→v1.0 flip are all that remain). So **these are post-1.0** — captured now so the intent isn't lost, not added to the 1.0 sprint. "Sprint to 1.0, stop adding features, converge" stands.

**Already BUILT on main — do NOT rebuild** (the stale session wrongly thought these were missing): inline `[ ]`→task promotion with `^id` block anchors (ADR-090); meeting→event rename + calendar pull-on-add + `tag` type + configurable event task-pull (ADR-094); meeting recording/transcripts (ADR-087-089); MCP propertyPatch/relations/views/templates *read+apply* (ADR-071); the `project` type + configurable statuses (ADR-082) + priority P1-P6 (ADR-096); MS Graph calendar matcher engine (ADR-023/024). Task status model confirmed: done/not-done via a fixed `done` category toggle, with richer workflow statuses living on `project`/bespoke types (ADR-082) — as Brandon expected.

**The one orphaned-branch feature worth carrying** (verified NOT on main):
- [ ] **Two-way relations — a derived reverse relation section.** A typed relation (ADR-067) shows as a named field on the source but only as a generic type-grouped backlink on the target; derive the named reverse section from the incoming edge + the source type's schema. Small read-derivation + a panel section + a glyph; no schema change. **CORE (touches the relations read model) → both-agree + ADR.** Full sketch: `explorations/two-way-relations.md`. *Recommend: small enough to build post-1.0 if Brandon wants; otherwise parked.*

**New parked design ideas** (genuinely uncovered; written up as explorations):
- [x] **AI shapes the workspace over MCP. ✅ DONE (2026-06-23, ADR-102; CORE, additive; Tyler-approved direction; branch `mcp-workspace-shaping`).** Eight new MCP tools (12 → 20), each a thin wrapper over the validated owner-scoped `parse*`+`create*`/`update*` libs so safety lives in the parsers: `describe_workspace` (read-before-write summary snapshot) + `create_type`/`update_type` + `create_view`/`update_view` + `create_dashboard`/`add_widget` + `update_nav`. The orientation guide ships as an **MCP resource** (new `resources` capability + `resources/list`/`resources/read` in `server.ts`; `src/lib/mcp/guide.ts`). No config delete tool (destruction stays in Build). `describe_workspace` = summaries + drill-down (Brandon's call), confirm-in-chat + soft-delete is the config-write safety stance (no new infra). `verify-mcp` **101/101** on live Neon + a live HTTP round-trip through `/api/mcp`; tsc/eslint/`next build` clean; no schema/dependency change. Resolves the write/shape half of `explorations/ai-workspace-shaping-over-mcp.md` (ADR-071 = read/apply half).
- [ ] **Universal, host-scoped placeable widgets** — drop a widget (e.g. "open tasks related to the people on this item") onto any item/type canvas, auto-scoped to its host; ship per-type default layouts. Both building blocks exist (dashboard `focusItemId`/`applyFocus`; the event `@people` task-pull) but aren't wired into the canvas vocabulary. Converges with `flexible-surfaces.md` §2 on one block/layout engine. Note added to `explorations/dashboard-widgets.md`.
- [ ] **"Initiative / arc / season" layer (above a project)** — a multi-month, no-due-date "keep tabs on everything related" type that's built-in-but-hideable/renamable; "a tag that is a type." Largely expressible with existing primitives (hide + rename + body + Related + dashboard focus); the one missing piece is a **"duplicate type" button/API** (mirror the existing `DuplicateViewButton`). Confirm the `project` type + a focused dashboard don't already deliver it first. Note added to `explorations/project-items.md`.
- [ ] **In-app "Update to latest" button (Tyler, 2026-07-01).** A button in Build → Maintain, owner-only, that updates the running instance in one click: advance this instance's `prod-*` branch to `main`, apply new migrations, deploy, then poll + show status ("Updating… → Live"), so an owner never touches git/Vercel/`db:migrate` by hand. **Now unblocked** — it's a UI front-end over the per-instance release model we just adopted (`prod-brandon`/`prod-tyler` + `npm run release:prod`; see [[deploy-and-shared-main]]). **Make-or-break: where migrations run** — fire a GitHub Action (`workflow_dispatch`) that runs `db:migrate` **then** deploy (deterministic migrate-before-deploy, the exact trap that broke the 2026-07-01 PJ merge), NOT a bare Vercel deploy hook unless the build itself migrates. Pair with expand-contract migrations. Token is a server-side, per-instance-scoped secret (one instance can't deploy another). Later: a "roll back to previous" companion (Vercel keeps prior deployments). Full sketch was drafted in `explorations/self-update-button.md` on the `integrate-pj` branch (not on main).

**Refinements on already-shipped features** (verified PARTIAL / not-yet):
- [ ] **Provisional events + a stale-sweep.** ADR-094 built cache-and-list (non-matched calendar events sit in the `calendar_events` cache as one-click "Add", never auto-becoming items — already fixes "every event becomes a meeting"). **Brandon confirmed (2026-06-24) the "From your calendar" feed + Add UI is live and working in his instance** — the shipped cache-and-list, which is most of the value. Still genuinely unbuilt: the deeper *ideal* refinement of floating non-matches as **provisional** event items rendered *inside* the calendar/agenda (tap to confirm) + a deterministic sweep removing ones unconfirmed ~1 week past their date. There is no provisional/suggested status on events and no sweep cron today. *Recommend: the shipped cache-and-list already covers Brandon's daily need; revisit the in-calendar provisional + sweep only if it feels insufficient in real use.*
- [ ] **Calendar matcher rule-building UI.** The matcher *engine* + `matchers` table + CRUD API exist (ADR-023/024); the UX does not. Brandon's preference: **primary path = learn-by-confirmation** (confirm a suggested edge → offer "always link roger@… → Roger" as a saved rule), inline where he already is; plus a **Build → "Calendar"** page to view/edit/remove rules (name "Calendar" deliberately, distinct from the `event` type). Setup-wizard de-prioritized. Today confirming an edge only upgrades its match-state; nothing writes a rule, and there's no Build calendar page. (Already tracked as a deferred follow-up; this records Brandon's preferred shape.)
- [ ] **Inbox grouped/sorted by arrival source** (small, standalone). Group the Inbox by how an item arrived (quick-capture vs create-on-miss `unmarked`) with a per-source indicator. **Prerequisite:** arrival source isn't recorded today (quick-capture and create-on-miss write no `source`); add a capture-source marker first, then group. Inbox is currently a flat oldest-first queue.
- [ ] **Mind map / spatial canvas** (post-v1, parked). Obsidian-Canvas-style freeform draggable markdown cards; any item placeable. Brandon: "probably a whole separate product." Sits in slight tension with markdown-canonical (a spatial layer outside the body). Park; relates to `canvas-drag-and-drop.md`.

**CLAUDE.md working conventions — ✅ ADOPTED (2026-06-23).** Brandon's repeated norms from the call, now live in CLAUDE.md → Working conventions (Tyler heads-up in COLLAB to peek + flag):
- **"Scope the UI when you build something big."** A significant feature must be understandable to the other builder without a walkthrough, even if tucked behind *More*; don't leave unexplained bespoke controls (the cautionary case: an unlabeled event-canvas control).
- **"Defer by hiding, not deleting/commenting."** Over-built / not-ready features get hidden (scoped to a default), not ripped out.
- (Mobile-as-its-own-surface is captured as a roadmap stance in `explorations/mobile-swipe-navigation.md`, not a coding convention.)

- [x] **Favorites — star toggle + nav flyout. ✅ BUILT (2026-06-21, ADR-098; branch `claude/favorites-feature-ifih88`).** Instant access to a small, owner-curated set of key items. State is an ordered id list in `users.settings` (`favorites: string[]`, no schema change, same posture as `navSlots`/ADR-056) — order = array order, membership = presence, validated/deduped/uuid-checked/capped (100) in `parseSettings`. New `src/lib/favorites.ts` (pure `isFavorited`/`addFavorite`/`removeFavorite`/`applyReorder` + DB-backed `getFavoriteItems`/`setFavorite`/`reorderFavorites`/`isItemFavorited`, body-free resolve that self-heals dead ids) + `GET/POST/PATCH /api/favorites`. **Favorites nav slot opens a flyout, not a page** (Brandon's call): `NavShell` special-cases `FAVORITES_HREF` into `FavoritesFlyout` (same hook as the `/search` palette slot, reusing the tools-popover open-state + outside-click closer) — fetch-on-open, each row links to its canvas, **pointer-event drag reorder** (mouse + touch, no DnD dep — Principle 5) → `PATCH /api/favorites`, unstar-in-place. Added to `DEFAULT_NAV_SLOTS` + the destination picker. **Star toggle** sits in the canvas `ItemActionsMenu` beside Lock (optimistic → `POST /api/favorites`); page + modal resolve membership via `isItemFavorited`. `tsc`/eslint clean; `verify-nav-and-deletes.mts` favorites pure checks all PASS. **Remaining:** in-browser eyeball (drag on a touch device; flyout in each nav layout).
- [x] **Item lock toggle + canvas "⋯" actions menu. ✅ BUILT (2026-06-21, ADR-097; branch `claude/item-lock-toggle-menu-s25e3c`).** A 3-dot `ItemActionsMenu` top-right of the item (modal header + full-page chrome) gathers **Lock/Unlock · Customize layout · Save as template · Apply template…** (Trash stays in the modal header); "Customize layout" moved out of `MarkdownCanvas` into the menu. **Lock** = `items.properties.locked` toggled via `propertyPatch` (no schema change, non-core); a locked item renders title, body (Tiptap `setEditable(false)` + toolbar hidden), the field strip, and custom properties read-only and un-clickable (`disabled <fieldset>` + `pointer-events-none`). Subtasks/relations/recurrence left interactive (out of scope). `tsc`/eslint/`next build` clean. **Remaining:** in-browser eyeball.
- [ ] **OPEN QUESTION — cross-type discovery without the list-tabs strip.** The old `ListTabs` strip (Tasks · Events · Notes · People · Project · …) was **removed from all list pages** (Tyler, 2026-06-21 — "no reason for it, just in the way"). Open: **how does a user find *all* their things of a type now?** Tyler has ideas; capture + decide. Candidates to weigh: the command palette (⌘K) as the primary jump-to; the Build → System Overview / nav destination picker; a per-type entry only where pinned to the Work nav; search. Decide the canonical "see everything of type X" path before relying on its absence. (`ListTabs.tsx` is now orphaned — keep or delete with the resolution.)

- [ ] **Relation picker `targetFilter`** — promoted to its own kick-off-ready section at the top of this file (⏭️ NEXT UP, 2026-08-17). The full decision, exposure posture (MCP-write / Build-read-only), CORE gate, and build slices live there; this line is just so backlog sweeps don't re-derive it.
- [ ] **Per-destination count badges ("inbox for anything").** Today the nav has exactly one count badge: the untriaged Inbox count (`countInbox`, PRD §4.11, rides `items_inbox_idx`, rendered via `CountBubble`/`IconWithCount` in `NavShell`). Generalize it so **any nav slot/destination can opt into a trackable number**, with a configurable source: (a) the inbox/untriaged count for a given type, (b) the count of a saved view, or (c) an "in-process" count for a type (items not in a done status category). So a "Songs" slot (icon already exists) could also show how many songs are in process; a "Projects" slot shows active projects; "Tasks" shows today/overdue. The config rides the existing owner-configurable nav slots (`settings.navSlots`, additive — same pattern as ADR-056, non-core nav chrome, no ADR needed). **Perf rule:** counts must be cheap — index-backed, owner-scoped, no `body` (Principle 8). Pairs with the existing `inbox` flag (a per-type "inbox" is just `type=X & inbox=true`). Raised by Tyler 2026-06-20; general enhancement both instances would want — worth a quick heads-up to Brandon, but solo-buildable.
- [ ] **Notes cleanup in Data Hygiene.** A feature on the Data Hygiene page (`/build/hygiene`, Build → MAINTAIN) that surfaces notes no longer needed, for either **cleanup** (→ Trash, soft-delete + 30-day recovery, never hard-delete) or a long-term **archive** (kept but out of the way). A **deterministic cron** computes the candidates — plain rule code, no model (Principle 3: no AI in a cron); it only *surfaces* a review list, the user confirms the action (matches soft-delete-only + the no-silent-failures / staged-for-review ethos). Scheduler = the Vercel daily cron or the GitHub-Actions authenticated-endpoint pattern (provider seam). **Candidate rules:** untouched for N days, orphan notes (no relations/links/mentions), empty or near-empty, never opened since creation, lingering `unmarked` Inbox stubs. **Archive shape:** `status_category` already has an `archived` value (ADR-082, currently task-side) — reuse it for notes, or a dedicated archive flag/tag; pairs with the labels/tags proposal. Raised by Tyler 2026-06-20; general (both instances).
- [ ] **Built-in starter templates (a "grab one" set).** Ship a curated set of useful templates a user can grab, rather than starting every template from scratch. Rides the Templates redesign (ADR-093) directly — each built-in is a **seeded prototype item + registry row** (like `seed.mjs` seeds types), so this is *content*, not new mechanism (non-core; worth curating the set with Brandon, but no ADR). Uses the variable + date-rule machinery already shipped (`{{today}}`, `{{nextSunday}}`, `{{ask:Label}}`, offset due/scheduled). Starter set (scope ~4–6 first, expand later):
  - **Meeting / Agenda** (`meeting`) — title `Meeting — {{today}}`; body sections *Agenda · Discussion · Decisions · Action Items*; `{{ask:Attendees}}`. Action-item lines promote to tasks via the existing block-anchor promote (ADR-090).
  - **1:1 Check-in** (`meeting`, recurring-friendly) — *Wins · Challenges · Priorities this week · Follow-ups from last time*; relate to the person. A repeatable cadence template.
  - **Weekly Review** (`task` with subtasks) — GTD-style subtask checklist: clear inbox · review calendar · review projects · pick next actions · someday/maybe; scheduled `{{nextSunday}}` (or set recurring weekly).
  - **Daily Note / Journal** (`note`) — title `{{today}}`; sections *Top 3 · Notes · Reflections*. Supports the Apple-Notes-replacement workflow.
  - **Sermon prep** (`sermon`, once the type exists) — the shape scaffold from `explorations/sermons-module-prd.md` (sticky statement + intro + 3 points). Ties the sermon-shape library to the template system.
  - **Project kickoff** (`project`, once the type exists) — *Goal/outcome · Milestones · Stakeholders (relations) · Initial tasks (subtasks) · Status*; a dev-app variant pre-fills repo/live-URL/stack properties.
  - *Stretch:* Event plan (VBS/Fall-Festival style task+subtasks), Reading/book notes (summary · key quotes · takeaways), Decision one-pager (problem · options · decision · rationale). Seed per-owner on setup (or a catalog to copy from); ADR-093 TPL4's "default + chooser" surfaces them at "+ New". Raised by Tyler 2026-06-20.
- [x] **Canvas tabs (Google-Docs-style, across the top). ✅ BUILT on branch `canvas-tabs` (ADR-095); in-browser eyeball pending.** CORE-ADJACENT (Brandon agreed before build). Marker-based (`<!-- tab: Title -->`) sections of the one body; pure codec `src/lib/editor/canvas-tabs.ts` (verify 19/19) + `TabbedBody.tsx` wrapper; auto-on for `note`, attachable elsewhere via the `tabs` capability (ADR-051); readers flatten markers to `## Title`. No migration. PRD: `explorations/canvas-tabs.md`. Original ask: Split one item's canvas into named tabs (a tab strip + "+ Add tab" across the top, a per-tab inline title), each a section of the *same* markdown body. Tyler's case: 5–6 lyric versions + notes on one song's note. **Tabs = named sections within the single body** (the canonical-body contract holds; all readers see the whole doc if the delimiter is render-safe), auto-on for `note` and attachable to any type via the ADR-051 capability seam. Encoding fork to settle: invisible tab marker (robust, lean this) vs `# H1`-as-tab (upload-friendly — use as the import path). Likely **no migration** (tabs live in the body; active-tab ephemeral or a tiny property). Raised by Tyler 2026-06-20.
- [ ] **Markdown editor chrome redesign + SVG toolbar icons (editor-wide).** Tyler doesn't like the current editor's **boxed black surface** (the `rounded border border-neutral-800 bg-neutral-950` wrapper in `MarkdownEditor.tsx`, Image #13). Redesign: a **clean, borderless typing surface** (no dark box), **auto-growing height** (no fixed min-height), and a subtle **bottom divider line** separating the text field from the footer actions (e.g. `+ Relate` / `+ Task` on the default canvas). The **toolbar** redesigned to feel integrated/"belonging," with a **consistent inline-SVG icon set replacing the current text/emoji labels** (B/I/S/H1/H2/lists/Tasks/quote/code/Table/Image/Link + the Color/Highlight dropdowns) — no icon-font, no dependency (Principle 5). **Editor-wide:** `MarkdownEditor.tsx` renders on every markdown surface (notes, the summoned task description, papers, the changelog notes, scratch), so this is general polish, not tasks-specific — both instances benefit; a heads-up to Brandon, but non-core (no ADR). Pairs with the tasks capture-card/row SVGs (those are separate, task-only icons). Raised by Tyler 2026-06-21.
- [ ] **Configurable editor toolbar (User Settings, app-wide).** A User Settings section — **same pattern as the tasks "Quick Add"** (a "Show toolbar items" list, drag-to-reorder, each − to hide; a "Hidden" pool, each + to show) — letting the user **show/hide (and order) every toolbar item above the canvas**: Bold · Italic · Strikethrough · H1 · H2 · Bullet list · Numbered list · Tasks · Quote · Code · Table · Image · Link · Color · Highlight (· the @-mention hint). **The choice applies to `MarkdownEditor` on every canvas across the app** (notes, task description, papers, changelog notes, scratch). Hidden items optionally live behind a **"…" overflow** in the toolbar (the capture-card kebab pattern) so they're still reachable. **Storage:** additive jsonb on `users.settings` (`editorToolbar: {shown: [...], showOverflow?: bool}`), tolerant parse — the `navSlots`/`quickAddActions`/`highlightGradient` pattern (ADR-056). Ideally a **shared reorderable show/hide settings component** reused by both Quick Add and this. Non-core UI; general (both instances); no ADR. Raised by Tyler 2026-06-21.
- [ ] **Drag-and-drop on board *widgets* (and any board surface).** The board *view* has full DnD (`src/components/views/BoardDnd.tsx`, ADR-072 desktop HTML5 + K1/K2 touch long-press), but a board rendered as a **dashboard widget** is read-only by design: `ViewRenderer` exposes a `boardDraggable` prop that defaults off and `WidgetBody` leaves it off (`src/components/views/ViewRenderer.tsx` ~line 685, "dashboards leave it false, so a board widget stays read-only"). So a tasks-as-board widget can't drag-to-re-bucket. Make DnD available on **any** board rendering, widget included: flip `boardDraggable` on for the widget path and reuse the existing `BoardDnd` drop logic (`boardDropPatch`, optimistic + revert). Confirm the optimistic patch + `router.refresh()` behave inside the dashboard grid (no view URL context the way the full view has). Non-core (one widget surface, no new dependency, matches ADR-072's no-DnD-lib stance); no ADR. Raised by Brandon 2026-06-23.
- [ ] **Calendar views/widgets default to full available width.** A calendar view renders inside the standard reading column (the view page wraps content in `mx-auto w-full max-w-5xl`, `src/app/views/[id]/page.tsx` ~line 92), which suits tables and lists but **squishes the month grid** (`CalendarLayout` / `grid grid-cols-7` in `src/components/views/ViewRenderer.tsx`). A calendar wants room. Default calendar (and likely board/timeline) layouts to **expand to the max width available** (full page / container) rather than the narrow reading column. Direction: let a layout declare a "wide" width preference the page honors (raise/drop the `max-w-5xl` cap for those layouts); widgets already fill their grid cell, so this is mostly the standalone view page. Non-core (view chrome/CSS); no ADR. Raised by Brandon 2026-06-23.

## Known mobile bugs & UX gaps

Captured 2026-06-14 from real mobile use. Fix against priority, not all at once.

**Bugs:**
- **Saving a link from Android creates 2 copies.** The share-target → link creation produces a duplicate item.

**UX fixes (hover-dependent UI doesn't work on mobile):**
- **Delete button is hover-only.** The Trash affordance appears on hover, which has no equivalent on touch. Needs a persistent or long-press-triggered control on mobile.
- **Build button overlaps content on mobile.** The floating `BuildModeButton` is positioned for desktop hover interaction and covers page content or other controls at small viewport sizes.
- **Canvas too narrow on mobile.** Excess margin on the item canvas; should be full-width or near-full-width.
- **Close button is small and at the top of the canvas.** It exists but is easy to miss on mobile; worth considering a more prominent or thumb-reachable dismiss control.
- **Text wrapping on mobile.** Long lines don't wrap well; consider forcing word-wrap by default or adding a view-settings toggle.

**Missing features / gaps:**
- **Kanban drag-and-drop not implemented.** Board views have no DnD on any platform; items can't be moved between columns by dragging.
- ✅ **`@`-mention links are now tappable in read/share view** (fixed 2026-06-15). `markdown-render.ts` rewrites a mention's `ledgr://item/<id>` href to an in-app `<a href="/items/<id>" class="mention">`, so it's a real link on the print view and share page (was a hover-dead `.mention` span). A malformed/empty-id mention still flattens to a span so no broken anchor leaks. FTS text and the existing `.mention` styling are unchanged. `verify-print` updated + green; tsc + `next build` + eslint clean. *(Note: on a public share page the link targets the auth-gated `/items/<id>`, so an anonymous viewer hits the sign-in wall — the owner viewing their own print/items gets the intended jump. Revisit if cross-item navigation from a public share is ever wanted.)*
- **No undo button on the canvas toolbar.** Tiptap history is active (Ctrl+Z works on desktop) but there's no undo button in the toolbar itself — not accessible on mobile at all.

**Explorations opened (2026-06-14):** `explorations/mobile-swipe-navigation.md`, `explorations/notes-organization.md`, `explorations/meeting-recording.md`, `explorations/canvas-drag-and-drop.md`, `explorations/rich-export-and-theming.md`.

**Build surface follow-up (2026-06-14) — custom properties on system types:**
The infrastructure for this already exists: `updateType` allows editing system types (adding properties is explicitly permitted in the code comment), and the `TypeBuilder` already shows "System type — can be extended, not deleted" when you open one via `/build/types/[key]/edit`. What's missing is **discoverability and framing**:
- The types list in `/build/types` should make it clearer that system types are editable (an "Add property" shortcut or a more inviting edit link, not just a greyed-out system badge).
- In the property editor for a system type, distinguish between **built-in properties** (connected to plumbing — Todoist sync, calendar fields, etc.) and **custom properties** (organizational only — sorting, grouping, filtering). A visual divider or a label like "Custom properties" below the system ones would set the right expectation.
- No schema change needed — custom properties on system types ride `properties` jsonb the same way they do on user types.

---

## Next up (in order)

**Slices 30–32 shipped this session (push notifications, public share links, provider-interface confirmation) — see the session-3 summary above and "Recently done."** Phase 2 is code-complete; what remains is the §1c-blocked matcher UIs below and Phase 3.

### Phase 3 (after Phase M): the Build surface UX
- **Reordered 2026-06-13 (Brandon):** building the UX is the priority after Phase M, ahead of the Claude layer and Notion migration. **Tier 1 — the Build surface shell + the custom type & property builder — is DONE (2026-06-13, ADR-044, slice 33; incl. the type/kind authoring UX).**
- **Tier 2 — templates — DONE (2026-06-13).** (a) **Workflow & wiki templates** ("New Workflow"/"New Wiki" → type + properties + starter views, wired into Work via the dashboard pin) — ADR-046, slice 35. (b) **Per-type item templates** (property defaults + starter body; the Phase-2 meeting-prep template is the forerunner) — ADR-045, slice 34. Both built on slice 33's type/property machinery.
- **Tier 3 — Claude layer — IN PROGRESS.** (a) **MCP server — DONE (2026-06-13, ADR-047, slice 36):** `POST /api/mcp`, hand-rolled Streamable HTTP, `mcp`-scoped machine token, six item tools. **Next: (b) scheduled Claude tasks** (morning briefing / weekly health check over the same token), then **(c) selective Notion migration.** Then **Tier 4** (planning rhythms + parked items). See `roadmap.md` Phase 3.

### (parallel) Editor parity / completeness — gaps from the M3 markdown cutover
The Tiptap editor (ADR-040) shipped the core path; a few BlockNote-era affordances didn't get ported. The R2 attachment backend (`/api/attachments`, presign, `src/lib/attachments.ts`, storage `putObject`) is all still in place, so these are re-wirings, not rebuilds. Not tier-ordered — slot against priority.
- **Inline image paste — REGRESSION (do first).** Phase-1-complete feature dropped by the cutover; `src/components/markdown-editor/` has no paste/drop/image handling at all. Add a Tiptap paste/drop handler → existing R2 presign → insert `![](url)`. Brandon confirmed broken (2026-06-13).
- **Tables.** GFM `| col |`: **editor-side only** — markdown-it already renders tables (default preset, `markdown-render.ts`), so just the first-party `@tiptap/extension-table` (+ row/cell/header) + toolbar + CSS. Low risk (same `@tiptap/*` vendor, markdown stays canonical, standard GFM serialization). Verify `@tiptap/markdown` table round-trip is lossless.
- **Paste / embed documents.** Was in the BlockNote canvas, not the markdown one. Decide markdown shape (R2 file link vs inline embed), then paste/drop → R2. Lower priority than image paste; recorded for later.
- Minor: strikethrough toolbar button (Strike already round-trips, just no button).

### (follow-up) Matchers: setup wizard + learn-by-confirmation UI
- The matcher **engine + config + suggested/confirmed states are done** (slice 23). Remaining: (a) **setup wizard** — sample recent/upcoming events and let Brandon pick matches → write first rules (needs live calendar; **§1c now live 2026-06-19, ready to build**); (b) **learn-by-confirmation** — when Brandon confirms a suggested match on a meeting (slice-15 confirm path), offer to save a standing rule (POST `/api/matchers`); the data backbone (`properties.match.matcherIds`, the matchers API) is already in place. §1c is now live (2026-06-19), so pick these up whenever; the meeting canvas shows matches.

---

## Open decisions to make as we build
- ~~Entity `kind`~~: decided, real column on `items` (ADR-003); **later dropped** with the `entity`→`person` collapse (ADR-055) — sub-classification is now a `select` property, and list-filtering by it is a near-term view-engine item (see Views).
- ~~OneDrive export file scope~~: decided, app-only client credentials (ADR-017).
- ~~Error capture~~: decided, `error_log` table over Sentry (ADR-020).
- Desktop navigation: floating bottom bar vs right sidebar (PRD Q9). Both built behind the same slot model; Brandon picks, loser gets deleted.
- Per-type item templates (tasks/meetings/notes storing property choices + starter content) parked for a later phase (roadmap Phase 3); the meeting-prep template is the Phase 2 forerunner.
- ~~"Project" treatment for subtask-having items~~: confirmed a project is **relations, not a type** (ADR-061); a project treatment, if built, is a presentation layer only. Still parked in `explorations/project-items.md`.
- ~~Notes organization (files/folders vs sub-pages vs tags)~~: decided **relations-primary, notes-as-a-module-on-every-type** (ADR-061). The remaining open piece is the **export → MD-file-tree mapping** (`explorations/storage-organization.md`).
- ~~What does Share output~~: decided **Share/Print renders the canvas**, with a per-property "include in share?" toggle (ADR-062). Per-type field sets still decided type by type.
- ~~Entity vs. custom type~~: **RESOLVED** — the `entity` meta-type was retired → bespoke `person`, `kind` dropped, the related list made universal (ADR-055); the `relation` property kind stays deferred. See `explorations/entity-vs-custom-type.md`.
- See `decisions.md` for the running log and PRD §10/§11 for what's already frozen vs still open.

---
